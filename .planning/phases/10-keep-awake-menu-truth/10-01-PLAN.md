---
phase: 10-keep-awake-menu-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/KeepAwakePresentation.swift"
  - "Tools Cat/StatusBarController.swift"
  - "Tools CatTests/KeepAwakeMenuStateTests.swift"
  - "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
autonomous: true
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
must_haves:
  truths:
    - "Idle keep-awake menus no longer show `关闭常亮` when `confirmedMode` is `.off` and there is no stop transition in flight."
    - "Confirmed active keep-awake sessions and explicit stop transitions still expose exactly one direct `关闭常亮` row."
    - "The shipped start rows, countdown status row, and pending/failure semantics remain truthful while the stop-row visibility changes."
  artifacts:
    - path: "Tools Cat/KeepAwakePresentation.swift"
      provides: "Visibility contract for when the keep-awake stop row should be shown"
      contains: "showsStopAction"
    - path: "Tools Cat/StatusBarController.swift"
      provides: "Runtime application of the truthful stop-row visibility rule in the menu render path"
      contains: "keepAwakeOffItem.isHidden"
    - path: "Tools CatTests/KeepAwakeMenuStateTests.swift"
      provides: "Presentation-level regression coverage for idle/startup/active/stopping stop-row visibility"
      contains: "testStopActionVisibilityFollowsConfirmedAndPendingState"
    - path: "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
      provides: "Controller-level proof that visible keep-awake rows now match the truthful runtime state"
      contains: "testIdleMenuHidesStopRowWhenKeepAwakeIsOff"
  key_links:
    - from: "Tools Cat/KeepAwakePresentation.swift"
      to: "Tools Cat/KeepAwakeSessionModel.swift"
      via: "stop-row visibility derives from the confirmed keep-awake mode plus pending action instead of a new state machine"
      pattern: "KeepAwakeMode|KeepAwakePendingAction"
    - from: "Tools Cat/StatusBarController.swift"
      to: "Tools Cat/KeepAwakePresentation.swift"
      via: "rendering the menu uses the presentation visibility contract to hide or show `关闭常亮`"
      pattern: "renderKeepAwakePresentation\\(|keepAwakeOffItem.isHidden"
---

<objective>
Implement the truthful stop-row visibility rule in the keep-awake render path without reopening the broader root-menu structure.

Purpose: The current bug is a presentation leak, not a missing state model. This plan should make the runtime menu hide `关闭常亮` only when it is non-actionable, while preserving every other shipped keep-awake behavior.
Output: A presentation-level stop-row visibility contract, controller wiring that applies it, and focused regression coverage proving idle, active, startup, and stopping visibility semantics.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-keep-awake-menu-truth/10-CONTEXT.md
@.planning/phases/10-keep-awake-menu-truth/10-RESEARCH.md
@.planning/phases/10-keep-awake-menu-truth/10-VALIDATION.md
@Tools Cat/KeepAwakePresentation.swift
@Tools Cat/KeepAwakeSessionModel.swift
@Tools Cat/StatusBarController.swift
@Tools CatTests/KeepAwakeMenuStateTests.swift
@Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a presentation-level stop-row visibility contract keyed off confirmed state plus pending action</name>
  <files>Tools Cat/KeepAwakePresentation.swift, Tools CatTests/KeepAwakeMenuStateTests.swift</files>
  <read_first>
    - .planning/phases/10-keep-awake-menu-truth/10-CONTEXT.md
    - .planning/phases/10-keep-awake-menu-truth/10-RESEARCH.md
    - Tools Cat/KeepAwakePresentation.swift
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools CatTests/KeepAwakeMenuStateTests.swift
  </read_first>
  <behavior>
    - Idle `.off` with no pending action hides the stop row.
    - Startup from `.off` while `pendingAction` is `.startingIndefinite` or `.startingTimed(...)` still hides the stop row.
    - Replacement from an already active mode keeps the stop row visible because the confirmed mode is still active until the new mode is confirmed.
    - `.stopping` always keeps the stop row visible while the action group is disabled.
  </behavior>
  <action>In `Tools Cat/KeepAwakePresentation.swift`, add a new computed property named `showsStopAction`. Its contract must be exact: return `true` when `pendingAction == .stopping`; otherwise return `false` when `confirmedMode == .off`; otherwise return `true` for `.indefinite` and `.timed`. Do not add new session states or new user-facing idle copy. Preserve all existing `statusText`, `buttonToolTip`, `iconSymbol`, countdown formatting, and `isPending` behavior. In `Tools CatTests/KeepAwakeMenuStateTests.swift`, add a test named `testStopActionVisibilityFollowsConfirmedAndPendingState()` that covers these exact cases: off + no pending → hidden, off + `.startingTimed(.minutes15)` → hidden, timed + `.startingTimed(.hour1)` → visible, indefinite + nil → visible, timed + `.stopping` → visible.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakePresentation.swift` contains `var showsStopAction: Bool`
    - `Tools Cat/KeepAwakePresentation.swift` contains `if pendingAction == .stopping`
    - `Tools Cat/KeepAwakePresentation.swift` contains `case .off`
    - `Tools CatTests/KeepAwakeMenuStateTests.swift` contains `testStopActionVisibilityFollowsConfirmedAndPendingState`
    - `Tools CatTests/KeepAwakeMenuStateTests.swift` contains `.startingTimed(.minutes15)`
    - `Tools CatTests/KeepAwakeMenuStateTests.swift` contains `.stopping`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeMenuStateTests'</automated>
  </verify>
  <done>The keep-awake presentation contract now exposes one truthful, test-locked signal for when `关闭常亮` should appear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the visibility rule in the controller render path while preserving the shipped keep-awake structure</name>
  <files>Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift</files>
  <read_first>
    - .planning/phases/10-keep-awake-menu-truth/10-CONTEXT.md
    - .planning/phases/10-keep-awake-menu-truth/10-RESEARCH.md
    - Tools Cat/StatusBarController.swift
    - Tools Cat/KeepAwakePresentation.swift
    - Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
  </read_first>
  <behavior>
    - Idle menus show the five start rows and hide `关闭常亮`.
    - Active indefinite or timed sessions show `关闭常亮`.
    - Stopping keeps `关闭常亮` visible but disabled with the rest of the action group.
    - The controller does not rebuild separators or menu ordering; it only changes stop-row visibility and keeps the status-row render rules intact.
  </behavior>
  <action>In `Tools Cat/StatusBarController.swift`, update `renderKeepAwakePresentation()` so `keepAwakeOffItem.isHidden = !presentation.showsStopAction`. Keep `keepAwakeOffItem.state = .off`, keep the existing `keepAwakeActionItems.forEach { $0.isEnabled = !presentation.isPending }` disable pattern, and do not insert/remove menu items or separators dynamically. In `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift`, add these exact tests: `testIdleMenuHidesStopRowWhenKeepAwakeIsOff()`, `testConfirmedActiveSessionShowsStopRow()`, and `testStoppingStateKeepsStopRowVisibleButDisabled()`. Use visible-title assertions that filter `isHidden` rows, and keep the existing countdown/status-row tests intact.</action>
  <acceptance_criteria>
    - `Tools Cat/StatusBarController.swift` contains `keepAwakeOffItem.isHidden = !presentation.showsStopAction`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testIdleMenuHidesStopRowWhenKeepAwakeIsOff`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testConfirmedActiveSessionShowsStopRow`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testStoppingStateKeepsStopRowVisibleButDisabled`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `.filter { !$0.isHidden }`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests' -only-testing:'Tools CatTests/KeepAwakeMenuStateTests'</automated>
  </verify>
  <done>The live controller render path now hides `关闭常亮` only in non-actionable states, while active and stopping states keep one direct stop row.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the presentation layer and controller agree on one truthful stop-row visibility rule, idle menus visibly omit `关闭常亮`, active/stopping menus keep it, and the targeted keep-awake tests pass without regressing countdown/status behavior.
</verification>

<success_criteria>
The runtime keep-awake menu now matches the real current state: idle menus no longer advertise a meaningless stop action, and active or stopping sessions still keep a direct stop path.
</success_criteria>

<output>
After completion, create `.planning/phases/10-keep-awake-menu-truth/10-01-SUMMARY.md`
</output>
