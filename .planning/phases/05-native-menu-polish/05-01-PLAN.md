---
phase: 05-native-menu-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
  - "Mac OS Swiss Knife/StatusBarController.swift"
  - "Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift"
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "The root menu reads as three native groups in this order: keep-awake, wake, then management."
    - "Idle menu states stay short because keep-awake and wake status rows hide when they carry no meaningful state."
    - "The wake section still exposes manual send plus truthful wake feedback even when the saved-device library is empty."
  artifacts:
    - path: "Mac OS Swiss Knife/StatusBarController.swift"
      provides: "Fixed three-section root menu anchors, separator placement, and contextual status-row visibility"
      contains: "NSMenuItem.separator()"
    - path: "Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift"
      provides: "Regression coverage for separator order, idle collapse, and manual-only wake status visibility"
      contains: "final class StatusBarControllerMenuPolishTests"
    - path: "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
      provides: "Phase 5 menu-polish test target registration"
      contains: "StatusBarControllerMenuPolishTests.swift"
  key_links:
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/KeepAwakePresentation.swift"
      via: "keep-awake status visibility continues to render from presentation.statusText"
      pattern: "presentation\\.statusText"
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/WOLSessionModel.swift"
      via: "wake status row renders from sendState and lastCompletedWake even with no saved devices"
      pattern: "lastCompletedWake"
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift"
      via: "fixed separator anchors and hidden-state rules are locked by controller tests"
      pattern: "separator|isHidden"
---

<objective>
Reshape the root status menu into the exact compact three-section contract for Phase 5 without changing wake or keep-awake semantics.

Purpose: UX-01 depends on the menu reading as a small native utility instead of an accumulated feature list.
Output: Fixed separator anchors, contextual status-row collapse, empty-library wake-status support, and dedicated controller regression coverage for the menu contract.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-menu-polish/05-CONTEXT.md
@.planning/phases/05-native-menu-polish/05-RESEARCH.md
@.planning/phases/05-native-menu-polish/05-UI-SPEC.md
@.planning/phases/05-native-menu-polish/05-VALIDATION.md
@.planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
@.planning/phases/04-timed-keep-awake/04-02-SUMMARY.md
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
@Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift

<interfaces>
Current controller seams:
```swift
final class StatusBarController: NSObject {
    private(set) var keepAwakeStatusItem: NSMenuItem!
    private(set) var recentWakeItems: [NSMenuItem] = []
    private(set) var allDevicesItem: NSMenuItem?
    private(set) var wakeStatusItem: NSMenuItem?

    var wolMenuIndexForTesting: Int { menu.index(of: wolItem) }
    func menuIndexForTesting(of item: NSMenuItem) -> Int
}
```

Current shared wake and keep-awake state:
```swift
final class WOLSessionModel: ObservableObject {
    @Published var sendState: WakeSendState
    @Published private(set) var lastCompletedWake: CompletedWakeAttempt?
    var isSending: Bool { get }
}

struct KeepAwakePresentation: Equatable {
    var statusText: String?
    var isPending: Bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated controller regression coverage for the Phase 5 menu contract</name>
  <files>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: The root menu uses two native separators so the groups read keep-awake -> wake -> management per D-01, D-02, and D-03.
    - Test 2: When keep-awake is off and no truthful wake result exists, both status rows are hidden per D-04 and D-05.
    - Test 3: The wake section still contains `发送 WOL …` and can surface a truthful wake-status row even when there are no saved devices, preserving D-06.
    - Test 4: `管理设备…` and `退出 Swiss Knife` remain the last two actionable rows regardless of wake-section content.
  </behavior>
  <action>Per D-01 through D-06, create `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` and register it in `project.pbxproj`. Build the file around the same fake-store/fake-session patterns already used by `StatusBarControllerWakeMenuTests.swift` and `StatusBarControllerKeepAwakeMenuTests.swift`; do not introduce a second controller abstraction just for tests. Add these exact test functions: `testRootMenuUsesThreeSectionOrderWithTwoNativeSeparators()`, `testIdleMenuCollapsesBothStatusRows()`, `testWakeSectionKeepsManualSendRowWhenLibraryIsEmpty()`, and `testManagementActionsRemainLastRows()`. The tests must assert separator indexes, hidden-state behavior, and root-row order using `menuItemsForTesting`, `menuIndexForTesting(of:)`, `wolMenuIndexForTesting`, `keepAwakeStatusItem`, and `wakeStatusItem`. Keep this task scoped to test coverage and project wiring only; the controller implementation change belongs in Task 2.</action>
  <acceptance_criteria>
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` exists
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `final class StatusBarControllerMenuPolishTests`
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `testRootMenuUsesThreeSectionOrderWithTwoNativeSeparators`
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `testIdleMenuCollapsesBothStatusRows`
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `testWakeSectionKeepsManualSendRowWhenLibraryIsEmpty`
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `testManagementActionsRemainLastRows`
    - `Mac OS Swiss Knife.xcodeproj/project.pbxproj` contains `StatusBarControllerMenuPolishTests.swift`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests'</automated>
  </verify>
  <done>The Phase 5 menu contract has a dedicated controller-test file, that new test target executes successfully, and the expected menu-contract assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild the root menu around fixed section anchors and on-demand status density</name>
  <files>Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-RESEARCH.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - .planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
    - .planning/phases/04-timed-keep-awake/04-02-SUMMARY.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/KeepAwakePresentation.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift
  </read_first>
  <behavior>
    - Test 1: The root menu always has exactly two native separators and the groups stay ordered keep-awake, wake, management.
    - Test 2: In idle state, `keepAwakeStatusItem` and `wakeStatusItem` are both hidden.
    - Test 3: The wake group order is recent devices (when present), `所有设备` (when the library is non-empty), `发送 WOL …`, then the truthful wake-status row when it exists.
    - Test 4: A truthful wake-status row can appear after a manual WOL send even when `deviceLibrary.devices` is empty.
  </behavior>
  <action>Per D-01, D-02, and D-03, refactor `StatusBarController.configure()` so the root menu has permanent section anchors in this exact order: the existing keep-awake action group plus `keepAwakeStatusItem`; one native separator; the wake section; one native separator; `管理设备…`; `退出 Swiss Knife`. Use `NSMenuItem.separator()` for both section boundaries and keep those separators outside the dynamic wake rebuild path. Per D-04, D-05, and D-06, rewrite `rebuildWakeMenu()` so the wake section always contains `发送 WOL …`, adds up to three recent rows ahead of it, adds `所有设备` only when `deviceLibrary.devices` is non-empty, and creates `wakeStatusItem` independently of library size. `wakeStatusItem` must stay disabled, show `WakeSendMessage.sending.text` while sending, show `lastCompletedWake.message` for truthful completed results, and hide when no meaningful wake state exists. Keep `keepAwakeStatusItem` visibility driven by `KeepAwakePresentation.statusText == nil`; do not alter keep-awake truth semantics, wake-send semantics, submenu copy, or management actions. Do not add text headers, attributed menu rows, badges, or any new commands outside the locked scope.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `NSMenuItem.separator()`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"发送 WOL …"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"管理设备…"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"退出 Swiss Knife"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `WakeSendMessage.sending.text`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `lastCompletedWake?.message`
    - `Mac OS Swiss Knife/StatusBarController.swift` does not contain any new menu text-header title beyond the existing action labels
    - `Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift` contains `testIdleMenuCollapsesBothStatusRows`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests'</automated>
  </verify>
  <done>The root menu now reads as keep-awake, wake, then management, and both status rows collapse out of idle state without breaking truthful feedback.</done>
</task>

</tasks>

<verification>
Run the dedicated Phase 5 controller slice together with the existing wake and keep-awake controller suites. Treat the plan as complete only when the root menu keeps two separators, idle states stay compact, and manual-only wake results can still appear truthfully.
</verification>

<success_criteria>
UX-01 is implemented at the controller level: the root menu is compact, clearly grouped, and still preserves truthful state across keep-awake and wake flows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-menu-polish/05-01-SUMMARY.md`
</output>
