---
phase: 05-native-menu-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
autonomous: false
requirements:
  - UX-01
  - UX-04
must_haves:
  truths:
    - "The executor can open the WOL window and device-library window directly in XCUITest and prove the polished section hierarchy exists."
    - "A human can verify the final menu reads keep-awake -> wake -> management and that both windows remain restrained native utility surfaces."
    - "Phase 5 sign-off combines automated UI smoke with one explicit manual visual gate rather than relying on screenshots or inference."
  artifacts:
    - path: "Mac OS Swiss Knife/AppDelegate.swift"
      provides: "Launch-argument seam for direct WOL-window UI smoke without menu-bar automation"
      contains: "--ui-test-open-wol-window"
    - path: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      provides: "Phase 5 UI smoke coverage for the WOL and device-library windows"
      contains: "testLaunchWithWOLWindowShowsPolishedSections"
  key_links:
    - from: "Mac OS Swiss Knife/AppDelegate.swift"
      to: "Mac OS Swiss Knife/WOLWindow.swift"
      via: "launch arguments open the retained WOL window directly for automation"
      pattern: "openWOLWindow\\("
    - from: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      to: "Mac OS Swiss Knife/WOLView.swift"
      via: "XCUITest queries the section identifiers added in Plan 02"
      pattern: "wol-mode-group|wol-action-row|wol-send-button"
    - from: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "XCUITest queries the polished manager hierarchy identifiers added in Plan 02"
      pattern: "device-library-top-actions|device-library-empty-add-button|device-library-form-actions"
---

<objective>
Finish Phase 5 with direct UI-smoke coverage for the polished windows and one explicit human visual approval gate for the final native menu and window feel.

Purpose: UX-01 and UX-04 both have visual/native aspects that need one automated proof path plus one human confirmation step.
Output: Launch seams for WOL-window smoke coverage, extended XCUITests, and a blocking manual verification checkpoint.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-menu-polish/05-CONTEXT.md
@.planning/phases/05-native-menu-polish/05-UI-SPEC.md
@.planning/phases/05-native-menu-polish/05-VALIDATION.md
@.planning/phases/05-native-menu-polish/05-01-PLAN.md
@.planning/phases/05-native-menu-polish/05-02-PLAN.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/DeviceLibraryView.swift
@Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift

<interfaces>
Current launch seam:
```swift
private struct LaunchConfiguration {
    let shouldOpenDeviceLibrary: Bool
    let userDefaults: UserDefaults?
    let seededDeviceLibraryData: Data?
}
```

Current UI test entry:
```swift
final class Mac_OS_Swiss_KnifeUITests: XCTestCase {
    @MainActor
    func testLaunchWithSeededDeviceLibraryShowsManagementWindow() throws
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend launch seams and UI smoke coverage for the polished native windows</name>
  <files>Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/WOLView.swift
    - Mac OS Swiss Knife/DeviceLibraryView.swift
    - Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
  </read_first>
  <action>Per D-07 through D-12 and the validation contract, add a second direct-launch automation seam for the WOL window. In `AppDelegate.swift`, extend `LaunchConfiguration` with the exact flag `--ui-test-open-wol-window`, switch the app to `.regular` activation when either window-launch flag is present, and open the requested retained window after the status controller is wired. In `Mac_OS_Swiss_KnifeUITests.swift`, keep the existing seeded manager smoke but expand it to assert the polished hierarchy identifiers added in Plan 02, including entering device-library form mode so the final smoke explicitly verifies `device-library-form-actions` and the form-secondary contract from D-12. That seeded manager smoke must prove the list remains primary before opening the form, then assert the form shows labels above controls and keeps validation or save-error feedback directly under the affected fields. Add the exact test `testLaunchWithWOLWindowShowsPolishedSections()` that launches with `--ui-test-open-wol-window` and asserts `wol-mode-group`, one of `wol-custom-mac-field` or `wol-saved-device-picker`, `wol-action-row`, and `wol-send-button`. Add a second manager test named `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState()` that launches the device-library window without seeded devices and asserts `device-library-empty-state` plus `device-library-empty-add-button`. Keep these tests structural; do not attempt screenshot or pixel assertions.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `--ui-test-open-wol-window`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `shouldOpenWOLWindow`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `openWOLWindow()`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `testLaunchWithWOLWindowShowsPolishedSections`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `wol-mode-group`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `device-library-top-actions`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `device-library-empty-add-button`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `device-library-form-actions`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithWOLWindowShowsPolishedSections' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState'</automated>
  </verify>
  <done>Phase 5 has direct UI-smoke coverage for both polished windows, including the WOL window and the device-library form mode that were previously not fully automation-addressable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manually approve the final native menu and utility-window polish</name>
  <files>none</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/WOLView.swift
    - Mac OS Swiss Knife/DeviceLibraryView.swift
  </read_first>
  <action>Review the already-built Phase 5 menu and windows against the exact visual contract below. Do not change code in this task; this step exists to confirm the native grouping and restraint that automated tests cannot judge.</action>
  <what_built>Phase 5 menu regrouping, status-density collapse, WOL window hierarchy polish, and device-library window hierarchy polish</what_built>
  <how-to-verify>
    1. Run the app from Xcode or `xcodebuild` and open the root menu in its idle state.
    2. Confirm the root menu reads as three groups with native separators only: keep-awake rows first, wake rows second, `管理设备…` plus `退出 Swiss Knife` last. Idle state should hide both status rows.
    3. Trigger an active state in each domain: start a timed keep-awake session and confirm only the keep-awake status row appears; trigger a WOL send and confirm only one truthful wake-status row appears in the wake section.
    4. Open the WOL window and confirm it remains compact, single-column, and restrained, with one heading, one visible input control, one optional status block, and one clear primary action.
    5. Open the device-library window with saved devices and confirm the list remains the primary anchor, the top action row feels deliberate, device rows have clearer hierarchy, and reorder mode does not mix drag affordances with edit/delete actions.
    6. Enter add/edit form mode in the device-library window and confirm the form still reads as secondary to the list, labels stay above their controls, validation or save-error feedback sits directly under the affected field, and the action row aligns with `device-library-form-actions`.
    7. If your local library can be emptied safely, reopen the manager with no saved devices and confirm the empty state is centered, restrained, and still points to `添加设备` as the only primary action.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the menu visually reads as keep-awake -> wake -> management
    - Human confirms idle state hides both status rows
    - Human confirms the WOL window remains compact and native with one clear primary action
    - Human confirms the device-library window remains list-first and restrained
    - Human confirms device-library form mode keeps labels above controls, validation directly under fields, and a clear `device-library-form-actions` action row
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual checkpoint follows successful Task 1 UI smoke execution"</automated>
  </verify>
  <done>The user explicitly approves the final visual contract or returns concrete issues for another execution pass.</done>
  <resume-signal>Type `approved` if the visual contract holds, or describe the issues that still violate Phase 5.</resume-signal>
</task>

</tasks>

<verification>
Run the Phase 5 UI-smoke slice first, then wait for explicit human approval. Treat the phase as complete only when the automated window checks pass, the device-library form mode is explicitly covered, and the human checkpoint confirms the menu and both windows match the native polish contract.
</verification>

<success_criteria>
Phase 5 is complete only when the polished windows, including device-library form mode, are automation-covered and the user explicitly approves the final native menu grouping and restrained utility-window feel.
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-menu-polish/05-03-SUMMARY.md`
</output>
