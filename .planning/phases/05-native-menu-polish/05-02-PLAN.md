---
phase: 05-native-menu-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife/WOLView.swift"
  - "Mac OS Swiss Knife/WOLWindow.swift"
  - "Mac OS Swiss Knife/DeviceLibraryView.swift"
  - "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "The WOL window feels like a compact native tool window with one clear heading, one visible input area, one optional status block, and one primary action."
    - "The device-library window remains list-first, with a stronger top action row, deliberate empty state, and clearer device-row hierarchy."
    - "Both windows expose stable accessibility hooks so a later UI-smoke plan can prove the hierarchy without pixel assertions."
  artifacts:
    - path: "Mac OS Swiss Knife/WOLView.swift"
      provides: "Polished WOL hierarchy and accessibility identifiers for mode group, status block, and action row"
      contains: "wol-mode-group"
    - path: "Mac OS Swiss Knife/WOLWindow.swift"
      provides: "Compact WOL utility-window shell tuned for the refined hierarchy"
      contains: "width: 460"
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "List-first manager hierarchy with stable action-row and form identifiers"
      contains: "device-library-top-actions"
    - path: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      provides: "Manager shell that stays compact while leaving room for the refined list-first layout"
      contains: "width: 520"
  key_links:
    - from: "Mac OS Swiss Knife/WOLWindow.swift"
      to: "Mac OS Swiss Knife/WOLView.swift"
      via: "retained AppKit shell hosts the refined WOL SwiftUI view without changing ownership"
      pattern: "NSHostingView\\(rootView: WOLView"
    - from: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "retained manager shell continues to host the list-first management view"
      pattern: "NSHostingView\\(rootView: DeviceLibraryView"
    - from: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      to: "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
      via: "list-first and form-secondary hierarchy still binds to the existing screen and reorder state"
      pattern: "session\\.screen|session\\.isReordering"
---

<objective>
Polish the Wake-on-LAN and device-library windows so they read as restrained native utility surfaces rather than shipping-era scaffolding.

Purpose: UX-04 requires stronger hierarchy and status cues across both windows without reopening any Phase 2, 3, or 4 behavior decisions.
Output: Refined WOL and device-library window layouts plus stable accessibility hooks for later UI smoke verification.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-menu-polish/05-CONTEXT.md
@.planning/phases/05-native-menu-polish/05-RESEARCH.md
@.planning/phases/05-native-menu-polish/05-UI-SPEC.md
@.planning/phases/05-native-menu-polish/05-VALIDATION.md
@.planning/phases/02-device-library-management/02-05-SUMMARY.md
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/WOLWindow.swift
@Mac OS Swiss Knife/DeviceLibraryView.swift
@Mac OS Swiss Knife/DeviceLibraryWindow.swift
@Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift

<interfaces>
Current WOL and manager view contracts:
```swift
struct WOLView: View {
    @ObservedObject var session: WOLSessionModel
    @ObservedObject var deviceLibrary: SavedDeviceLibraryStore
}

final class DeviceLibraryWindow: NSWindowController {
    init(session: DeviceLibrarySessionModel)
    func show()
}

struct DeviceLibraryView: View {
    @ObservedObject var session: DeviceLibrarySessionModel
}
```

Current session signals the views must preserve:
```swift
final class WOLSessionModel: ObservableObject {
    @Published var inputMode: InputMode
    @Published var selectedSavedDeviceID: UUID?
    @Published var customMac: String
    @Published var validation: ManualMACValidation
    @Published var sendState: WakeSendState
    var canSend: Bool { get }
    var isSending: Bool { get }
}

@MainActor
final class DeviceLibrarySessionModel: ObservableObject {
    @Published var devices: [SavedDevice]
    @Published var screen: DeviceLibraryScreen
    @Published var isReordering: Bool
    @Published var pendingDeleteDevice: SavedDevice?
    @Published var saveErrorMessage: String?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine the WOL utility window hierarchy and add UI-smoke identifiers</name>
  <files>Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLWindow.swift</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - Mac OS Swiss Knife/WOLView.swift
    - Mac OS Swiss Knife/WOLWindow.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
  </read_first>
  <action>Per D-07, D-08, and D-09, keep the WOL surface as one compact single-column utility window, but strengthen hierarchy with explicit section grouping instead of extra chrome. In `WOLView.swift`, keep the existing heading text `唤醒局域网设备 (WOL)`, use a 16pt major stack spacing baseline, expose the mode-selector row with `.accessibilityIdentifier("wol-mode-group")`, and ensure only one input control is visible at a time with identifiers `wol-custom-mac-field` and `wol-saved-device-picker`. Keep the status block directly above the action row, hide it entirely when `visibleStatusText == nil`, and tag it `wol-status-block`. Keep the trailing action row limited to `取消` and `发送唤醒包`, separated by 16pt, and tag it `wol-action-row`; tag the primary button `wol-send-button`. In `WOLWindow.swift`, retain the AppKit ownership pattern but adjust the shell size only as much as needed to fit the clearer hierarchy while preserving the 460pt width baseline. Do not add subtitles, cards, shadows, segmented-control rewrites, new input modes, or optimistic status copy.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-mode-group`
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-custom-mac-field`
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-saved-device-picker`
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-status-block`
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-action-row`
    - `Mac OS Swiss Knife/WOLView.swift` contains `wol-send-button`
    - `Mac OS Swiss Knife/WOLView.swift` contains `唤醒局域网设备 (WOL)`
    - `Mac OS Swiss Knife/WOLWindow.swift` contains `width: 460`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS'</automated>
  </verify>
  <done>The WOL window keeps its compact utility shape while exposing a clearer native hierarchy and deterministic automation hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Polish the device-library window without breaking the list-first interaction model</name>
  <files>Mac OS Swiss Knife/DeviceLibraryView.swift, Mac OS Swiss Knife/DeviceLibraryWindow.swift</files>
  <read_first>
    - .planning/phases/05-native-menu-polish/05-CONTEXT.md
    - .planning/phases/05-native-menu-polish/05-RESEARCH.md
    - .planning/phases/05-native-menu-polish/05-UI-SPEC.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - .planning/phases/02-device-library-management/02-05-SUMMARY.md
    - Mac OS Swiss Knife/DeviceLibraryView.swift
    - Mac OS Swiss Knife/DeviceLibraryWindow.swift
    - Mac OS Swiss Knife/DeviceLibrarySessionModel.swift
    - Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift
  </read_first>
  <action>Per D-10, D-11, and D-12, keep the manager list-first and preserve the Phase 2 scroll-stack-versus-`List` split, but strengthen hierarchy inside `DeviceLibraryView.swift`. Add a stable top action-row identifier `device-library-top-actions` around the title + reorder + add controls. Keep the empty state centered with the existing presentation copy, expose the primary empty-state button as `device-library-empty-add-button`, and avoid adding any new explanatory chrome. In populated list mode, increase row breathing room to the UI-spec target, keep the device name as the strongest line, keep the MAC line monospaced, and keep the note as one-line secondary support. In reorder mode, show only the drag affordance instead of edit/delete actions. In form mode, keep labels above controls, leave validation directly under fields, and tag the action row `device-library-form-actions`. Touch `DeviceLibraryWindow.swift` only if a minimal shell-size or title treatment adjustment is needed to support the refined spacing. Do not replace the current list-first flow with a split view, do not add inline row editing, and do not remove the existing `device-library-list`, `device-library-empty-state`, or `device-row-*` identifiers.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-library-top-actions`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-library-empty-add-button`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-library-form-actions`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-library-list`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-library-empty-state`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `device-row-`
    - `Mac OS Swiss Knife/DeviceLibraryWindow.swift` contains `width: 520`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow'</automated>
  </verify>
  <done>The device-library window feels more deliberate and native while staying list-first, reorder-safe, and automation-visible.</done>
</task>

</tasks>

<verification>
Compile the app after the WOL window hierarchy change, then rerun the seeded device-library UI smoke after the manager polish. Treat the plan as complete only when the identifiers needed for Phase 5 automation exist and the manager smoke still passes.
</verification>

<success_criteria>
UX-04 is implemented in both native windows: the WOL and device-library surfaces are clearer, more restrained, and still grounded in the existing AppKit/SwiftUI ownership model.
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-menu-polish/05-02-SUMMARY.md`
</output>
