---
phase: 15-device-library-ui-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01-PLAN.md"
files_modified:
  - "Tools Cat/DeviceLibraryView.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - DEVS-08
  - DEVS-09
must_haves:
  truths:
    - "设备行里的 `编辑` 与 `删除` 在视觉上要一眼可区分：`编辑` 使用应用强调色语义，`删除` 使用 destructive 红色语义。"
    - "新的动作语义必须保持克制和原生，不把 `设备库` 工具窗口做成装饰性很强的界面。"
    - "动作样式和列表润色完成后，既有 add/edit/delete/reorder/direct-launch 行为与 copy seam 不能回退。"
  artifacts:
    - path: "Tools Cat/DeviceLibraryView.swift"
      provides: "设备行的语义编辑/删除动作样式"
      contains: "Button(\"编辑\""
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "设备库窗口抛光后的 UI 烟雾保护"
      contains: "testLaunchWithSeededDeviceLibraryShowsManagementWindow"
    - path: "Tools CatTests/DeviceLibrarySessionModelTests.swift"
      provides: "设备 CRUD 与排序真值回归"
      contains: "testReorderPersistsCanonicalOrder"
  key_links:
    - from: "Tools Cat/DeviceLibraryView.swift"
      to: "Tools Cat/KeepAwakeDurationManagementView.swift"
      via: "Phase 15 的语义按钮样式应直接跟随时长管理面的 accent/destructive 约定"
      pattern: "foregroundStyle\\(Color\\.accentColor\\)|role: \\.destructive"
    - from: "Tools Cat/DeviceLibraryView.swift"
      to: "Tools CatTests/DeviceLibrarySessionModelTests.swift"
      via: "动作语义只改表现层，不能改变现有 session 真值"
      pattern: "saveDraft|requestDelete|confirmDelete|moveDevices"
---

<objective>
Finish the device-library parity pass by making row actions semantically obvious and re-locking the focused regression slice around the polished manager surface.

Purpose: After the manager uses the correct list and sheet structure, the remaining parity gap is action semantics. Users should immediately recognize edit as the safe accent action and delete as destructive red, while the existing device-library truth remains intact.
Output: A native device-library manager whose row actions match the duration manager semantically and whose focused UI/session regressions still pass unchanged.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-device-library-ui-parity/15-CONTEXT.md
@.planning/phases/15-device-library-ui-parity/15-RESEARCH.md
@.planning/phases/15-device-library-ui-parity/15-VALIDATION.md
@.planning/phases/15-device-library-ui-parity/15-01-PLAN.md
@Tools Cat/Assets.xcassets/AccentColor.colorset/Contents.json
@Tools Cat/DeviceLibraryView.swift
@Tools CatTests/DeviceLibrarySessionModelTests.swift
@Tools CatTests/DeviceLibraryManagementPresentationTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply semantic edit/delete styling and tighten the focused device-library regression slice</name>
  <files>Tools Cat/DeviceLibraryView.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/15-device-library-ui-parity/15-CONTEXT.md
    - .planning/phases/15-device-library-ui-parity/15-RESEARCH.md
    - .planning/phases/15-device-library-ui-parity/15-01-PLAN.md
    - Tools Cat/Assets.xcassets/AccentColor.colorset/Contents.json
    - Tools Cat/DeviceLibraryView.swift
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools CatTests/DeviceLibrarySessionModelTests.swift
    - Tools CatTests/DeviceLibraryManagementPresentationTests.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: Device rows show `编辑` with accent-color semantics and `删除` with destructive red semantics.
    - Test 2: The polished manager still opens from the direct-launch path, still shows seeded devices and empty state correctly, and still keeps the list present during add/edit.
    - Test 3: Existing device-library session and presentation seams stay green, proving the UI polish did not reopen CRUD, reorder, or copy truth.
  </behavior>
  <action>Refine the row action controls in `Tools Cat/DeviceLibraryView.swift` so the non-reordering `编辑` button uses the same accent-color semantics as `KeepAwakeDurationManagementView.swift`, for example `.foregroundStyle(Color.accentColor)` on the existing borderless button, while `删除` remains a native `role: .destructive` action with restrained styling. Keep the layout compact: do not add custom badges, backgrounds, or extra chrome around the actions. If the shift to a native list or shared sheet changed any focus or accessibility behavior, update `Tools CatUITests/Tools_CatUITests.swift` only enough to keep the existing device-library smokes deterministic, including assertions that the live list surface still exists when the form opens and that the polished populated view remains reachable after direct launch. Do not move CRUD logic out of the existing session callbacks, do not rename existing presentation copy without necessity, and prove the polish did not regress shipped behavior by rerunning `DeviceLibrarySessionModelTests`, `DeviceLibraryManagementPresentationTests`, `testLaunchWithSeededDeviceLibraryShowsManagementWindow`, `testLaunchWithSeededDeviceLibraryShowsManagementListSurface`, and `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState`.</action>
  <acceptance_criteria>
    - `Tools Cat/DeviceLibraryView.swift` contains `Button("编辑"`
    - `Tools Cat/DeviceLibraryView.swift` styles the edit control with `.foregroundStyle(Color.accentColor)` or another explicit accent-color semantic
    - `Tools Cat/DeviceLibraryView.swift` contains `Button("删除", role: .destructive`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededDeviceLibraryShowsManagementWindow`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededDeviceLibraryShowsManagementListSurface`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState`
    - `Tools CatTests/DeviceLibrarySessionModelTests.swift` still contains `testReorderPersistsCanonicalOrder`
    - `Tools CatTests/DeviceLibraryManagementPresentationTests.swift` still contains `testDeleteConfirmationIncludesDeviceName`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/DeviceLibrarySessionModelTests' -only-testing:'Tools CatTests/DeviceLibraryManagementPresentationTests' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededDeviceLibraryShowsManagementListSurface' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState'</automated>
  </verify>
  <done>The device-library manager now communicates edit versus delete intent immediately while the existing direct-launch, CRUD, reorder, and copy seams remain unchanged.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when edit/delete semantics are visually obvious, the manager remains compact and native, and the focused device-library regression slice proves no behavior drift.
</verification>

<success_criteria>
Phase 15 finishes with a device-library manager that feels like the same product family as the shipped duration manager without reopening saved-device truth.
</success_criteria>

<output>
After completion, create `.planning/phases/15-device-library-ui-parity/15-02-SUMMARY.md`
</output>
