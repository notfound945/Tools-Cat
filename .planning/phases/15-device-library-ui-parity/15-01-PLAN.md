---
phase: 15-device-library-ui-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/DeviceLibraryView.swift"
  - "Tools Cat/DeviceLibrarySessionModel.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - DEVS-06
  - DEVS-07
must_haves:
  truths:
    - "已保存 WOL 设备的普通浏览态使用明显的原生 macOS `List` 语义，而不是继续停留在 `ScrollView` + `LazyVStack` 的自绘堆叠表面。"
    - "添加和编辑通过覆盖在列表上的共享 sheet 完成，列表上下文保持可见，不再整页切换到独立表单视图。"
    - "重新排序仍然保持独立模式，进入 add/edit 时仍会退出排序模式，避免把 Phase 2 的行为真值一起改掉。"
  artifacts:
    - path: "Tools Cat/DeviceLibraryView.swift"
      provides: "设备库管理窗口的原生列表与共享 sheet 壳层"
      contains: ".sheet("
    - path: "Tools Cat/DeviceLibrarySessionModel.swift"
      provides: "驱动共享 sheet 的表单模式状态，而不是整页 screen 切换"
      contains: "currentFormMode"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "直接启动设备库窗口后的列表与 sheet 流程烟雾测试"
      contains: "testLaunchWithSeededDeviceLibraryShowsManagementWindow"
  key_links:
    - from: "Tools Cat/DeviceLibraryView.swift"
      to: "Tools Cat/KeepAwakeDurationManagementView.swift"
      via: "设备库管理面需要显式复用时长管理面已经出货的 retained-shell + list + shared-sheet 结构"
      pattern: "sheet\\(|List\\(|formSheetContent"
    - from: "Tools Cat/DeviceLibrarySessionModel.swift"
      to: "Tools CatTests/DeviceLibrarySessionModelTests.swift"
      via: "UI 改为 sheet 后，add/edit/delete/reorder 真值仍必须由现有 session 测试锁定"
      pattern: "beginAdd|beginEdit|saveDraft|moveDevices"
---

<objective>
Refactor the device-library manager so the normal populated view uses a native macOS list and add/edit flows present through a shared sheet over the retained list surface.

Purpose: Phase 15 exists because the current device-library UI still swaps the whole view between list and form, while the shipped duration manager already established the desired compact retained-shell pattern. This plan aligns the underlying surface shape first without yet spending scope on semantic action styling.
Output: A `设备库` manager that keeps the list visible, shows add/edit in a shared sheet, and preserves the shipped reorder and CRUD truth.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-device-library-ui-parity/15-CONTEXT.md
@.planning/phases/15-device-library-ui-parity/15-RESEARCH.md
@.planning/phases/15-device-library-ui-parity/15-VALIDATION.md
@.planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
@Tools Cat/DeviceLibraryView.swift
@Tools Cat/DeviceLibrarySessionModel.swift
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools CatTests/DeviceLibrarySessionModelTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace full-view list/form swapping with a native list plus shared sheet presentation</name>
  <files>Tools Cat/DeviceLibraryView.swift, Tools Cat/DeviceLibrarySessionModel.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/15-device-library-ui-parity/15-CONTEXT.md
    - .planning/phases/15-device-library-ui-parity/15-RESEARCH.md
    - .planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
    - Tools Cat/DeviceLibraryView.swift
    - Tools Cat/DeviceLibrarySessionModel.swift
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools CatTests/DeviceLibrarySessionModelTests.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: Opening the seeded device library shows a native list-backed populated surface instead of the current normal-mode `ScrollView` + `LazyVStack` shell.
    - Test 2: Clicking `添加设备` or a row `编辑` action keeps the underlying list surface present and shows one shared sheet-backed form over it.
    - Test 3: Empty state remains unchanged, and entering add/edit still exits reorder mode before the form appears.
    - Test 4: Session truth for invalid save, add normalization, edit persistence, delete confirmation, and reorder persistence remains intact.
  </behavior>
  <action>Refactor `Tools Cat/DeviceLibraryView.swift` so `body` no longer switches between `.list` and `.form` views. Keep `listContent` as the always-visible manager shell, keep the current empty state, and add a `.sheet(isPresented:)` binding driven by a new `formSheetIsPresented` binding that maps to `session.currentFormMode != nil`. Add a `formSheetContent` builder similar to the duration manager so the existing `formContent(mode:)` renders inside a shared sheet with compact padding and a deterministic accessibility seam such as `device-library-form-sheet`, while preserving `device-library-form-actions`. Replace the normal non-reordering branch inside `populatedListContent` with a native SwiftUI `List` that still renders each `device-row-<uuid>` marker and keeps `device-library-list` as the list-level accessibility identifier; do not change the explicit reorder-mode `List { ... }.onMove(...)` behavior beyond keeping it compatible with the new surrounding shell. In `Tools Cat/DeviceLibrarySessionModel.swift`, remove the full-screen `screen` route as the source of presentation truth and make add/edit/cancel/save drive sheet presentation through `currentFormMode` and a boolean-compatible form state while preserving the existing draft fields, validation messages, delete flow, and `isReordering = false` guardrail on add/edit. Update `Tools CatUITests/Tools_CatUITests.swift` so the seeded device-library smoke proves the list surface remains present when the add form opens, waits for `device-library-form-sheet` or `device-library-form-actions`, and continues asserting the seeded rows plus add flow through `--ui-test-open-device-library`. Do not change saved-device persistence, delete confirmation copy, reorder semantics, or direct-launch entry wiring.</action>
  <acceptance_criteria>
    - `Tools Cat/DeviceLibraryView.swift` contains `.sheet(isPresented: formSheetIsPresented)`
    - `Tools Cat/DeviceLibraryView.swift` contains `device-library-form-sheet`
    - `Tools Cat/DeviceLibraryView.swift` contains `List {`
    - `Tools Cat/DeviceLibraryView.swift` no longer contains `switch session.screen`
    - `Tools Cat/DeviceLibraryView.swift` no longer contains `ScrollView {` inside the normal populated browse path
    - `Tools Cat/DeviceLibrarySessionModel.swift` contains `var currentFormMode: DeviceLibraryFormMode?`
    - `Tools Cat/DeviceLibrarySessionModel.swift` contains `isReordering = false` in both `beginAdd` and `beginEdit`
    - `Tools CatUITests/Tools_CatUITests.swift` contains `device-library-form-sheet`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededDeviceLibraryShowsManagementWindow`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededDeviceLibraryShowsManagementListSurface`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/DeviceLibrarySessionModelTests' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededDeviceLibraryShowsManagementListSurface'</automated>
  </verify>
  <done>The device-library manager now keeps its list shell visible, uses a native populated list surface, and presents add/edit through a shared compact sheet without regressing the existing session truth.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the populated device-library browse surface is native-list-first, add/edit is sheet-backed over the list, and the focused session plus direct-launch UI smoke still prove the existing CRUD and reorder truth.
</verification>

<success_criteria>
Phase 15 starts from the correct manager shape: the device library is no longer a two-screen list/form workflow and now matches the shipped duration manager's retained-shell interaction pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/15-device-library-ui-parity/15-01-SUMMARY.md`
</output>
