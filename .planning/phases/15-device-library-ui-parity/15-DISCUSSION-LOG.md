# Phase 15: Device Library UI Parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-16
**Phase:** 15-device-library-ui-parity
**Areas discussed:** add/edit presentation parity, reorder mode coexistence

---

## Add/Edit Presentation Parity

| Option | Description | Selected |
|--------|-------------|----------|
| Shared sheet over list | Match the duration manager exactly: keep the list visible and present one shared add/edit sheet above it. | ✓ |
| Full-view screen swap | Keep the current device-library pattern where add/edit replaces the whole list view. | |

**User's choice:** `1 要`
**Notes:** User confirmed the device-library add/edit flow should match the duration manager's list-local shared sheet pattern rather than the older full-screen form swap.

---

## Reorder Mode Coexistence

| Option | Description | Selected |
|--------|-------------|----------|
| Keep reorder mode unchanged | Leave the explicit separate reorder mode as-is and limit this phase to browse/add/edit/delete style parity. | ✓ |
| Redesign reorder with the rest of the UI | Reopen how sorting mode is presented so it changes together with the normal list UI. | |

**User's choice:** `2 保持不变`
**Notes:** User explicitly kept reorder out of the parity redesign so the phase remains presentation-only.

---

## the agent's Discretion

- Exact native list framing details such as count captions, row density, and insets.
- Exact sheet dimensions and final spacing of the shared add/edit form.

## Deferred Ideas

None.
