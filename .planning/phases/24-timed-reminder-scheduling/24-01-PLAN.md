---
phase: 24-timed-reminder-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/KeepAwakeReminderScheduling.swift"
  - "Tools Cat/AppDelegate.swift"
  - "Tools Cat/KeepAwakeSessionModel.swift"
  - "Tools CatTests/AppDelegateNotificationTests.swift"
  - "Tools CatTests/KeepAwakeSessionModelTests.swift"
  - "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-04

must_haves:
  truths:
    - "Normal app launch requests local-notification authorization for timed keep-awake reminders without blocking menu-bar startup or becoming a prerequisite for keep-awake use."
    - "Starting a timed keep-awake session with more than `2 分钟` remaining schedules exactly one pending local reminder for about two minutes before that confirmed session ends."
    - "Starting a timed keep-awake session with `2 分钟` or less remaining leaves no pending pre-expiry reminder."
    - "Replacing a timed session, stopping it successfully, or switching to `无限常亮` leaves only the current confirmed session eligible to notify later."
    - "If reminder scheduling is unavailable, timed keep-awake still enters the confirmed timed state and the existing keep-awake status row carries the reminder-unavailable message instead of a new UI surface."
  artifacts:
    - path: "Tools Cat/KeepAwakeReminderScheduling.swift"
      provides: "Launch-time authorization and pending reminder scheduling/cancellation contract around `UNUserNotificationCenter`"
      contains: "protocol KeepAwakeReminderScheduling"
    - path: "Tools Cat/AppDelegate.swift"
      provides: "Launch-time reminder authorization wiring and shared reminder scheduler injection into the keep-awake session"
      contains: "keepAwakeReminderScheduler.requestAuthorizationAtLaunch()"
    - path: "Tools Cat/KeepAwakeSessionModel.swift"
      provides: "Session-scoped pre-expiry reminder ownership, skip rules, stale cancellation, and non-blocking unavailable-state messaging"
      contains: "keep-awake.session."
    - path: "Tools CatTests/AppDelegateNotificationTests.swift"
      provides: "Focused launch-time authorization regression coverage through an injected fake reminder scheduler"
      contains: "testApplicationDidFinishLaunchingRequestsReminderAuthorizationOnce"
    - path: "Tools CatTests/KeepAwakeSessionModelTests.swift"
      provides: "Deterministic unit coverage for schedule, skip, replace, cancel, and unavailable reminder outcomes"
      contains: "testTimedSessionLongerThanTwoMinutesSchedulesOnePreExpiryReminder"
    - path: "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
      provides: "Controller regression proving reminder-unavailable copy reuses the existing keep-awake status row while timed mode stays active"
      contains: "testReminderPermissionUnavailableReusesKeepAwakeStatusRow"
  key_links:
    - from: "Tools Cat/AppDelegate.swift"
      to: "Tools Cat/KeepAwakeReminderScheduling.swift"
      via: "launch-time requestAuthorizationAtLaunch call plus shared service injection"
      pattern: "requestAuthorizationAtLaunch\\(\\)|reminderScheduler: keepAwakeReminderScheduler"
    - from: "Tools Cat/KeepAwakeSessionModel.swift"
      to: "Tools Cat/KeepAwakeReminderScheduling.swift"
      via: "schedulePreExpiryReminder and cancelPendingReminder for one session-scoped identifier"
      pattern: "schedulePreExpiryReminder|cancelPendingReminder|keep-awake\\.session\\."
    - from: "Tools Cat/KeepAwakeSessionModel.swift"
      to: "Tools Cat/StatusBarController.swift"
      via: "existing message -> KeepAwakePresentation.statusText -> keepAwakeStatusItem rendering path"
      pattern: "message =|statusText|keepAwakeStatusItem\\.title"
---

<objective>
Add the Phase 24 scheduling layer for timed keep-awake reminders: request notification authorization at launch, schedule one truthful pre-expiry reminder only for eligible timed sessions, and cancel stale pending reminders whenever the confirmed timed session changes.

Purpose: Implement D-01 through D-11 for the pre-expiry reminder path only, keeping notification behavior tied to the existing `KeepAwakeSessionModel` lifecycle instead of creating a second keep-awake truth source or broader notification UI.
Output: One `UNUserNotificationCenter`-backed reminder scheduling seam, launch-time authorization wiring in `AppDelegate`, session-scoped reminder orchestration in `KeepAwakeSessionModel`, and focused XCTest coverage proving authorization, schedule/skip behavior, stale cancellation, and non-blocking unavailable-state messaging.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-timed-reminder-scheduling/24-CONTEXT.md
@.planning/phases/24-timed-reminder-scheduling/24-RESEARCH.md
@.planning/phases/24-timed-reminder-scheduling/24-VALIDATION.md
@.planning/phases/04-timed-keep-awake/04-01-SUMMARY.md
@.planning/phases/04-timed-keep-awake/04-02-SUMMARY.md
@.planning/phases/22-wol-result-timeout/22-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@Tools Cat/AppDelegate.swift
@Tools Cat/KeepAwakeSessionModel.swift
@Tools Cat/KeepAwakePresentation.swift
@Tools Cat/StatusBarController.swift
@Tools Cat/PowerAssertionManager.swift
@Tools CatTests/KeepAwakeSessionModelTests.swift
@Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift

<interfaces>
From `Tools Cat/AppDelegate.swift`:
```swift
func applicationDidFinishLaunching(_ notification: Notification) {
    configureSharedStores()

    let status = StatusBarController(
        deviceLibrary: savedDeviceLibrary,
        wolSession: wolSession,
        keepAwakeSession: keepAwakeSession,
        keepAwakeDurationStore: keepAwakeDurationStore
    )
}
```

From `Tools Cat/KeepAwakeSessionModel.swift`:
```swift
enum KeepAwakeMode: Equatable {
    case off
    case indefinite
    case timed(duration: ManagedKeepAwakeDuration, endDate: Date)
}

init(
    powerController: KeepAwakePowerControlling,
    scheduler: KeepAwakeCountdownScheduling,
    nowProvider: @escaping () -> Date = Date.init
)

func startTimed(_ duration: ManagedKeepAwakeDuration)
func stop(completion: (() -> Void)? = nil)
```

From `Tools Cat/KeepAwakePresentation.swift` and `Tools Cat/StatusBarController.swift`:
```swift
var statusText: String? {
    if let pendingAction { ... }
    if let message, !message.isEmpty { return message }
    ...
}

keepAwakeStatusItem.title = presentation.statusText ?? ""
keepAwakeStatusItem.isHidden = presentation.statusText == nil
```

From `Tools Cat/PowerAssertionManager.swift`:
```swift
protocol KeepAwakePowerControlling {
    var isEnabled: Bool { get }
    func setKeepAwakeEnabled(
        _ enabled: Bool,
        completion: @escaping (KeepAwakeToggleOutcome) -> Void
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reminder scheduling contract and request authorization during normal app launch</name>
  <files>Tools Cat/KeepAwakeReminderScheduling.swift, Tools Cat/AppDelegate.swift, Tools Cat/KeepAwakeSessionModel.swift, Tools CatTests/AppDelegateNotificationTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/24-timed-reminder-scheduling/24-CONTEXT.md
    - .planning/phases/24-timed-reminder-scheduling/24-RESEARCH.md
    - .planning/phases/24-timed-reminder-scheduling/24-VALIDATION.md
    - .planning/phases/04-timed-keep-awake/04-02-SUMMARY.md
    - Tools Cat/AppDelegate.swift
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools Cat/PowerAssertionManager.swift
  </read_first>
  <behavior>
    - Test 1: `testApplicationDidFinishLaunchingRequestsReminderAuthorizationOnce` proves normal app launch calls the injected reminder scheduler exactly once.
    - Test 2: `testApplicationDidFinishLaunchingUsesInjectedReminderSchedulerWithoutRealSystemPrompt` proves the launch test stays on a fake reminder scheduler and does not talk to `UNUserNotificationCenter.current()` directly.
  </behavior>
  <action>
    Create `Tools Cat/KeepAwakeReminderScheduling.swift` per D-03 and D-04 with these exact contracts and no third-party wrappers:

    - `protocol KeepAwakeReminderScheduling`
    - `enum KeepAwakeReminderScheduleResult: Equatable { case scheduled, permissionUnavailable, failed }`
    - `func requestAuthorizationAtLaunch()`
    - `func schedulePreExpiryReminder(identifier: String, fireAfter: TimeInterval, title: String, body: String, completion: @escaping @MainActor (KeepAwakeReminderScheduleResult) -> Void)`
    - `func cancelPendingReminder(identifier: String)`
    - `struct UserNotificationKeepAwakeReminderScheduler: KeepAwakeReminderScheduling`
    - `struct NoopKeepAwakeReminderScheduler: KeepAwakeReminderScheduling`

    In the production adapter, import `UserNotifications`, call `requestAuthorization(options: [.alert, .sound])` in `requestAuthorizationAtLaunch()`, and keep it fire-and-forget. In `schedulePreExpiryReminder(...)`, first call `getNotificationSettings`; only `.authorized` may proceed to `UNNotificationRequest`. Treat every other status as `.permissionUnavailable` so reminder truth stays conservative after the launch-time prompt. When authorized, create one nonrepeating `UNTimeIntervalNotificationTrigger(timeInterval: fireAfter, repeats: false)` and complete on `MainActor` with `.scheduled` or `.failed`. `cancelPendingReminder(identifier:)` must call `removePendingNotificationRequests(withIdentifiers: [identifier])`. Do not remove delivered notifications and do not add any expiry reminder logic per D-09 and D-10.

    Update `KeepAwakeSessionModel` only enough for compile-safe injection in this task: add a stored `reminderScheduler: KeepAwakeReminderScheduling` dependency with default `NoopKeepAwakeReminderScheduler()`, thread it through the designated and convenience initializers, and leave the actual scheduling behavior for Task 2.

    Update `AppDelegate.swift` so one shared reminder scheduler is created and reused:
    - add `private var keepAwakeReminderScheduler: KeepAwakeReminderScheduling!`
    - add one testable injection seam named `makeKeepAwakeReminderScheduler` that defaults to `UserNotificationKeepAwakeReminderScheduler`
    - build `keepAwakeReminderScheduler` inside `configureSharedStores()`
    - pass it into `KeepAwakeSessionModel(... reminderScheduler: keepAwakeReminderScheduler ...)`
    - call `keepAwakeReminderScheduler.requestAuthorizationAtLaunch()` immediately after `configureSharedStores()` in `applicationDidFinishLaunching(_:)` per D-03

    Add `Tools CatTests/AppDelegateNotificationTests.swift` with a fake reminder scheduler that records `requestAuthorizationAtLaunch()` calls and uses an isolated `UserDefaults` suite so the launch test does not touch real app defaults. Do not manually edit `Tools Cat.xcodeproj/project.pbxproj` unless `xcodebuild` does not pick up the new test file through the synchronized group.
  </action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `protocol KeepAwakeReminderScheduling`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `enum KeepAwakeReminderScheduleResult: Equatable { case scheduled, permissionUnavailable, failed }`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `requestAuthorization(options: [.alert, .sound])`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `getNotificationSettings`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `removePendingNotificationRequests(withIdentifiers: [identifier])`.
    - `Tools Cat/AppDelegate.swift` contains `private var keepAwakeReminderScheduler: KeepAwakeReminderScheduling!`.
    - `Tools Cat/AppDelegate.swift` contains `makeKeepAwakeReminderScheduler`.
    - `Tools Cat/AppDelegate.swift` contains `keepAwakeReminderScheduler.requestAuthorizationAtLaunch()`.
    - `Tools Cat/AppDelegate.swift` contains `reminderScheduler: keepAwakeReminderScheduler`.
    - `Tools CatTests/AppDelegateNotificationTests.swift` contains `testApplicationDidFinishLaunchingRequestsReminderAuthorizationOnce`.
    - `Tools CatTests/AppDelegateNotificationTests.swift` contains `testApplicationDidFinishLaunchingUsesInjectedReminderSchedulerWithoutRealSystemPrompt`.
    - The focused launch-authorization test command exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/AppDelegateNotificationTests'</automated>
  </verify>
  <done>The app has one reusable reminder scheduling abstraction, `AppDelegate` requests notification authorization during normal launch through an injected shared scheduler, and a focused XCTest slice proves launch-time authorization without a real system prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make pre-expiry reminders session-scoped, skippable under two minutes, and cancellable only on confirmed session changes</name>
  <files>Tools Cat/KeepAwakeSessionModel.swift, Tools CatTests/KeepAwakeSessionModelTests.swift, Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/24-timed-reminder-scheduling/24-CONTEXT.md
    - .planning/phases/24-timed-reminder-scheduling/24-RESEARCH.md
    - .planning/phases/24-timed-reminder-scheduling/24-VALIDATION.md
    - .planning/phases/04-timed-keep-awake/04-01-SUMMARY.md
    - .planning/phases/04-timed-keep-awake/04-02-SUMMARY.md
    - .planning/phases/22-wol-result-timeout/22-01-SUMMARY.md
    - Tools Cat/KeepAwakeReminderScheduling.swift
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools Cat/KeepAwakePresentation.swift
    - Tools Cat/StatusBarController.swift
    - Tools CatTests/KeepAwakeSessionModelTests.swift
    - Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
  </read_first>
  <behavior>
    - Test 1: `testTimedSessionLongerThanTwoMinutesSchedulesOnePreExpiryReminder` proves a confirmed `15 分钟` timed session schedules one reminder with identifier prefix `keep-awake.session.`, title `常亮即将结束`, body `2 分钟后将关闭常亮`, and fire delay `780`.
    - Test 2: `testTimedSessionAtOrBelowTwoMinutesSkipsPreExpiryReminder` proves a confirmed `120` second timed session schedules nothing and leaves no pending reminder identifier.
    - Test 3: `testReplacingTimedSessionWithSameDurationCancelsPreviousReminderAfterConfirmation` proves a second confirmed timed session with the same duration cancels the old identifier and schedules a new identifier instead of reusing the previous one per D-08.
    - Test 4: `testFailedTimedReplacementKeepsPreviousReminderIdentifier` proves a failed replacement attempt does not cancel the still-confirmed session's reminder.
    - Test 5: `testStoppingTimedSessionOrSwitchingToIndefiniteCancelsActiveReminder` proves confirmed stop and confirmed switch to `无限常亮` both cancel the active reminder identifier.
    - Test 6: `testReminderPermissionUnavailableDoesNotBlockTimedMode` proves a permission-unavailable scheduling result still leaves `confirmedMode == .timed(...)` and sets the existing message surface to `提醒不可用：通知权限未开启`.
    - Test 7: `testReminderPermissionUnavailableReusesKeepAwakeStatusRow` proves the controller keeps the timed row checked while `keepAwakeStatusItem.title == "提醒不可用：通知权限未开启"` and no new menu rows are added.
  </behavior>
  <action>
    Extend `KeepAwakeSessionModel` per D-01, D-02, D-05, D-06, D-07, D-08, and D-11 so reminder ownership stays inside the confirmed timed-session lifecycle:

    - add `private let reminderScheduler: KeepAwakeReminderScheduling`
    - add `private var activeTimedReminderSessionID: UUID?`
    - add `private var activeTimedReminderIdentifier: String?`
    - add a helper that returns `keep-awake.session.<UUID>.pre-expiry`
    - add one constant lead time of `120` seconds

    Use these exact reminder strings for the pre-expiry notification:
    - title: `常亮即将结束`
    - body: `2 分钟后将关闭常亮`

    Keep reminder side effects aligned with confirmed state transitions, not pending clicks:
    - do not schedule on menu click before `handleEnableOutcome(...)` confirms the timed mode
    - do not cancel the current reminder when a replacement or stop merely becomes pending
    - mutate reminder state only inside `handleEnableOutcome(...)`, `handleStopOutcome(...)`, and any shared helper they call after the underlying keep-awake outcome is known

    On confirmed timed success:
    - generate a fresh session UUID even if the duration is unchanged per D-08
    - compute `fireAfter = endDate.timeIntervalSince(now) - 120`
    - if `fireAfter <= 0`, clear any previous active reminder identifier and skip scheduling per D-06
    - if `fireAfter > 0`, cancel the previous active identifier only after the new timed mode is confirmed, then schedule exactly one reminder through `reminderScheduler.schedulePreExpiryReminder(...)`

    On confirmed switch away from timed truth:
    - cancel and clear the active reminder identifier when the session confirms `.off` or `.indefinite`
    - do not add end-of-session notifications, do not add settings toggles, and do not remove delivered notifications per D-09 and D-10

    Handle reminder scheduling results without blocking keep-awake:
    - `.scheduled` keeps `message = nil`
    - `.permissionUnavailable` leaves the confirmed timed mode intact and sets `message = "提醒不可用：通知权限未开启"` per D-01 and D-02
    - `.failed` leaves the confirmed timed mode intact and sets `message = "提醒不可用：无法安排提醒"`
    - ignore stale completion callbacks whose session UUID no longer matches `activeTimedReminderSessionID`

    Update `Tools CatTests/KeepAwakeSessionModelTests.swift` with a deterministic `FakeKeepAwakeReminderScheduler` that records `requestedAuthorizationCount`, scheduled requests `{ identifier, fireAfter, title, body }`, completion handlers, and `canceledIdentifiers`. Reuse the existing fake power controller and fake countdown scheduler. Add the exact unit-test method names from the behavior list. In the replacement test, use the same `15 分钟` duration twice so the assertion really proves session-scoped identity instead of duration-scoped identity.

    Extend `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` with `testReminderPermissionUnavailableReusesKeepAwakeStatusRow`. Build the controller fixture with the fake reminder scheduler, complete a timed start successfully, then trigger the fake reminder scheduler completion with `.permissionUnavailable`. Assert:
    - the chosen timed row stays `.on`
    - `keepAwakeOffItem.isHidden == false`
    - `keepAwakeStatusItem.title == "提醒不可用：通知权限未开启"`
    - the visible keep-awake action titles remain exactly `["无限常亮", "15 分钟", "30 分钟", "1 小时", "2 小时", "关闭常亮"]`

    Do not modify `KeepAwakePresentation` or `StatusBarController` unless a targeted test proves the existing `message -> statusText -> keepAwakeStatusItem` path is insufficient. The locked decision is to reuse that surface, not to add another one.
  </action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `private var activeTimedReminderSessionID: UUID?`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `private var activeTimedReminderIdentifier: String?`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `keep-awake.session.`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `fireAfter`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `reminderScheduler.schedulePreExpiryReminder(`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `reminderScheduler.cancelPendingReminder(identifier:`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `常亮即将结束`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `2 分钟后将关闭常亮`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `提醒不可用：通知权限未开启`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `提醒不可用：无法安排提醒`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `FakeKeepAwakeReminderScheduler`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testTimedSessionLongerThanTwoMinutesSchedulesOnePreExpiryReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testTimedSessionAtOrBelowTwoMinutesSkipsPreExpiryReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testReplacingTimedSessionWithSameDurationCancelsPreviousReminderAfterConfirmation`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testFailedTimedReplacementKeepsPreviousReminderIdentifier`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testStoppingTimedSessionOrSwitchingToIndefiniteCancelsActiveReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testReminderPermissionUnavailableDoesNotBlockTimedMode`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testReminderPermissionUnavailableReusesKeepAwakeStatusRow`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `提醒不可用：通知权限未开启`.
    - The focused keep-awake reminder test slice exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeSessionModelTests' -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'</automated>
  </verify>
  <done>Timed keep-awake now owns one session-scoped pre-expiry reminder path that schedules only when more than two minutes remain, skips misleading reminders for shorter sessions, cancels stale pending reminders only when the confirmed session changes, and keeps reminder-unavailable states visible through the existing keep-awake status row without blocking timed keep-awake.</done>
</task>

</tasks>

<verification>
Run the task-level focused command after each task.

Before writing the phase summary, run the full Phase 24 automated slice:
`xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/AppDelegateNotificationTests' -only-testing:'Tools CatTests/KeepAwakeSessionModelTests' -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'`

If `AppDelegateNotificationTests.swift` is not automatically discovered by the synchronized Xcode group, add only the minimum project-file registration needed for that file and rerun the same command.
</verification>

<success_criteria>
`NOTF-01`, `NOTF-02`, and `NOTF-04` are complete when app launch requests reminder authorization, confirmed timed sessions longer than two minutes schedule exactly one pre-expiry local notification two minutes before end, shorter timed sessions schedule none, confirmed replacements/stops/indefinite switches leave no stale pending reminder behind, and reminder-unavailable outcomes stay visible through the existing keep-awake status row without blocking timed keep-awake itself.
</success_criteria>

<output>
After completion, create `.planning/phases/24-timed-reminder-scheduling/24-01-SUMMARY.md`
</output>
