---
phase: 25-expiry-reminder-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/KeepAwakeReminderScheduling.swift"
  - "Tools Cat/KeepAwakeSessionModel.swift"
  - "Tools Cat/KeepAwakePresentation.swift"
  - "Tools Cat/StatusBarController.swift"
  - "Tools Cat/AppDelegate.swift"
  - "Tools CatTests/AppDelegateNotificationTests.swift"
  - "Tools CatTests/KeepAwakeSessionModelTests.swift"
  - "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
autonomous: true
requirements:
  - NOTF-03
  - NOTF-05

must_haves:
  truths:
    - "When a timed keep-awake session naturally expires and the underlying keep-awake state actually turns off, the app delivers exactly one local notification that the session ended."
    - "Replacing a timed session, stopping it manually, or failing to turn keep-awake off never produces a stale or false expiry notification."
    - "Timed keep-awake still starts, counts down, and ends correctly when local notifications are denied or unavailable."
    - "During an active timed keep-awake session with unavailable reminders, the existing keep-awake status area shows the countdown on one line and the reminder-unavailable truth on a second line."
  artifacts:
    - path: "Tools Cat/KeepAwakeReminderScheduling.swift"
      provides: "The existing reminder scheduler seam now owns authorization checks, immediate expiry delivery, and foreground presentation for keep-awake reminder identifiers"
      contains: "deliverExpiryReminder"
    - path: "Tools Cat/KeepAwakeSessionModel.swift"
      provides: "Confirmed timed-session identity, expiry stop-reason truth, expiry reminder delivery, and dedicated reminder-availability state"
      contains: "KeepAwakeStopReason"
    - path: "Tools Cat/KeepAwakePresentation.swift"
      provides: "Structured keep-awake status lines so countdown truth and reminder-unavailable truth can be visible together"
      contains: "KeepAwakeStatusLines"
    - path: "Tools Cat/StatusBarController.swift"
      provides: "The existing disabled keep-awake status row rendered as one or two lines without creating a new notification menu section"
      contains: "makeKeepAwakeStatusTitle"
    - path: "Tools Cat/AppDelegate.swift"
      provides: "Launch wiring that installs keep-awake reminder foreground presentation before requesting notification authorization"
      contains: "installForegroundPresentationDelegate"
    - path: "Tools CatTests/KeepAwakeSessionModelTests.swift"
      provides: "Deterministic regression coverage for truthful expiry reminder delivery, stale-reminder suppression, and non-blocking unavailable-state ownership"
      contains: "testTimedExpirySuccessfulStopDeliversOneExpiryReminder"
    - path: "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
      provides: "Controller coverage for two-line keep-awake status rendering without expanding the menu surface"
      contains: "testReminderPermissionUnavailableShowsCountdownAndUnavailableTextInExistingStatusRow"
    - path: "Tools CatTests/AppDelegateNotificationTests.swift"
      provides: "Launch/foreground reminder bootstrap coverage through the injected reminder scheduler seam"
      contains: "testBootstrapLaunchServicesInstallsForegroundPresentationBeforeAuthorizationRequest"
  key_links:
    - from: "Tools Cat/KeepAwakeSessionModel.swift"
      to: "Tools Cat/KeepAwakeReminderScheduling.swift"
      via: "authorization checks, pre-expiry scheduling, and expiry delivery guarded by active timed-session UUID plus stop reason"
      pattern: "fetchAuthorizationState|schedulePreExpiryReminder|deliverExpiryReminder|KeepAwakeStopReason"
    - from: "Tools Cat/KeepAwakeSessionModel.swift"
      to: "Tools Cat/KeepAwakePresentation.swift"
      via: "dedicated reminder-availability state passed into structured status-line presentation"
      pattern: "reminderAvailability|KeepAwakeStatusLines"
    - from: "Tools Cat/KeepAwakePresentation.swift"
      to: "Tools Cat/StatusBarController.swift"
      via: "status lines rendered through the existing keep-awake status row via attributedTitle"
      pattern: "statusLines|attributedTitle|makeKeepAwakeStatusTitle"
    - from: "Tools Cat/AppDelegate.swift"
      to: "Tools Cat/KeepAwakeReminderScheduling.swift"
      via: "launch-time foreground presentation install followed by the existing authorization request"
      pattern: "installForegroundPresentationDelegate\\(\\)|requestAuthorizationAtLaunch\\(\\)"
---

<objective>
Deliver the Phase 25 truth layer for timed keep-awake reminders: send one end-of-session notification only after a timed session has actually turned off, and keep reminder-unavailable state visible inside the existing keep-awake status area without blocking timed keep-awake itself.

Purpose: Close `NOTF-03` and `NOTF-05` per D-01 through D-11 by extending the Phase 24 scheduler/session seam rather than adding a second notification path or a new UI surface.
Output: One updated reminder scheduler seam, one session-owned expiry-reminder and reminder-availability flow, one two-line keep-awake status presentation path, and focused XCTest coverage for launch wiring, expiry delivery, stale-reminder suppression, and status-row rendering.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-expiry-reminder-truth/25-CONTEXT.md
@.planning/phases/25-expiry-reminder-truth/25-RESEARCH.md
@.planning/phases/25-expiry-reminder-truth/25-VALIDATION.md
@.planning/phases/24-timed-reminder-scheduling/24-01-SUMMARY.md
@.planning/phases/04-timed-keep-awake/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@Tools Cat/KeepAwakeReminderScheduling.swift
@Tools Cat/KeepAwakeSessionModel.swift
@Tools Cat/KeepAwakePresentation.swift
@Tools Cat/StatusBarController.swift
@Tools Cat/AppDelegate.swift
@Tools CatTests/AppDelegateNotificationTests.swift
@Tools CatTests/KeepAwakeSessionModelTests.swift
@Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift

<interfaces>
From `Tools Cat/KeepAwakeReminderScheduling.swift`:
```swift
protocol KeepAwakeReminderScheduling {
    func requestAuthorizationAtLaunch()
    func schedulePreExpiryReminder(
        identifier: String,
        fireAfter: TimeInterval,
        title: String,
        body: String,
        completion: @escaping @MainActor (KeepAwakeReminderScheduleResult) -> Void
    )
    func cancelPendingReminder(identifier: String)
}
```

From `Tools Cat/KeepAwakeSessionModel.swift`:
```swift
@Published private(set) var confirmedMode: KeepAwakeMode
@Published private(set) var pendingAction: KeepAwakePendingAction?
@Published private(set) var message: String?
@Published private(set) var countdownNow: Date

func startTimed(_ duration: ManagedKeepAwakeDuration)
func stop(completion: (() -> Void)? = nil)
```

From `Tools Cat/KeepAwakePresentation.swift`:
```swift
struct KeepAwakePresentation: Equatable {
    let confirmedMode: KeepAwakeMode
    let pendingAction: KeepAwakePendingAction?
    let message: String?
    let now: Date

    var statusText: String? { ... }
}
```

From `Tools Cat/StatusBarController.swift`:
```swift
keepAwakeStatusItem.isEnabled = false
keepAwakeStatusItem.title = presentation.statusText ?? ""
keepAwakeStatusItem.isHidden = presentation.statusText == nil
```

From `Tools Cat/StatusBarController.swift` multi-line pattern:
```swift
private func makeWakeMenuTitle(for device: SavedDevice) -> NSAttributedString {
    let paragraphStyle = NSMutableParagraphStyle()
    paragraphStyle.lineSpacing = 1

    let title = NSMutableAttributedString(
        string: device.name,
        attributes: [
            .font: NSFont.menuFont(ofSize: 0),
            .paragraphStyle: paragraphStyle,
        ]
    )
    title.append(
        NSAttributedString(
            string: "\n\(device.macAddress)",
            attributes: [
                .font: NSFont.systemFont(ofSize: NSFont.smallSystemFontSize),
                .foregroundColor: NSColor.secondaryLabelColor,
                .paragraphStyle: paragraphStyle,
            ]
        )
    )
    return title
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the reminder scheduler and session lifecycle for truthful expiry delivery</name>
  <files>Tools Cat/KeepAwakeReminderScheduling.swift, Tools Cat/KeepAwakeSessionModel.swift, Tools Cat/AppDelegate.swift, Tools CatTests/KeepAwakeSessionModelTests.swift, Tools CatTests/AppDelegateNotificationTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-expiry-reminder-truth/25-CONTEXT.md
    - .planning/phases/25-expiry-reminder-truth/25-RESEARCH.md
    - .planning/phases/25-expiry-reminder-truth/25-VALIDATION.md
    - .planning/phases/24-timed-reminder-scheduling/24-01-SUMMARY.md
    - Tools Cat/KeepAwakeReminderScheduling.swift
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools Cat/AppDelegate.swift
    - Tools CatTests/KeepAwakeSessionModelTests.swift
    - Tools CatTests/AppDelegateNotificationTests.swift
  </read_first>
  <behavior>
    - Test 1: `testTimedExpirySuccessfulStopDeliversOneExpiryReminder` proves a confirmed timed-expiry stop delivers exactly one immediate reminder with identifier prefix `keep-awake.session.`, suffix `.expiry`, title `常亮已结束`, and body `已按时关闭常亮`.
    - Test 2: `testTimedExpiryDisableFailureDoesNotDeliverExpiryReminder` proves reaching the stop path is not enough; if the disable attempt fails or keep-awake remains on, no expiry reminder is delivered per D-01 and D-02.
    - Test 3: `testManualStopDoesNotDeliverExpiryReminder` proves a user-triggered stop never sends the expiry reminder even if the session had an active timed-session UUID per D-03.
    - Test 4: `testShortTimedSessionStillSurfacesReminderUnavailableTruth` proves a `120` second timed session still checks reminder availability and can enter the unavailable state without scheduling a pre-expiry reminder per D-08 and D-09.
    - Test 5: `testBootstrapLaunchServicesInstallsForegroundPresentationBeforeAuthorizationRequest` proves launch wiring installs keep-awake reminder foreground presentation and then requests authorization through the injected reminder scheduler seam instead of talking to `UNUserNotificationCenter.current()` directly.
  </behavior>
  <action>
    Extend `Tools Cat/KeepAwakeReminderScheduling.swift` so the existing Phase 24 seam owns all reminder-side effects needed by D-01 through D-04 and D-08 through D-09. Use these exact additions:

    - `func installForegroundPresentationDelegate()`
    - `func fetchAuthorizationState(completion: @escaping @MainActor (KeepAwakeReminderAuthorizationState) -> Void)`
    - `func deliverExpiryReminder(identifier: String, title: String, body: String, completion: @escaping @MainActor (KeepAwakeReminderDeliveryResult) -> Void)`
    - `enum KeepAwakeReminderAuthorizationState: Equatable { case authorized, unavailable }`
    - `enum KeepAwakeReminderDeliveryResult: Equatable { case delivered, permissionUnavailable, failed }`

    Convert `UserNotificationKeepAwakeReminderScheduler` to `final class UserNotificationKeepAwakeReminderScheduler: NSObject, KeepAwakeReminderScheduling, UNUserNotificationCenterDelegate`. Keep `center: UNUserNotificationCenter` injection, then implement:

    - `installForegroundPresentationDelegate()` by setting `center.delegate = self`
    - `fetchAuthorizationState(...)` by calling `getNotificationSettings` and mapping only `.authorized` to `.authorized`; map `.notDetermined`, `.denied`, `.provisional`, `.ephemeral`, and any future non-authorized values to `.unavailable`
    - `schedulePreExpiryReminder(...)` by reusing `fetchAuthorizationState(...)`; keep the existing nonrepeating `UNTimeIntervalNotificationTrigger(timeInterval: fireAfter, repeats: false)` and return `.permissionUnavailable` or `.failed` exactly as before
    - `deliverExpiryReminder(...)` by checking authorization first and, when authorized, adding `UNNotificationRequest(identifier: identifier, content: content, trigger: nil)` so delivery happens immediately only after confirmed shutdown
    - `userNotificationCenter(_:willPresent:)` so identifiers beginning with `keep-awake.session.` and ending in `.pre-expiry` or `.expiry` return `[.banner, .list, .sound]`; all other identifiers return `[]`

    Update `NoopKeepAwakeReminderScheduler` to implement the new methods as deterministic no-ops: `installForegroundPresentationDelegate()` does nothing, `fetchAuthorizationState(...)` completes with `.unavailable`, and `deliverExpiryReminder(...)` completes with `.failed`.

    Extend `Tools Cat/KeepAwakeSessionModel.swift` with concrete state instead of overloading `message`:

    - add `enum KeepAwakeReminderAvailability: Equatable { case available, unavailable(String) }`
    - add `@Published private(set) var reminderAvailability: KeepAwakeReminderAvailability`
    - add `private enum KeepAwakeStopReason: Equatable { case manual, timedExpiry(sessionID: UUID) }`
    - replace the Phase 24-only reminder fields with `private var activeTimedSessionID: UUID?`, `private var activePreExpiryReminderIdentifier: String?`, and `private var pendingStopReason: KeepAwakeStopReason?`
    - add `private func preExpiryReminderIdentifier(for sessionID: UUID) -> String` returning `keep-awake.session.<UUID>.pre-expiry`
    - add `private func expiryReminderIdentifier(for sessionID: UUID) -> String` returning `keep-awake.session.<UUID>.expiry`

    Keep reminder truth aligned with confirmed lifecycle transitions:

    - When a timed session is confirmed in `handleEnableOutcome(...)`, create a fresh `sessionID`, assign it to `activeTimedSessionID`, set `reminderAvailability = .available`, call `fetchAuthorizationState(...)` for that `sessionID`, and only then schedule the pre-expiry reminder if `endDate.timeIntervalSince(now) > 120`
    - For all timed sessions, including `<= 120` seconds, `fetchAuthorizationState(...)` must be called so `NOTF-05` can surface unavailable truth even when no pre-expiry reminder exists per D-08 and D-09
    - Use the exact unavailable copy `提醒不可用：通知权限未开启` for authorization unavailability and `提醒不可用：无法安排提醒` when the scheduler returns `.failed`
    - Keep the existing pre-expiry reminder copy unchanged: title `常亮即将结束`, body `2 分钟后将关闭常亮`
    - Change `stop()` to call `beginStop(reason: .manual, completion: completion)`
    - Change `handleCountdownTick(endDate:)` so natural expiry calls `beginStop(reason: .timedExpiry(sessionID: activeTimedSessionID), completion: nil)` only when the confirmed timed session still matches the current `endDate`
    - In `handleStopOutcome(_:)`, deliver the expiry reminder only on `.success(false)` or `.unchanged(false)` when `pendingStopReason == .timedExpiry(sessionID: activeTimedSessionID)`; use title `常亮已结束` and body `已按时关闭常亮`
    - On manual stop, replacement, switch to `无限常亮`, or failed disable attempts, never call `deliverExpiryReminder(...)`
    - After any confirmed move away from timed mode, clear `activeTimedSessionID`, clear `activePreExpiryReminderIdentifier`, clear `pendingStopReason`, cancel any pending pre-expiry reminder, and reset `reminderAvailability` to `.available`
    - Do not restore unavailable status into `message`; `message` remains reserved for real keep-awake transition failures and other existing one-line status text

    Update `Tools Cat/AppDelegate.swift` so `bootstrapLaunchServices()` calls `keepAwakeReminderScheduler.installForegroundPresentationDelegate()` immediately after `configureSharedStores()` and before `keepAwakeReminderScheduler.requestAuthorizationAtLaunch()`. Keep the existing `makeKeepAwakeReminderScheduler` seam and do not introduce direct `UNUserNotificationCenter.current()` calls inside `AppDelegate`.

    Expand the existing fake schedulers in `Tools CatTests/KeepAwakeSessionModelTests.swift` and `Tools CatTests/AppDelegateNotificationTests.swift` so they can:

    - record `installForegroundPresentationDelegate()` calls
    - record authorization checks separately from pre-expiry schedule calls
    - record delivered expiry reminders separately from scheduled pre-expiry reminders
    - preserve the existing session-scoped identifier assertions for `.pre-expiry`
    - add a new `events` array in the AppDelegate fake so the launch test can assert `["installForegroundPresentationDelegate", "requestAuthorizationAtLaunch"]`

    Do not add a second notification service, do not pre-schedule the expiry reminder at timed-session start, and do not let expiry delivery depend on `endDate` alone without confirmed `.off`.
  </action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `func installForegroundPresentationDelegate()`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `func fetchAuthorizationState`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `func deliverExpiryReminder`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `enum KeepAwakeReminderAuthorizationState: Equatable { case authorized, unavailable }`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `enum KeepAwakeReminderDeliveryResult: Equatable { case delivered, permissionUnavailable, failed }`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `center.delegate = self`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `UNNotificationRequest(identifier: identifier, content: content, trigger: nil)`.
    - `Tools Cat/KeepAwakeReminderScheduling.swift` contains `return [.banner, .list, .sound]`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `enum KeepAwakeReminderAvailability: Equatable`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `enum KeepAwakeStopReason: Equatable`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `keep-awake.session.` and `.expiry`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `提醒不可用：通知权限未开启`.
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `已按时关闭常亮`.
    - `Tools Cat/AppDelegate.swift` contains `installForegroundPresentationDelegate()`.
    - `Tools Cat/AppDelegate.swift` contains `requestAuthorizationAtLaunch()`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testTimedExpirySuccessfulStopDeliversOneExpiryReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testTimedExpiryDisableFailureDoesNotDeliverExpiryReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testManualStopDoesNotDeliverExpiryReminder`.
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testShortTimedSessionStillSurfacesReminderUnavailableTruth`.
    - `Tools CatTests/AppDelegateNotificationTests.swift` contains `testBootstrapLaunchServicesInstallsForegroundPresentationBeforeAuthorizationRequest`.
    - The focused reminder lifecycle and launch bootstrap test command exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/AppDelegateNotificationTests' -only-testing:'Tools CatTests/KeepAwakeSessionModelTests'</automated>
  </verify>
  <done>The reminder scheduler seam owns foreground presentation, authorization checks, and immediate expiry delivery; `KeepAwakeSessionModel` delivers the end reminder only from the confirmed timed-expiry shutdown path; all timed sessions can surface reminder-unavailable truth without blocking keep-awake behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render countdown truth and reminder-unavailable truth together in the existing keep-awake status row</name>
  <files>Tools Cat/KeepAwakePresentation.swift, Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-expiry-reminder-truth/25-CONTEXT.md
    - .planning/phases/25-expiry-reminder-truth/25-RESEARCH.md
    - .planning/phases/25-expiry-reminder-truth/25-VALIDATION.md
    - .planning/phases/24-timed-reminder-scheduling/24-01-SUMMARY.md
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools Cat/KeepAwakePresentation.swift
    - Tools Cat/StatusBarController.swift
    - Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
  </read_first>
  <behavior>
    - Test 1: `testReminderPermissionUnavailableShowsCountdownAndUnavailableTextInExistingStatusRow` proves a confirmed `15 分钟` timed session with unavailable reminders renders `还剩 15 分钟` as the primary line and `提醒不可用：通知权限未开启` as the secondary line on the existing disabled keep-awake status item.
    - Test 2: `testTwoMinuteTimedSessionStillShowsReminderUnavailableInExistingStatusRow` proves a `2 分钟` session also renders the two-line unavailable state even though it never schedules a pre-expiry reminder per D-08 and D-09.
    - Test 3: `testReminderAvailableTimedSessionKeepsSingleLineCountdownStatus` proves available reminders keep the existing single-line countdown row instead of always switching to an attributed two-line title.
  </behavior>
  <action>
    Update `Tools Cat/KeepAwakePresentation.swift` so reminder-unavailable truth no longer competes with countdown truth per D-05, D-06, and D-07. Use this exact shape:

    - add `struct KeepAwakeStatusLines: Equatable { let primary: String; let secondary: String? }`
    - add `let reminderAvailability: KeepAwakeReminderAvailability` to `KeepAwakePresentation`
    - replace the current one-string status computation with `var statusLines: KeepAwakeStatusLines?`

    Keep status precedence concrete and unchanged where scope does not require new behavior:

    - if `pendingAction != nil`, return one line only using the existing pending copy
    - else if `message` is non-empty, return one line only using `message`
    - else if `confirmedMode == .indefinite`, return `KeepAwakeStatusLines(primary: "当前：无限常亮", secondary: nil)`
    - else if `confirmedMode == .timed(_, let endDate)`, return `KeepAwakeStatusLines(primary: "还剩 \(formattedDuration(until: endDate))", secondary: reminderUnavailableTextIfAny)`
    - else if `confirmedMode == .off`, return `nil`

    Derive `reminderUnavailableTextIfAny` only from `reminderAvailability`:

    - `.available` -> `nil`
    - `.unavailable(let text)` -> `text`

    Keep `iconSymbol`, `buttonToolTip`, `isPending`, `showsStopAction`, and the existing `formattedDuration(until:)` implementation intact apart from compiling against `statusLines`.

    Update `Tools Cat/StatusBarController.swift` to keep one status row and render it in two modes:

    - pass `reminderAvailability: keepAwakeSession.reminderAvailability` into `KeepAwakePresentation`
    - when `presentation.statusLines == nil`, set `keepAwakeStatusItem.title = ""`, `keepAwakeStatusItem.attributedTitle = nil`, and `keepAwakeStatusItem.isHidden = true`
    - when `presentation.statusLines?.secondary == nil`, set `keepAwakeStatusItem.title = primary`, `keepAwakeStatusItem.attributedTitle = nil`, and `keepAwakeStatusItem.isHidden = false`
    - when `presentation.statusLines?.secondary` exists, set `keepAwakeStatusItem.title = primary` for accessibility fallback, set `keepAwakeStatusItem.attributedTitle = makeKeepAwakeStatusTitle(primary: primary, secondary: secondary)`, and keep `isHidden = false`

    Implement `private func makeKeepAwakeStatusTitle(primary: String, secondary: String) -> NSAttributedString` by copying the existing `makeWakeMenuTitle(for:)` typography rules exactly:

    - paragraph style with `lineSpacing = 1`
    - primary line font `NSFont.menuFont(ofSize: 0)`
    - secondary line font `NSFont.systemFont(ofSize: NSFont.smallSystemFontSize)`
    - secondary line color `NSColor.secondaryLabelColor`

    Do not add a second keep-awake status item, do not use `NSMenuItem.subtitle`, do not introduce a custom `NSView` row, and do not add any notification-specific section to the menu.

    Update `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` so the controller assertions verify the real rendered output:

    - for the unavailable `15 分钟` case, assert `keepAwakeStatusItem.attributedTitle?.string == "还剩 15 分钟\n提醒不可用：通知权限未开启"` and `keepAwakeStatusItem.title == "还剩 15 分钟"`
    - for the unavailable `2 分钟` case, assert `keepAwakeStatusItem.attributedTitle?.string == "还剩 2 分钟\n提醒不可用：通知权限未开启"`
    - for the available timed case, assert `keepAwakeStatusItem.attributedTitle == nil` and `keepAwakeStatusItem.title == "还剩 15 分钟"`
    - keep the existing visibility assertion that the visible keep-awake action titles remain exactly `["无限常亮", "15 分钟", "30 分钟", "1 小时", "2 小时", "关闭常亮"]`
  </action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakePresentation.swift` contains `struct KeepAwakeStatusLines: Equatable`.
    - `Tools Cat/KeepAwakePresentation.swift` contains `let reminderAvailability: KeepAwakeReminderAvailability`.
    - `Tools Cat/KeepAwakePresentation.swift` contains `var statusLines: KeepAwakeStatusLines?`.
    - `Tools Cat/KeepAwakePresentation.swift` contains `KeepAwakeStatusLines(primary: "当前：无限常亮", secondary: nil)`.
    - `Tools Cat/KeepAwakePresentation.swift` contains `KeepAwakeStatusLines(primary: "还剩`.
    - `Tools Cat/StatusBarController.swift` contains `makeKeepAwakeStatusTitle(primary: String, secondary: String)`.
    - `Tools Cat/StatusBarController.swift` contains `keepAwakeStatusItem.attributedTitle = makeKeepAwakeStatusTitle`.
    - `Tools Cat/StatusBarController.swift` contains `keepAwakeStatusItem.attributedTitle = nil`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testReminderPermissionUnavailableShowsCountdownAndUnavailableTextInExistingStatusRow`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testTwoMinuteTimedSessionStillShowsReminderUnavailableInExistingStatusRow`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testReminderAvailableTimedSessionKeepsSingleLineCountdownStatus`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `还剩 15 分钟\n提醒不可用：通知权限未开启`.
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `还剩 2 分钟\n提醒不可用：通知权限未开启`.
    - The focused keep-awake status-row controller test command exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'</automated>
  </verify>
  <done>The existing keep-awake status row can show a timed countdown and reminder-unavailable truth together as two lines, available reminders still render as the current single-line countdown, and the menu structure remains unchanged.</done>
</task>

</tasks>

<verification>
- Focused automation after Task 1: `xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/AppDelegateNotificationTests' -only-testing:'Tools CatTests/KeepAwakeSessionModelTests'`
- Focused automation after Task 2: `xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'`
- Wave gate before summary: `xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/AppDelegateNotificationTests' -only-testing:'Tools CatTests/KeepAwakeSessionModelTests' -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'`
- Manual follow-up remains the Phase 25 live notification checks in `.planning/phases/25-expiry-reminder-truth/25-VALIDATION.md`: one allowed-notifications expiry run and one denied-notifications timed-session run.
</verification>

<success_criteria>
- A natural timed-session expiry that really turns keep-awake off schedules one immediate `.expiry` notification and no false expiry reminder is delivered for manual stop, replacement, or failed disable outcomes.
- Every confirmed timed session, including `<= 2 分钟`, can surface reminder-unavailable truth while countdown and actual keep-awake behavior remain intact.
- The keep-awake status area stays inside the existing menu row and renders countdown truth plus unavailable truth together without adding a new notification UI surface.
</success_criteria>

<output>
After completion, create `.planning/phases/25-expiry-reminder-truth/25-01-SUMMARY.md`
</output>
