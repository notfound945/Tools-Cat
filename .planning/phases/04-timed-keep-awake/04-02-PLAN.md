---
phase: 04-timed-keep-awake
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss Knife/StatusBarController.swift"
  - "Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift"
autonomous: true
requirements:
  - AWAKE-01
  - AWAKE-02
  - AWAKE-03
must_haves:
  truths:
    - "The root menu shows one fixed keep-awake action group in this exact order: `无限常亮`, `15 分钟`, `30 分钟`, `1 小时`, `2 小时`, `关闭常亮`, followed by one disabled status row."
    - "Exactly one confirmed keep-awake row is checked at a time, all six keep-awake rows disable during pending transitions, and the `关闭常亮` row remains visible in every state."
    - "The menu bar icon, tooltip, and keep-awake status row are driven by one shared `KeepAwakeSessionModel` retained by the app lifecycle, not by controller-local optimistic booleans."
  artifacts:
    - path: "Mac OS Swiss Knife/AppDelegate.swift"
      provides: "One retained keep-awake session shared with the status controller"
      contains: "private var keepAwakeSession: KeepAwakeSessionModel!"
    - path: "Mac OS Swiss Knife/StatusBarController.swift"
      provides: "Fixed keep-awake menu wiring and menu-bar presentation driven by the shared session"
      contains: "keepAwakeOffItem"
    - path: "Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift"
      provides: "Regression coverage for menu order, checkmarks, pending disabling, and stable titles"
      contains: "final class StatusBarControllerKeepAwakeMenuTests"
  key_links:
    - from: "Mac OS Swiss Knife/AppDelegate.swift"
      to: "Mac OS Swiss Knife/StatusBarController.swift"
      via: "shared KeepAwakeSessionModel injection at launch"
      pattern: "keepAwakeSession:"
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      via: "fixed keep-awake rows dispatch through startIndefinite, startTimed, and stop"
      pattern: "startIndefinite\\(|startTimed\\(|stop\\("
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/KeepAwakePresentation.swift"
      via: "menu checkmarks, status row, icon, and tooltip render from one presentation object"
      pattern: "KeepAwakePresentation\\("
---

<objective>
Wire the shared timed keep-awake session into the native menu bar surface with the exact Phase 4 menu structure and row behavior.

Purpose: The shared session from `04-01` must become a direct menu-bar control surface without regressing Phase 1 truth rules or Phase 3 compact-menu constraints.
Output: Fixed root-menu keep-awake rows, shared-session AppDelegate injection, stable action titles, and controller regression tests for the keep-awake menu group.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-timed-keep-awake/04-CONTEXT.md
@.planning/phases/04-timed-keep-awake/04-RESEARCH.md
@.planning/phases/04-timed-keep-awake/04-UI-SPEC.md
@.planning/phases/04-timed-keep-awake/04-01-PLAN.md
@.planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss Knife/KeepAwakeSessionModel.swift
@Mac OS Swiss Knife/KeepAwakePresentation.swift

<interfaces>
Expected shared keep-awake session contract from `04-01`:
```swift
@MainActor
final class KeepAwakeSessionModel: ObservableObject {
    @Published private(set) var confirmedMode: KeepAwakeMode
    @Published private(set) var pendingAction: KeepAwakePendingAction?
    @Published private(set) var message: String?
    @Published private(set) var countdownNow: Date

    func startIndefinite()
    func startTimed(_ preset: KeepAwakeDurationPreset)
    func stop(completion: (() -> Void)? = nil)
}
```

Expected presentation helpers from `04-01`:
```swift
struct KeepAwakePresentation: Equatable {
    let confirmedMode: KeepAwakeMode
    let pendingAction: KeepAwakePendingAction?
    let message: String?
    let now: Date

    var isIndefiniteActive: Bool
    var activeTimedPreset: KeepAwakeDurationPreset?
    var statusText: String?
    var iconSymbol: String
    var buttonToolTip: String
    var isPending: Bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the root keep-awake menu group around the shared session model</name>
  <files>Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/StatusBarController.swift</files>
  <read_first>
    - .planning/phases/04-timed-keep-awake/04-CONTEXT.md
    - .planning/phases/04-timed-keep-awake/04-UI-SPEC.md
    - .planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/KeepAwakeSessionModel.swift
    - Mac OS Swiss Knife/KeepAwakePresentation.swift
  </read_first>
  <action>Per D-01, D-02, D-03, D-04, and D-12, add `private var keepAwakeSession: KeepAwakeSessionModel!` to `AppDelegate.swift`, initialize it inside `applicationDidFinishLaunching` with one shared `SystemKeepAwakePowerController(manager: .shared)`, and pass it into `StatusBarController(...)` with the exact initializer label `keepAwakeSession:`. In `StatusBarController.swift`, remove the old single `keepAwakeItem` toggle path and replace it with six fixed properties named exactly `keepAwakeIndefiniteItem`, `keepAwake15MinutesItem`, `keepAwake30MinutesItem`, `keepAwake1HourItem`, `keepAwake2HoursItem`, and `keepAwakeOffItem`, plus the existing `keepAwakeStatusItem`. Insert them in this exact order before `发送 WOL …`: `无限常亮`, `15 分钟`, `30 分钟`, `1 小时`, `2 小时`, `关闭常亮`, then the disabled keep-awake status row. Wire the actions so `无限常亮` calls `keepAwakeSession.startIndefinite()`, `15 分钟` calls `keepAwakeSession.startTimed(.minutes15)`, `30 分钟` calls `keepAwakeSession.startTimed(.minutes30)`, `1 小时` calls `keepAwakeSession.startTimed(.hour1)`, `2 小时` calls `keepAwakeSession.startTimed(.hours2)`, and `关闭常亮` calls `keepAwakeSession.stop()`. Keep this task limited to session injection plus menu-item creation/wiring; controller-level assertions about final row order and action dispatch are locked in Task 2's controller tests. Do not reintroduce the old `保持屏幕常亮` row, do not move keep-awake into a submenu, and do not add any explanatory paragraph rows.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `private var keepAwakeSession: KeepAwakeSessionModel!`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `keepAwakeSession: keepAwakeSession`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeIndefiniteItem`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwake15MinutesItem`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwake2HoursItem`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeOffItem`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"无限常亮"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"15 分钟"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"30 分钟"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"1 小时"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"2 小时"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `"关闭常亮"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeSession.startTimed(.minutes15)`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeSession.startIndefinite()`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeSession.stop()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS'</automated>
  </verify>
  <done>The root menu now exposes the exact Phase 4 keep-awake action group with one visible manual stop row and no legacy toggle entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind keep-awake menu state, icon, and tooltip to the shared presentation contract and lock it with controller tests</name>
  <files>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift</files>
  <read_first>
    - .planning/phases/04-timed-keep-awake/04-CONTEXT.md
    - .planning/phases/04-timed-keep-awake/04-UI-SPEC.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/KeepAwakeSessionModel.swift
    - Mac OS Swiss Knife/KeepAwakePresentation.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: Exactly one confirmed row is checked at a time: `无限常亮` for indefinite mode, or the matching timed preset for timed mode.
    - Test 2: The keep-awake section appears before the WOL section, keeps the exact row order `无限常亮`, `15 分钟`, `30 分钟`, `1 小时`, `2 小时`, `关闭常亮`, then one disabled status row, and each action dispatches to the shared session entry point instead of controller-local toggles.
    - Test 3: During any pending start, replacement, or stop, all six keep-awake action rows are disabled while titles stay fixed.
    - Test 4: Countdown text appears only in the disabled status row; none of the six action titles change as seconds tick.
    - Test 5: The menu-bar symbol and tooltip follow the presentation contract: `bolt.slash` / `常亮已关闭`, `bolt.fill` / `常亮已开启：无限常亮`, `bolt.fill` / `常亮已开启：剩余 {formattedDuration}`, and `bolt.slash` / `常亮状态更新中`.
  </behavior>
  <action>Per D-08, D-09, D-10, and D-11, subscribe `StatusBarController` to `keepAwakeSession.objectWillChange` the same way the controller already follows the WOL session. Replace the old `confirmedKeepAwakeEnabled`, `pendingTransition`, `keepAwakeMessage`, `currentKeepAwakeSymbolName`, and direct `powerController` rendering path with a presentation-driven path: build `KeepAwakePresentation(confirmedMode: keepAwakeSession.confirmedMode, pendingAction: keepAwakeSession.pendingAction, message: keepAwakeSession.message, now: keepAwakeSession.countdownNow)` and render from it. Apply the exact menu rules from the UI spec: `keepAwakeIndefiniteItem.state = .on` only when `presentation.isIndefiniteActive` is true; each timed row is `.on` only when `presentation.activeTimedPreset` matches its preset; `keepAwakeOffItem.state` must stay `.off`; all six keep-awake rows set `isEnabled = !presentation.isPending`; `keepAwakeStatusItem.isEnabled = false`; `keepAwakeStatusItem.title = presentation.statusText ?? ""`; `keepAwakeStatusItem.isHidden = presentation.statusText == nil`. Update the status-item button so `button.image` uses `presentation.iconSymbol` and `button.toolTip = presentation.buttonToolTip`. Keep the quit path compact: if `keepAwakeSession.confirmedMode == .off` and `keepAwakeSession.pendingAction == nil`, terminate immediately; otherwise call `keepAwakeSession.stop { NSApp.terminate(nil) }`. Add `StatusBarControllerKeepAwakeMenuTests.swift` with the exact test functions `testKeepAwakeMenuOrderMatchesTheFixedActionGroup()`, `testKeepAwakeActionItemsDispatchThroughSharedSession()`, `testConfirmedTimedModeChecksOnlyTheSelectedPresetRow()`, `testPendingTransitionDisablesAllKeepAwakeActionRows()`, `testCountdownNeverAppearsInAnyActionTitle()`, `testKeepAwakeStatusRowRendersPresentationStatusText()`, and `testStatusItemToolTipAndSymbolFollowPresentationState()`. Register the new test file in `project.pbxproj`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `KeepAwakePresentation(`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeSession.objectWillChange`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeIndefiniteItem.state =`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeStatusItem.title = presentation.statusText ?? ""`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeStatusItem.isHidden = presentation.statusText == nil`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `button.toolTip = presentation.buttonToolTip`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `keepAwakeSession.stop { NSApp.terminate(nil) }`
    - `Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testKeepAwakeMenuOrderMatchesTheFixedActionGroup`
    - `Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testKeepAwakeActionItemsDispatchThroughSharedSession`
    - `Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testPendingTransitionDisablesAllKeepAwakeActionRows`
    - `Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testCountdownNeverAppearsInAnyActionTitle`
    - `Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `testStatusItemToolTipAndSymbolFollowPresentationState`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests'</automated>
  </verify>
  <done>The native menu now reflects one shared timed keep-awake session: fixed titles stay stable, pending transitions block duplicate actions, checkmarks remain truthful, and the icon/tooltip/status row all agree.</done>
</task>

</tasks>

<verification>
Run the new controller tests together with the Phase 4 keep-awake presentation/session tests. Treat this plan as complete only when the root keep-awake group renders in the exact fixed order, active state is checkmark-driven, and countdown updates are visible only in the status row.
</verification>

<success_criteria>
The Phase 4 keep-awake feature is now a first-class native menu surface: users can start indefinite or timed sessions directly from the root menu, the menu remains compact and stable, and all visual state flows from the shared session model.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timed-keep-awake/04-02-SUMMARY.md`
</output>
