---
phase: 04-timed-keep-awake
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: false
requirements:
  - AWAKE-01
  - AWAKE-03
  - AWAKE-04
must_haves:
  truths:
    - "A live build lets the user start indefinite keep-awake, switch to a timed preset, watch the countdown update in the status row, and stop manually with `关闭常亮`."
    - "Timed replacement works without confirmation, and a naturally expiring timed session returns to the normal off presentation."
    - "No action title flickers with countdown text, and the live menu keeps the explicit manual-off row visible throughout."
  artifacts:
    - path: "Mac OS Swiss Knife/StatusBarController.swift"
      provides: "Live keep-awake action group and status-row wiring used in the manual smoke"
      contains: "keepAwakeOffItem"
    - path: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      provides: "Timed session lifecycle and expiry behavior exercised in the live menu smoke"
      contains: "final class KeepAwakeSessionModel"
  key_links:
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      via: "root-menu actions drive one shared timed keep-awake session"
      pattern: "keepAwakeSession"
    - from: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      to: "Mac OS Swiss Knife/PowerAssertionManager.swift"
      via: "natural expiry still disables through the truthful power-controller seam"
      pattern: "setKeepAwakeEnabled\\(false"
---

<objective>
Validate the live timed keep-awake flow on the real menu bar after the code and tests are in place.

Purpose: Unit tests prove state semantics, but Phase 4 still needs one human check that the native menu feels correct, countdown text stays in the right row, and expiry returns to off cleanly on a live build.
Output: A blocking human-verification checkpoint covering the full timed keep-awake interaction.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-timed-keep-awake/04-CONTEXT.md
@.planning/phases/04-timed-keep-awake/04-UI-SPEC.md
@.planning/phases/04-timed-keep-awake/04-01-PLAN.md
@.planning/phases/04-timed-keep-awake/04-02-PLAN.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/KeepAwakeSessionModel.swift
@Mac OS Swiss Knife/StatusBarController.swift
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the live timed keep-awake menu smoke and confirm the native interaction</name>
  <read_first>
    - .planning/phases/04-timed-keep-awake/04-CONTEXT.md
    - .planning/phases/04-timed-keep-awake/04-UI-SPEC.md
    - Mac OS Swiss Knife/KeepAwakeSessionModel.swift
    - Mac OS Swiss Knife/StatusBarController.swift
  </read_first>
  <action>Run the exact automated unit-test command in `<verify>` first, then launch the app and perform the live menu smoke from `<how-to-verify>`. The checkpoint is specifically validating D-01 through D-04 and D-06 through D-10 on the real menu surface: fixed root-menu ordering, explicit `关闭常亮`, immediate timed replacement with no confirmation, countdown confined to the status row, and natural expiry returning directly to off with no extra completion banner.</action>
  <what-built>The root menu now has fixed keep-awake rows, one disabled status row, one explicit `关闭常亮` row, and a shared timed session that should replace modes, count down live, and auto-stop on expiry.</what-built>
  <how-to-verify>
    1. Run the task-scoped smoke command from `<verify>` and confirm the keep-awake regression suites pass before launching the app.
    2. Launch the app from Xcode or the built Debug app, open the menu bar item, and confirm the keep-awake section appears before `发送 WOL …` in this exact order: `无限常亮`, `15 分钟`, `30 分钟`, `1 小时`, `2 小时`, `关闭常亮`, then one disabled status row.
    3. Select `无限常亮` and confirm only that row is checked, the menu-bar icon changes to `bolt.fill`, and the status row reads `当前：无限常亮`.
    4. Select `15 分钟` and confirm the active checkmark moves to `15 分钟`, the status row changes to `还剩 ...`, and none of the six action titles gain countdown text.
    5. While the timed session is active, select `30 分钟` and confirm the app replaces the active session immediately with no confirmation dialog and the status row resets to the longer remaining time.
    6. Select `关闭常亮` and confirm the menu returns to off: no keep-awake row is checked, the icon returns to `bolt.slash`, and the status row hides unless there is a failure message.
    7. Start `15 分钟` again, wait for natural expiry or shorten the timer in a debug-assisted run, and confirm the menu returns directly to the normal off presentation with no `已结束` or extra completion banner.
  </how-to-verify>
  <acceptance_criteria>
    - The visible root-menu order is exactly `无限常亮`, `15 分钟`, `30 分钟`, `1 小时`, `2 小时`, `关闭常亮`, then one disabled status row
    - `关闭常亮` is visible before, during, and after an active timed session
    - Countdown text appears only in the disabled status row and never in any of the six action titles
    - Switching from `15 分钟` to `30 分钟` performs an immediate replacement with no confirmation dialog
    - Natural expiry returns the menu to the normal off state with no `已结束` copy
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests'</automated>
  </verify>
  <done>The user confirms the live menu interaction matches the Phase 4 contract or returns concrete issues to fix.</done>
  <resume-signal>Type `approved` if the live timed keep-awake menu smoke passes, or describe the observed mismatch.</resume-signal>
</task>

</tasks>

<verification>
This plan is complete only after the targeted keep-awake smoke suites are green, the human verification confirms the live menu behavior for indefinite mode, timed mode, replacement, manual stop, and expiry, and the full `Mac OS Swiss KnifeTests` suite is rerun as the wave gate before phase sign-off.
</verification>

<success_criteria>
Phase 4 is ready to ship forward only when both the automated keep-awake tests and one real menu-bar smoke agree that timed keep-awake behaves truthfully and stays compact under live macOS interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timed-keep-awake/04-03-SUMMARY.md`
</output>
