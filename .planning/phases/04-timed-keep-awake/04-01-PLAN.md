---
phase: 04-timed-keep-awake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
  - "Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift"
  - "Mac OS Swiss Knife/KeepAwakeDurationPreset.swift"
  - "Mac OS Swiss Knife/KeepAwakePresentation.swift"
  - "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
  - "Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift"
  - "Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests.swift"
autonomous: true
requirements:
  - AWAKE-01
  - AWAKE-02
  - AWAKE-03
  - AWAKE-04
must_haves:
  truths:
    - "Users can have exactly one confirmed keep-awake mode at a time: closed, infinite keep-awake, or one timed preset."
    - "When a timed keep-awake session is running, users see a live remaining-time status without any keep-awake action label changing every second."
    - "If users stop keep-awake manually or let a timer expire, the menu stays on the last active mode until the system confirms shutdown, and any shutdown failure is shown as a message."
  artifacts:
    - path: "Mac OS Swiss Knife/KeepAwakeDurationPreset.swift"
      provides: "Fixed timed preset catalog with exact menu titles and durations"
      contains: "enum KeepAwakeDurationPreset"
    - path: "Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift"
      provides: "Cancellable countdown scheduler seam for live timer updates"
      contains: "protocol KeepAwakeCountdownScheduling"
    - path: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      provides: "Lifecycle-owned timed keep-awake session state above the power-controller seam"
      contains: "final class KeepAwakeSessionModel"
    - path: "Mac OS Swiss Knife/KeepAwakePresentation.swift"
      provides: "Exact keep-awake status-row, icon, and accessibility copy for off, indefinite, timed, pending, and failure states"
      contains: "struct KeepAwakePresentation"
    - path: "Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests.swift"
      provides: "Regression coverage for timed start, replacement, countdown tick, expiry, and failure retention"
      contains: "final class KeepAwakeSessionModelTests"
  key_links:
    - from: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      to: "Mac OS Swiss Knife/PowerAssertionManager.swift"
      via: "start and stop requests stay behind KeepAwakePowerControlling"
      pattern: "setKeepAwakeEnabled\\("
    - from: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      to: "Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift"
      via: "exactly one cancellable repeating timer drives countdown updates"
      pattern: "KeepAwakeCountdownScheduling"
    - from: "Mac OS Swiss Knife/KeepAwakePresentation.swift"
      to: "Mac OS Swiss Knife/KeepAwakeSessionModel.swift"
      via: "presentation derives status text, icon, and active selection from confirmed mode plus pending action"
      pattern: "KeepAwakeMode|KeepAwakePendingAction"
---

<objective>
Create the timed keep-awake state, preset catalog, countdown seam, and presentation contract before any AppKit menu rewiring starts.

Purpose: Phase 4 needs one lifecycle-owned source of truth for off / indefinite / timed keep-awake so countdown, replacement, expiry, and failure semantics stay testable and truthful.
Output: A shared `KeepAwakeSessionModel`, fixed timed presets, cancellable countdown scheduling, exact keep-awake presentation rules, and focused XCTest coverage.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-timed-keep-awake/04-CONTEXT.md
@.planning/phases/04-timed-keep-awake/04-RESEARCH.md
@.planning/phases/04-timed-keep-awake/04-UI-SPEC.md
@.planning/phases/01-truthful-foundations/01-03-SUMMARY.md
@Mac OS Swiss Knife/KeepAwakePresentation.swift
@Mac OS Swiss Knife/PowerAssertionManager.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift

<interfaces>
Existing truthful power seam:
```swift
protocol KeepAwakePowerControlling {
    var isEnabled: Bool { get }
    func setKeepAwakeEnabled(
        _ enabled: Bool,
        completion: @escaping (KeepAwakeToggleOutcome) -> Void
    )
}
```

Existing session-owner pattern to mirror:
```swift
final class WOLSessionModel: ObservableObject {
    @Published var sendState: WakeSendState
    @Published private(set) var lastCompletedWake: CompletedWakeAttempt?
}
```

Phase 4 contracts this plan must create:
```swift
enum KeepAwakeDurationPreset: CaseIterable, Equatable {
    case minutes15
    case minutes30
    case hour1
    case hours2
}

enum KeepAwakeMode: Equatable {
    case off
    case indefinite
    case timed(preset: KeepAwakeDurationPreset, endDate: Date)
}

enum KeepAwakePendingAction: Equatable {
    case startingIndefinite
    case startingTimed(KeepAwakeDurationPreset)
    case stopping
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the timed keep-awake session model, preset catalog, and countdown scheduler seam</name>
  <files>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift, Mac OS Swiss Knife/KeepAwakeDurationPreset.swift, Mac OS Swiss Knife/KeepAwakeSessionModel.swift, Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/04-timed-keep-awake/04-CONTEXT.md
    - .planning/phases/04-timed-keep-awake/04-RESEARCH.md
    - .planning/phases/04-timed-keep-awake/04-UI-SPEC.md
    - Mac OS Swiss Knife/PowerAssertionManager.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: `startIndefinite()` keeps the confirmed mode unchanged until the power controller completes, then lands on `.indefinite` after `.success(true)` or `.unchanged(true)`.
    - Test 2: `startTimed(.minutes15)` stores `.timed(preset: .minutes15, endDate: ...)`, starts exactly one repeating countdown token, and exposes a live `countdownNow` value for later presentation updates.
    - Test 3: Replacing a running timed or indefinite session with a different timed preset or with indefinite mode reuses `setKeepAwakeEnabled(true)`, accepts `.unchanged(true)`, and cancels the previous countdown token before installing a new one.
    - Test 4: When the end date is reached, the session calls `setKeepAwakeEnabled(false)` and only returns to `.off` after `.success(false)` or `.unchanged(false)`.
    - Test 5: If expiry or manual stop fails with `.failure(current: true, message: ...)`, the session keeps the last confirmed active mode and preserves the exact failure message.
  </behavior>
  <action>Per D-05, D-06, D-07, and D-11, create `KeepAwakeDurationPreset.swift` with the exact cases `.minutes15`, `.minutes30`, `.hour1`, and `.hours2`; each case must expose the exact `menuTitle` values `15 分钟`, `30 分钟`, `1 小时`, and `2 小时`, plus exact `duration` values `900`, `1800`, `3600`, and `7200`. Create `KeepAwakeCountdownScheduler.swift` with `protocol KeepAwakeCountdownToken { func cancel() }`, `protocol KeepAwakeCountdownScheduling { func startRepeating(interval: TimeInterval, tolerance: TimeInterval, handler: @escaping () -> Void) -> KeepAwakeCountdownToken }`, and a concrete `TimerKeepAwakeCountdownScheduler` that uses one `Timer` with `interval: 1` and `tolerance: 0.1`. Create `KeepAwakeSessionModel.swift` as `@MainActor final class KeepAwakeSessionModel: ObservableObject` with the exact published properties `confirmedMode: KeepAwakeMode`, `pendingAction: KeepAwakePendingAction?`, `message: String?`, and `countdownNow: Date`; initialize `confirmedMode` from `powerController.isEnabled ? .indefinite : .off`. Add the exact public methods `startIndefinite()`, `startTimed(_ preset: KeepAwakeDurationPreset)`, and `stop(completion: (() -> Void)? = nil)`. `startIndefinite()` must set `pendingAction = .startingIndefinite`, clear `message`, call `setKeepAwakeEnabled(true)`, and only set `confirmedMode = .indefinite` after `.success(true)` or `.unchanged(true)`. `startTimed(_:)` must set `pendingAction = .startingTimed(preset)`, call `setKeepAwakeEnabled(true)`, and on success compute `let endDate = nowProvider().addingTimeInterval(preset.duration)`, set `confirmedMode = .timed(preset: preset, endDate: endDate)`, set `countdownNow = nowProvider()`, cancel any existing token, and install one new repeating token. On every tick, update `countdownNow = nowProvider()`; when `endDate.timeIntervalSince(countdownNow) <= 0`, cancel the token, set `pendingAction = .stopping`, and call `setKeepAwakeEnabled(false)`. For both manual stop and expiry stop, return to `.off` only after `.success(false)` or `.unchanged(false)`; on `.failure(current: true, message: ...)`, keep the last confirmed mode and assign the exact failure message. Register the new source and test files in `project.pbxproj`. Add `KeepAwakeSessionModelTests.swift` with the exact test functions `testStartIndefiniteWaitsForConfirmedEnableBeforeSwitchingModes()`, `testStartTimedSessionStoresPresetAndEndDateAfterConfirmedEnable()`, `testReplacingTimedSessionCancelsThePreviousCountdownToken()`, `testTimedExpiryReturnsToOffOnlyAfterConfirmedDisable()`, and `testDisableFailureAfterExpiryKeepsLastConfirmedModeAndMessage()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/KeepAwakeDurationPreset.swift` contains `enum KeepAwakeDurationPreset`
    - `Mac OS Swiss Knife/KeepAwakeDurationPreset.swift` contains `case minutes15`
    - `Mac OS Swiss Knife/KeepAwakeDurationPreset.swift` contains `case hours2`
    - `Mac OS Swiss Knife/KeepAwakeDurationPreset.swift` contains `900`
    - `Mac OS Swiss Knife/KeepAwakeDurationPreset.swift` contains `7200`
    - `Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift` contains `protocol KeepAwakeCountdownScheduling`
    - `Mac OS Swiss Knife/KeepAwakeCountdownScheduler.swift` contains `func startRepeating(interval: TimeInterval, tolerance: TimeInterval, handler: @escaping () -> Void) -> KeepAwakeCountdownToken`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `final class KeepAwakeSessionModel`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `@Published private(set) var confirmedMode: KeepAwakeMode`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `@Published private(set) var countdownNow: Date`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `func startIndefinite()`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `func startTimed(_ preset: KeepAwakeDurationPreset)`
    - `Mac OS Swiss Knife/KeepAwakeSessionModel.swift` contains `func stop(completion: (() -> Void)? = nil)`
    - `Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests.swift` contains `testStartTimedSessionStoresPresetAndEndDateAfterConfirmedEnable`
    - `Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests.swift` contains `testTimedExpiryReturnsToOffOnlyAfterConfirmedDisable`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests'</automated>
  </verify>
  <done>The repo now has one shared timed keep-awake session owner with fixed presets, one cancellable countdown scheduler, truthful replacement semantics, and regression tests for expiry and failure retention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the keep-awake presentation contract for fixed menu titles, countdown status copy, and exact accessibility text</name>
  <files>Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</files>
  <read_first>
    - .planning/phases/04-timed-keep-awake/04-CONTEXT.md
    - .planning/phases/04-timed-keep-awake/04-UI-SPEC.md
    - Mac OS Swiss Knife/KeepAwakePresentation.swift
    - Mac OS Swiss Knife/KeepAwakeSessionModel.swift
    - Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift
  </read_first>
  <behavior>
    - Test 1: Indefinite mode shows `当前：无限常亮`, `bolt.fill`, and `常亮已开启：无限常亮`.
    - Test 2: Timed mode shows `还剩 {formattedDuration}` in the status row only, with `>= 1 小时` using up to two units, `>= 1 分钟` using minutes only, and `< 1 分钟` using seconds only.
    - Test 3: Pending state uses the exact status copy `正在切换为无限常亮...`, `正在切换为 15 分钟常亮...`, `正在切换为 30 分钟常亮...`, `正在切换为 1 小时常亮...`, `正在切换为 2 小时常亮...`, or `正在关闭常亮...`, while leaving confirmed selection unchanged.
    - Test 4: Failure state preserves the last confirmed active selection and shows the exact failure message without any `已结束` banner.
  </behavior>
  <action>Per D-08, D-09, D-10, and D-12, replace the old boolean presentation contract in `KeepAwakePresentation.swift` with a new `struct KeepAwakePresentation` that accepts the exact inputs `confirmedMode: KeepAwakeMode`, `pendingAction: KeepAwakePendingAction?`, `message: String?`, and `now: Date`. Add the exact computed properties `isIndefiniteActive`, `activeTimedPreset`, `statusText`, `iconSymbol`, `buttonToolTip`, and `isPending`. `statusText` must follow the exact UI contract: off with no message returns `nil`; indefinite returns `当前：无限常亮`; timed returns `还剩 {formattedDuration}`; pending returns one of `正在切换为无限常亮...`, `正在切换为 15 分钟常亮...`, `正在切换为 30 分钟常亮...`, `正在切换为 1 小时常亮...`, `正在切换为 2 小时常亮...`, or `正在关闭常亮...`; failure returns the exact `message`. `buttonToolTip` must be exactly `常亮已关闭`, `常亮已开启：无限常亮`, `常亮已开启：剩余 {formattedDuration}`, or `常亮状态更新中`. Use `DateComponentsFormatter` so timed formatting follows these exact rules from the UI spec: `>= 1 小时` shows at most two units such as `1 小时 28 分钟`; `>= 1 分钟` and `< 1 小时` shows minutes only such as `28 分钟`; `< 1 分钟` shows seconds only such as `42 秒`. Do not expose countdown values through any action-title property. Update `KeepAwakeMenuStateTests.swift` so it becomes presentation-focused coverage only, with the exact test functions `testIndefinitePresentationShowsCurrentInfiniteStatusRow()`, `testTimedPresentationShowsCountdownInStatusRowOnly()`, `testPendingPresentationUsesExactModeSpecificStatusCopy()`, and `testFailurePresentationKeepsMessageVisibleWithoutEndedBanner()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `struct KeepAwakePresentation`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `let confirmedMode: KeepAwakeMode`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `var activeTimedPreset: KeepAwakeDurationPreset?`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `var statusText: String?`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `var buttonToolTip: String`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `DateComponentsFormatter`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `当前：无限常亮`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `正在切换为无限常亮...`
    - `Mac OS Swiss Knife/KeepAwakePresentation.swift` contains `正在关闭常亮...`
    - `Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift` contains `testTimedPresentationShowsCountdownInStatusRowOnly`
    - `Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift` contains `testPendingPresentationUsesExactModeSpecificStatusCopy`
    - `Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift` contains `testFailurePresentationKeepsMessageVisibleWithoutEndedBanner`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests' -only-testing:'Mac OS Swiss KnifeTests/KeepAwakeSessionModelTests'</automated>
  </verify>
  <done>The keep-awake presentation contract now exposes exact Phase 4 status text, tooltip, icon, and active-selection state for off, indefinite, timed, pending, and failure flows without leaking countdown into action titles.</done>
</task>

</tasks>

<verification>
Run the new session-model tests first, then the expanded keep-awake presentation tests. Treat this plan as complete only when timed sessions replace each other truthfully, countdown data comes from an absolute end date, and every visible string needed by the menu contract is locked by XCTest.
</verification>

<success_criteria>
Phase 4 has a concrete keep-awake state foundation: fixed preset choices exist, timed sessions have a real lifecycle owner, countdown updates are cancellable and testable, and exact presentation copy is defined before AppKit menu wiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timed-keep-awake/04-01-SUMMARY.md`
</output>
