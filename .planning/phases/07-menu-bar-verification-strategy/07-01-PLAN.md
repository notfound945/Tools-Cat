---
phase: 07-menu-bar-verification-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
  - "Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift"
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - "Automated coverage explicitly proves the root `发送 WOL …` and `管理 WOL 设备…` rows dispatch through `StatusBarController` callback seams."
    - "Automated coverage explicitly proves the wake entry row disables during an in-flight send while the management row remains available."
    - "The new tests are clearly controller-seam coverage, not fake live tray-click automation."
  artifacts:
    - path: "Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift"
      provides: "Dedicated controller tests for wake and management entry-point coverage"
      contains: "final class StatusBarControllerEntryFlowTests"
    - path: "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
      provides: "Registers the new entry-flow regression file"
      contains: "StatusBarControllerEntryFlowTests.swift"
  key_links:
    - from: "Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift"
      to: "Mac OS Swiss Knife/StatusBarController.swift"
      via: "controller tests trigger the real menu item actions and observe `onOpenWOL` / `onOpenDeviceLibrary` callbacks"
      pattern: "onOpenWOL|onOpenDeviceLibrary"
    - from: "Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift"
      to: "Mac OS Swiss Knife/WOLSessionModel.swift"
      via: "the wake entry disabled-state test uses the shared send-state seam rather than a fake menu-local flag"
      pattern: "isSending|sendState"
---

<objective>
Add explicit automated coverage for the tray wake and management entry rows at the controller seam.

Purpose: AUTO-01 requires maintainers to see exactly which entry flows are covered automatically. Right now the repo proves menu grouping and saved-device dispatch, but it does not explicitly lock the root `发送 WOL …` and `管理 WOL 设备…` rows as covered seams.
Output: One dedicated controller test file that proves wake-entry dispatch, management-entry dispatch, and wake-entry disabled behavior during an in-flight send.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-menu-bar-verification-strategy/07-CONTEXT.md
@.planning/phases/07-menu-bar-verification-strategy/07-RESEARCH.md
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift
@Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock root wake and management entry coverage with explicit controller tests</name>
  <files>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift</files>
  <read_first>
    - .planning/phases/07-menu-bar-verification-strategy/07-CONTEXT.md
    - .planning/phases/07-menu-bar-verification-strategy/07-RESEARCH.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: Triggering the root `发送 WOL …` menu item dispatches exactly once through `onOpenWOL`.
    - Test 2: Triggering the root `管理 WOL 设备…` menu item dispatches exactly once through `onOpenDeviceLibrary`.
    - Test 3: While the shared `WOLSessionModel` is in-flight, the root `发送 WOL …` row is disabled but `管理 WOL 设备…` remains enabled.
    - Test 4: The test names and file make it obvious that this is controller-seam coverage, not real live-tray click automation.
  </behavior>
  <action>Create `Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift` as a dedicated regression file for entry-flow coverage. Use the same in-memory repository and fake keep-awake/session patterns already established in the menu test suite, but keep the file self-contained so it can be run as a focused slice. Trigger the real `NSMenuItem` actions from `StatusBarController.menuItemsForTesting` and assert callback counts through `onOpenWOL` and `onOpenDeviceLibrary`. Add one in-flight wake test that uses a blocking wake sender to prove the root `发送 WOL …` row disables while the shared session is sending, while `管理 WOL 设备…` remains enabled. Register the new test file in `project.pbxproj`. Do not claim these tests click the live `NSStatusItem`; they must stay framed as controller-seam coverage only.</action>
  <acceptance_criteria>
    - `Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift` contains `final class StatusBarControllerEntryFlowTests`
    - `Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift` contains `testWakeMenuEntryDispatchesThroughOnOpenWOL`
    - `Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift` contains `testManagementMenuEntryDispatchesThroughOnOpenDeviceLibrary`
    - `Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests.swift` contains `testWakeMenuEntryDisablesDuringInFlightSendWhileManagementEntryRemainsEnabled`
    - `Mac OS Swiss Knife.xcodeproj/project.pbxproj` contains `StatusBarControllerEntryFlowTests.swift`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerEntryFlowTests' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests'</automated>
  </verify>
  <done>The root wake and management entry rows now have explicit automated coverage that maintainers can point to without overstating live tray-click automation.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when one focused XCTest slice proves both root entry callbacks and the in-flight disabled-state rule, and the new file is clearly discoverable as the controller-seam coverage artifact for tray entry paths.
</verification>

<success_criteria>
Maintainers can identify one concrete automated artifact that proves the wake and management entry rows are covered at the controller seam.
</success_criteria>

<output>
After completion, create `.planning/phases/07-menu-bar-verification-strategy/07-01-SUMMARY.md`
</output>
