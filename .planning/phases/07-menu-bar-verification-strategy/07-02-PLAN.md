---
phase: 07-menu-bar-verification-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/run_menu_bar_verification_slice.sh"
  - ".planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md"
autonomous: true
requirements:
  - AUTO-01
  - AUTO-03
must_haves:
  truths:
    - "The repo exposes one stable command that runs the intended controller and utility-window regression slice for the polished wake and management surfaces."
    - "The phase validation doc maps each automated slice to the exact requirements and states plainly that the automated slice does not prove live tray clicks."
    - "Maintainers no longer have to rediscover scattered `xcodebuild` commands to rerun the intended hardening regression slice."
  artifacts:
    - path: "scripts/run_menu_bar_verification_slice.sh"
      provides: "Single entry point for the intended Phase 7 regression slice"
      contains: "StatusBarControllerEntryFlowTests"
    - path: ".planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md"
      provides: "Canonical Phase 7 validation contract and manual tray-entry checklist"
      contains: "AUTO-03"
  key_links:
    - from: "scripts/run_menu_bar_verification_slice.sh"
      to: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      via: "stable script runs the existing direct-launch WOL and device-library UI smoke tests"
      pattern: "testLaunchWithWOLWindowShowsPolishedSections|testLaunchWithSeededDeviceLibraryShowsManagementWindow|testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState"
    - from: ".planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md"
      to: "scripts/run_menu_bar_verification_slice.sh"
      via: "phase validation points to one canonical regression command"
      pattern: "run_menu_bar_verification_slice\\.sh"
---

<objective>
Publish one stable regression slice and a phase-owned validation contract for the menu-bar verification strategy.

Purpose: AUTO-03 requires a maintainer to run one clear regression slice, and AUTO-01 benefits when the exact automated coverage inventory is centralized in one validation artifact.
Output: One executable regression script plus `07-VALIDATION.md` that maps the intended controller/UI smoke slice and the remaining manual tray-entry layer.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-menu-bar-verification-strategy/07-CONTEXT.md
@.planning/phases/07-menu-bar-verification-strategy/07-RESEARCH.md
@.planning/phases/05-native-menu-polish/05-VALIDATION.md
@Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
@Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift
@Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
@Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a stable regression runner for the polished menu and utility-window slice</name>
  <files>scripts/run_menu_bar_verification_slice.sh</files>
  <read_first>
    - .planning/phases/07-menu-bar-verification-strategy/07-CONTEXT.md
    - .planning/phases/07-menu-bar-verification-strategy/07-RESEARCH.md
    - Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerMenuPolishTests.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerKeepAwakeMenuTests.swift
  </read_first>
  <action>Create `scripts/run_menu_bar_verification_slice.sh` as an executable shell script that runs one deterministic `xcodebuild test` slice for the current menu-bar strategy. The slice must cover the controller menu grouping tests, saved-device wake menu tests, keep-awake menu tests, the new entry-flow tests from Plan 01, and the three current direct-launch utility-window UI smoke tests. Keep the script simple: `set -euo pipefail`, use the existing Xcode project/scheme/destination, and print a short note that this slice proves controller seams and direct-launched windows but does not claim to click the live tray icon.</action>
  <acceptance_criteria>
    - `scripts/run_menu_bar_verification_slice.sh` contains `set -euo pipefail`
    - `scripts/run_menu_bar_verification_slice.sh` contains `StatusBarControllerEntryFlowTests`
    - `scripts/run_menu_bar_verification_slice.sh` contains `StatusBarControllerMenuPolishTests`
    - `scripts/run_menu_bar_verification_slice.sh` contains `StatusBarControllerWakeMenuTests`
    - `scripts/run_menu_bar_verification_slice.sh` contains `StatusBarControllerKeepAwakeMenuTests`
    - `scripts/run_menu_bar_verification_slice.sh` contains `testLaunchWithWOLWindowShowsPolishedSections`
    - `scripts/run_menu_bar_verification_slice.sh` contains `testLaunchWithSeededDeviceLibraryShowsManagementWindow`
    - `scripts/run_menu_bar_verification_slice.sh` contains `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run_menu_bar_verification_slice.sh</automated>
  </verify>
  <done>The repo now has one stable regression command for the intended menu-bar hardening slice.</done>
</task>

<task type="auto">
  <name>Task 2: Write the phase validation contract around the layered coverage truth</name>
  <files>.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-menu-bar-verification-strategy/07-CONTEXT.md
    - .planning/phases/07-menu-bar-verification-strategy/07-RESEARCH.md
    - .planning/phases/05-native-menu-polish/05-VALIDATION.md
    - scripts/run_menu_bar_verification_slice.sh
  </read_first>
  <action>Create `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` as the canonical Phase 7 validation contract. It must map `AUTO-01`, `AUTO-02`, and `AUTO-03` to the concrete controller tests, direct-launch UI smoke tests, the new regression script, and one explicit manual tray-entry checklist. State plainly that the automated slice covers controller seams and retained utility-window surfaces, while the live tray icon path remains manual unless separately automated later. Do not re-open Phase 8's broader `wave_0_complete` debt in this file beyond a narrow boundary note.</action>
  <acceptance_criteria>
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `AUTO-01`
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `AUTO-02`
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `AUTO-03`
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `run_menu_bar_verification_slice.sh`
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `manual tray-entry`
    - `.planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md` contains `does not prove live tray clicks`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "AUTO-01|AUTO-02|AUTO-03|run_menu_bar_verification_slice\\.sh|manual tray-entry|does not prove live tray clicks" .planning/phases/07-menu-bar-verification-strategy/07-VALIDATION.md</automated>
  </verify>
  <done>The Phase 7 validation contract now makes the automated/manual coverage boundary explicit and points to one stable automated slice.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when one named script executes the intended regression slice successfully and `07-VALIDATION.md` maps every Phase 7 requirement to explicit automated or manual coverage.
</verification>

<success_criteria>
Maintainers can run one stable regression slice and read one validation contract that explains exactly what that slice proves and what it does not.
</success_criteria>

<output>
After completion, create `.planning/phases/07-menu-bar-verification-strategy/07-02-SUMMARY.md`
</output>
