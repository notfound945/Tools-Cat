---
phase: 02-device-library-management
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - "Mac OS Swiss Knife/WOLSessionModel.swift"
  - "Mac OS Swiss Knife/WOLView.swift"
  - "Mac OS Swiss Knife/WOLWindow.swift"
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
  - "Mac OS Swiss KnifeTests/WOLSessionModelTests.swift"
  - "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
autonomous: true
requirements:
  - DEVS-01
  - DEVS-04
  - UX-02
must_haves:
  truths:
    - "Saved devices become the real preset source for Wake-on-LAN instead of a hardcoded array in the view."
    - "The canonical saved-device order is reused in the WOL picker and survives relaunch because both surfaces read the same store."
    - "A UI smoke path can open the manager window against isolated saved-device data without manual menu-bar automation."
  artifacts:
    - path: "Mac OS Swiss Knife/WOLView.swift"
      provides: "WOL picker bound to the shared saved-device library"
      contains: "ForEach(deviceLibrary.devices"
    - path: "Mac OS Swiss Knife/WOLSessionModel.swift"
      provides: "Preset selection state keyed to saved-device identity rather than a hardcoded MAC list"
      contains: "selectedSavedDeviceID"
    - path: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      provides: "Automated smoke path for the manager window using seeded device-library data"
      contains: "testLaunchWithSeededDeviceLibraryShowsManagementWindow"
  key_links:
    - from: "Mac OS Swiss Knife/AppDelegate.swift"
      to: "Mac OS Swiss Knife/WOLWindow.swift"
      via: "shared SavedDeviceLibraryStore injection"
      pattern: "WOLWindow\\(session: .*deviceLibrary:"
    - from: "Mac OS Swiss Knife/WOLView.swift"
      to: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      via: "picker content sourced from ordered saved devices"
      pattern: "deviceLibrary\\.devices"
    - from: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      to: "Mac OS Swiss Knife/AppDelegate.swift"
      via: "launch arguments that auto-open the manager window for smoke coverage"
      pattern: "--ui-test-open-device-library"
---

<objective>
Replace the last hardcoded device preset path with the shared saved-device library and add an automated smoke seam for the new management window.

Purpose: Finish Phase 2 by making the saved-device library the actual source of truth across both windows, not just the management surface.
Output: WOL integration with the shared library, updated WOL tests, and a UI smoke harness for the device manager.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-device-library-management/02-CONTEXT.md
@.planning/phases/02-device-library-management/02-RESEARCH.md
@.planning/phases/02-device-library-management/02-VALIDATION.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/WOLWindow.swift
@Mac OS Swiss Knife/SavedDevice.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss Knife/DeviceLibraryWindow.swift
@Mac OS Swiss Knife/DeviceLibraryView.swift

<interfaces>
Expected from `02-01` and `02-02`:

```swift
@MainActor
final class SavedDeviceLibraryStore: ObservableObject {
    @Published private(set) var devices: [SavedDevice]
}

final class DeviceLibraryWindow: NSWindowController {
    func show()
}
```

Current WOL contracts to evolve:

```swift
final class WOLSessionModel: ObservableObject {
    @Published var inputMode: InputMode
    @Published var selectedMac: String
    func sendCurrentSelection()
}

struct WOLView: View {
    @ObservedObject var session: WOLSessionModel
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded WOL presets with the shared saved-device library</name>
  <files>Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLWindow.swift, Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/SavedDeviceLibraryStore.swift, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-RESEARCH.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife/WOLView.swift
    - Mac OS Swiss Knife/WOLWindow.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
    - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
  </read_first>
  <behavior>
    - Test 1: Preset mode sends the MAC for the selected saved device instead of reading a hardcoded option array.
    - Test 2: When the selected saved device is deleted from the shared store, preset-mode send becomes disabled instead of using stale data.
    - Test 3: The WOL picker preserves the canonical saved-device order from `SavedDeviceLibraryStore` for `DEVS-04`.
    - Test 4: Manual MAC mode still behaves exactly as Phase 1 defined.
  </behavior>
  <action>Update the WOL flow so the saved-device library is the only preset source per the integration note in `02-CONTEXT.md`. Change `WOLSessionModel` to track `selectedSavedDeviceID: UUID?` instead of `selectedMac` for preset selection, while leaving manual mode untouched. Inject the shared `SavedDeviceLibraryStore` into `WOLView` and `WOLWindow`, and have the picker render `deviceLibrary.devices` in stored order with both device name and MAC visible. `sendCurrentSelection()` must resolve the currently selected saved device by `UUID`, use its `macAddress`, and return early if the device no longer exists. Remove the hardcoded `options` array from `WOLView.swift`. Update `AppDelegate.swift` so the same retained `SavedDeviceLibraryStore` instance is passed into both the manager window and the WOL window, allowing the two windows to stay open simultaneously per D-03 without diverging data.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `@Published var selectedSavedDeviceID: UUID?`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `guard let selectedSavedDeviceID`
    - `Mac OS Swiss Knife/WOLView.swift` does not contain `private let options`
    - `Mac OS Swiss Knife/WOLView.swift` contains `@ObservedObject var deviceLibrary: SavedDeviceLibraryStore`
    - `Mac OS Swiss Knife/WOLView.swift` contains `ForEach(deviceLibrary.devices`
    - `Mac OS Swiss Knife/WOLWindow.swift` contains `init(session: WOLSessionModel, deviceLibrary: SavedDeviceLibraryStore)`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `WOLWindow(session: wolSession, deviceLibrary: savedDeviceLibrary)`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `func testPresetModeUsesSelectedSavedDeviceMAC()`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `func testPresetSendDisablesWhenSelectedSavedDeviceDisappears()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/WOLSessionModelTests'</automated>
  </verify>
  <done>The WOL window no longer depends on source-edited presets and instead reads the same canonical saved-device library as the management surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add an isolated UI smoke seam for the management window</name>
  <files>Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift, Mac OS Swiss Knife.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-VALIDATION.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
    - Mac OS Swiss Knife/SavedDeviceRepository.swift
    - Mac OS Swiss Knife/DeviceLibraryWindow.swift
    - Mac OS Swiss Knife/DeviceLibraryView.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <action>Add a deterministic UI-test launch seam so automated smoke coverage can open the manager window without brittle menu-bar automation. Teach `AppDelegate.swift` to recognize launch arguments `--ui-test-open-device-library` and `--ui-test-user-defaults-suite <suite-name>`. When those arguments are present, build `SavedDeviceLibraryStore` with `UserDefaults(suiteName: suiteName)` and call `openDeviceLibraryWindow()` during launch. In `Mac_OS_Swiss_KnifeUITests.swift`, seed that suite with at least two `SavedDevice` records in persisted order, launch the app with both arguments, and assert the `设备库` window shows the seeded name, MAC, and note preview text. Keep this smoke focused on deterministic window launch and list rendering, not drag automation.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `--ui-test-open-device-library`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `--ui-test-user-defaults-suite`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `UserDefaults(suiteName:`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `func testLaunchWithSeededDeviceLibraryShowsManagementWindow()`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `app.launchArguments.append("--ui-test-open-device-library")`
    - `Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift` contains `XCTAssertTrue(app.windows["设备库"].waitForExistence`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow'</automated>
  </verify>
  <done>Phase 2 ends with an automated smoke path proving the manager window can render real persisted device-library data inside the app process.</done>
</task>

</tasks>

<verification>
Run the updated WOL session tests plus the targeted UI smoke. Manual follow-up from `02-VALIDATION.md` should still confirm real drag-reorder behavior and delete-cancel behavior on a live macOS surface after the automated work is green.
</verification>

<success_criteria>
The device library is now the shared local source of truth across management and WOL surfaces, and the phase has an automated app-launch smoke that exercises the new manager window against seeded persisted data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-library-management/02-03-SUMMARY.md`
</output>
