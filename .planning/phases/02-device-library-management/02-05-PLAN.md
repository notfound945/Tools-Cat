---
phase: 02-device-library-management
plan: 05
type: execute
wave: 5
depends_on:
  - "02-02"
  - "02-04"
files_modified:
  - "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
  - "Mac OS Swiss Knife/DeviceLibraryView.swift"
autonomous: true
gap_closure: true
requirements:
  - DEVS-01
  - DEVS-02
  - DEVS-03
  - DEVS-04
  - DEVS-05
  - RELY-01
  - UX-02
must_haves:
  truths:
    - "The live `设备库` window title is sourced from `DeviceLibraryManagementPresentation` instead of a hardcoded string."
    - "The live list title, empty-state copy, and save CTA are sourced from `DeviceLibraryManagementPresentation` instead of hardcoded strings."
    - "The existing compact list/form flow, delete confirmation, explicit reorder mode, and `device-row-*` smoke seam remain intact."
  artifacts:
    - path: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      provides: "Manager window title wired to the presentation contract"
      contains: "DeviceLibraryManagementPresentation.windowTitle"
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "Manager list title wired to the presentation contract"
      contains: "DeviceLibraryManagementPresentation.listTitle"
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "Manager empty-state copy wired to the presentation contract"
      contains: "DeviceLibraryManagementPresentation.emptyStateHeading"
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "Manager save CTA wired to the presentation contract while preserving the automation seam"
      contains: "DeviceLibraryManagementPresentation.saveButtonTitle"
  key_links:
    - from: "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      via: "live window title assignment"
      pattern: "window\\.title = DeviceLibraryManagementPresentation\\.windowTitle"
    - from: "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "list header, empty-state, and save button all consume the copy contract"
      pattern: "DeviceLibraryManagementPresentation\\.(listTitle|emptyStateHeading|emptyStateBody|saveButtonTitle)"
    - from: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      to: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      via: "existing deterministic accessibility seam stays queryable"
      pattern: "device-library-list|device-library-empty-state|device-row-"
---

<objective>
Close the remaining Phase 2 verification gap by wiring the live manager window and view to the existing presentation-copy contract.

Purpose: Make `DeviceLibraryManagementPresentation` the real source of truth for visible manager copy without reopening already-closed CRUD, reorder, delete-confirmation, or UI-smoke work.
Output: One focused wiring fix for `DeviceLibraryWindow.swift` and `DeviceLibraryView.swift`, plus verification that the contract is consumed live and the `device-row-*` seam still holds.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-device-library-management/02-CONTEXT.md
@.planning/phases/02-device-library-management/02-VERIFICATION.md
@.planning/phases/02-device-library-management/02-02-SUMMARY.md
@.planning/phases/02-device-library-management/02-04-SUMMARY.md
@Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift
@Mac OS Swiss Knife/DeviceLibraryWindow.swift
@Mac OS Swiss Knife/DeviceLibraryView.swift
@Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift
@Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift

<interfaces>
From `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift`:

```swift
enum DeviceLibraryManagementPresentation {
    static let windowTitle = "设备库"
    static let listTitle = "已保存设备"
    static let emptyStateHeading = "还没有已保存设备"
    static let emptyStateBody = "点击“添加设备”创建第一台设备。名称和 MAC 地址为必填项，备注为可选。"
    static let saveButtonTitle = "保存设备"
}
```

Existing automation seam in `Mac OS Swiss Knife/DeviceLibraryView.swift` that must survive unchanged:

```swift
.accessibilityIdentifier("device-library-list")
.accessibilityIdentifier("device-library-empty-state")
.accessibilityIdentifier("device-row-\\(device.id.uuidString)")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the remaining hardcoded manager copy with presentation-contract lookups</name>
  <files>Mac OS Swiss Knife/DeviceLibraryWindow.swift, Mac OS Swiss Knife/DeviceLibraryView.swift</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-VERIFICATION.md
    - .planning/phases/02-device-library-management/02-02-SUMMARY.md
    - .planning/phases/02-device-library-management/02-04-SUMMARY.md
    - Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift
    - Mac OS Swiss Knife/DeviceLibraryWindow.swift
    - Mac OS Swiss Knife/DeviceLibraryView.swift
    - Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift
    - Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
  </read_first>
  <action>Implement only the copy-contract wiring gap called out in `02-VERIFICATION.md`. In `Mac OS Swiss Knife/DeviceLibraryWindow.swift`, replace the hardcoded `设备库` assignment with `DeviceLibraryManagementPresentation.windowTitle`. In `Mac OS Swiss Knife/DeviceLibraryView.swift`, replace only the remaining contract-owned hardcoded strings with the existing presentation API: use `DeviceLibraryManagementPresentation.listTitle` for the list header, `DeviceLibraryManagementPresentation.emptyStateHeading` and `.emptyStateBody` for the empty state, and `DeviceLibraryManagementPresentation.saveButtonTitle` for the form save button. Keep the already-wired `reorderButtonTitle`, `formTitle(mode:)`, and `deleteConfirmationMessage(deviceName:)` calls intact. Per the verification gap and prior plans, do not reopen or weaken the `device-row-*` seam from `02-04`, do not change the scroll-stack vs `List` split that preserves reorder mode plus XCUITest queryability, and do not alter delete confirmation, reorder gating, or the compact list/form interaction model. This is a contract-consumption fix, not a redesign.</action>
  <acceptance_criteria>
    - `rg -n 'window.title = DeviceLibraryManagementPresentation\\.windowTitle' 'Mac OS Swiss Knife/DeviceLibraryWindow.swift'` finds the live window-title contract consumption
    - `rg -n 'Text\\(DeviceLibraryManagementPresentation\\.listTitle\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the live list-title contract consumption
    - `rg -n 'Text\\(DeviceLibraryManagementPresentation\\.emptyStateHeading\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the live empty-state heading contract consumption
    - `rg -n 'Text\\(DeviceLibraryManagementPresentation\\.emptyStateBody\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the live empty-state body contract consumption
    - `rg -n 'Button\\(DeviceLibraryManagementPresentation\\.saveButtonTitle\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the live save-button contract consumption
    - `rg -n 'DeviceLibraryManagementPresentation\\.(reorderButtonTitle|formTitle|deleteConfirmationMessage)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` still finds the already-closed presentation wiring that must not regress
    - `rg -n 'device-library-list|device-library-empty-state|device-row-' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` still finds the seeded smoke seam from `02-04`
    - `rg -n 'window.title = \"设备库\"|Text\\(\"已保存设备\"\\)|Text\\(\"还没有已保存设备\"\\)|Text\\(\"点击“添加设备”创建第一台设备。名称和 MAC 地址为必填项，备注为可选。\"\\)|Button\\(\"保存设备\"\\)' 'Mac OS Swiss Knife/DeviceLibraryWindow.swift' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>bash -lc '! rg -n "window.title = \"设备库\"|Text\\(\"已保存设备\"\\)|Text\\(\"还没有已保存设备\"\\)|Text\\(\"点击“添加设备”创建第一台设备。名称和 MAC 地址为必填项，备注为可选。\"\\)|Button\\(\"保存设备\"\\)" "Mac OS Swiss Knife/DeviceLibraryWindow.swift" "Mac OS Swiss Knife/DeviceLibraryView.swift" &gt;/dev/null &amp;&amp; rg -n "DeviceLibraryManagementPresentation\\.windowTitle" "Mac OS Swiss Knife/DeviceLibraryWindow.swift" &amp;&amp; rg -n "DeviceLibraryManagementPresentation\\.(listTitle|emptyStateHeading|emptyStateBody|saveButtonTitle)" "Mac OS Swiss Knife/DeviceLibraryView.swift" &amp;&amp; rg -n "device-library-list|device-library-empty-state|device-row-" "Mac OS Swiss Knife/DeviceLibraryView.swift" &amp;&amp; xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination "platform=macOS" -only-testing:"Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests" -only-testing:"Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow"'</automated>
  </verify>
  <done>The only remaining live-copy drift is removed: the manager window and view consume `DeviceLibraryManagementPresentation` for the required strings, and the existing manager behavior plus `device-row-*` smoke seam remain unchanged.</done>
</task>

</tasks>

<verification>
Treat this as a narrow contract-wiring closure. The implementation is complete only when grep proves the live window/view consume the presentation API, the old hardcoded strings are gone from those code paths, and the existing seeded manager-window smoke still passes.
</verification>

<success_criteria>
`02-VERIFICATION.md`'s remaining gap can be closed because the live manager window and view now consume the exact-copy presentation contract for the missing strings without regressing reorder mode, delete confirmation, compact list/form behavior, or the seeded `device-row-*` automation seam.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-library-management/02-05-SUMMARY.md`
</output>
