---
phase: 02-device-library-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife/SavedDevice.swift"
  - "Mac OS Swiss Knife/SavedDeviceRepository.swift"
  - "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
  - "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
  - "Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests.swift"
  - "Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift"
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
autonomous: true
requirements:
  - DEVS-01
  - DEVS-02
  - DEVS-03
  - DEVS-04
  - DEVS-05
  - RELY-01
must_haves:
  truths:
    - "Saved devices can be created, edited, deleted, and reordered without editing source code."
    - "Invalid device drafts never persist because save stays gated by name and MAC validation."
    - "Device identity stays stable across edits and reorder operations."
  artifacts:
    - path: "Mac OS Swiss Knife/SavedDevice.swift"
      provides: "Stable saved-device model with UUID identity, note field, and persisted order"
      contains: "struct SavedDevice"
    - path: "Mac OS Swiss Knife/SavedDeviceRepository.swift"
      provides: "UserDefaults-backed persistence seam and testable repository contract"
      contains: "protocol SavedDeviceRepository"
    - path: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      provides: "Shared observable source of truth for the ordered device library"
      contains: "final class SavedDeviceLibraryStore"
    - path: "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
      provides: "List/form/reorder/delete state machine for the management window"
      contains: "final class DeviceLibrarySessionModel"
  key_links:
    - from: "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
      to: "Mac OS Swiss Knife/ManualMACValidator.swift"
      via: "save gating and MAC normalization"
      pattern: "ManualMACValidator\\.validate"
    - from: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      to: "Mac OS Swiss Knife/SavedDeviceRepository.swift"
      via: "load and save operations"
      pattern: "repository\\.(loadDevices|saveDevices)"
    - from: "Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift"
      to: "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
      via: "draft, delete-confirm, and reorder coverage"
      pattern: "saveDraft|requestDelete|confirmDelete|moveDevices"
---

<objective>
Establish the saved-device data contracts, persistence seam, and session-state logic that every later Phase 2 surface will build on.

Purpose: Make device CRUD and ordering truthful before any new window/UI wiring lands.
Output: Persisted saved-device model, shared observable store, management session model, and focused XCTest coverage.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-device-library-management/02-CONTEXT.md
@.planning/phases/02-device-library-management/02-RESEARCH.md
@.planning/phases/02-device-library-management/02-VALIDATION.md
@Mac OS Swiss Knife/ManualMACValidator.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife.xcodeproj/project.pbxproj

<interfaces>
From `Mac OS Swiss Knife/ManualMACValidator.swift`:

```swift
enum ManualMACValidator {
    static func validate(_ input: String) -> ManualMACValidation
}
```

From `Mac OS Swiss Knife/WOLSessionModel.swift`:

```swift
final class WOLSessionModel: ObservableObject {
    @Published var inputMode: InputMode
    @Published var sendState: WakeSendState
}
```

Phase 2 foundation should introduce these contracts for downstream plans:

```swift
struct SavedDevice: Codable, Equatable, Identifiable {
    let id: UUID
    var name: String
    var macAddress: String
    var note: String
    var sortOrder: Int
}

protocol SavedDeviceRepository {
    func loadDevices() throws -> [SavedDevice]
    func saveDevices(_ devices: [SavedDevice]) throws
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create saved-device persistence contracts and repository coverage</name>
  <files>Mac OS Swiss Knife/SavedDevice.swift, Mac OS Swiss Knife/SavedDeviceRepository.swift, Mac OS Swiss Knife/SavedDeviceLibraryStore.swift, Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests.swift, Mac OS Swiss Knife.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-RESEARCH.md
    - .planning/phases/02-device-library-management/02-VALIDATION.md
    - Mac OS Swiss Knife/ManualMACValidator.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: Loading from an empty `UserDefaults` suite returns `[]`.
    - Test 2: Saving devices with `name`, normalized `macAddress`, `note`, and `sortOrder` reloads the same ordered records.
    - Test 3: Saving a reordered array and reloading preserves the user-defined order for `DEVS-04`.
    - Test 4: Deleting a device and saving persists the removal across reload for `DEVS-03`.
  </behavior>
  <action>Create `SavedDevice.swift`, `SavedDeviceRepository.swift`, and `SavedDeviceLibraryStore.swift` per D-10 and D-13. `SavedDevice` must declare `id: UUID`, `name: String`, `macAddress: String`, `note: String`, and `sortOrder: Int`. `SavedDeviceRepository` must expose exactly `loadDevices() throws -> [SavedDevice]` and `saveDevices(_ devices: [SavedDevice]) throws`. Implement `UserDefaultsSavedDeviceRepository(defaults: UserDefaults = .standard)` with the storage key `saved_devices`, `JSONEncoder`/`JSONDecoder`, and contiguous order normalization before saving. Add `@MainActor final class SavedDeviceLibraryStore: ObservableObject` with `@Published private(set) var devices: [SavedDevice] = []` plus `reload()`, `replaceAll(_:)`, and helper mutations that later plans can reuse. Register all new app and test files in `Mac OS Swiss Knife.xcodeproj/project.pbxproj`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/SavedDevice.swift` contains `struct SavedDevice: Codable, Equatable, Identifiable`
    - `Mac OS Swiss Knife/SavedDevice.swift` contains `var note: String` and `var sortOrder: Int`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `protocol SavedDeviceRepository`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `final class UserDefaultsSavedDeviceRepository`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `private let key = "saved_devices"`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `@MainActor final class SavedDeviceLibraryStore: ObservableObject`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `@Published private(set) var devices: [SavedDevice] = []`
    - `Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests.swift` contains `func testEmptySuiteLoadsNoDevices()`
    - `Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests.swift` contains `func testSaveAndReloadPreservesNameMACNoteAndOrder()`
    - `Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests.swift` contains `func testDeletePersistsAcrossReload()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/SavedDeviceRepositoryTests'</automated>
  </verify>
  <done>Saved-device persistence is test-backed, uses a fakeable repository seam, and stores the canonical ordered library needed by later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement device-library session logic for add, edit, delete, and reorder</name>
  <files>Mac OS Swiss Knife/DeviceLibrarySessionModel.swift, Mac OS Swiss Knife/SavedDeviceLibraryStore.swift, Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift, Mac OS Swiss Knife.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-UI-SPEC.md
    - Mac OS Swiss Knife/ManualMACValidator.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
    - Mac OS Swiss Knife/DeviceLibrarySessionModel.swift
    - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: Empty trimmed `名称` or invalid MAC leaves `saveDraft()` blocked and keeps the draft onscreen for `RELY-01`.
    - Test 2: Add flow saves a device with uppercase colon-separated MAC and optional note for `DEVS-01` and `DEVS-05`.
    - Test 3: Edit flow preloads an existing device, persists changes, and keeps the same `UUID` for `DEVS-02`.
    - Test 4: Delete requires `requestDelete(deviceID:)` before `confirmDelete()` removes the row for `DEVS-03`.
    - Test 5: `moveDevices(fromOffsets:toOffset:)` rewrites persisted order and survives `reload()` for `DEVS-04`.
  </behavior>
  <action>Create `DeviceLibrarySessionModel.swift` per D-08 through D-12 as an `@MainActor final class DeviceLibrarySessionModel: ObservableObject` that owns `devices`, `screen`, `draftName`, `draftMACAddress`, `draftNote`, `isReordering`, `pendingDeleteDevice`, `validationMessage`, and `saveErrorMessage`. Reuse `ManualMACValidator.validate(_:)` so save rules match Phase 1 exactly. Add concrete methods `reloadDevices()`, `beginAdd()`, `beginEdit(deviceID:)`, `cancelForm()`, `saveDraft()`, `requestDelete(deviceID:)`, `cancelDelete()`, `confirmDelete()`, and `moveDevices(fromOffsets:toOffset:)`. `saveDraft()` must trim the name, uppercase the validated MAC from `ManualMACValidation.valid`, preserve `UUID` on edit, append new devices at the end of the canonical order, and push every successful mutation through `SavedDeviceLibraryStore`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `@MainActor final class DeviceLibrarySessionModel: ObservableObject`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func beginAdd()`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func beginEdit(deviceID: UUID)`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func saveDraft()`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func requestDelete(deviceID: UUID)`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func confirmDelete()`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `func moveDevices(fromOffsets: IndexSet, toOffset: Int)`
    - `Mac OS Swiss Knife/DeviceLibrarySessionModel.swift` contains `ManualMACValidator.validate`
    - `Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift` contains `func testInvalidDraftBlocksSave()`
    - `Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift` contains `func testAddNormalizesMACAndPersistsOptionalNote()`
    - `Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift` contains `func testEditPreservesIdentityAndPersistsChanges()`
    - `Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift` contains `func testDeleteRequiresConfirmation()`
    - `Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests.swift` contains `func testReorderPersistsCanonicalOrder()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests'</automated>
  </verify>
  <done>The management window now has a test-backed session owner that enforces validation-before-save and produces persisted CRUD/reorder outcomes without view-local persistence logic.</done>
</task>

</tasks>

<verification>
Run the targeted repository and session-model suites, then confirm the Xcode project includes the new app and test files so later waves do not fail on missing target membership.
</verification>

<success_criteria>
Phase 2 now has a reusable saved-device source of truth, a fakeable local persistence seam, and a session model that can drive the native management UI without reintroducing view-local state or duplicated MAC validation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-library-management/02-01-SUMMARY.md`
</output>
