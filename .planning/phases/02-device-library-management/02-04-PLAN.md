---
phase: 02-device-library-management
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss Knife/DeviceLibraryView.swift"
  - "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
autonomous: true
gap_closure: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Launching the manager through the isolated defaults-suite path opens the `设备库` window directly instead of leaving the user at an empty or hidden surface."
    - "When seeded persisted devices exist, the launched manager renders those saved rows with their real device content instead of falling back to the empty state."
    - "The seeded persisted library order is visible as soon as the manager window appears, proving the launched surface is reading stored device-library data."
  artifacts:
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "Deterministic accessibility seam for populated and empty device-library states"
      contains: "device-row-"
    - path: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      provides: "Seeded manager-window smoke test that asserts `device-row-*` visibility"
      contains: "testLaunchWithSeededDeviceLibraryShowsManagementWindow"
    - path: "Mac OS Swiss Knife/AppDelegate.swift"
      provides: "Isolated launch-argument seam for opening the manager window against a dedicated defaults suite"
      contains: "--ui-test-open-device-library"
  key_links:
    - from: "Mac OS Swiss Knife/AppDelegate.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "launch arguments seed a dedicated UserDefaults suite that reloads into the manager surface on show"
      pattern: "--ui-test-user-defaults-suite|session\\.reloadDevices\\(\\)"
    - from: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      to: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      via: "row accessibility identifiers queried by seeded UUID"
      pattern: "device-row-"
    - from: "Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift"
      to: "Mac OS Swiss Knife/AppDelegate.swift"
      via: "launch arguments that auto-open the manager window for the seeded smoke path"
      pattern: "--ui-test-open-device-library"
---

<objective>
Close the one remaining Phase 2 verification gap by making seeded device-library rows queryable under XCUITest on the real manager window launch path.

Purpose: Turn the existing isolated launch seam into a passing regression gate instead of a hollow smoke test.
Output: A manager-surface accessibility seam that exposes seeded `device-row-*` elements plus a passing targeted XCUITest that keeps asserting those row identifiers.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-device-library-management/02-CONTEXT.md
@.planning/phases/02-device-library-management/02-RESEARCH.md
@.planning/phases/02-device-library-management/02-UI-SPEC.md
@.planning/phases/02-device-library-management/02-VERIFICATION.md
@.planning/phases/02-device-library-management/02-03-SUMMARY.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/DeviceLibraryWindow.swift
@Mac OS Swiss Knife/DeviceLibraryView.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift

<interfaces>
From `Mac OS Swiss Knife/AppDelegate.swift`:

```swift
private struct LaunchConfiguration {
    let shouldOpenDeviceLibrary: Bool
    let userDefaults: UserDefaults?
}
```

From `Mac OS Swiss Knife/DeviceLibraryWindow.swift`:

```swift
final class DeviceLibraryWindow: NSWindowController {
    func show()
}
```

From `Mac OS Swiss Knife/DeviceLibraryView.swift`:

```swift
struct DeviceLibraryView: View {
    @ObservedObject var session: DeviceLibrarySessionModel
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make seeded manager rows queryable and keep the smoke as the hard gate</name>
  <files>Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/DeviceLibraryView.swift, Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-UI-SPEC.md
    - .planning/phases/02-device-library-management/02-VERIFICATION.md
    - .planning/phases/02-device-library-management/02-03-SUMMARY.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/DeviceLibraryWindow.swift
    - Mac OS Swiss Knife/DeviceLibraryView.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
    - Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift
  </read_first>
  <action>Keep the existing isolated defaults-suite launch path from `02-03` intact, then close the row-observability gap without changing the compact manager-window behavior locked by D-01, D-03, D-05, D-06, D-07, and D-11. Update `DeviceLibraryView.swift` so the populated manager state exposes a deterministic root identifier such as `device-library-list`, the empty branch exposes `device-library-empty-state`, and every rendered saved device exposes a queryable outer accessibility element with the exact identifier format `device-row-\(device.id.uuidString)`. Do not weaken the seam to name-only or MAC-only text assertions; preserve the explicit row identifier contract from the verification report. If the current SwiftUI `List` continues to swallow row identifiers on macOS, replace only the populated row container with the smallest queryable alternative that still preserves the same list-first layout, row actions, and reorder-mode behavior, rather than changing window ownership or moving CRUD logic out of `DeviceLibrarySessionModel`. Then update `testLaunchWithSeededDeviceLibraryShowsManagementWindow` to keep seeding the isolated suite, keep launching through `--ui-test-open-device-library` plus `--ui-test-user-defaults-suite`, and wait for the manager window plus both seeded `device-row-*` identifiers. The test should still assert that the empty-state copy is absent after seeded launch.</action>
  <acceptance_criteria>
    - `rg -n 'accessibilityIdentifier\\("device-library-list"\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the populated-state root seam
    - `rg -n 'accessibilityIdentifier\\("device-library-empty-state"\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the empty-state seam
    - `rg -n 'device-row-\\\\\\(device.id.uuidString\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` finds the row identifier contract
    - `rg -n 'device-name-\\\\\\(device.id.uuidString\\)|device-mac-\\\\\\(device.id.uuidString\\)' 'Mac OS Swiss Knife/DeviceLibraryView.swift'` still finds the per-field identifiers
    - `rg -n 'testLaunchWithSeededDeviceLibraryShowsManagementWindow|device-row-\\\\\\(seededDevices\\[0\\].id.uuidString\\)|device-row-\\\\\\(seededDevices\\[1\\].id.uuidString\\)' 'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests.swift'` finds the targeted regression gate and both seeded row queries
    - `rg -n -- '--ui-test-open-device-library|--ui-test-user-defaults-suite' 'Mac OS Swiss Knife/AppDelegate.swift'` confirms the launch seam remains intact
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeUITests/Mac_OS_Swiss_KnifeUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow'</automated>
  </verify>
  <done>The seeded manager-window smoke path goes green because XCUITest can observe the seeded `device-row-*` elements in the launched app, and the empty state no longer appears during that seeded run.</done>
</task>

</tasks>

<verification>
Run the targeted UI smoke test from the verification gap report. Treat a passing `testLaunchWithSeededDeviceLibraryShowsManagementWindow` as the required closure signal for this gap; do not replace it with a weaker text-only smoke.
</verification>

<success_criteria>
`$gsd-execute-phase 2 --gaps-only` can execute a single focused plan that makes `device-row-*` visible to XCUITest under the isolated launch seam and turns the existing targeted smoke into a passing regression gate.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-library-management/02-04-SUMMARY.md`
</output>
