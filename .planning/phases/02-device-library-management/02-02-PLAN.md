---
phase: 02-device-library-management
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
  - "Mac OS Swiss Knife/DeviceLibraryView.swift"
  - "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
  - "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss Knife/StatusBarController.swift"
  - "Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift"
  - "Mac OS Swiss Knife.xcodeproj/project.pbxproj"
autonomous: true
requirements:
  - UX-02
  - DEVS-02
  - DEVS-03
  - DEVS-04
  - DEVS-05
  - RELY-01
must_haves:
  truths:
    - "Users can open a dedicated `设备库` window from a menu item labeled `管理设备…`."
    - "The management window stays compact, list-first, and switches into one shared add/edit form instead of a permanent split editor."
    - "Delete requires confirmation and drag reordering appears only in explicit reorder mode."
  artifacts:
    - path: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      provides: "Dedicated retained AppKit window for device management"
      contains: "final class DeviceLibraryWindow"
    - path: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      provides: "SwiftUI list/form management surface with empty, list, form, and delete-confirm states"
      contains: "struct DeviceLibraryView: View"
    - path: "Mac OS Swiss Knife/StatusBarController.swift"
      provides: "Menu entry point for opening the manager window"
      contains: "管理设备…"
    - path: "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
      provides: "Exact copy contract for window titles, button labels, and delete confirmation"
      contains: "enum DeviceLibraryManagementPresentation"
  key_links:
    - from: "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      via: "window title and other visible chrome sourced from the exact-copy presentation contract"
      pattern: "DeviceLibraryManagementPresentation\\.windowTitle"
    - from: "Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "list, form, empty-state, reorder, and delete-confirm copy sourced from the exact-copy presentation contract"
      pattern: "DeviceLibraryManagementPresentation\\.(listTitle|emptyStateHeading|emptyStateBody|saveButtonTitle|reorderButtonTitle|formTitle|deleteConfirmationMessage)"
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/AppDelegate.swift"
      via: "onOpenDeviceLibrary callback"
      pattern: "onOpenDeviceLibrary"
    - from: "Mac OS Swiss Knife/DeviceLibraryWindow.swift"
      to: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      via: "NSHostingView(rootView: DeviceLibraryView"
      pattern: "NSHostingView\\(rootView: DeviceLibraryView"
    - from: "Mac OS Swiss Knife/DeviceLibraryView.swift"
      to: "Mac OS Swiss Knife/DeviceLibrarySessionModel.swift"
      via: "ObservedObject-driven list/form state"
      pattern: "@ObservedObject var session"
---

<objective>
Build the dedicated native device-library management surface and expose it from the menu without collapsing it into the existing WOL window.

Purpose: Deliver the actual user-facing management workflow required by `UX-02` while preserving the compact, native utility feel locked in CONTEXT and UI-SPEC.
Output: Menu entry, retained management window, SwiftUI list/form UI, and exact-copy presentation tests.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-device-library-management/02-CONTEXT.md
@.planning/phases/02-device-library-management/02-UI-SPEC.md
@.planning/phases/02-device-library-management/02-VALIDATION.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss Knife/WOLWindow.swift
@Mac OS Swiss Knife/SavedDevice.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss Knife/DeviceLibrarySessionModel.swift

<interfaces>
Expected from `02-01`:

```swift
@MainActor
final class SavedDeviceLibraryStore: ObservableObject {
    @Published private(set) var devices: [SavedDevice]
    func reload() throws
}

@MainActor
final class DeviceLibrarySessionModel: ObservableObject {
    @Published var devices: [SavedDevice]
    @Published var isReordering: Bool
    @Published var pendingDeleteDevice: SavedDevice?
    func beginAdd()
    func beginEdit(deviceID: UUID)
    func cancelForm()
    func saveDraft()
    func requestDelete(deviceID: UUID)
    func confirmDelete()
    func moveDevices(fromOffsets: IndexSet, toOffset: Int)
}
```

From `Mac OS Swiss Knife/WOLWindow.swift`:

```swift
final class WOLWindow: NSWindowController, NSWindowDelegate {
    func show()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the manager-window copy and state labels with presentation tests</name>
  <files>Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift, Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift, Mac OS Swiss Knife.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-UI-SPEC.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/WOLWindow.swift
    - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: The window title is `设备库`, list title is `已保存设备`, and the primary CTA is `保存设备`.
    - Test 2: The reorder action toggles between `重新排序` and `完成排序`.
    - Test 3: Empty-state heading/body and delete-confirmation text exactly match the approved UI spec.
    - Test 4: Form titles switch only between `添加设备` and `编辑设备`.
  </behavior>
  <action>Create `DeviceLibraryManagementPresentation.swift` as the exact-copy contract for D-02, D-08, D-11, and D-12. Use concrete API names that the view can consume directly, such as `windowTitle`, `listTitle`, `emptyStateHeading`, `emptyStateBody`, `saveButtonTitle`, `reorderButtonTitle(isReordering:)`, `formTitle(mode:)`, and `deleteConfirmationMessage(deviceName:)`. Add `DeviceLibraryManagementPresentationTests.swift` to assert the strings `设备库`, `已保存设备`, `还没有已保存设备`, `点击“添加设备”创建第一台设备。名称和 MAC 地址为必填项，备注为可选。`, `保存设备`, `重新排序`, `完成排序`, and the destructive confirmation format `删除设备: 删除后不会恢复。确定删除“{设备名称}”吗？`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `enum DeviceLibraryManagementPresentation`
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `static let windowTitle = "设备库"`
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `static let listTitle = "已保存设备"`
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `static let saveButtonTitle = "保存设备"`
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `static func reorderButtonTitle(isReordering: Bool) -> String`
    - `Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift` contains `static func deleteConfirmationMessage(deviceName: String) -> String`
    - `Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift` contains `func testStaticCopyMatchesUISpec()`
    - `Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift` contains `func testReorderButtonTitles()`
    - `Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests.swift` contains `func testDeleteConfirmationIncludesDeviceName()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests'</automated>
  </verify>
  <done>The exact visible manager-window copy is codified in one place and protected by unit tests before the window/UI wiring uses it.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the compact management window, list/form flow, and menu entry</name>
  <files>Mac OS Swiss Knife/DeviceLibraryView.swift, Mac OS Swiss Knife/DeviceLibraryWindow.swift, Mac OS Swiss Knife/DeviceLibrarySessionModel.swift, Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift</files>
  <read_first>
    - .planning/phases/02-device-library-management/02-CONTEXT.md
    - .planning/phases/02-device-library-management/02-UI-SPEC.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/WOLWindow.swift
    - Mac OS Swiss Knife/DeviceLibrarySessionModel.swift
    - Mac OS Swiss Knife/DeviceLibraryManagementPresentation.swift
  </read_first>
  <action>Implement the user-facing management surface per D-01 through D-12. Create `DeviceLibraryWindow.swift` as a retained `NSWindowController` with `NSRect(x: 0, y: 0, width: 520, height: 420)`, `.titled` and `.closable` style masks, and `NSHostingView(rootView: DeviceLibraryView(session: session))`. Do not hardcode visible window copy in the window or view; source the window title and all shared user-facing copy from `DeviceLibraryManagementPresentation.swift` so `DeviceLibraryWindow.swift` consumes `DeviceLibraryManagementPresentation.windowTitle` and `DeviceLibraryView.swift` consumes the exact-copy contract for list, empty, form, reorder, save, and delete-confirm text. Create `DeviceLibraryView.swift` with exactly two internal modes: default list mode and one shared add/edit form mode per D-09, so create and edit use the same fields, validation messaging, save CTA behavior, and cancel path instead of diverging flows. The list mode must show the contract-backed list title, the row layout name + MAC + optional one-line note preview, a right-aligned add button, and a reorder button that flips through `DeviceLibraryManagementPresentation.reorderButtonTitle(isReordering:)`. While `isReordering` is true, expose drag handles and hide or disable edit/delete actions per D-11. The form mode must render the fields `名称`, `MAC 地址`, and `备注（可选）`, show inline validation using the session model, keep the contract-backed save CTA disabled until `session.canSaveDraft` is true, and use `取消` to return without persisting. Update `StatusBarController.swift` to add `var onOpenDeviceLibrary: (() -> Void)?`, place the dedicated `NSMenuItem(title: "管理设备…", action: #selector(openDeviceLibrary), keyEquivalent: "")` entry directly below `发送 WOL …` and above the first separator per D-04, and activate the app before invoking the callback. Update `AppDelegate.swift` to retain one shared `SavedDeviceLibraryStore`, one `DeviceLibrarySessionModel`, and one `DeviceLibraryWindow`, while keeping the manager and WOL windows independent per D-03.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `var onOpenDeviceLibrary: (() -> Void)?`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `NSMenuItem(title: "管理设备…"`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `@objc private func openDeviceLibrary()`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `private var deviceLibraryWindow: DeviceLibraryWindow?`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `private var savedDeviceLibrary = SavedDeviceLibraryStore`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `private func openDeviceLibraryWindow()`
    - `Mac OS Swiss Knife/DeviceLibraryWindow.swift` contains `width: 520, height: 420`
    - `Mac OS Swiss Knife/DeviceLibraryWindow.swift` contains `DeviceLibraryManagementPresentation.windowTitle`
    - `Mac OS Swiss Knife/DeviceLibraryWindow.swift` contains `NSHostingView(rootView: DeviceLibraryView(session: session`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.listTitle`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.emptyStateHeading`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.emptyStateBody`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `Text("名称")`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `Text("MAC 地址")`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `Text("备注（可选）")`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.saveButtonTitle`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.reorderButtonTitle`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.formTitle`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `Button("取消")`
    - `Mac OS Swiss Knife/DeviceLibraryView.swift` contains `DeviceLibraryManagementPresentation.deleteConfirmationMessage`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/DeviceLibraryManagementPresentationTests' -only-testing:'Mac OS Swiss KnifeTests/DeviceLibrarySessionModelTests'</automated>
  </verify>
  <done>The app exposes a dedicated compact device manager from the menu, with the list-first native flow, inline validation, reorder mode gating, and delete confirmation required for Phase 2.</done>
</task>

</tasks>

<verification>
Run the exact-copy presentation tests and the session-model suite together. The resulting implementation should leave the existing WOL window intact and add a second retained native window instead of merging the surfaces.
</verification>

<success_criteria>
Users can now discover and use a dedicated native management surface from the menu, and that surface matches the locked compact-window/list-form interaction contract instead of introducing inline menu editing or a heavy settings panel.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-library-management/02-02-SUMMARY.md`
</output>
