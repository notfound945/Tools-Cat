---
phase: 16-release-signing-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "release.sh"
  - "scripts/release/preflight-signing.sh"
  - "scripts/release/export-options-developer-id.plist.template"
  - "scripts/release/inspect-signature.sh"
  - "scripts/release/verify-release-readiness.sh"
  - "Tools Cat.xcodeproj/project.pbxproj"
autonomous: true
requirements:
  - DIST-01
user_setup:
  - service: apple-developer
    why: "Developer ID archive/export signing and future notarization bootstrap"
    env_vars:
      - name: RELEASE_TEAM_ID
        source: "Apple Developer Team ID for the maintainer account"
      - name: RELEASE_SIGNING_IDENTITY
        source: "Full Keychain certificate label for the Developer ID Application identity"
      - name: RELEASE_NOTARY_PROFILE
        source: "Keychain profile name created with xcrun notarytool store-credentials"
    dashboard_config:
      - task: "Install a Developer ID Application certificate for Team Y2YJ48R9GL"
        location: "Apple Developer Certificates / Keychain Access"
      - task: "Store a validated notarytool profile named TOOLS_CAT_NOTARY"
        location: "Terminal: xcrun notarytool store-credentials"
must_haves:
  truths:
    - "The maintainer still uses `release.sh` as the only public release command, but that command now fails before the build unless the explicit release identity inputs are present."
    - "The Phase 16 release path produces `dist/export/Tools Cat.app` from an `xcodebuild archive` plus `xcodebuild -exportArchive` boundary instead of picking a Release app from DerivedData."
    - "Release signing readiness is explicit in both automation and project settings: Release uses hardened runtime and the release flow names the Team ID, Developer ID Application identity, and notary profile it expects."
  artifacts:
    - path: "release.sh"
      provides: "The sole maintainer-facing release entrypoint for archive/export signing"
      contains: "xcodebuild archive"
    - path: "scripts/release/preflight-signing.sh"
      provides: "Fail-fast validation of tools, Team ID, Developer ID Application identity, and notary profile"
      contains: "RELEASE_TEAM_ID"
    - path: "scripts/release/export-options-developer-id.plist.template"
      provides: "Deterministic Developer ID export policy"
      contains: "<string>developer-id</string>"
    - path: "scripts/release/inspect-signature.sh"
      provides: "Post-export signature and entitlements inspection for the exported app"
      contains: "codesign -d --entitlements :- --verbose=4"
    - path: "Tools Cat.xcodeproj/project.pbxproj"
      provides: "Release hardened-runtime and Team-ID readiness in the Xcode project"
      contains: "ENABLE_HARDENED_RUNTIME = YES;"
  key_links:
    - from: "release.sh"
      to: "scripts/release/preflight-signing.sh"
      via: "release entrypoint must run the fail-fast preflight before archive/export work starts"
      pattern: "preflight-signing\\.sh|RELEASE_TEAM_ID|RELEASE_SIGNING_IDENTITY|RELEASE_NOTARY_PROFILE"
    - from: "release.sh"
      to: "scripts/release/export-options-developer-id.plist.template"
      via: "archive export must use a deterministic developer-id export policy instead of local Xcode defaults"
      pattern: "export-options|developer-id|-exportArchive"
    - from: "Tools Cat.xcodeproj/project.pbxproj"
      to: "release.sh"
      via: "Release hardened runtime and Team ID must support the archive/export signing boundary"
      pattern: "ENABLE_HARDENED_RUNTIME = YES;|DEVELOPMENT_TEAM = Y2YJ48R9GL;"
---

<objective>
Move Phase 16 onto a distribution-grade signing boundary by turning `release.sh` into a fail-fast archive/export pipeline that emits a Developer ID signed `Tools Cat.app`.

Purpose: The repo currently builds a local Release app from DerivedData and packages an explicitly untrusted DMG. Phase 16 must replace that app build seam now, so later phases can add signed DMG packaging and notarization without inventing a second release path.
Output: One public `release.sh` entrypoint, helper scripts under `scripts/release/`, a deterministic Developer ID export policy, and explicit Release hardened-runtime readiness in the Xcode project.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-release-signing-readiness/16-CONTEXT.md
@.planning/phases/16-release-signing-readiness/16-RESEARCH.md
@README.md
@release.sh
@build_dmg.sh
@Tools Cat.xcodeproj/project.pbxproj
@Tools Cat/Tools_Cat.entitlements

<interfaces>
Current verified release/build state on 2026-04-16:
```text
CODE_SIGN_STYLE = Automatic
ENABLE_HARDENED_RUNTIME = NO
PROVISIONING_PROFILE_REQUIRED = NO
_DEVELOPMENT_TEAM_IS_EMPTY = YES
release.sh => xcodebuild clean build + DerivedData app pickup + build_dmg.sh
```

Release contract to implement in this plan:
```text
Public command: sh ./release.sh
Required env vars:
  RELEASE_TEAM_ID=Y2YJ48R9GL
  RELEASE_SIGNING_IDENTITY='Developer ID Application: ... (Y2YJ48R9GL)'
  RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY

Archive path: build/archive/Tools Cat.xcarchive
Export options path: build/export-options/developer-id.plist
Export path: dist/export/Tools Cat.app
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the DerivedData release path with explicit preflight plus archive/export signing</name>
  <files>release.sh, scripts/release/preflight-signing.sh, scripts/release/export-options-developer-id.plist.template, scripts/release/inspect-signature.sh</files>
  <read_first>
    - .planning/phases/16-release-signing-readiness/16-CONTEXT.md
    - .planning/phases/16-release-signing-readiness/16-RESEARCH.md
    - README.md
    - release.sh
    - build_dmg.sh
    - Tools Cat.xcodeproj/project.pbxproj
    - Tools Cat/Tools_Cat.entitlements
  </read_first>
  <behavior>
    - Test 1: `release.sh` remains the only public release command per D-01 and fails before any archive work when `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, or `RELEASE_NOTARY_PROFILE` is missing or invalid.
    - Test 2: The release flow no longer runs `xcodebuild clean build` or searches `build/Build/Products/Release/*.app`; it archives to `build/archive/Tools Cat.xcarchive`, exports to `dist/export`, and prints the exported app path.
    - Test 3: The export policy is deterministic: `method=developer-id`, `signingStyle=manual`, `signingCertificate=Developer ID Application`, and `teamID` comes from `RELEASE_TEAM_ID`.
    - Test 4: The exported app is inspected with `codesign` after export, and Phase 16 stops at the signed `.app` instead of continuing into DMG packaging/notarization work.
  </behavior>
  <action>Create `scripts/release/preflight-signing.sh` with `set -euo pipefail` and a fail-fast contract that requires exactly these environment variables: `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, and `RELEASE_NOTARY_PROFILE`. The script must check for `xcodebuild`, `security`, `codesign`, `xcrun`, and `plutil`; reject a signing identity string that does not start with `Developer ID Application:`; verify `security find-identity -v -p codesigning | grep -F "$RELEASE_SIGNING_IDENTITY"`; and verify the named notary profile with `xcrun notarytool history --keychain-profile "$RELEASE_NOTARY_PROFILE" >/dev/null 2>&1`. Add `scripts/release/export-options-developer-id.plist.template` containing `<key>method</key><string>developer-id</string>`, `<key>signingStyle</key><string>manual</string>`, `<key>signingCertificate</key><string>Developer ID Application</string>`, and a `__TEAM_ID__` placeholder for the team string. Add `scripts/release/inspect-signature.sh` that takes one app path, runs `codesign -d --entitlements :- --verbose=4 "$APP_PATH"` and `codesign -v --verbose=4 "$APP_PATH"`, and fails if the path is missing or not an `.app`. Rewrite `release.sh` so it stays the single maintainer-facing entrypoint per D-01/D-02, calls the preflight helper before any long build work per D-05/D-09, generates `build/export-options/developer-id.plist` from the template with `RELEASE_TEAM_ID`, archives with `xcodebuild archive -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -configuration Release -destination "generic/platform=macOS" -archivePath "$ROOT_DIR/build/archive/Tools Cat.xcarchive" DEVELOPMENT_TEAM="$RELEASE_TEAM_ID"`, exports with `xcodebuild -exportArchive -archivePath "$ROOT_DIR/build/archive/Tools Cat.xcarchive" -exportPath "$ROOT_DIR/dist/export" -exportOptionsPlist "$ROOT_DIR/build/export-options/developer-id.plist"`, runs `scripts/release/inspect-signature.sh "$ROOT_DIR/dist/export/Tools Cat.app"`, and ends by printing the signed app path. Keep `build_dmg.sh` untouched as a downstream seam for later phases, but remove the current `build_dmg.sh` call and all DerivedData wildcard pickup from `release.sh` in this phase.</action>
  <acceptance_criteria>
    - `release.sh` contains `scripts/release/preflight-signing.sh`
    - `release.sh` contains `xcodebuild archive`
    - `release.sh` contains `xcodebuild -exportArchive`
    - `release.sh` contains `build/archive/Tools Cat.xcarchive`
    - `release.sh` contains `dist/export`
    - `release.sh` contains `scripts/release/inspect-signature.sh`
    - `release.sh` contains `RELEASE_TEAM_ID`
    - `release.sh` contains `RELEASE_SIGNING_IDENTITY`
    - `release.sh` contains `RELEASE_NOTARY_PROFILE`
    - `release.sh` does not contain `clean build`
    - `release.sh` does not contain `Build/Products/Release`
    - `release.sh` does not contain `build_dmg.sh`
    - `scripts/release/preflight-signing.sh` contains `Developer ID Application:`
    - `scripts/release/preflight-signing.sh` contains `security find-identity -v -p codesigning`
    - `scripts/release/preflight-signing.sh` contains `xcrun notarytool history --keychain-profile`
    - `scripts/release/export-options-developer-id.plist.template` contains `<string>developer-id</string>`
    - `scripts/release/export-options-developer-id.plist.template` contains `<string>manual</string>`
    - `scripts/release/export-options-developer-id.plist.template` contains `<string>Developer ID Application</string>`
    - `scripts/release/export-options-developer-id.plist.template` contains `__TEAM_ID__`
    - `scripts/release/inspect-signature.sh` contains `codesign -d --entitlements :- --verbose=4`
    - `scripts/release/inspect-signature.sh` contains `codesign -v --verbose=4`
  </acceptance_criteria>
  <verify>
    <automated>tmp_log="$(mktemp)"; set +e; bash scripts/release/preflight-signing.sh >"$tmp_log" 2>&1; status=$?; set -e; test "$status" -ne 0 && rg -q 'RELEASE_TEAM_ID|RELEASE_SIGNING_IDENTITY|RELEASE_NOTARY_PROFILE' "$tmp_log" && rg -n 'xcodebuild archive|xcodebuild -exportArchive|build/archive/Tools Cat.xcarchive|dist/export|inspect-signature.sh' release.sh</automated>
  </verify>
  <done>`release.sh` now enforces explicit release-time identity inputs and produces an exported Developer ID app from an archive/export boundary instead of a DerivedData build product.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make Release signing readiness explicit in project settings and add a reusable shell verification gate</name>
  <files>Tools Cat.xcodeproj/project.pbxproj, scripts/release/verify-release-readiness.sh</files>
  <read_first>
    - .planning/phases/16-release-signing-readiness/16-CONTEXT.md
    - .planning/phases/16-release-signing-readiness/16-RESEARCH.md
    - Tools Cat.xcodeproj/project.pbxproj
    - release.sh
    - scripts/release/preflight-signing.sh
    - scripts/release/export-options-developer-id.plist.template
    - scripts/release/inspect-signature.sh
  </read_first>
  <behavior>
    - Test 1: The app target Release configuration keeps `CODE_SIGN_STYLE = Automatic` for day-to-day development per D-03, but now explicitly sets `DEVELOPMENT_TEAM = Y2YJ48R9GL` and `ENABLE_HARDENED_RUNTIME = YES` for release readiness.
    - Test 2: A repo-local shell gate can prove the old DerivedData path is gone, the new Developer ID export policy exists, and the Xcode project carries the hardened-runtime/team settings required for later notarization work.
    - Test 3: The verification gate is fast, grep-based, and reusable by later phases without requiring a real Developer ID certificate on every machine.
  </behavior>
  <action>Update the app target Release build settings in `Tools Cat.xcodeproj/project.pbxproj` so `ENABLE_HARDENED_RUNTIME = YES;` and `DEVELOPMENT_TEAM = Y2YJ48R9GL;` are explicit while `CODE_SIGN_STYLE = Automatic;` remains unchanged for the normal Xcode workflow. Add `scripts/release/verify-release-readiness.sh` with `set -euo pipefail` and an automated static gate that checks these exact conditions with `rg`: `release.sh` contains `xcodebuild archive`, `xcodebuild -exportArchive`, `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, `RELEASE_NOTARY_PROFILE`, and no `clean build` or `Build/Products/Release`; `scripts/release/preflight-signing.sh` contains the `security find-identity` and `notarytool history` checks; `scripts/release/export-options-developer-id.plist.template` contains `developer-id`, `manual`, and `Developer ID Application`; `scripts/release/inspect-signature.sh` contains both `codesign` inspection commands; and `Tools Cat.xcodeproj/project.pbxproj` contains `ENABLE_HARDENED_RUNTIME = YES;`, `DEVELOPMENT_TEAM = Y2YJ48R9GL;`, and `CODE_SIGN_STYLE = Automatic;`. The script should exit non-zero on any missing pattern so later phases can reuse it as a fast preflight/readiness gate. Do not add provisioning-profile plumbing, do not switch the project to manual signing for development, and do not move DMG/notarization work forward into this phase.</action>
  <acceptance_criteria>
    - `Tools Cat.xcodeproj/project.pbxproj` contains `ENABLE_HARDENED_RUNTIME = YES;`
    - `Tools Cat.xcodeproj/project.pbxproj` contains `DEVELOPMENT_TEAM = Y2YJ48R9GL;`
    - `Tools Cat.xcodeproj/project.pbxproj` contains `CODE_SIGN_STYLE = Automatic;`
    - `scripts/release/verify-release-readiness.sh` contains `set -euo pipefail`
    - `scripts/release/verify-release-readiness.sh` contains `xcodebuild archive`
    - `scripts/release/verify-release-readiness.sh` contains `xcodebuild -exportArchive`
    - `scripts/release/verify-release-readiness.sh` contains `RELEASE_TEAM_ID`
    - `scripts/release/verify-release-readiness.sh` contains `RELEASE_SIGNING_IDENTITY`
    - `scripts/release/verify-release-readiness.sh` contains `RELEASE_NOTARY_PROFILE`
    - `scripts/release/verify-release-readiness.sh` contains `ENABLE_HARDENED_RUNTIME = YES;`
    - `scripts/release/verify-release-readiness.sh` contains `DEVELOPMENT_TEAM = Y2YJ48R9GL;`
    - `scripts/release/verify-release-readiness.sh` contains `CODE_SIGN_STYLE = Automatic;`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -showBuildSettings | rg 'ENABLE_HARDENED_RUNTIME = YES|DEVELOPMENT_TEAM = Y2YJ48R9GL|CODE_SIGN_STYLE = Automatic' && bash scripts/release/verify-release-readiness.sh</automated>
  </verify>
  <done>The repo now exposes hardened-runtime and Team-ID readiness in the Xcode project, and a reusable shell gate proves the new archive/export signing seam without requiring a real release certificate on every run.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when `release.sh` is the sole release command, preflight blocks missing identity inputs before the build, the app export comes from `archive` plus `-exportArchive`, and the static readiness gate proves hardened-runtime plus Developer ID export configuration are present.
</verification>

<success_criteria>
Phase 16 has a real signing boundary: the repo can prove the code path for a Developer ID signed `Tools Cat.app`, and later phases can build DMG signing/notarization on top of the same archive/export seam.
</success_criteria>

<output>
After completion, create `.planning/phases/16-release-signing-readiness/16-01-SUMMARY.md`
</output>
