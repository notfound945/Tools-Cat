---
phase: 16-release-signing-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01-PLAN.md"
files_modified:
  - "README.md"
  - "docs/release/signing-readiness.md"
  - "scripts/release/verify-release-docs.sh"
autonomous: true
requirements:
  - DIST-05
must_haves:
  truths:
    - "A maintainer can find the Phase 16 release prerequisites from `README.md` without reading old unsigned-DMG guidance first."
    - "The repo documents the exact release inputs and bootstrap steps for Team ID, Developer ID Application identity, and the named `notarytool` profile without committing any credential values to source control."
    - "Documentation matches the new release-script contract exactly: `release.sh` is the public command, the output is `dist/export/Tools Cat.app`, and DMG signing/notarization are explicitly deferred to Phase 17."
  artifacts:
    - path: "README.md"
      provides: "Short maintainer-facing release entrypoint that points to the dedicated signing doc"
      contains: "docs/release/signing-readiness.md"
    - path: "docs/release/signing-readiness.md"
      provides: "Detailed signing identity bootstrap, notary profile bootstrap, and release preflight checklist"
      contains: "RELEASE_TEAM_ID"
    - path: "scripts/release/verify-release-docs.sh"
      provides: "Fast docs gate for exact env-var names, commands, and removal of stale manual-allow guidance"
      contains: "RELEASE_SIGNING_IDENTITY"
  key_links:
    - from: "README.md"
      to: "docs/release/signing-readiness.md"
      via: "README stays the short release entrypoint while the detailed checklist lives in the dedicated doc per D-08"
      pattern: "signing-readiness\\.md|release\\.sh|dist/export/Tools Cat\\.app"
    - from: "docs/release/signing-readiness.md"
      to: "release.sh"
      via: "documentation must describe the same required inputs and archive/export outputs as the release script"
      pattern: "RELEASE_TEAM_ID|RELEASE_SIGNING_IDENTITY|RELEASE_NOTARY_PROFILE|dist/export/Tools Cat\\.app"
    - from: "scripts/release/verify-release-docs.sh"
      to: "README.md"
      via: "docs gate must reject stale unsigned-DMG and manual Gatekeeper-bypass instructions"
      pattern: "隐私与安全|右键-打开|未公证"
---

<objective>
Document the Phase 16 signing/readiness flow so the maintainer can bootstrap release credentials safely and run the new signed-app export path without reading stale unsigned-DMG instructions.

Purpose: DIST-05 is not satisfied by shell changes alone. The repo must explain the signing identity, notary profile bootstrap, and preflight contract without storing secrets, and it must make the new `release.sh` path the only public maintainer story.
Output: A short README release entrypoint, a dedicated `docs/release/signing-readiness.md` checklist, and a docs verification script that rejects drift back to the old unnotarized/manual-allow guidance.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-release-signing-readiness/16-CONTEXT.md
@.planning/phases/16-release-signing-readiness/16-RESEARCH.md
@.planning/phases/16-release-signing-readiness/16-01-PLAN.md
@README.md
@release.sh

<interfaces>
Expected release contract from `16-01`:
```text
Public command: sh ./release.sh
Required env vars:
  RELEASE_TEAM_ID=Y2YJ48R9GL
  RELEASE_SIGNING_IDENTITY='Developer ID Application: ... (Y2YJ48R9GL)'
  RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY
Required tools: xcodebuild, security, codesign, xcrun, plutil
Archive output: build/archive/Tools Cat.xcarchive
Export output: dist/export/Tools Cat.app
Phase boundary: DMG signing, notarization, stapling, and Gatekeeper closure are Phase 17+
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the maintainer release docs around the new signed-app export contract</name>
  <files>README.md, docs/release/signing-readiness.md</files>
  <read_first>
    - .planning/phases/16-release-signing-readiness/16-CONTEXT.md
    - .planning/phases/16-release-signing-readiness/16-RESEARCH.md
    - .planning/phases/16-release-signing-readiness/16-01-PLAN.md
    - README.md
    - release.sh
  </read_first>
  <behavior>
    - Test 1: `README.md` becomes the short entrypoint that tells maintainers to run `sh ./release.sh`, lists the three required release env vars, and points to `docs/release/signing-readiness.md` for the full checklist.
    - Test 2: The dedicated doc explains how to obtain a Developer ID Application certificate, how to create the named `TOOLS_CAT_NOTARY` profile with `xcrun notarytool store-credentials --validate`, and how to run the Phase 16 preflight/export flow without committing secrets.
    - Test 3: Old public instructions about unsigned DMGs, manual `隐私与安全` approval, or right-click open are removed from the release path and explicitly deferred to later notarization work.
  </behavior>
  <action>Rewrite the release sections in `README.md` so there is one short maintainer-facing release entrypoint instead of the current “构建 Release（二选一）” plus “打包 DMG（不公证）” split. The new README release section must say that Phase 16 uses `sh ./release.sh`, that the maintainer must provide `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, and `RELEASE_NOTARY_PROFILE`, that the output is `dist/export/Tools Cat.app`, and that the detailed procedure lives in `docs/release/signing-readiness.md`. Create `docs/release/signing-readiness.md` with four concrete sections: `Prerequisites` listing `xcodebuild`, `security`, `codesign`, `xcrun`, and `plutil`; `Certificate bootstrap` explaining that the required certificate label must look like `Developer ID Application: <Common Name> (Y2YJ48R9GL)` and that the Team ID is `Y2YJ48R9GL`; `Notary profile bootstrap` showing `xcrun notarytool store-credentials TOOLS_CAT_NOTARY --apple-id "APPLE_ID" --team-id "Y2YJ48R9GL" --password "<app-specific-password>" --validate` as the primary setup path while clearly stating the password value must never be stored in the repo; and `Phase 16 release runbook` showing `export RELEASE_TEAM_ID=Y2YJ48R9GL`, `export RELEASE_SIGNING_IDENTITY='Developer ID Application: <Common Name> (Y2YJ48R9GL)'`, `export RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY`, then `sh ./release.sh`. The runbook must describe the expected outputs `build/archive/Tools Cat.xcarchive` and `dist/export/Tools Cat.app`, the preflight failure modes for missing tools/identity/profile, and an explicit note that signed DMG creation, notarization submission, stapling, and Gatekeeper install verification are Phase 17+ work. Remove the current README text that tells recipients to allow the app in `隐私与安全`, use right-click open, or build/publish an unnotarized DMG.</action>
  <acceptance_criteria>
    - `README.md` contains `sh ./release.sh`
    - `README.md` contains `RELEASE_TEAM_ID`
    - `README.md` contains `RELEASE_SIGNING_IDENTITY`
    - `README.md` contains `RELEASE_NOTARY_PROFILE`
    - `README.md` contains `docs/release/signing-readiness.md`
    - `README.md` contains `dist/export/Tools Cat.app`
    - `README.md` does not contain `构建 Release（二选一）`
    - `README.md` does not contain `打包 DMG（不公证）`
    - `README.md` does not contain `隐私与安全`
    - `README.md` does not contain `右键-打开`
    - `docs/release/signing-readiness.md` contains `RELEASE_TEAM_ID=Y2YJ48R9GL`
    - `docs/release/signing-readiness.md` contains `RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY`
    - `docs/release/signing-readiness.md` contains `xcrun notarytool store-credentials TOOLS_CAT_NOTARY`
    - `docs/release/signing-readiness.md` contains `Developer ID Application: <Common Name> (Y2YJ48R9GL)`
    - `docs/release/signing-readiness.md` contains `build/archive/Tools Cat.xcarchive`
    - `docs/release/signing-readiness.md` contains `dist/export/Tools Cat.app`
    - `docs/release/signing-readiness.md` contains `Phase 17`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'sh \\./release\\.sh|RELEASE_TEAM_ID|RELEASE_SIGNING_IDENTITY|RELEASE_NOTARY_PROFILE|docs/release/signing-readiness\\.md|dist/export/Tools Cat\\.app' README.md && ! rg -n '构建 Release（二选一）|打包 DMG（不公证）|隐私与安全|右键-打开' README.md && rg -n 'RELEASE_TEAM_ID=Y2YJ48R9GL|RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY|xcrun notarytool store-credentials TOOLS_CAT_NOTARY|Developer ID Application: <Common Name> \\(Y2YJ48R9GL\\)|build/archive/Tools Cat\\.xcarchive|dist/export/Tools Cat\\.app|Phase 17' docs/release/signing-readiness.md</automated>
  </verify>
  <done>The repo’s public maintainer docs now describe the new signed-app export flow and bootstrap requirements without exposing secrets or leaving the old unsigned-DMG instructions as the main story.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add a docs drift gate for release prerequisites and stale manual-allow language</name>
  <files>scripts/release/verify-release-docs.sh</files>
  <read_first>
    - .planning/phases/16-release-signing-readiness/16-CONTEXT.md
    - .planning/phases/16-release-signing-readiness/16-RESEARCH.md
    - .planning/phases/16-release-signing-readiness/16-01-PLAN.md
    - README.md
    - docs/release/signing-readiness.md
  </read_first>
  <behavior>
    - Test 1: One fast shell gate proves the docs still mention the exact env vars, the `TOOLS_CAT_NOTARY` bootstrap command, the archive/export outputs, and the Phase 17 boundary.
    - Test 2: The gate fails if README drifts back to unsigned-DMG/manual Gatekeeper-bypass language or if the dedicated doc stops matching the release-script contract.
  </behavior>
  <action>Create `scripts/release/verify-release-docs.sh` with `set -euo pipefail` and `rg` assertions that enforce the exact release-doc contract introduced in Task 1. The script must require `README.md` to contain `sh ./release.sh`, `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, `RELEASE_NOTARY_PROFILE`, `docs/release/signing-readiness.md`, and `dist/export/Tools Cat.app`; require `docs/release/signing-readiness.md` to contain `RELEASE_TEAM_ID=Y2YJ48R9GL`, `RELEASE_NOTARY_PROFILE=TOOLS_CAT_NOTARY`, `xcrun notarytool store-credentials TOOLS_CAT_NOTARY`, `Developer ID Application: <Common Name> (Y2YJ48R9GL)`, `build/archive/Tools Cat.xcarchive`, `dist/export/Tools Cat.app`, and `Phase 17`; and fail if `README.md` contains `构建 Release（二选一）`, `打包 DMG（不公证）`, `隐私与安全`, `右键-打开`, or `未公证`. Keep the script focused on docs drift only; it should not require a signing identity or run any long build work.</action>
  <acceptance_criteria>
    - `scripts/release/verify-release-docs.sh` contains `set -euo pipefail`
    - `scripts/release/verify-release-docs.sh` contains `sh ./release.sh`
    - `scripts/release/verify-release-docs.sh` contains `RELEASE_TEAM_ID`
    - `scripts/release/verify-release-docs.sh` contains `RELEASE_SIGNING_IDENTITY`
    - `scripts/release/verify-release-docs.sh` contains `RELEASE_NOTARY_PROFILE`
    - `scripts/release/verify-release-docs.sh` contains `TOOLS_CAT_NOTARY`
    - `scripts/release/verify-release-docs.sh` contains `dist/export/Tools Cat.app`
    - `scripts/release/verify-release-docs.sh` contains `build/archive/Tools Cat.xcarchive`
    - `scripts/release/verify-release-docs.sh` contains `Phase 17`
    - `scripts/release/verify-release-docs.sh` contains `隐私与安全`
    - `scripts/release/verify-release-docs.sh` contains `未公证`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release/verify-release-docs.sh</automated>
  </verify>
  <done>A reusable docs gate now keeps the release documentation aligned with the new Phase 16 signing contract and blocks drift back to the old unsigned/manual-install story.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the README points to one canonical signed-app release path, the dedicated signing doc contains the exact bootstrap/runbook contract, and the docs drift gate can reject stale unsigned/manual-allow guidance automatically.
</verification>

<success_criteria>
Phase 16 documentation is operationally trustworthy: a maintainer can bootstrap certificates and notary credentials safely, run the release preflight/export flow from the documented inputs, and see exactly what remains deferred to later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/16-release-signing-readiness/16-02-SUMMARY.md`
</output>
