---
phase: 14-duration-management-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01-PLAN.md"
files_modified:
  - "Tools Cat/KeepAwakeDurationManagementView.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - AWAKE-15
  - AWAKE-16
must_haves:
  truths:
    - "编辑和删除动作在时长行里一眼可区分：编辑使用应用主题/强调色语义，删除保持 destructive 红色语义。"
    - "新的动作语义不能把窗口做得花哨或喧宾夺主，仍然要保持工具型 macOS 界面的克制感。"
    - "列表样式改动后，既有 add/edit/delete、排序、根菜单实时同步真值都不能回退。"
  artifacts:
    - path: "Tools Cat/KeepAwakeDurationManagementView.swift"
      provides: "时长行的语义动作样式"
      contains: "Button(\"编辑\""
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "原生列表与动作语义改动后的 UI 烟雾保护"
      contains: "testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationManagementView.swift"
      to: "Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift"
      via: "动作样式只改表现层，不得破坏既有 CRUD/session 真值"
      pattern: "saveDraft|requestDelete|confirmDelete"
    - from: "Tools Cat/KeepAwakeDurationManagementView.swift"
      to: "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
      via: "Phase 14 polish 不能回退根菜单 keep-awake 真值"
      pattern: "KeepAwake|duration"
---

<objective>
Finish the Phase 14 polish pass by making row actions semantically obvious while preserving all shipped behavior behind the new native list presentation.

Purpose: Once the timed-duration region uses a native list, the remaining polish requirement is action clarity. Users should immediately understand which action is safe edit versus destructive delete, without reopening any underlying CRUD or menu-truth logic.
Output: A native duration list whose row actions are semantically styled and whose existing session/menu regressions still pass unchanged.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
@.planning/phases/14-duration-management-ui-polish/14-RESEARCH.md
@.planning/phases/14-duration-management-ui-polish/14-VALIDATION.md
@.planning/phases/14-duration-management-ui-polish/14-01-PLAN.md
@Tools Cat/Assets.xcassets/AccentColor.colorset/Contents.json
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift
@Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
@Tools CatTests/StatusBarControllerMenuPolishTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply semantic edit/delete styling and re-lock the shipped duration behavior</name>
  <files>Tools Cat/KeepAwakeDurationManagementView.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
    - .planning/phases/14-duration-management-ui-polish/14-RESEARCH.md
    - Tools Cat/Assets.xcassets/AccentColor.colorset/Contents.json
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift
    - Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
    - Tools CatTests/StatusBarControllerMenuPolishTests.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: The edit action visibly uses the app accent/theme color while delete remains destructive red.
    - Test 2: The manager still opens the shared add/edit sheet, still shows seeded durations in sorted order, and still keeps delete confirmation intact.
    - Test 3: The existing session/menu regression slice stays green, proving the polish did not reopen CRUD or root-menu truth behavior.
  </behavior>
  <action>Refine the row action controls in `KeepAwakeDurationManagementView.swift` so the `编辑` button uses native accent-color semantics sourced from the app theme, and the `删除` button keeps explicit destructive red semantics through native SwiftUI button styling. Keep the actions restrained: no oversized badges, custom gradients, or decorative chrome. If the move to `List` changes any row layout or control placement, adjust spacing only enough to keep the actions readable and compact. Update `Tools_CatUITests.swift` only if a minimal accessibility seam is needed to keep the manager smoke deterministic after the action-style and native-list changes. Do not move CRUD logic out of the existing callbacks, and prove the polish did not regress shipped behavior by rerunning the focused UI smoke together with `KeepAwakeDurationManagementSessionModelTests`, `StatusBarControllerKeepAwakeMenuTests`, and `StatusBarControllerMenuPolishTests`.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `Button("编辑"`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` styles the edit control with a tint or foreground style, such as `.tint(` or `.foregroundStyle(`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `Button("删除", role: .destructive`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` remains unchanged or continues to contain `testAddDurationPersistsAndSortsBySeconds`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` remains unchanged or continues to contain `testKeepAwakeMenuOrderMatchesTheFixedActionGroup`
    - `Tools CatTests/StatusBarControllerMenuPolishTests.swift` remains unchanged or continues to contain `testKeepAwakeTimedRowsRefreshAfterDurationStoreMutations`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeDurationManagementSessionModelTests' -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests' -only-testing:'Tools CatTests/StatusBarControllerMenuPolishTests' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface'</automated>
  </verify>
  <done>The duration-manager list now communicates edit versus delete intent immediately, while the shipped CRUD and keep-awake menu truth remain unchanged.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when edit/delete semantics are visually obvious, the manager remains restrained and native, and the existing regression slice proves no behavior drift.
</verification>

<success_criteria>
Phase 14 finishes with a duration manager that feels more obviously native and self-explanatory without reopening the already-shipped duration behavior contract.
</success_criteria>

<output>
After completion, create `.planning/phases/14-duration-management-ui-polish/14-02-SUMMARY.md`
</output>
