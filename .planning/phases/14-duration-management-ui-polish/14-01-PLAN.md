---
phase: 14-duration-management-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/KeepAwakeDurationManagementView.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - AWAKE-14
must_haves:
  truths:
    - "`常亮时长` 窗口中的定时时长区域使用明显的原生 macOS 列表语义，而不是继续停留在自绘 `ScrollView` + `LazyVStack` 堆叠表面。"
    - "管理窗口仍保持当前紧凑结构：标题与 `添加时长` 按钮、空状态、列表内 add/edit 小弹窗、删除确认都不回退。"
    - "现有时长行和列表级可访问性标识继续稳定存在，便于 UI 烟雾测试锁定新的原生列表表面。"
  artifacts:
    - path: "Tools Cat/KeepAwakeDurationManagementView.swift"
      provides: "定时时长区域的原生列表呈现"
      contains: "List"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "打开管理窗口后的原生列表语义烟雾测试"
      contains: "keep-awake-duration-list"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationManagementView.swift"
      to: "Tools CatUITests/Tools_CatUITests.swift"
      via: "直接启动的管理窗口烟雾测试锁定新的列表结构和列表级 seam"
      pattern: "keep-awake-duration-list|keep-awake-duration-list-surface|testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface"
---

<objective>
Rework the populated timed-duration region so the `常亮时长` manager uses a clearly native macOS list presentation instead of the current custom stacked scroll surface.

Purpose: Phase 13 made the list visually acceptable, but Phase 14 is explicitly about moving from a custom list-like treatment to true native list semantics. This plan changes only the populated timed-duration presentation layer while preserving the shipped manager structure and behaviors.
Output: A manager window whose timed durations render through a native list-first presentation that reads as unmistakably macOS at a glance.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
@.planning/phases/14-duration-management-ui-polish/14-RESEARCH.md
@.planning/phases/14-duration-management-ui-polish/14-VALIDATION.md
@.planning/phases/13-duration-management-surface/13-03-SUMMARY.md
@.planning/phases/13-duration-management-surface/13-04-SUMMARY.md
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the custom timed-duration stack with a native macOS list presentation</name>
  <files>Tools Cat/KeepAwakeDurationManagementView.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/14-duration-management-ui-polish/14-CONTEXT.md
    - .planning/phases/14-duration-management-ui-polish/14-RESEARCH.md
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: Opening the duration manager with seeded durations shows the timed-duration region through a native list-first surface instead of the current custom `ScrollView` + stacked row shell.
    - Test 2: The manager still shows `添加时长`, the seeded timed rows, the existing list-level accessibility seams, and the shared add/edit sheet flow.
    - Test 3: The list presentation remains compact and native, without reopening the underlying CRUD or root-menu behavior.
  </behavior>
  <action>Refactor `KeepAwakeDurationManagementView.swift` so `populatedListContent` uses a native SwiftUI `List` as the primary timed-duration presentation instead of the current `ScrollView { LazyVStack { ... } }` structure. Keep the existing heading row with `定时时长` and the item count above the list, keep the outer manager padding and frame unchanged, keep the list-level accessibility identifiers `keep-awake-duration-list-surface` and `keep-awake-duration-list`, and keep each row reachable through the existing `keep-awake-duration-row-<uuid>` markers. Use native list styling that reads as macOS and avoid bespoke card chrome that would recreate the old custom-stack feel. Update the direct-launch UI smoke in `Tools_CatUITests.swift` so it still proves the manager window opens, the list surface exists, the seeded rows render, and the add-sheet flow still overlays on top of the live list.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `List {`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` no longer contains `ScrollView {` inside `populatedListContent`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` no longer contains `LazyVStack(` inside `populatedListContent`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` still contains `accessibilityIdentifier("keep-awake-duration-list-surface")`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` still contains `accessibilityIdentifier("keep-awake-duration-list")`
    - `Tools CatUITests/Tools_CatUITests.swift` still contains `testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface`
    - `Tools CatUITests/Tools_CatUITests.swift` still asserts `keep-awake-duration-list-surface`
    - `Tools CatUITests/Tools_CatUITests.swift` still asserts `keep-awake-duration-form-sheet`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface'</automated>
  </verify>
  <done>The populated timed-duration area now reads as a real native macOS list while the manager shell and existing edit flow stay intact.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the populated duration region uses native list semantics and the existing manager-window smoke still proves the live list plus shared sheet flow.
</verification>

<success_criteria>
Phase 14 starts from a truly native list foundation: the timed-duration region is no longer a custom stacked scroll surface masquerading as a list.
</success_criteria>

<output>
After completion, create `.planning/phases/14-duration-management-ui-polish/14-01-SUMMARY.md`
</output>
