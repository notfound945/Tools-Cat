---
phase: 20-first-use-device-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/SavedDeviceRepository.swift"
  - "Tools CatTests/SavedDeviceRepositoryTests.swift"
  - "Tools CatTests/SavedDeviceLibraryStoreTests.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - DEVS-13
  - DEVS-14
must_haves:
  truths:
    - "首次读取一个从未初始化过的保存设备库时，应用会自动得到且只得到一个默认设备：名称 `UGREEN NAS`，MAC `6C:1F:F7:75:C7:0E`。"
    - "默认设备内置只发生在 first-use empty library 路径；已有非空个人设备库绝不能被这个路径修改。"
    - "重新启动或重新加载后不会重复插入第二个默认设备。"
    - "测试夹具必须能继续表达一个显式空库，这样空态 UI 与 Phase 15/19 直启回归仍然可验证。"
  artifacts:
    - path: "Tools Cat/SavedDeviceRepository.swift"
      provides: "repository-owned first-use seed truth and canonical default-device payload"
      contains: "loadDevices()"
    - path: "Tools CatTests/SavedDeviceRepositoryTests.swift"
      provides: "first-load seed, no-duplicate, and no-overwrite regression coverage"
      contains: "testEmptySuiteLoadsNoDevices"
    - path: "Tools CatTests/SavedDeviceLibraryStoreTests.swift"
      provides: "store-level proof that fresh repository-backed libraries expose the seeded device once"
      contains: "SavedDeviceLibraryStore"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "fresh-suite seeded-device launch proof and explicit-empty-library UI fixture separation"
      contains: "makeLaunchContext"
  key_links:
    - from: "Tools Cat/SavedDeviceRepository.swift"
      to: "Tools CatTests/SavedDeviceRepositoryTests.swift"
      via: "repository truth must prove first-use seed, no duplicate on reload, and no overwrite of existing libraries"
      pattern: "saved_devices|loadDevices|saveDevices"
    - from: "Tools Cat/SavedDeviceRepository.swift"
      to: "Tools CatTests/SavedDeviceLibraryStoreTests.swift"
      via: "store initialization should surface the repository seed without adding extra store-only seeding logic"
      pattern: "SavedDeviceLibraryStore"
    - from: "Tools CatUITests/Tools_CatUITests.swift"
      to: "Tools Cat/SavedDeviceRepository.swift"
      via: "UI launch fixtures must distinguish missing payload (seed path) from explicit empty payload (empty-state path)"
      pattern: "makeLaunchContext|saved_devices"
---

<objective>
Seed one default `UGREEN NAS` device exactly once for first-use saved-device libraries, while leaving existing personal libraries untouched and keeping UI regression fixtures honest about the difference between “fresh” and “explicitly empty.”

Purpose: Phase 20 exists because the saved-device onboarding flow still starts from a completely blank library even though one stable NAS target is known and should be immediately useful. The phase should add one practical first-use default without reintroducing devices into already-initialized libraries or weakening existing persistence truth.
Output: repository-backed first-use seed behavior, unit/store regressions for no-duplicate/no-overwrite guarantees, and direct-launch UI tests that cover both seeded-first-use and intentionally empty-library startup paths.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-first-use-device-seed/20-RESEARCH.md
@.planning/phases/20-first-use-device-seed/20-VALIDATION.md
@.planning/phases/15-device-library-ui-parity/15-CONTEXT.md
@Tools Cat/SavedDeviceRepository.swift
@Tools Cat/SavedDeviceLibraryStore.swift
@Tools Cat/SavedDevice.swift
@Tools Cat/AppDelegate.swift
@Tools CatTests/SavedDeviceRepositoryTests.swift
@Tools CatTests/SavedDeviceLibraryStoreTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add repository-owned first-use seed truth and lock it with repository/store regressions</name>
  <files>Tools Cat/SavedDeviceRepository.swift, Tools CatTests/SavedDeviceRepositoryTests.swift, Tools CatTests/SavedDeviceLibraryStoreTests.swift</files>
  <read_first>
    - .planning/phases/20-first-use-device-seed/20-RESEARCH.md
    - .planning/phases/20-first-use-device-seed/20-VALIDATION.md
    - Tools Cat/SavedDeviceRepository.swift
    - Tools Cat/SavedDeviceLibraryStore.swift
    - Tools Cat/SavedDevice.swift
    - Tools CatTests/SavedDeviceRepositoryTests.swift
    - Tools CatTests/SavedDeviceLibraryStoreTests.swift
  </read_first>
  <behavior>
    - Test 1: A repository with no `saved_devices` payload returns exactly one seeded device named `UGREEN NAS` with MAC `6C:1F:F7:75:C7:0E`.
    - Test 2: Loading the same repository again does not duplicate the seeded device.
    - Test 3: Repositories that already contain non-empty device data are not overwritten by the seed path.
    - Test 4: A repository/store backed by an explicitly persisted empty device array remains empty and does not reseed.
  </behavior>
  <action>Refactor `Tools Cat/SavedDeviceRepository.swift` so `UserDefaultsSavedDeviceRepository.loadDevices()` treats the absence of `saved_devices` as the first-use signal. In that path, create one canonical default `SavedDevice` named `UGREEN NAS` with MAC `6C:1F:F7:75:C7:0E`, note `""`, and `sortOrder` `0`, persist it immediately through the existing `saveDevices` normalization path, and return it. Do not seed when `saved_devices` already exists, even if that payload decodes to an empty array; that explicit-empty state must remain empty. Keep wake metadata logic unchanged. Extend `Tools CatTests/SavedDeviceRepositoryTests.swift` to replace the old “empty suite loads no devices” assumption with explicit first-use seed assertions, duplicate-prevention assertions, and a regression that explicitly persisted empty arrays stay empty. Extend `Tools CatTests/SavedDeviceLibraryStoreTests.swift` with a store-level proof that a fresh repository-backed store surfaces the seeded device once and that reload does not duplicate it.</action>
  <acceptance_criteria>
    - `Tools Cat/SavedDeviceRepository.swift` contains one canonical default-device source for `UGREEN NAS`
    - `Tools Cat/SavedDeviceRepository.swift` seeds only from the missing-`saved_devices` path
    - `Tools Cat/SavedDeviceRepository.swift` still reuses `saveDevices` for normalization/persistence
    - `Tools CatTests/SavedDeviceRepositoryTests.swift` contains a first-load default-seed regression
    - `Tools CatTests/SavedDeviceRepositoryTests.swift` contains a no-duplicate-on-reload regression
    - `Tools CatTests/SavedDeviceRepositoryTests.swift` contains a persisted-empty-library regression
    - `Tools CatTests/SavedDeviceLibraryStoreTests.swift` contains a fresh-store seed regression
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/SavedDeviceRepositoryTests' -only-testing:'Tools CatTests/SavedDeviceLibraryStoreTests'</automated>
  </verify>
  <done>The repository now owns first-use default-device seeding, and unit/store regressions prove it happens once without mutating already-initialized libraries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Separate fresh-seed and explicit-empty startup fixtures in direct-launch UI coverage</name>
  <files>Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/20-first-use-device-seed/20-RESEARCH.md
    - .planning/phases/20-first-use-device-seed/20-VALIDATION.md
    - Tools CatUITests/Tools_CatUITests.swift
    - Tools Cat/SavedDeviceRepository.swift
    - Tools Cat/AppDelegate.swift
  </read_first>
  <behavior>
    - Test 1: Launching the device-library manager with a fresh defaults suite shows the seeded `UGREEN NAS` device instead of the empty state.
    - Test 2: Launching with an explicitly injected empty device payload still shows the polished empty state and does not auto-seed.
    - Test 3: Existing seeded-library direct-launch coverage remains green.
  </behavior>
  <action>Refactor the UI-test launch helper in `Tools CatUITests/Tools_CatUITests.swift` so it can distinguish between “no saved-device payload injected” and “explicit empty device payload injected.” A practical pattern is to let `makeLaunchContext` accept an optional seeded-device array where `nil` means no payload (exercise the repository seed path) and `[]` means encode and inject an explicit empty array. Add a direct-launch test such as `testLaunchWithFreshDeviceLibrarySeedsDefaultDevice` that opens the device-library manager on a fresh suite and asserts one row exists for `UGREEN NAS` with MAC `6C:1F:F7:75:C7:0E`. Update the current empty-state smoke so it becomes an explicit-empty-library case rather than a fresh-suite case. Keep the existing seeded-library manager smoke tests intact.</action>
  <acceptance_criteria>
    - `Tools CatUITests/Tools_CatUITests.swift` can represent nil/no payload separately from an explicit empty payload
    - `Tools CatUITests/Tools_CatUITests.swift` contains a direct-launch fresh-seed UI regression
    - `Tools CatUITests/Tools_CatUITests.swift` still contains an explicit empty-state UI regression
    - Existing `testLaunchWithSeededDeviceLibraryShowsManagementWindow` still exists and stays green
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithFreshDeviceLibrarySeedsDefaultDevice' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithExplicitlyEmptyDeviceLibraryShowsPolishedEmptyState' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededDeviceLibraryShowsManagementWindow'</automated>
  </verify>
  <done>Direct-launch UI coverage now proves both first-use seeding and intentional empty-library startup without conflating the two states.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when a truly fresh saved-device library gets exactly one practical default device, already-initialized libraries stay untouched, and direct-launch UI tests can still prove both the seeded and explicit-empty startup paths.
</verification>

<success_criteria>
Phase 20 is complete when first-use onboarding is more useful immediately, while the app still respects existing personal device libraries and never duplicates the default seed on later loads.
</success_criteria>

<output>
After completion, create `.planning/phases/20-first-use-device-seed/20-01-SUMMARY.md`
</output>
