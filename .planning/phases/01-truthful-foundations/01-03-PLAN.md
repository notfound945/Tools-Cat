---
phase: 01-truthful-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Mac OS Swiss Knife/KeepAwakePresentation.swift
  - Mac OS Swiss Knife/PowerAssertionManager.swift
  - Mac OS Swiss Knife/StatusBarController.swift
  - Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift
autonomous: true
requirements:
  - RELY-05
must_haves:
  truths:
    - "While a keep-awake change is still in progress, users can see explicit turning-on or turning-off copy on a concrete menu surface instead of guessing whether the request started."
    - "The status-bar icon and checkmark change only after the power assertion call reports success."
    - "A failed keep-awake toggle leaves the last confirmed state intact and surfaces a human-readable failure message in the menu."
  artifacts:
    - path: Mac OS Swiss Knife/KeepAwakePresentation.swift
      provides: "Pure menu-rendering contract for confirmed, pending, and failed keep-awake states"
      contains: "var title: String"
    - path: Mac OS Swiss Knife/PowerAssertionManager.swift
      provides: "Typed power-assertion outcomes plus an injectable async adapter instead of optimistic void toggles"
      contains: "protocol KeepAwakePowerControlling"
    - path: Mac OS Swiss Knife/StatusBarController.swift
      provides: "Controller-owned pending state, injectable assertion seam, and menu failure feedback"
      contains: "keepAwakeStatusItem"
    - path: Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift
      provides: "Confirmed-state, pending-surface, and failure-path coverage"
      contains: "final class KeepAwakeMenuStateTests"
  key_links:
    - from: Mac OS Swiss Knife/StatusBarController.swift
      to: Mac OS Swiss Knife/PowerAssertionManager.swift
      via: "Injected KeepAwakePowerControlling adapter and async outcome handling"
      pattern: "KeepAwakePowerControlling"
    - from: Mac OS Swiss Knife/StatusBarController.swift
      to: Mac OS Swiss Knife/KeepAwakePresentation.swift
      via: "Menu title, pending status row, checkmark, icon, and message rendering"
      pattern: "KeepAwakePresentation"
    - from: Mac OS Swiss Knife/PowerAssertionManager.swift
      to: IOKit
      via: "Assertion create/release results gated behind return values"
      pattern: "IOPMAssertionCreateWithName"
---

<objective>
Make keep-awake menu state derive from confirmed power-assertion outcomes instead of optimistic UI toggles.

Purpose: Remove the current trust gap where the menu and icon can imply keep-awake is on or off before the underlying assertion change actually succeeds.
Output: A typed keep-awake presentation contract, outcome-returning power assertion API, controller-owned pending state, and targeted unit tests for pending/success/failure menu behavior.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-truthful-foundations/01-CONTEXT.md
@.planning/phases/01-truthful-foundations/01-RESEARCH.md
@.planning/phases/01-truthful-foundations/01-UI-SPEC.md
@.planning/phases/01-truthful-foundations/01-VALIDATION.md
@Mac OS Swiss Knife/KeepAwakePresentation.swift
@Mac OS Swiss Knife/PowerAssertionManager.swift
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift

<interfaces>
Existing manager/controller seam:
```swift
final class PowerAssertionManager {
    static let shared = PowerAssertionManager()
    private(set) var isEnabled: Bool = false
    func enable()
    func disable()
}

final class StatusBarController: NSObject {
    private func updateIcon()
    @objc private func toggleKeepAwake(_ sender: NSMenuItem)
}
```

Contracts from 01-01:
```swift
enum KeepAwakeTransition {
    case turningOn
    case turningOff
}

enum KeepAwakeToggleOutcome {
    case unchanged(Bool)
    case success(Bool)
    case failure(current: Bool, message: String)
}

struct KeepAwakePresentation {
    var confirmedEnabled: Bool
    var pending: KeepAwakeTransition?
    var message: String?
}
```

Revised seam required by this revision:
```swift
protocol KeepAwakePowerControlling {
    var isEnabled: Bool { get }
    func setKeepAwakeEnabled(
        _ enabled: Bool,
        completion: @escaping (KeepAwakeToggleOutcome) -> Void
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure keep-awake presentation rules and typed power-assertion outcomes</name>
  <files>Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss Knife/PowerAssertionManager.swift, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</files>
  <read_first>Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss Knife/PowerAssertionManager.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-RESEARCH.md, .planning/phases/01-truthful-foundations/01-UI-SPEC.md, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</read_first>
  <action>Implement the pure menu presentation and typed assertion outcomes required by D-10 through D-13, and introduce the fakeable controller seam required for RELY-05 automation. In `KeepAwakePresentation.swift`, add computed properties `title`, `menuState`, `iconSymbol`, `visibleMessage`, and `pendingStatusText`. The exact titles must be `保持屏幕常亮` for steady state, `保持屏幕常亮（正在开启...）` for `.turningOn`, and `保持屏幕常亮（正在关闭...）` for `.turningOff`. The exact icon symbols must be `bolt.fill` only when `confirmedEnabled == true && pending == nil`, otherwise `bolt.slash`. The menu state must stay `.off` during `.turningOn` and stay `.on` during `.turningOff`, because confirmed state has not changed yet. `pendingStatusText` must surface `正在开启保持屏幕常亮...` or `正在关闭保持屏幕常亮...` so Task 2 can mirror the transitional copy onto a dedicated disabled status row while keeping the toggle label aligned with D-11 and D-12. In `PowerAssertionManager.swift`, change `enable()` to return `KeepAwakeToggleOutcome`; when already enabled, return `.unchanged(true)`; on successful `IOPMAssertionCreateWithName`, update `assertionID`, set `isEnabled = true`, and return `.success(true)`; on failure, keep `isEnabled = false` and return `.failure(current: false, message: "未能开启保持屏幕常亮，当前状态未改变")`. Change `disable()` to return `KeepAwakeToggleOutcome`; when already disabled, return `.unchanged(false)`; call `IOPMAssertionRelease(assertionID)` and only clear `assertionID` / `isEnabled` when the return value is `kIOReturnSuccess`; otherwise return `.failure(current: true, message: "未能关闭保持屏幕常亮，当前状态未改变")`. While touching the create call, replace `kIOPMAssertionTypeNoDisplaySleep` with `kIOPMAssertPreventUserIdleDisplaySleep` to avoid deepening the deprecated constant surfaced in research. In the same file, add `protocol KeepAwakePowerControlling { var isEnabled: Bool { get } func setKeepAwakeEnabled(_ enabled: Bool, completion: @escaping (KeepAwakeToggleOutcome) -> Void) }` and `struct SystemKeepAwakePowerController: KeepAwakePowerControlling` that wraps `PowerAssertionManager`, performs the enable/disable work off the menu action stack on a background queue, and invokes `completion` back on the main queue. In `KeepAwakeMenuStateTests.swift`, add pure-state tests for confirmed off, pending turning on, confirmed on, pending turning off, the dedicated pending status-row copy, and failure-message visibility using the exact titles and symbols above.</action>
  <acceptance_criteria>`rg -n "保持屏幕常亮（正在开启...）|保持屏幕常亮（正在关闭...）|正在开启保持屏幕常亮...|正在关闭保持屏幕常亮...|bolt.fill|bolt.slash" "Mac OS Swiss Knife/KeepAwakePresentation.swift"` finds all required labels, pending-row copy, and symbols; `rg -n "protocol KeepAwakePowerControlling|struct SystemKeepAwakePowerController|func setKeepAwakeEnabled\\(" "Mac OS Swiss Knife/PowerAssertionManager.swift"` finds the injectable seam and system adapter; `rg -n "func enable\\(\\) -> KeepAwakeToggleOutcome|func disable\\(\\) -> KeepAwakeToggleOutcome|kIOPMAssertPreventUserIdleDisplaySleep" "Mac OS Swiss Knife/PowerAssertionManager.swift"` finds both typed methods and the updated assertion constant; `rg -n "未能开启保持屏幕常亮，当前状态未改变|未能关闭保持屏幕常亮，当前状态未改变" "Mac OS Swiss Knife/PowerAssertionManager.swift" "Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift"` finds the exact failure copy; `rg -n "testConfirmedOffPresentation|testTurningOnKeepsConfirmedOff|testConfirmedOnPresentation|testTurningOffKeepsConfirmedOn|testPendingStatusText|testFailureMessageVisibility" "Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift"` finds all targeted state tests.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests"</automated>
  </verify>
  <done>The app now has explicit data contracts for confirmed, pending, and failed keep-awake states, and the power assertion boundary can report success or failure without mutating UI state first.</done>
</task>

<task type="auto">
  <name>Task 2: Rework StatusBarController so menu state follows confirmed keep-awake outcomes only</name>
  <files>Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</files>
  <read_first>Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss Knife/PowerAssertionManager.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-UI-SPEC.md, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</read_first>
  <action>Refactor `StatusBarController` to render from `KeepAwakePresentation` and an injected `KeepAwakePowerControlling` instead of hard-coding `PowerAssertionManager.shared`. Add stored properties `confirmedKeepAwakeEnabled`, `pendingTransition`, and `keepAwakeMessage`, plus `private let powerController: KeepAwakePowerControlling`, initializing `confirmedKeepAwakeEnabled` from `powerController.isEnabled`. Update the controller initializer to accept the injected power controller while keeping a default `SystemKeepAwakePowerController(manager: .shared)` for production. Add a disabled menu item directly under the keep-awake toggle named `keepAwakeStatusItem`; keep it hidden or empty unless either `presentation.pendingStatusText` or `presentation.visibleMessage` is non-nil. Replace `updateIcon()` with a render method that builds `KeepAwakePresentation(confirmedEnabled: confirmedKeepAwakeEnabled, pending: pendingTransition, message: keepAwakeMessage)`, then sets the toggle title, toggle state, status item title, status item visibility, and status bar icon from `presentation.title`, `presentation.menuState`, `presentation.pendingStatusText ?? presentation.visibleMessage`, and `presentation.iconSymbol`. In `toggleKeepAwake(_:)`, if `pendingTransition != nil`, return early to prevent duplicate toggles. Otherwise compute the requested target state, set `pendingTransition` to `.turningOn` or `.turningOff`, clear any stale `keepAwakeMessage`, render immediately, and then call `powerController.setKeepAwakeEnabled(target)` so the pending title and mirrored status-row copy exist on a concrete user-visible menu surface before the completion returns. On `.success(enabled)` or `.unchanged(enabled)`, clear `pendingTransition`, set `confirmedKeepAwakeEnabled = enabled`, clear `keepAwakeMessage`, and render. On `.failure(current: message:)`, clear `pendingTransition`, restore `confirmedKeepAwakeEnabled` from `current`, set `keepAwakeMessage = message`, and render without changing the steady-state icon/checkmark. Keep the toggle menu item enabled only when `pendingTransition == nil`. In `KeepAwakeMenuStateTests.swift`, add controller-behavior tests using a fake `KeepAwakePowerControlling` that captures the completion so tests can assert: the injected controller seam is used instead of the singleton; pending toggle title and pending status-row copy appear before completion; success flips the confirmed icon/checkmark only after completion; failure keeps the old confirmed state while surfacing the failure message; and duplicate toggles are ignored while a completion is outstanding.</action>
  <acceptance_criteria>`rg -n "keepAwakeStatusItem|confirmedKeepAwakeEnabled|pendingTransition|keepAwakeMessage|KeepAwakePowerControlling" "Mac OS Swiss Knife/StatusBarController.swift"` finds the controller-owned state and injected seam; `rg -n "SystemKeepAwakePowerController\\(manager: \\.shared\\)|powerController\\.setKeepAwakeEnabled" "Mac OS Swiss Knife/StatusBarController.swift"` finds the default adapter and the fakeable call path; `rg -n "pendingStatusText \\?\\? presentation\\.visibleMessage|KeepAwakePresentation\\(" "Mac OS Swiss Knife/StatusBarController.swift"` finds presentation-driven rendering on the concrete status row; `rg -n "testControllerUsesInjectedPowerController|testPendingToggleShowsVisibleTransitionCopyBeforeCompletion|testControllerAppliesConfirmedSuccessAfterCompletion|testControllerKeepsConfirmedStateOnFailure|testDuplicateToggleIgnoredWhilePending" "Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift"` finds the controller-behavior coverage; `rg -n "PowerAssertionManager\\.shared" "Mac OS Swiss Knife/StatusBarController.swift"` returns no matches outside the default production adapter construction.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests"</automated>
  </verify>
  <done>The keep-awake menu item, auxiliary status line, checkmark, and menu bar icon now tell the truth: transition copy appears immediately on concrete menu surfaces, but steady-state visuals change only after the injected assertion controller reports success, and failures leave the last confirmed state intact.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests"` after each task. At the shared Wave 2 gate, after Plans `01-02` and `01-03` both finish, run the broader unit-target sweep `xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests"`, then do the single native menu-bar smoke test from `01-VALIDATION.md` to confirm the transitional copy is observable on the real menu surface before settling.
</verification>

<success_criteria>
Keep-awake visuals become lifecycle-owned truth instead of optimistic UI: users see turning-on or turning-off copy during transitions, the checkmark and icon change only on confirmed success, and failures leave the prior state visible with a clear human-readable error.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-foundations/01-03-SUMMARY.md`
</output>
