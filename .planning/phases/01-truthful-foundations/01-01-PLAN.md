---
phase: 01-truthful-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Mac OS Swiss Knife.xcodeproj/project.pbxproj
  - Mac OS Swiss Knife/ManualMACValidator.swift
  - Mac OS Swiss Knife/WakeSendPresentation.swift
  - Mac OS Swiss Knife/WOLSessionModel.swift
  - Mac OS Swiss Knife/KeepAwakePresentation.swift
  - Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift
  - Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift
  - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
  - Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift
autonomous: true
requirements:
  - WOL-02
  - RELY-02
  - RELY-03
  - RELY-05
must_haves:
  truths:
    - "Manual MAC input can be judged as empty, malformed, or valid, and each malformed case has one exact user-facing explanation."
    - "Only colon-delimited MAC input is accepted for sending, and a valid address resolves to an uppercase send-ready value."
    - "Wake feedback can represent idle, sending, local-send success, and local failure without claiming the target device actually woke or exposing transport/debug text."
  artifacts:
    - path: Mac OS Swiss Knife/ManualMACValidator.swift
      provides: "Colon-delimited MAC validation enum, user copy, and validator entry point"
      contains: "enum ManualMACValidation"
    - path: Mac OS Swiss Knife/WakeSendPresentation.swift
      provides: "User-facing wake send copy that distinguishes idle, sending, success, and failure"
      contains: "enum WakeSendMessage"
    - path: Mac OS Swiss Knife/WOLSessionModel.swift
      provides: "ObservableObject session contracts and wake sender seam"
      contains: "final class WOLSessionModel"
    - path: Mac OS Swiss Knife/KeepAwakePresentation.swift
      provides: "Confirmed-vs-pending keep-awake presentation types"
      contains: "struct KeepAwakePresentation"
    - path: Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift
      provides: "Validator contract coverage"
      contains: "final class MACAddressValidatorTests"
  key_links:
    - from: Mac OS Swiss Knife/ManualMACValidator.swift
      to: Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift
      via: "Exact enum-case and user-message assertions"
      pattern: "ManualMACValidator.validate"
    - from: Mac OS Swiss Knife/WOLSender.swift
      to: Mac OS Swiss Knife/WakeSendPresentation.swift
      via: "Typed WOLSenderError mapping"
      pattern: "extension WOLSenderError"
    - from: Mac OS Swiss Knife/WakeSendPresentation.swift
      to: Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift
      via: "Exact local-send and failure-copy assertions"
      pattern: "WakeSendPresentation.successMessage"
---

<objective>
Create the Phase 1 truth contracts and unit-test harnesses that later plans will implement against.

Purpose: Lock the manual MAC rules, local-send copy rules, session ownership seam, and keep-awake presentation seam into concrete code before UI/controller rewiring starts.
Output: Compileable contract files, Xcode project entries for all Phase 1 test files, and passing unit tests for validator and wake-result presentation behavior.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-truthful-foundations/01-CONTEXT.md
@.planning/phases/01-truthful-foundations/01-RESEARCH.md
@.planning/phases/01-truthful-foundations/01-UI-SPEC.md
@.planning/phases/01-truthful-foundations/01-VALIDATION.md
@README.md
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/WOLSender.swift
@Mac OS Swiss Knife/PowerAssertionManager.swift
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss KnifeTests/Mac_OS_Swiss_KnifeTests.swift
@Mac OS Swiss Knife.xcodeproj/project.pbxproj

<interfaces>
From Mac OS Swiss Knife/WOLSender.swift:
```swift
enum WOLSenderError: Error {
    case invalidMAC
    case socketFailed
    case setsockoptFailed
    case sendFailed
}

struct WOLSender {
    static func send(to macString: String) throws
}
```

From Mac OS Swiss Knife/PowerAssertionManager.swift:
```swift
final class PowerAssertionManager {
    static let shared = PowerAssertionManager()
    private(set) var isEnabled: Bool = false
    func enable()
    func disable()
}
```

From Mac OS Swiss Knife/WOLView.swift:
```swift
enum InputMode: String {
    case custom = "手动填写 MAC"
    case preset = "预设列表"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Phase 1 contract files and declare the shared interfaces</name>
  <files>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift, Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift, Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests.swift</files>
  <read_first>Mac OS Swiss Knife.xcodeproj/project.pbxproj, Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/KeepAwakePresentation.swift, Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLSender.swift, Mac OS Swiss Knife/PowerAssertionManager.swift, Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/Mac_OS_Swiss_KnifeTests.swift</read_first>
  <action>Add exact Xcode project file references, sources, and unit-test build entries for `ManualMACValidator.swift`, `WakeSendPresentation.swift`, `WOLSessionModel.swift`, `KeepAwakePresentation.swift`, `MACAddressValidatorTests.swift`, `WOLSendPresentationTests.swift`, `WOLSessionModelTests.swift`, and `KeepAwakeMenuStateTests.swift`. In `ManualMACValidator.swift`, declare `enum ManualMACValidation { case empty, invalidCharacters, missingSeparators, wrongGroupCount, wrongByteLength, valid(String) }` plus `var userMessage: String?` and `var isValid: Bool`, and declare `enum ManualMACValidator { static func validate(_ input: String) -> ManualMACValidation }`. In `WakeSendPresentation.swift`, declare `enum WakeSendMessage { case idle, sending, success(String), failure(String) }`, `extension WakeSendMessage { var text: String? { get } }`, `extension WOLSenderError { var userMessage: String { get } }`, and `enum WakeSendPresentation { static func successMessage(for macAddress: String) -> String }`. In `WOLSessionModel.swift`, declare `protocol WakeSending { func send(to macAddress: String) throws }`, `enum WakeSendState { case idle, sending(macAddress: String), success(message: String), failure(message: String) }`, and `final class WOLSessionModel: ObservableObject` with published properties `inputMode`, `selectedMac`, `customMac`, `validation`, and `sendState`. In `KeepAwakePresentation.swift`, declare `enum KeepAwakeTransition { case turningOn, turningOff }`, `enum KeepAwakeToggleOutcome { case unchanged(Bool), success(Bool), failure(current: Bool, message: String) }`, and `struct KeepAwakePresentation` with stored properties `confirmedEnabled`, `pending`, and `message`. Create compileable test files with exact case-class names `MACAddressValidatorTests`, `WOLSendPresentationTests`, `WOLSessionModelTests`, and `KeepAwakeMenuStateTests` so later plans can extend them without changing file ownership. This task establishes interfaces per D-01 through D-17 and the validation Wave 0 file list.</action>
  <acceptance_criteria>`rg -n "enum ManualMACValidation|enum ManualMACValidator" "Mac OS Swiss Knife/ManualMACValidator.swift"` finds both declarations; `rg -n "enum WakeSendMessage|enum WakeSendPresentation|extension WOLSenderError" "Mac OS Swiss Knife/WakeSendPresentation.swift"` finds all three declarations; `rg -n "protocol WakeSending|enum WakeSendState|final class WOLSessionModel" "Mac OS Swiss Knife/WOLSessionModel.swift"` finds all three declarations; `rg -n "enum KeepAwakeTransition|enum KeepAwakeToggleOutcome|struct KeepAwakePresentation" "Mac OS Swiss Knife/KeepAwakePresentation.swift"` finds all three declarations; `rg -n "MACAddressValidatorTests|WOLSendPresentationTests|WOLSessionModelTests|KeepAwakeMenuStateTests" "Mac OS Swiss KnifeTests"` finds all four test classes; `rg -n "ManualMACValidator.swift|WakeSendPresentation.swift|WOLSessionModel.swift|KeepAwakePresentation.swift|MACAddressValidatorTests.swift|WOLSendPresentationTests.swift|WOLSessionModelTests.swift|KeepAwakeMenuStateTests.swift" "Mac OS Swiss Knife.xcodeproj/project.pbxproj"` finds every filename.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/MACAddressValidatorTests" -only-testing:"Mac OS Swiss KnifeTests/WOLSendPresentationTests" -only-testing:"Mac OS Swiss KnifeTests/WOLSessionModelTests" -only-testing:"Mac OS Swiss KnifeTests/KeepAwakeMenuStateTests"</automated>
  </verify>
  <done>The Xcode project compiles with the new Phase 1 contract files present, and downstream plans can import named validator, presentation, session, and keep-awake types without creating their own ad hoc seams.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the colon-delimited manual MAC validator and prove its error taxonomy</name>
  <files>Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift</files>
  <read_first>Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss Knife/WOLView.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-UI-SPEC.md, Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift</read_first>
  <action>Implement `ManualMACValidator.validate(_:)` for the exact manual-entry contract in D-01, D-02, D-03, D-04, and D-05. The validator must treat `""` and whitespace-only input as `.empty` with user copy `请填写 MAC 地址`; any character outside `[0-9A-Fa-f:]` as `.invalidCharacters` with copy `MAC 地址只能包含 0-9、A-F 和冒号`; any 12-hex sequence without colons, such as `AA11BB22CC33`, as `.missingSeparators` with copy `请输入冒号分隔格式，例如 AA:BB:CC:DD:EE:FF`; any candidate that does not split into exactly 6 groups by `:` as `.wrongGroupCount` with copy `MAC 地址必须是 6 组两位十六进制字符`; any group that is not exactly 2 hex digits as `.wrongByteLength` with the same `MAC 地址必须是 6 组两位十六进制字符` copy; and any fully valid address as `.valid("AA:BB:CC:DD:EE:FF")`, returning the uppercase normalized value while leaving UI input rewriting to callers. Add explicit unit tests for empty input, invalid characters, missing separators, too few groups, group length mismatch, invalid hex inside a group, lowercase valid input, and already uppercase valid input. The tests must assert the exact enum case and the exact user message string for each invalid state.</action>
  <acceptance_criteria>`rg -n "请填写 MAC 地址|MAC 地址只能包含 0-9、A-F 和冒号|请输入冒号分隔格式，例如 AA:BB:CC:DD:EE:FF|MAC 地址必须是 6 组两位十六进制字符" "Mac OS Swiss Knife/ManualMACValidator.swift"` finds all four user-facing messages; `rg -n "testEmptyInput|testInvalidCharacters|testMissingSeparators|testWrongGroupCount|testWrongByteLength|testLowercaseValidInput" "Mac OS Swiss KnifeTests/MACAddressValidatorTests.swift"` finds the named validator coverage; the validator source contains `.valid("AA:BB:CC:DD:EE:FF")` behavior or equivalent uppercase normalization logic and contains no auto-insert or auto-rewrite UI code such as `replacingOccurrences(of: ":", with:` inside a setter path.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/MACAddressValidatorTests"</automated>
  </verify>
  <done>Manual MAC validation is defined by a reusable code contract rather than ad hoc view logic, and the validator proves the colon-only format and specific error copy required for WOL-02 and RELY-02.</done>
</task>

<task type="auto">
  <name>Task 3: Implement truthful local-send presentation mapping for WOL outcomes</name>
  <files>Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift</files>
  <read_first>Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss Knife/WOLSender.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-UI-SPEC.md, Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift</read_first>
  <action>Implement the wake-result presentation contract for D-06, D-07, D-08, and D-09. `WakeSendMessage.idle.text` must return `nil`. `WakeSendMessage.sending.text` must return exactly `正在发送唤醒包...`. `WakeSendPresentation.successMessage(for:)` must return a success string that explicitly means local send only, using the exact Chinese copy `已从这台 Mac 发出唤醒包`. `WOLSenderError.userMessage` must map `.invalidMAC` to `MAC 地址格式无效，请输入 AA:BB:CC:DD:EE:FF`, `.socketFailed` to `无法创建本地网络发送通道`, `.setsockoptFailed` to `无法启用局域网广播发送`, and `.sendFailed` to `未能从这台 Mac 发出唤醒包，请确认这台 Mac 已连接局域网后重试`. Add tests that assert each exact string, assert the success message does not contain `唤醒成功` or `设备已开启`, and assert the failure messages do not contain raw `WOLSenderError`, `errno`, or `sendto` text. Keep all user-facing runtime copy in Chinese, matching the repo convention.</action>
  <acceptance_criteria>`rg -n "正在发送唤醒包...|已从这台 Mac 发出唤醒包|MAC 地址格式无效，请输入 AA:BB:CC:DD:EE:FF|无法创建本地网络发送通道|无法启用局域网广播发送|未能从这台 Mac 发出唤醒包，请确认这台 Mac 已连接局域网后重试" "Mac OS Swiss Knife/WakeSendPresentation.swift"` finds every required message; `rg -n "testSendingMessage|testSuccessMessageIsLocalOnly|testInvalidMACMessage|testSocketFailureMessage|testSendFailureMessage" "Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift"` finds targeted message tests; `rg -n "唤醒成功|设备已开启|errno|sendto|WOLSenderError" "Mac OS Swiss Knife/WakeSendPresentation.swift" "Mac OS Swiss KnifeTests/WOLSendPresentationTests.swift"` returns no matches in asserted user-facing copy paths.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/WOLSendPresentationTests"</automated>
  </verify>
  <done>Wake results now have a dedicated copy contract that can only describe local-send truth, giving later UI work a safe source of success and failure strings for RELY-03.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests"` after the plan finishes. Confirm the Xcode project includes all Phase 1 test files and that the validator and wake-result message suites pass independently before any view/controller rewiring begins.
</verification>

<success_criteria>
Phase 1 now has compileable source-of-truth contracts for validation, WOL result copy, session ownership, and keep-awake presentation; the Xcode test target contains dedicated files for every validation entry in `01-VALIDATION.md`; and the validator plus wake-result contracts already prove the most error-prone user-facing strings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-foundations/01-01-SUMMARY.md`
</output>
