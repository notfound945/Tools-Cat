---
phase: 01-truthful-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Mac OS Swiss Knife/WOLSessionModel.swift
  - Mac OS Swiss Knife/WOLView.swift
  - Mac OS Swiss Knife/WOLWindow.swift
  - Mac OS Swiss Knife/AppDelegate.swift
  - Mac OS Swiss Knife/WOLSender.swift
  - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
autonomous: true
requirements:
  - WOL-02
  - RELY-02
  - RELY-03
must_haves:
  truths:
    - "Manual MAC entry validates on every keystroke and the send button stays disabled until the entry is fully valid."
    - "Starting a new wake attempt clears the previous result immediately, and the visible result always belongs to the current attempt."
    - "Closing and reopening the WOL window preserves unfinished input, while in-flight sends finish in the background and surface the final result after reopen."
  artifacts:
    - path: Mac OS Swiss Knife/WOLSessionModel.swift
      provides: "Long-lived WOL draft, validation, send, and lifecycle owner"
      contains: "func sendCurrentSelection()"
    - path: Mac OS Swiss Knife/WOLView.swift
      provides: "SwiftUI WOL form bound to shared session state"
      contains: "@ObservedObject var session"
    - path: Mac OS Swiss Knife/WOLWindow.swift
      provides: "Window owner that retains and reuses the shared session"
      contains: "init(session: WOLSessionModel)"
    - path: Mac OS Swiss Knife/AppDelegate.swift
      provides: "Single app-session WOL model owner"
      contains: "private var wolSession"
    - path: Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
      provides: "Lifecycle and send-state coverage"
      contains: "final class WOLSessionModelTests"
  key_links:
    - from: Mac OS Swiss Knife/AppDelegate.swift
      to: Mac OS Swiss Knife/WOLWindow.swift
      via: "Retained WOLSessionModel injection"
      pattern: "WOLWindow(session:"
    - from: Mac OS Swiss Knife/WOLView.swift
      to: Mac OS Swiss Knife/WOLSessionModel.swift
      via: "ObservedObject binding and send actions"
      pattern: "@ObservedObject var session"
    - from: Mac OS Swiss Knife/WOLSessionModel.swift
      to: Mac OS Swiss Knife/WakeSendPresentation.swift
      via: "Mapped send-state results"
      pattern: "WakeSendPresentation.successMessage"
---

<objective>
Implement the WOL session owner and rewire the window/view so validation, send state, and reopen behavior stay truthful across the app session.

Purpose: Replace fragile view-local state and notification resets with one long-lived model that preserves drafts, clears stale results, and keeps background send completion visible after reopen.
Output: A retained `WOLSessionModel`, an injected `WOLView(session:)`, and unit-tested lifecycle/send behavior covering manual validation and truthful local-send feedback.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-truthful-foundations/01-CONTEXT.md
@.planning/phases/01-truthful-foundations/01-RESEARCH.md
@.planning/phases/01-truthful-foundations/01-UI-SPEC.md
@.planning/phases/01-truthful-foundations/01-VALIDATION.md
@Mac OS Swiss Knife/ManualMACValidator.swift
@Mac OS Swiss Knife/WakeSendPresentation.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/WOLWindow.swift
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/WOLSender.swift
@Mac OS Swiss KnifeTests/WOLSessionModelTests.swift

<interfaces>
Contracts from 01-01:
```swift
enum ManualMACValidation {
    case empty
    case invalidCharacters
    case missingSeparators
    case wrongGroupCount
    case wrongByteLength
    case valid(String)
}

protocol WakeSending {
    func send(to macAddress: String) throws
}

enum WakeSendState {
    case idle
    case sending(macAddress: String)
    case success(message: String)
    case failure(message: String)
}
```

Current WOL window/controller seam:
```swift
final class WOLWindow: NSWindowController, NSWindowDelegate {
    func show()
}

final class AppDelegate: NSObject, NSApplicationDelegate {
    private var wolWindow: WOLWindow?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WOLSessionModel as the lifecycle owner and prove its background-send semantics</name>
  <files>Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift, Mac OS Swiss Knife/WOLSender.swift</files>
  <read_first>Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss Knife/WOLSender.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-VALIDATION.md, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift</read_first>
  <action>Turn `WOLSessionModel` into the single owner of WOL draft, validation, and send lifecycle. Add exact stored or published properties `inputMode`, `selectedMac`, `customMac`, `validation`, `sendState`, `isWindowVisible`, and a derived `canSend`. Implement `updateCustomMac(_:)` to run `ManualMACValidator.validate(_:)` on every keystroke per D-01 through D-05 without mutating the user’s raw text. Implement `sendCurrentSelection()` so it: picks the selected preset MAC when `inputMode == .preset`, or the validated custom MAC when `inputMode == .custom`; returns early when `canSend == false`; explicitly hops onto `MainActor` / the main queue before every `sendState` mutation; sets `sendState = .sending(macAddress: target)` and clears any previous success/failure result immediately per D-08; executes `WakeSending.send(to:)` on a background queue; then hops back to `MainActor` before mapping success to `.success(message: WakeSendPresentation.successMessage(for: target))` or failures to `.failure(message: error.userMessage)`. Do not mutate `@Published` session state from the background send callback. Add lifecycle hooks `handleWindowWillShow()` and `handleWindowWillClose()` so closing does not clear `customMac`, `selectedMac`, or `inputMode` per D-14, reopening clears stale completed results to `.idle` per D-15, in-flight sends continue while `isWindowVisible == false` per D-16, and any send that completes while hidden is surfaced as the final `sendState` when the window becomes visible again per D-17. Add a concrete system adapter `struct SystemWakeSender: WakeSending { func send(to macAddress: String) throws { try WOLSender.send(to: macAddress) } }`. In `WOLSessionModelTests.swift`, use a fake `WakeSending` implementation and async expectations to assert exact behaviors for invalid custom input blocking sends, valid custom input enabling send, new send clearing the previous result immediately, hidden-window completion preserving the final result, reopen preserving draft input while resetting stale finished results, and `testSendCompletionPublishesOnMainThread`, which proves the completion-driven `sendState` publication is observed on the main thread.</action>
  <acceptance_criteria>`rg -n "func updateCustomMac\\(|func sendCurrentSelection\\(|func handleWindowWillShow\\(|func handleWindowWillClose\\(|var canSend" "Mac OS Swiss Knife/WOLSessionModel.swift"` finds all required APIs; `rg -n "MainActor|DispatchQueue\\.main" "Mac OS Swiss Knife/WOLSessionModel.swift"` finds an explicit main-thread hop for `sendState` updates; `rg -n "struct SystemWakeSender: WakeSending" "Mac OS Swiss Knife/WOLSessionModel.swift" "Mac OS Swiss Knife/WOLSender.swift"` finds the system adapter; `rg -n "ManualMACValidator.validate|WakeSendPresentation.successMessage|error.userMessage" "Mac OS Swiss Knife/WOLSessionModel.swift"` finds all three dependencies wired together; `rg -n "testInvalidCustomInputBlocksSend|testValidCustomInputEnablesSend|testNewSendClearsPreviousResult|testHiddenWindowReceivesFinalResult|testReopenPreservesDraftAndClearsStaleResult|testSendCompletionPublishesOnMainThread" "Mac OS Swiss KnifeTests/WOLSessionModelTests.swift"` finds lifecycle, send-state, and main-thread publication coverage.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/WOLSessionModelTests"</automated>
  </verify>
  <done>The WOL flow now has a retained, testable state owner that enforces real-time validation, background send continuity, main-thread `@Published` updates for completion results, and the close/reopen semantics required for WOL-02, RELY-02, and RELY-03.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the WOL window and SwiftUI form to the shared session model</name>
  <files>Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLWindow.swift, Mac OS Swiss Knife/AppDelegate.swift</files>
  <read_first>Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLWindow.swift, Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/ManualMACValidator.swift, Mac OS Swiss Knife/WakeSendPresentation.swift, .planning/phases/01-truthful-foundations/01-CONTEXT.md, .planning/phases/01-truthful-foundations/01-UI-SPEC.md</read_first>
  <action>Replace view-local WOL state with the shared session model. In `AppDelegate.swift`, retain a single `WOLSessionModel` instance as `private var wolSession` and inject it when constructing `WOLWindow(session: wolSession)`. In `WOLWindow.swift`, change the initializer to `init(session: WOLSessionModel)`, store the session, and inject `WOLView(session: session)` into `NSHostingView`. Keep `window.isReleasedWhenClosed = false` so the session survives close/reopen. On `show()`, call `session.handleWindowWillShow()` before bringing the window forward. On `windowWillClose`, call `session.handleWindowWillClose()` instead of clearing input by notification side effect. In `WOLView.swift`, replace local `@State` properties for `inputMode`, `selectedMac`, `customMac`, `statusText`, and `isSending` with `@ObservedObject var session: WOLSessionModel`. Bind the custom text field through `Binding(get:set:)` so free typing updates `session.customMac` and validation runs on every change without filtering characters out of the field per D-04. Keep the existing inline status area, but drive it from `session.sendState` and `session.validation.userMessage` instead of string inspection; use five states only: idle, validation error, sending, local-send success, and local-send failure. Change the send button label to `发送唤醒包`, disable it with the exact condition `session.isSending || !session.canSend`, and show `ProgressView()` only while `sendState == .sending(...)`. Remove the current `statusText.contains("成功")` color heuristic and replace it with explicit state-based colors: validation/failure orange, success green, idle helper text secondary. Do not clear `customMac` or `selectedMac` on close, and do not recreate the session on reopen.</action>
  <acceptance_criteria>`rg -n "@ObservedObject var session" "Mac OS Swiss Knife/WOLView.swift"` finds the shared model binding; `rg -n "WOLWindow\\(session:" "Mac OS Swiss Knife/AppDelegate.swift"` finds injected construction; `rg -n "init\\(session: WOLSessionModel\\)" "Mac OS Swiss Knife/WOLWindow.swift"` finds the session-aware window initializer; `rg -n "\\.disabled\\(session\\.isSending \\|\\| !session\\.canSend\\)" "Mac OS Swiss Knife/WOLView.swift"` finds the exact send-button gate; `rg -n "statusText.contains\\(\"成功\"\\)" "Mac OS Swiss Knife/WOLView.swift"` returns no matches; `rg -n "session.handleWindowWillShow\\(|session.handleWindowWillClose\\(" "Mac OS Swiss Knife/WOLWindow.swift"` finds both lifecycle calls.</acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/WOLSessionModelTests"</automated>
  </verify>
  <done>The WOL window now behaves like a resumed session instead of a reset form: draft input survives close/reopen, send state is truthful, and the current attempt’s result is the only visible result in the existing inline status area.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests/WOLSessionModelTests"` after each task. At the shared Wave 2 gate, after Plans `01-02` and `01-03` both finish, run the broader unit-target sweep `xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:"Mac OS Swiss KnifeTests"` to confirm the new session wiring still respects the validator, wake-result copy, and keep-awake contracts from the rest of Phase 1.
</verification>

<success_criteria>
The Phase 1 WOL flow becomes trustworthy: manual entry validates in real time, the button cannot fire an invalid send, stale results disappear at the correct time, and a background send still lands its final local-send result after the window is reopened.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-foundations/01-02-SUMMARY.md`
</output>
