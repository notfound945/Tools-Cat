---
phase: 22-wol-result-timeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat/WOLSessionModel.swift"
  - "Tools Cat/StatusBarController.swift"
  - "Tools CatTests/WOLSessionModelTests.swift"
  - "Tools CatTests/StatusBarControllerWakeMenuTests.swift"
autonomous: true
requirements:
  - WOLF-01
  - WOLF-02
must_haves:
  truths:
    - "A completed WOL success or failure stays visible in the WOL window for about 3 seconds, then the shared session returns that surface to its non-result state."
    - "The menu-bar wake status row shows the same completed WOL message for about 3 seconds, then hides automatically when the shared session clears."
    - "Starting a new wake action cancels any older pending clear so no stale timer can erase newer sending or completed feedback."
  artifacts:
    - path: "Tools Cat/WOLSessionModel.swift"
      provides: "Shared WOL result timeout ownership, cancellation, and completed-result clearing"
      contains: "wakeResultClearing.schedule(after: 3)"
    - path: "Tools CatTests/WOLSessionModelTests.swift"
      provides: "Deterministic timeout and cancellation regression coverage for the shared WOL session"
      contains: "testCompletedWakeResultClearsAfterThreeSeconds"
    - path: "Tools Cat/StatusBarController.swift"
      provides: "Menu-bar wake status rendering derived only from shared session state"
      contains: "if let message = wolSession.lastCompletedWake?.message"
    - path: "Tools CatTests/StatusBarControllerWakeMenuTests.swift"
      provides: "Menu wake-status expiry coverage tied to the shared WOL timeout seam"
      contains: "testWakeStatusRowHidesAfterShared"
  key_links:
    - from: "Tools Cat/WOLSessionModel.swift"
      to: "Tools CatTests/WOLSessionModelTests.swift"
      via: "fake wake-result scheduler and cancellation assertions"
      pattern: "FakeWakeResultClearing|cancelCount|scheduledDelays"
    - from: "Tools Cat/WOLSessionModel.swift"
      to: "Tools Cat/WOLView.swift"
      via: "WOLView visibleStatusText switch over session.sendState"
      pattern: "switch session.sendState"
    - from: "Tools Cat/WOLSessionModel.swift"
      to: "Tools Cat/StatusBarController.swift"
      via: "StatusBarController.updateWakeStatusItem() reads sendState and lastCompletedWake"
      pattern: "updateWakeStatusItem|lastCompletedWake"
---

<objective>
Complete and stabilize the already-started Phase 22 WOL result-timeout seam so one shared `WOLSessionModel` timer governs both the WOL window and the menu-bar wake status row.

Purpose: v1.8 only changes result lifetime, not WOL copy, menu structure, or saved-device validation behavior. The current worktree already contains partial timeout edits, so execution must finish and verify that shared-session implementation instead of inventing a parallel surface-local solution.
Output: a deterministic 3-second shared result clear in `WOLSessionModel`, focused session tests proving expiry and cancellation, and menu wake-status tests proving the menu row hides when the shared session clears.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-wol-result-timeout/22-CONTEXT.md
@.planning/phases/22-wol-result-timeout/22-RESEARCH.md
@.planning/phases/22-wol-result-timeout/22-VALIDATION.md
@.planning/phases/03-saved-device-wake-flows/03-01-SUMMARY.md
@.planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
@.planning/phases/03-saved-device-wake-flows/03-03-SUMMARY.md
@.planning/phases/21-device-entry-verification-closure/21-VERIFICATION.md
@Tools Cat/WOLSessionModel.swift
@Tools Cat/WOLView.swift
@Tools Cat/StatusBarController.swift
@Tools CatTests/WOLSessionModelTests.swift
@Tools CatTests/StatusBarControllerWakeMenuTests.swift

<interfaces>
From `Tools Cat/WOLSessionModel.swift`:
```swift
protocol WakeResultClearing {
    func schedule(after delay: TimeInterval, _ action: @escaping @MainActor () -> Void) -> WakeResultClearToken
}

protocol WakeResultClearToken {
    func cancel()
}

enum WakeSendState: Equatable {
    case idle
    case sending(macAddress: String)
    case success(message: String)
    case failure(message: String)
}

struct CompletedWakeAttempt: Equatable {
    let deviceID: UUID?
    let message: String
    let wasSuccessful: Bool
}
```

From `Tools Cat/WOLView.swift`:
```swift
private var visibleStatusText: String? {
    switch session.sendState {
    case .idle:
        return session.inputMode == .custom ? session.validation.userMessage : nil
    case .sending:
        return WakeSendMessage.sending.text
    case .success(let message), .failure(let message):
        return message
    }
}
```

From `Tools Cat/StatusBarController.swift`:
```swift
private func updateWakeStatusItem() {
    switch wolSession.sendState {
    case .sending:
        wakeStatusItem.title = WakeSendMessage.sending.text ?? ""
        wakeStatusItem.isHidden = false
    case .idle, .success, .failure:
        if let message = wolSession.lastCompletedWake?.message {
            wakeStatusItem.title = message
            wakeStatusItem.isHidden = false
        } else {
            wakeStatusItem.title = ""
            wakeStatusItem.isHidden = true
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stabilize the shared WOL result timeout lifecycle in `WOLSessionModel`</name>
  <files>Tools Cat/WOLSessionModel.swift, Tools CatTests/WOLSessionModelTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/22-wol-result-timeout/22-CONTEXT.md
    - .planning/phases/22-wol-result-timeout/22-RESEARCH.md
    - .planning/phases/22-wol-result-timeout/22-VALIDATION.md
    - .planning/phases/03-saved-device-wake-flows/03-01-SUMMARY.md
    - .planning/phases/03-saved-device-wake-flows/03-03-SUMMARY.md
    - Tools Cat/WOLSessionModel.swift
    - Tools Cat/WOLView.swift
    - Tools CatTests/WOLSessionModelTests.swift
  </read_first>
  <behavior>
    - Test 1: `testCompletedWakeResultClearsAfterThreeSeconds` proves a completed wake schedules exactly one `3` second clear, and firing the fake scheduler changes `sendState` to `.idle` and `lastCompletedWake` to `nil`.
    - Test 2: `testNewSendCancelsPreviousWakeResultClear` proves a second wake start increments fake-scheduler `cancelCount` before the old clear can fire.
    - Test 3: `testHiddenWindowReceivesFinalResult` proves a hidden window can still reopen onto the just-completed result, while the timeout still originates at completion time rather than window reopen time.
  </behavior>
  <acceptance_criteria>
    - `Tools Cat/WOLSessionModel.swift` contains `wakeResultClearing.schedule(after: 3)`
    - `Tools Cat/WOLSessionModel.swift` contains `clearWakeResultToken?.cancel()`
    - `Tools Cat/WOLSessionModel.swift` contains `self.lastCompletedWake = nil`
    - `Tools Cat/WOLSessionModel.swift` contains `if self.sendState.isCompletedResult {`
    - `Tools Cat/WOLSessionModel.swift` does not introduce `Timer.scheduledTimer`, `DispatchSourceTimer`, or view-local timeout ownership
    - `Tools CatTests/WOLSessionModelTests.swift` contains `testCompletedWakeResultClearsAfterThreeSeconds`
    - `Tools CatTests/WOLSessionModelTests.swift` contains `testNewSendCancelsPreviousWakeResultClear`
    - `Tools CatTests/WOLSessionModelTests.swift` contains `testHiddenWindowReceivesFinalResult`
    - `Tools CatTests/WOLSessionModelTests.swift` contains `FakeWakeResultClearing`
    - The focused WOL session test command in this task's verify block exits successfully
  </acceptance_criteria>
  <action>Finish the shared timeout implementation already in `Tools Cat/WOLSessionModel.swift` and stabilize the associated tests per D-01, D-02, D-03, D-04, and D-05. Keep `WOLSessionModel` as the only timeout owner. In `send(targetMACAddress:savedDeviceID:)`, cancel and nil out any existing `clearWakeResultToken`, set `preservesHiddenCompletionResult = false`, and publish `.sending(macAddress: targetMACAddress)` before dispatching the send work so a new wake replaces stale completed UI immediately per D-05. When the send finishes on `MainActor`, publish the `CompletedWakeAttempt` into `lastCompletedWake`, publish `.success(message: outcome.message)` or `.failure(message: outcome.message)` without changing the shipped message copy per D-06, and then call a shared `scheduleWakeResultClear()` method. That clear method must call `wakeResultClearing.schedule(after: 3)` per D-01 and D-02, clear `lastCompletedWake`, reset `sendState` to `.idle` only when `sendState.isCompletedResult` is still true, and set `preservesHiddenCompletionResult = false`. Preserve the hidden-window reopen contract from Phase 03: `handleWindowWillShow()` may surface a just-completed hidden result once, but it must not restart the 3-second lifetime, because the timeout starts when the final result becomes renderable per D-03. Keep WOLView passive; do not add any timer or delayed clear to `WOLView.swift`.</action>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/WOLSessionModelTests/testCompletedWakeResultClearsAfterThreeSeconds' -only-testing:'Tools CatTests/WOLSessionModelTests/testNewSendCancelsPreviousWakeResultClear' -only-testing:'Tools CatTests/WOLSessionModelTests/testHiddenWindowReceivesFinalResult'</automated>
  </verify>
  <done>`WOLSessionModel` owns one cancellable 3-second completed-result timeout, older clears are canceled before new sends, and the focused WOL session regression slice is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock menu wake-status expiry to the shared WOL session state</name>
  <files>Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerWakeMenuTests.swift</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/22-wol-result-timeout/22-CONTEXT.md
    - .planning/phases/22-wol-result-timeout/22-RESEARCH.md
    - .planning/phases/22-wol-result-timeout/22-VALIDATION.md
    - .planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md
    - Tools Cat/WOLSessionModel.swift
    - Tools Cat/StatusBarController.swift
    - Tools CatTests/WOLSessionModelTests.swift
    - Tools CatTests/StatusBarControllerWakeMenuTests.swift
  </read_first>
  <behavior>
    - Test 1: a successful saved-device wake shows the completed message in `wakeStatusItem`, then the row becomes hidden after the shared clear fires.
    - Test 2: a failed saved-device wake shows `WOLSenderError.sendFailed.userMessage` in `wakeStatusItem`, then the row becomes hidden after the shared clear fires.
    - Test 3: while a send is in flight, `wakeStatusItem.title` stays equal to `WakeSendMessage.sending.text`, and no menu-local timeout implementation is introduced.
  </behavior>
  <acceptance_criteria>
    - `Tools Cat/StatusBarController.swift` contains `case .sending:`
    - `Tools Cat/StatusBarController.swift` contains `wakeStatusItem.title = WakeSendMessage.sending.text ?? ""`
    - `Tools Cat/StatusBarController.swift` contains `if let message = wolSession.lastCompletedWake?.message`
    - `Tools Cat/StatusBarController.swift` does not introduce `Timer.scheduledTimer`, `DispatchSourceTimer`, or any second result-clear token
    - `Tools CatTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeStatusRowHidesAfterSharedSuccessResultClear`
    - `Tools CatTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeStatusRowHidesAfterSharedFailureResultClear`
    - `Tools CatTests/StatusBarControllerWakeMenuTests.swift` contains `WakeSendMessage.sending.text`
    - The focused menu wake-status test command in this task's verify block exits successfully
  </acceptance_criteria>
  <action>Extend the menu wake-status regression slice so WOLF-02 is proven from the shared session seam rather than by adding a controller-owned timer. Reuse `WOLSessionModel` with the fake wake-result scheduler from Task 1 when constructing `StatusBarController` test fixtures. Add explicit tests named `testWakeStatusRowHidesAfterSharedSuccessResultClear` and `testWakeStatusRowHidesAfterSharedFailureResultClear` that send a saved-device wake, assert `wakeStatusItem?.isHidden == false` with the expected completed message, then fire the shared fake scheduler and assert `wakeStatusItem?.title == ""` and `wakeStatusItem?.isHidden == true`. If `StatusBarController.swift` needs adjustment, keep `updateWakeStatusItem()` as a pure renderer: `.sending` must show `WakeSendMessage.sending.text ?? ""`, and `.idle`, `.success`, and `.failure` must show `wolSession.lastCompletedWake?.message` when present and hide the row otherwise. Do not add a second timer, a second wake-status model, or any menu-structure changes per D-07, and do not touch device-form validation behavior per D-08.</action>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerWakeMenuTests'</automated>
  </verify>
  <done>The menu-bar wake status row derives transient completed-result visibility entirely from the shared WOL session, and the focused controller regression slice proves that the row hides after the shared timeout clears.</done>
</task>

</tasks>

<verification>
Run the focused Phase 22 quick slice after each task and the same command set from `22-VALIDATION.md` before closing the plan. Phase 22 is complete only when the shared WOL session tests and the menu wake-status controller tests are both green without adding any view-local or menu-local timer ownership.
</verification>

<success_criteria>
Phase 22 succeeds when one retained `WOLSessionModel` publishes a 3-second WOL result lifetime for both the WOL window and the menu-bar wake status row, new sends cancel stale clears before they can erase newer feedback, and the focused XCTest slices named in `22-VALIDATION.md` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/22-wol-result-timeout/22-01-SUMMARY.md`
</output>
