---
phase: 12.1
slug: macos-14-8-3-compatibility-support
status: ready-for-verification
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-15
---

# Phase 12.1 — Validation Strategy

> Canonical validation contract for restoring macOS 14.8.3 compatibility.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Xcode build settings inspection plus unsigned macOS build |
| **Config file** | `Tools Cat.xcodeproj/project.pbxproj` |
| **Quick run command** | `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -showBuildSettings | rg "MACOSX_DEPLOYMENT_TARGET = 14.0"` |
| **Full suite command** | `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build` |
| **Estimated runtime** | ~5-20 seconds depending on incremental build state |

---

## Sampling Rate

- **After every task commit:** Run the quick build-settings check first.
- **After every plan wave:** Run the full unsigned build command.
- **Before `$gsd-verify-work`:** The full unsigned build must be green, and the manual Sonoma launch boundary must be explicitly acknowledged.
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1 | compatibility baseline | config | `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -showBuildSettings | rg "MACOSX_DEPLOYMENT_TARGET = 14.0"` | ✅ | ⬜ pending |
| 12.1-01-02 | 01 | 1 | compatibility baseline | build | `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ manual-only boundary*

---

## Wave 0 Requirements

- [x] `Tools Cat.xcodeproj/project.pbxproj` already exists and is the canonical source for deployment-target truth.
- [x] Local Xcode CLI tooling is available and can run `xcodebuild -showBuildSettings`.
- [x] Local unsigned builds are available through `CODE_SIGNING_ALLOWED=NO`.

Existing infrastructure covers this phase without adding new test harness files.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Built app launches on a real macOS 14.8.3 machine without an OS-version incompatibility block | runtime confirmation | This machine can prove the 14.0 deployment target and a successful build, but it cannot prove a real Sonoma 14.8.3 launch unless executed on that OS version | Copy or build the app on a macOS 14.8.3 machine, launch `Tools Cat.app`, confirm the menu bar item appears, and confirm the process does not exit immediately because of unsupported OS version metadata |

---

## Validation Sign-Off

- [x] All tasks have automated verify commands or one explicit manual-only boundary
- [x] Sampling continuity is preserved
- [x] Wave 0 covers all required infrastructure
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` is justified because the remaining uncertainty is a clearly bounded OS-version manual launch smoke, not an untested code path hidden behind vague claims

**Approval:** verification-ready
