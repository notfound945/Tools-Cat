---
phase: 12.1-macos-14-8-3-compatibility-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat.xcodeproj/project.pbxproj"
  - "CLAUDE.md"
  - ".planning/codebase/STACK.md"
  - ".planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md"
autonomous: true
requirements: []
must_haves:
  truths:
    - "The shipped build baseline no longer excludes macOS 14.8.3 because the project deployment target is set to a 14.x-compatible value."
    - "Repo guidance no longer falsely claims macOS 15.6 is the minimum supported deployment target."
    - "Compatibility evidence distinguishes automated build proof from the manual-only real 14.8.3 launch boundary."
  artifacts:
    - path: "Tools Cat.xcodeproj/project.pbxproj"
      provides: "Canonical macOS deployment target for app and test targets"
      contains: "MACOSX_DEPLOYMENT_TARGET = 14.0;"
    - path: "CLAUDE.md"
      provides: "Updated project guidance that no longer states macOS 15.6 as the minimum target"
      contains: "macOS 14.0 minimum deployment target"
    - path: ".planning/codebase/STACK.md"
      provides: "Source stack documentation that generated repo guidance should agree with"
      contains: "macOS 14.0 minimum deployment target"
    - path: ".planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md"
      provides: "Compatibility validation contract with automated build checks and one explicit Sonoma manual boundary"
      contains: "macOS 14.8.3"
  key_links:
    - from: "Tools Cat.xcodeproj/project.pbxproj"
      to: "CLAUDE.md"
      via: "deployment-target truth in the Xcode project and repo guidance must stay aligned"
      pattern: "MACOSX_DEPLOYMENT_TARGET|minimum deployment target"
    - from: ".planning/codebase/STACK.md"
      to: "CLAUDE.md"
      via: "generated repo guidance should not drift away from the stack source document that describes the baseline platform"
      pattern: "minimum deployment target|macOS 14.0"
    - from: ".planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md"
      to: "Tools Cat.xcodeproj/project.pbxproj"
      via: "validation proves the new baseline through build settings and unsigned build commands"
      pattern: "showBuildSettings|CODE_SIGNING_ALLOWED=NO build"
---

<objective>
Restore runtime support for macOS 14.8.3 by aligning the actual build baseline, project guidance, and verification boundary.

Purpose: This phase is a compatibility truth fix, not a product feature. It should remove the 15.6 deployment-target blocker, update stale repo guidance that would otherwise drift the project back, and define a verification contract that separates local build proof from real Sonoma launch proof.
Output: A 14.0 deployment target in the Xcode project, corrected repo guidance, and a validation artifact that records both the automated and manual compatibility evidence boundary.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-RESEARCH.md
@Tools Cat.xcodeproj/project.pbxproj
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Normalize the deployment target to macOS 14 and align compatibility guidance with the actual build baseline</name>
  <files>Tools Cat.xcodeproj/project.pbxproj, CLAUDE.md, .planning/codebase/STACK.md, .planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-RESEARCH.md
    - Tools Cat.xcodeproj/project.pbxproj
    - CLAUDE.md
    - .planning/codebase/STACK.md
    - .planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md
  </read_first>
  <behavior>
    - App and test targets no longer exclude Sonoma because all deployment-target entries are `14.0`.
    - Project guidance no longer claims `15.6` as the minimum deployment target.
    - The source stack document and generated repo guidance stay aligned on the same `14.0` minimum deployment target.
    - Validation records one explicit manual-only boundary for a real macOS 14.8.3 launch smoke rather than implying local build proof is enough.
  </behavior>
  <action>Update `Tools Cat.xcodeproj/project.pbxproj` so every `MACOSX_DEPLOYMENT_TARGET` entry used by the app and test targets is exactly `14.0`. In both `CLAUDE.md` and `.planning/codebase/STACK.md`, replace the stale statement that the project uses a `macOS 15.6 minimum deployment target` with wording that the repo baseline is `macOS 14.0 minimum deployment target`, and keep the file references to `Tools Cat.xcodeproj/project.pbxproj` accurate. Create `.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md` that defines: `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -showBuildSettings | rg "MACOSX_DEPLOYMENT_TARGET = 14.0"` as the quick proof, `xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build` as the full automated proof, and one manual-only verification that a built app launches on a real macOS 14.8.3 machine without an OS-version incompatibility block. Do not expand scope into notarization, packaging, or unrelated runtime changes.</action>
  <acceptance_criteria>
    - `Tools Cat.xcodeproj/project.pbxproj` contains `MACOSX_DEPLOYMENT_TARGET = 14.0;` exactly 4 times
    - `CLAUDE.md` contains `macOS 14.0 minimum deployment target`
    - `CLAUDE.md` does not contain `macOS 15.6 minimum deployment target`
    - `.planning/codebase/STACK.md` contains `macOS 14.0 minimum deployment target`
    - `.planning/codebase/STACK.md` does not contain `macOS 15.6 minimum deployment target`
    - `.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md` contains `MACOSX_DEPLOYMENT_TARGET = 14.0`
    - `.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md` contains `CODE_SIGNING_ALLOWED=NO build`
    - `.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-VALIDATION.md` contains `macOS 14.8.3`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -showBuildSettings | rg "MACOSX_DEPLOYMENT_TARGET = 14.0" && xcodebuild -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build</automated>
  </verify>
  <done>The repo's compatibility baseline, guidance, and validation boundary now all agree that the app targets macOS 14 and requires one explicit manual Sonoma launch smoke for final runtime confirmation.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the Xcode project no longer declares macOS 15.6 as the minimum target, repo guidance is updated to the same 14.0 baseline, and the validation contract explicitly separates automated build evidence from the manual macOS 14.8.3 launch check.
</verification>

<success_criteria>
Anyone executing this phase can prove the app is built for macOS 14 compatibility, understand exactly what changed, and know the single remaining manual boundary for real 14.8.3 runtime confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-macos-14-8-3-compatibility-support/12.1-01-SUMMARY.md`
</output>
