---
phase: 23-device-form-save-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools Cat/DeviceLibrarySessionModel.swift
  - Tools Cat/DeviceLibraryView.swift
  - Tools CatTests/DeviceLibrarySessionModelTests.swift
  - Tools CatUITests/Tools_CatUITests.swift
autonomous: true
requirements: [DEVS-15, DEVS-16]

must_haves:
  truths:
    - "The saved-device form keeps `保存设备` disabled when the name field is empty or whitespace-only."
    - "The saved-device form keeps `保存设备` disabled when the MAC field is empty or whitespace-only."
    - "The saved-device form enables `保存设备` once both required fields contain trimmed input, even if the MAC is still malformed."
    - "Validation text still appears only after blur or explicit submit, not during ordinary typing."
    - "Submitting malformed-but-non-empty input still leaves the form open and shows the existing MAC validation message instead of persisting."
  artifacts:
    - path: "Tools Cat/DeviceLibrarySessionModel.swift"
      provides: "Session-owned required-field save gating without changing save-time validation truth"
      contains: "var canSaveDraft: Bool"
    - path: "Tools Cat/DeviceLibraryView.swift"
      provides: "Save button disabled binding to the session predicate"
      contains: ".disabled(!session.canSaveDraft)"
    - path: "Tools CatTests/DeviceLibrarySessionModelTests.swift"
      provides: "Unit coverage for trimmed required-field gating and preserved invalid-save behavior"
      contains: "testCanSaveDraftRequiresTrimmedNameAndMACInput"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "Direct-launch UI coverage for save-button enablement transitions and preserved submit-time validation"
      contains: "testDeviceLibrarySaveButtonEnablesAfterRequiredInput"
  key_links:
    - from: "Tools Cat/DeviceLibraryView.swift"
      to: "Tools Cat/DeviceLibrarySessionModel.swift"
      via: "Button(DeviceLibraryManagementPresentation.saveButtonTitle).disabled(!session.canSaveDraft)"
      pattern: "\\.disabled\\(!session\\.canSaveDraft\\)"
    - from: "Tools Cat/DeviceLibrarySessionModel.swift"
      to: "ManualMACValidator"
      via: "saveDraft() invalid-submit path"
      pattern: "guard case let \\.valid\\(normalizedMACAddress\\) = macValidation else"
    - from: "Tools CatUITests/Tools_CatUITests.swift"
      to: "Tools Cat/DeviceLibraryView.swift"
      via: "existing direct-launch device-library sheet helpers"
      pattern: "deviceLibrarySaveButton\\(in: app, formActions: formActions\\)"
---

<objective>
Gate the saved-device form’s `保存设备` button on trimmed required-field presence while preserving the delayed validation reveal and save-time validation boundary already shipped in v1.7.

Purpose: Implement the v1.8 affordance guardrail from D-01 through D-08 without changing MAC validation rules, normalization, save messaging, device seeding, or WOL behavior.
Output: One session-owned `canSaveDraft` predicate based on required-field presence, one view binding that disables the save button until that predicate is true, and focused unit/UI regressions that prove button-state transitions plus preserved invalid-submit behavior.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-device-form-save-guard/23-CONTEXT.md
@.planning/phases/23-device-form-save-guard/23-RESEARCH.md
@.planning/phases/23-device-form-save-guard/23-VALIDATION.md
@.planning/phases/19-deferred-device-form-validation/19-01-SUMMARY.md
@.planning/phases/15-device-library-ui-parity/15-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@Tools Cat/DeviceLibrarySessionModel.swift
@Tools Cat/DeviceLibraryView.swift
@Tools Cat/KeepAwakeDurationManagementSessionModel.swift
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools CatTests/DeviceLibrarySessionModelTests.swift
@Tools CatUITests/Tools_CatUITests.swift

<interfaces>
From `Tools Cat/DeviceLibrarySessionModel.swift`:
```swift
var nameValidationMessage: String? {
    draftName.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty ? "请填写设备名称" : nil
}

var macAddressValidation: ManualMACValidation {
    ManualMACValidator.validate(draftMACAddress)
}

var canSaveDraft: Bool {
    currentFormMode != nil && nameValidationMessage == nil && macAddressValidation.isValid
}

func revealValidationForSubmit()
func saveDraft()
```

From `Tools Cat/KeepAwakeDurationManagementView.swift`:
```swift
Button(KeepAwakeDurationManagementPresentation.saveButtonTitle) {
    session.saveDraft()
}
.buttonStyle(.borderedProminent)
.disabled(!session.canSaveDraft)
```

From `Tools CatUITests/Tools_CatUITests.swift`:
```swift
private func deviceLibrarySaveButton(in app: XCUIApplication, formActions: XCUIElement) -> XCUIElement
@discardableResult
private func openDeviceLibraryAddForm(in app: XCUIApplication, window: XCUIElement) -> XCUIElement
private func deviceLibraryMACValidationMessage(in app: XCUIApplication) -> XCUIElement
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redefine device-form save gating in the session model and lock it with focused unit coverage</name>
  <files>Tools Cat/DeviceLibrarySessionModel.swift, Tools CatTests/DeviceLibrarySessionModelTests.swift</files>
  <read_first>
    - Tools Cat/DeviceLibrarySessionModel.swift
    - Tools CatTests/DeviceLibrarySessionModelTests.swift
    - Tools Cat/KeepAwakeDurationManagementSessionModel.swift
    - .planning/phases/23-device-form-save-guard/23-CONTEXT.md
    - .planning/phases/23-device-form-save-guard/23-RESEARCH.md
    - .planning/phases/19-deferred-device-form-validation/19-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `canSaveDraft` is `false` in add mode when `draftName == ""` and `draftMACAddress == ""`.
    - Test 2: `canSaveDraft` is `false` when either required field contains only whitespace such as `"   "`.
    - Test 3: `canSaveDraft` is `false` when only one required field has trimmed input.
    - Test 4: `canSaveDraft` is `true` when both required fields contain trimmed input and the MAC is malformed but non-empty, for example name `书房 NAS` plus MAC `AA:BB:CC`.
    - Test 5: `canSaveDraft` is `true` when edit mode opens with an existing saved device and both prefilled required fields are non-empty.
    - Test 6: `saveDraft()` still reveals validation on submit and blocks malformed MAC persistence exactly as before.
  </behavior>
  <action>
    Update `DeviceLibrarySessionModel.canSaveDraft` per D-01, D-02, D-03, and D-08 so it returns `true` only when `currentFormMode != nil`, `draftName.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty == false`, and `draftMACAddress.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty == false`.

    Keep validation timing and persistence truth unchanged per D-04, D-05, and D-06:
    - leave `visibleNameValidationMessage`, `visibleMACAddressValidationMessage`, `revealValidation(for:)`, and `revealValidationForSubmit()` behavior intact
    - keep `saveDraft()` calling `revealValidationForSubmit()`
    - keep the `if let nameValidationMessage` early return
    - keep `guard case let .valid(normalizedMACAddress) = macValidation else` early return
    - do not change `ManualMACValidator`, normalization, or save error copy

    Add unit tests in `DeviceLibrarySessionModelTests.swift` before or alongside the predicate change. Create exact test methods named:
    - `testCanSaveDraftRequiresTrimmedNameAndMACInput`
    - `testCanSaveDraftAllowsMalformedButNonEmptyMACUntilSubmit`
    - `testBeginEditWithPrefilledRequiredFieldsCanSaveImmediately`

    The malformed-MAC test must prove both states in one flow: `canSaveDraft == true` after setting `draftName = "书房 NAS"` and `draftMACAddress = "AA:BB:CC"`, then `saveDraft()` leaves `currentFormMode == .add`, keeps `devices` empty, and sets `validationMessage == ManualMACValidation.wrongGroupCount.userMessage`.
  </action>
  <acceptance_criteria>
    - `Tools Cat/DeviceLibrarySessionModel.swift` contains `var canSaveDraft: Bool` with `currentFormMode != nil`, `draftName.trimmingCharacters(in: .whitespacesAndNewlines)`, and `draftMACAddress.trimmingCharacters(in: .whitespacesAndNewlines)` in the predicate.
    - `Tools Cat/DeviceLibrarySessionModel.swift` still contains `revealValidationForSubmit()` inside `saveDraft()`.
    - `Tools Cat/DeviceLibrarySessionModel.swift` still contains `guard case let .valid(normalizedMACAddress) = macValidation else`.
    - `Tools CatTests/DeviceLibrarySessionModelTests.swift` contains the exact test names `testCanSaveDraftRequiresTrimmedNameAndMACInput`, `testCanSaveDraftAllowsMalformedButNonEmptyMACUntilSubmit`, and `testBeginEditWithPrefilledRequiredFieldsCanSaveImmediately`.
    - Running the focused unit slice exits successfully and covers the new predicate without changing existing invalid-save expectations.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/DeviceLibrarySessionModelTests'</automated>
  </verify>
  <done>`canSaveDraft` now represents trimmed required-field presence only, and unit coverage proves malformed-but-non-empty drafts can enable save while `saveDraft()` still rejects invalid persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the save button to the session predicate and prove the direct-launch form behavior in UI tests</name>
  <files>Tools Cat/DeviceLibraryView.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - Tools Cat/DeviceLibraryView.swift
    - Tools CatUITests/Tools_CatUITests.swift
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools Cat/DeviceLibrarySessionModel.swift
    - .planning/phases/23-device-form-save-guard/23-CONTEXT.md
    - .planning/phases/23-device-form-save-guard/23-VALIDATION.md
    - .planning/phases/15-device-library-ui-parity/15-01-SUMMARY.md
    - .planning/phases/19-deferred-device-form-validation/19-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: The direct-launch add form opens with `保存设备` disabled.
    - Test 2: Entering only `名称` keeps `保存设备` disabled.
    - Test 3: Entering malformed but non-empty `MAC 地址` such as `AA:BB:CC` enables `保存设备`.
    - Test 4: Tapping the enabled button with malformed MAC leaves the form open and reveals `MAC 地址必须是 6 组两位十六进制字符`.
    - Test 5: Existing UI tests for name-submit reveal and MAC blur-or-submit reveal continue to pass unchanged.
  </behavior>
  <action>
    Update the `保存设备` button in `DeviceLibraryView.formContent(mode:)` per D-03 and D-08 so the existing button remains visible, still calls `session.saveDraft()`, keeps `.buttonStyle(.borderedProminent)`, and adds `.disabled(!session.canSaveDraft)`.

    Do not add any new reveal-on-typing behavior per D-04. Leave the current `@FocusState`, `.onSubmit`, and `revealValidationIfNeeded(afterBlurFrom:to:)` flow unchanged.

    Add one focused UI test method in `Tools_CatUITests.swift` named `testDeviceLibrarySaveButtonEnablesAfterRequiredInput`. Use the existing launch helpers and device-library sheet helpers already in the file:
    - launch with `--ui-test-open-device-library`
    - call `waitForDeviceLibraryWindow(in:)`
    - call `openDeviceLibraryAddForm(in:window:)`
    - resolve the save button with `deviceLibrarySaveButton(in:formActions:)`
    - resolve the field controls with `deviceLibraryNameField(in:)` and `deviceLibraryMACField(in:)`
    - use `replaceText(in:with:)` to set `书房 NAS` and `AA:BB:CC`

    Make the new UI test assert these exact states:
    - `saveButton.isEnabled == false` before typing
    - `saveButton.isEnabled == false` after setting only the name
    - `saveButton.isEnabled == true` after setting the malformed-but-non-empty MAC
    - after clicking `saveButton`, `deviceLibraryMACValidationMessage(in: app)` shows `MAC 地址必须是 6 组两位十六进制字符`
    - `app.descendants(matching: .any)["device-library-form-sheet"]` or `app.descendants(matching: .any)["device-library-form-actions"]` still exists after the failed submit
  </action>
  <acceptance_criteria>
    - `Tools Cat/DeviceLibraryView.swift` contains `.disabled(!session.canSaveDraft)` on the `保存设备` button.
    - `Tools CatUITests/Tools_CatUITests.swift` contains the exact test name `testDeviceLibrarySaveButtonEnablesAfterRequiredInput`.
    - `Tools CatUITests/Tools_CatUITests.swift` contains the string `AA:BB:CC` inside the new UI test.
    - `Tools CatUITests/Tools_CatUITests.swift` contains the string `MAC 地址必须是 6 组两位十六进制字符` in the failed-submit assertion path.
    - The focused UI slice exits successfully with the new enablement test plus the existing delayed-reveal tests green.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibrarySaveButtonEnablesAfterRequiredInput' -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibraryNameValidationRevealsAfterSubmit' -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibraryMACValidationRevealsAfterBlurOrSubmit'</automated>
  </verify>
  <done>The live device-library sheet now applies the session-owned save gate, and UI automation proves the button remains disabled until both required fields have trimmed input while submit-time validation timing remains unchanged.</done>
</task>

</tasks>

<verification>
Run the Phase 23 quick slice after each task:
`xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/DeviceLibrarySessionModelTests'`

Run the full Phase 23 validation slice before writing the summary:
`xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/DeviceLibrarySessionModelTests' -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibrarySaveButtonEnablesAfterRequiredInput' -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibraryNameValidationRevealsAfterSubmit' -only-testing:'Tools CatUITests/Tools_CatUITests/testDeviceLibraryMACValidationRevealsAfterBlurOrSubmit'`
</verification>

<success_criteria>
`DEVS-15` and `DEVS-16` are complete when the device-library add/edit sheet disables `保存设备` for empty or whitespace-only required fields, enables it once both required fields contain input, and still reveals the existing validation messages only on blur or explicit submit while malformed data cannot be persisted.
</success_criteria>

<output>
After completion, create `.planning/phases/23-device-form-save-guard/23-01-SUMMARY.md`
</output>
