---
phase: 09-mac-os-swiss-knife-tools-cat
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - "scripts/run_menu_bar_verification_slice.sh"
  - "release.sh"
  - "build_dmg.sh"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - RENAME-02
  - RENAME-03
must_haves:
  truths:
    - "The canonical regression entry point still works after the rename and names the `Tools Cat` project, scheme, and renamed test bundles explicitly."
    - "Release packaging emits `Tools Cat.app` inside `dist/Tools-Cat.dmg` with `Tools Cat` as the volume name."
    - "No canonical automation path still references the old `Mac OS Swiss Knife` project, scheme, or XCTest target names."
  artifacts:
    - path: "scripts/run_menu_bar_verification_slice.sh"
      provides: "Stable post-rename regression slice"
      contains: "PROJECT=\"Tools Cat.xcodeproj\""
    - path: "release.sh"
      provides: "Default release build and DMG naming for the new product identity"
      contains: "SCHEME=${SCHEME:-\"Tools Cat\"}"
    - path: "build_dmg.sh"
      provides: "DMG helper examples and defaults aligned with the renamed app"
      contains: "DMG_NAME=${2:-Tools-Cat.dmg}"
  key_links:
    - from: "scripts/run_menu_bar_verification_slice.sh"
      to: "Tools CatUITests/Tools_CatUITests.swift"
      via: "the canonical regression slice names the renamed UI smoke tests by their new target and file identifiers"
      pattern: "Tools CatUITests/Tools_CatUITests"
    - from: "release.sh"
      to: "build_dmg.sh"
      via: "release packaging passes the renamed app bundle and renamed DMG defaults into the shared DMG helper"
      pattern: "Tools-Cat.dmg|Tools Cat"
---

<objective>
Re-green the automation and packaging path under the renamed `Tools Cat` identity.

Purpose: A rename that only compiles in Xcode but leaves the canonical regression script and DMG flow on the old name is incomplete and brittle.
Output: The menu-bar regression slice, release wrapper, and DMG helper all point at `Tools Cat`, the renamed UI smoke tests still run, and the default packaging output becomes `dist/Tools-Cat.dmg`.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-mac-os-swiss-knife-tools-cat/09-CONTEXT.md
@.planning/phases/09-mac-os-swiss-knife-tools-cat/09-RESEARCH.md
@.planning/phases/09-mac-os-swiss-knife-tools-cat/09-VALIDATION.md
@scripts/run_menu_bar_verification_slice.sh
@release.sh
@build_dmg.sh
@Tools Cat.xcodeproj/project.pbxproj
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the canonical regression and release scripts to the renamed project and packaging outputs</name>
  <files>scripts/run_menu_bar_verification_slice.sh, release.sh, build_dmg.sh, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/09-mac-os-swiss-knife-tools-cat/09-CONTEXT.md
    - .planning/phases/09-mac-os-swiss-knife-tools-cat/09-RESEARCH.md
    - .planning/phases/09-mac-os-swiss-knife-tools-cat/09-VALIDATION.md
    - Tools Cat.xcodeproj/project.pbxproj
    - scripts/run_menu_bar_verification_slice.sh
    - release.sh
    - build_dmg.sh
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - The regression wrapper runs against `Tools Cat.xcodeproj` and the `Tools Cat` scheme.
    - The wrapper names the renamed unit and UI smoke tests with the `Tools CatTests/...` and `Tools CatUITests/Tools_CatUITests/...` identifiers.
    - `release.sh` defaults to `SCHEME="Tools Cat"`, `DMG_NAME="Tools-Cat.dmg"`, and `VOL_NAME="Tools Cat"`.
    - `build_dmg.sh` usage examples, default DMG name, and default volume name no longer mention `Mac OS Swiss Knife`.
  </behavior>
  <action>Update `scripts/run_menu_bar_verification_slice.sh` so `PROJECT="Tools Cat.xcodeproj"`, `SCHEME="Tools Cat"`, and every `-only-testing:` selector uses the renamed bundles and classes: `Tools CatTests/StatusBarControllerEntryFlowTests`, `Tools CatTests/StatusBarControllerMenuPolishTests`, `Tools CatTests/StatusBarControllerWakeMenuTests`, `Tools CatTests/StatusBarControllerKeepAwakeMenuTests`, and `Tools CatUITests/Tools_CatUITests/testLaunchWithWOLWindowShowsPolishedSections`, `testLaunchWithSeededDeviceLibraryShowsManagementListSurface`, `testLaunchWithEmptyDeviceLibraryShowsPolishedEmptyState`. Update `release.sh` so the documented defaults are `SCHEME="Tools Cat"`, `DMG_NAME="Tools-Cat.dmg"`, and `VOL_NAME="Tools Cat"`, and make sure it packages the one renamed `.app` bundle from the Release products directory. Update `build_dmg.sh` usage comments, example command, default `DMG_NAME`, default `VOL_NAME`, and the temporary directory prefix so all packaging terminology says `Tools Cat`. If any launch-argument UI smoke code or comments in `Tools CatUITests/Tools_CatUITests.swift` still mention the old bundle, scheme, or generated class name, rename those references in the same pass so the script and test file stay aligned.</action>
  <acceptance_criteria>
    - `scripts/run_menu_bar_verification_slice.sh` contains `PROJECT="Tools Cat.xcodeproj"`
    - `scripts/run_menu_bar_verification_slice.sh` contains `SCHEME="Tools Cat"`
    - `scripts/run_menu_bar_verification_slice.sh` contains `-only-testing:'Tools CatTests/StatusBarControllerEntryFlowTests'`
    - `scripts/run_menu_bar_verification_slice.sh` contains `-only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithWOLWindowShowsPolishedSections'`
    - `release.sh` contains `SCHEME=${SCHEME:-"Tools Cat"}`
    - `release.sh` contains `"${DMG_NAME:-Tools-Cat.dmg}"`
    - `release.sh` contains `"${VOL_NAME:-Tools Cat}"`
    - `build_dmg.sh` contains `DMG_NAME=${2:-Tools-Cat.dmg}`
    - `build_dmg.sh` contains `VOL_NAME=${3:-"Tools Cat"}`
    - `build_dmg.sh` contains `Tools Cat.app`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run_menu_bar_verification_slice.sh && SCHEME="Tools Cat" sh ./release.sh</automated>
  </verify>
  <done>The post-rename regression and packaging workflow is now one obvious path that maintainers can run without rediscovering old project names.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the stable regression slice passes against `Tools Cat` and the release flow emits the renamed DMG and app bundle by default.
</verification>

<success_criteria>
Maintainers can run one canonical automation path and one canonical packaging path without encountering the old product identity anywhere in the commands or outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mac-os-swiss-knife-tools-cat/09-02-SUMMARY.md`
</output>
