---
phase: 13-duration-management-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01-PLAN.md"
files_modified:
  - "Tools Cat.xcodeproj/project.pbxproj"
  - "Tools Cat/AppDelegate.swift"
  - "Tools Cat/StatusBarController.swift"
  - "Tools Cat/KeepAwakeDurationManagementView.swift"
  - "Tools Cat/KeepAwakeDurationManagementWindow.swift"
  - "Tools CatTests/StatusBarControllerEntryFlowTests.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
requirements:
  - AWAKE-06
  - AWAKE-07
  - AWAKE-08
  - AWAKE-09
must_haves:
  truths:
    - "Users can open a dedicated native duration-management surface from the app and inspect the current timed-duration list."
    - "The management surface reflects add, edit, and delete results from the shared duration session without pulling dynamic root-menu rendering into Phase 13."
    - "The management surface remains scoped to timed durations only; `无限常亮` stays fixed outside the managed list."
  artifacts:
    - path: "Tools Cat/KeepAwakeDurationManagementView.swift"
      provides: "SwiftUI list/form/delete UI for timed keep-awake durations"
      contains: "struct KeepAwakeDurationManagementView"
    - path: "Tools Cat/KeepAwakeDurationManagementWindow.swift"
      provides: "Reusable native window controller for the duration-management surface"
      contains: "final class KeepAwakeDurationManagementWindow"
    - path: "Tools Cat/AppDelegate.swift"
      provides: "Shared session/window composition and direct-launch hook for the duration manager"
      contains: "openKeepAwakeDurationManagementWindow"
    - path: "Tools Cat/StatusBarController.swift"
      provides: "Menu entry and callback for opening the duration-management surface"
      contains: "onOpenKeepAwakeDurationManagement"
    - path: "Tools CatTests/StatusBarControllerEntryFlowTests.swift"
      provides: "Regression coverage that the new management entry dispatches correctly"
      contains: "testKeepAwakeDurationManagementEntryDispatchesThroughCallback"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "Direct-launch UI smoke for the duration-management window"
      contains: "testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationManagementView.swift"
      to: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      via: "the window renders list/form/delete state from the session model instead of duplicating persistence logic in the view"
      pattern: "session\\.|ObservedObject"
    - from: "Tools Cat/AppDelegate.swift"
      to: "Tools Cat/KeepAwakeDurationManagementWindow.swift"
      via: "app launch holds one reusable duration-management window and exposes it through a callback path"
      pattern: "openKeepAwakeDurationManagementWindow|--ui-test-open-keep-awake-duration-management"
    - from: "Tools Cat/StatusBarController.swift"
      to: "Tools Cat/AppDelegate.swift"
      via: "the new menu item dispatches through a lightweight callback instead of making the controller own the window"
      pattern: "onOpenKeepAwakeDurationManagement"
---

<objective>
Ship the actual native duration-management surface and wire it into the app without advancing dynamic root-menu rendering ahead of schedule.

Purpose: Phase 13 becomes user-visible only once the app can open a small native manager window from the status menu and direct test launch.
Output: SwiftUI management view, reusable window controller, app/menu integration, and controller plus UI smoke coverage.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-duration-management-surface/13-RESEARCH.md
@.planning/phases/13-duration-management-surface/13-VALIDATION.md
@.planning/phases/13-duration-management-surface/13-01-PLAN.md
@Tools Cat/AppDelegate.swift
@Tools Cat/StatusBarController.swift
@Tools Cat/DeviceLibraryView.swift
@Tools Cat/DeviceLibraryWindow.swift
@Tools Cat/KeepAwakeDurationManagementSessionModel.swift
@Tools CatTests/StatusBarControllerEntryFlowTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the native duration-management window and direct-launch UI surface</name>
  <files>Tools Cat.xcodeproj/project.pbxproj, Tools Cat/AppDelegate.swift, Tools Cat/KeepAwakeDurationManagementView.swift, Tools Cat/KeepAwakeDurationManagementWindow.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-RESEARCH.md
    - .planning/phases/13-duration-management-surface/13-VALIDATION.md
    - .planning/phases/13-duration-management-surface/13-01-PLAN.md
    - Tools Cat/DeviceLibraryView.swift
    - Tools Cat/DeviceLibraryWindow.swift
    - Tools Cat/AppDelegate.swift
    - Tools CatUITests/Tools_CatUITests.swift
    - Tools Cat.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: Direct app launch with the duration-manager launch argument opens a native window that renders the current timed durations from the shared store.
    - Test 2: A fresh defaults suite shows the seeded timed rows from Phase 12 instead of an empty list.
    - Test 3: The management surface exposes an add button, edit buttons, delete buttons, and a form with minutes-based input and explicit save/cancel controls.
    - Test 4: The window can be reopened through the same shared session/window owner without leaking multiple independent windows.
  </behavior>
  <action>Create `Tools Cat/KeepAwakeDurationManagementView.swift` as a SwiftUI surface modeled after `DeviceLibraryView`, but scoped to timed durations only. The list screen must show the sorted timed rows, an empty-state-free seeded first-launch experience, and explicit add/edit/delete actions. The form screen must use a single minutes text field, save/cancel controls, and view-level display of `validationMessage` or `saveErrorMessage`. Create `Tools Cat/KeepAwakeDurationManagementWindow.swift` as an `NSWindowController` modeled after `DeviceLibraryWindow`, with a compact native window title and `show()` behavior that reloads the shared session before presentation. Update `AppDelegate.swift` to own one shared `KeepAwakeDurationManagementSessionModel` and one reusable `KeepAwakeDurationManagementWindow`, plus a launch argument named exactly `--ui-test-open-keep-awake-duration-management` for direct UI-test opening. Extend `Tools CatUITests/Tools_CatUITests.swift` with a direct-launch smoke named `testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface` that uses the existing defaults-suite harness, waits for the duration manager surface, and proves the seeded timed rows plus add/form affordances exist. Register new files in `Tools Cat.xcodeproj/project.pbxproj`.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `struct KeepAwakeDurationManagementView`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `@ObservedObject var session`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `TextField`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains `Button("添加时长")`
    - `Tools Cat/KeepAwakeDurationManagementWindow.swift` contains `final class KeepAwakeDurationManagementWindow`
    - `Tools Cat/KeepAwakeDurationManagementWindow.swift` contains `func show()`
    - `Tools Cat/AppDelegate.swift` contains `keepAwakeDurationManagementSession`
    - `Tools Cat/AppDelegate.swift` contains `keepAwakeDurationManagementWindow`
    - `Tools Cat/AppDelegate.swift` contains `openKeepAwakeDurationManagementWindow`
    - `Tools Cat/AppDelegate.swift` contains `--ui-test-open-keep-awake-duration-management`
    - `Tools CatUITests/Tools_CatUITests.swift` contains `testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface'</automated>
  </verify>
  <done>The app can now open a real duration-management window directly and the UI smoke proves the seeded timed list plus CRUD affordances render on launch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the duration manager into the status menu and protect the integration path with tests</name>
  <files>Tools Cat/AppDelegate.swift, Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerEntryFlowTests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-RESEARCH.md
    - .planning/phases/13-duration-management-surface/13-VALIDATION.md
    - Tools Cat/AppDelegate.swift
    - Tools Cat/StatusBarController.swift
    - Tools CatTests/StatusBarControllerEntryFlowTests.swift
  </read_first>
  <behavior>
    - Test 1: The status menu now exposes one explicit row that opens the duration-management surface through a callback, parallel to the WOL and device-library entries.
    - Test 2: Selecting that row dispatches exactly once through the new callback.
    - Test 3: Existing menu-entry behavior for WOL and device-library remains intact after the new row is inserted.
  </behavior>
  <action>Update `Tools Cat/StatusBarController.swift` to add one new root menu item for opening the duration-management surface, plus a callback property named `onOpenKeepAwakeDurationManagement`. Keep the new row inside the utility-management part of the menu and do not change the fixed keep-awake action rows in this phase. Update `AppDelegate.swift` so the status controller’s new callback opens the shared duration-management window. Extend `Tools CatTests/StatusBarControllerEntryFlowTests.swift` with focused coverage that the new row exists and dispatches correctly while existing WOL and device-library entries still work. Do not attempt menu-bar automation here; keep this slice at the controller-callback seam.</action>
  <acceptance_criteria>
    - `Tools Cat/StatusBarController.swift` contains `var onOpenKeepAwakeDurationManagement`
    - `Tools Cat/StatusBarController.swift` contains the menu title `管理常亮时长…`
    - `Tools Cat/StatusBarController.swift` contains `@objc private func openKeepAwakeDurationManagement()`
    - `Tools Cat/AppDelegate.swift` contains `status.onOpenKeepAwakeDurationManagement`
    - `Tools CatTests/StatusBarControllerEntryFlowTests.swift` contains `testKeepAwakeDurationManagementEntryDispatchesThroughCallback`
    - `Tools CatTests/StatusBarControllerEntryFlowTests.swift` still contains existing WOL and device-library entry tests
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerEntryFlowTests'</automated>
  </verify>
  <done>The duration manager is now reachable from the product, and the integration path is protected without prematurely dragging Phase 14’s menu-rendering work into scope.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the app can open the duration manager through both direct test launch and the status-menu callback path, and the visible surface shows the current timed list plus CRUD affordances without claiming root-menu dynamic updates yet.
</verification>

<success_criteria>
Phase 13 is user-visible and truthful: timed durations are manageable through a native window, entry wiring is test-backed, and Phase 14 remains the only place where root-menu rendering changes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-duration-management-surface/13-02-SUMMARY.md`
</output>
