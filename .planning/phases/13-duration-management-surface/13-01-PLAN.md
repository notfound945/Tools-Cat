---
phase: 13-duration-management-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat.xcodeproj/project.pbxproj"
  - "Tools Cat/KeepAwakeDurationManagementPresentation.swift"
  - "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
  - "Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift"
autonomous: true
requirements:
  - AWAKE-07
  - AWAKE-08
  - AWAKE-09
must_haves:
  truths:
    - "The duration-management flow owns a real list/form/delete state machine backed only by `KeepAwakeDurationStore`."
    - "Add and edit paths accept human-entered whole minutes, convert once to canonical seconds, and reject blank, non-positive, non-numeric, or duplicate durations."
    - "Delete requires explicit confirmation and persists through the shared managed-duration store."
  artifacts:
    - path: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      provides: "Store-backed list/form/delete state machine for timed keep-awake durations"
      contains: "final class KeepAwakeDurationManagementSessionModel"
    - path: "Tools Cat/KeepAwakeDurationManagementPresentation.swift"
      provides: "Window, form, validation, and delete-confirmation copy for the duration manager"
      contains: "enum KeepAwakeDurationManagementPresentation"
    - path: "Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift"
      provides: "Regression coverage for CRUD validation, sorting, identity preservation, and delete confirmation"
      contains: "final class KeepAwakeDurationManagementSessionModelTests"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      to: "Tools Cat/KeepAwakeDurationStore.swift"
      via: "all duration reload/add/update/delete mutations flow through the canonical shared store"
      pattern: "reload\\(|addDuration\\(|updateDuration\\(|deleteDuration\\("
    - from: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      to: "Tools Cat/ManagedKeepAwakeDuration.swift"
      via: "the manager lists timed rows only and reuses derived titles from the canonical duration model"
      pattern: "ManagedKeepAwakeDuration|menuTitle"
    - from: "Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift"
      to: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      via: "tests cover add, edit, delete, validation, and sorted reload behavior"
      pattern: "beginAdd|beginEdit|saveDraft|requestDelete|confirmDelete"
---

<objective>
Create the store-backed state machine for the keep-awake duration manager before any new window or menu-entry wiring lands.

Purpose: Phase 13 needs CRUD truth first. The native surface should consume a session model that already knows how to load the timed duration list, validate drafts, preserve identity on edit, and require confirmation on delete.
Output: A dedicated management session model, centralized user-facing copy for the manager, and focused XCTest coverage.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-timed-keep-awake/04-CONTEXT.md
@.planning/phases/12-duration-preset-persistence/12-RESEARCH.md
@.planning/phases/13-duration-management-surface/13-RESEARCH.md
@.planning/phases/13-duration-management-surface/13-VALIDATION.md
@Tools Cat/ManagedKeepAwakeDuration.swift
@Tools Cat/KeepAwakeDurationStore.swift
@Tools Cat/DeviceLibrarySessionModel.swift
@Tools CatTests/DeviceLibrarySessionModelTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the duration-management session model and CRUD validation contract</name>
  <files>Tools Cat.xcodeproj/project.pbxproj, Tools Cat/KeepAwakeDurationManagementPresentation.swift, Tools Cat/KeepAwakeDurationManagementSessionModel.swift, Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-RESEARCH.md
    - .planning/phases/13-duration-management-surface/13-VALIDATION.md
    - Tools Cat/ManagedKeepAwakeDuration.swift
    - Tools Cat/KeepAwakeDurationStore.swift
    - Tools Cat/DeviceLibrarySessionModel.swift
    - Tools CatTests/DeviceLibrarySessionModelTests.swift
    - Tools Cat.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: A fresh session reloads the shared store’s seeded timed durations in ascending order and exposes only timed rows.
    - Test 2: `beginAdd()` clears stale errors and opens a form whose save path blocks blank, non-numeric, zero, and negative minute drafts with Chinese validation copy.
    - Test 3: A valid add converts minutes to canonical seconds, persists through `KeepAwakeDurationStore.addDuration(seconds:)`, and inserts the new duration into the correct sorted position.
    - Test 4: `beginEdit(durationID:)` preloads the current value in minutes, `saveDraft()` preserves the row UUID on success, and duplicate collisions surface a stable error message instead of corrupting the list.
    - Test 5: `requestDelete(durationID:)` requires explicit confirmation before `confirmDelete()` removes the row from the shared store and list.
  </behavior>
  <action>Create `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` as an `@MainActor` `ObservableObject` modeled after `DeviceLibrarySessionModel`, but scoped to timed keep-awake durations. Introduce small supporting enums for list/form state and add/edit mode. The session must own at least `durations`, `screen`, `draftMinutesText`, `pendingDeleteDuration`, `validationMessage`, and `saveErrorMessage`, plus methods `reloadDurations()`, `beginAdd()`, `beginEdit(durationID:)`, `cancelForm()`, `saveDraft()`, `requestDelete(durationID:)`, `cancelDelete()`, and `confirmDelete()`. `saveDraft()` must trim whitespace, parse a positive integer minute value, convert it to seconds, and call either `KeepAwakeDurationStore.addDuration(seconds:)` or `updateDuration(id:seconds:)`. Map store errors into stable Chinese copy, keep delete confirmation explicit, and never synthesize or mutate `无限常亮` because it remains outside managed data. Create `Tools Cat/KeepAwakeDurationManagementPresentation.swift` to centralize titles, button copy, validation messages, and delete-confirmation text used by the manager. Add `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` covering seeded reload, invalid draft blocking, add/edit sorting, duplicate rejection, delete confirmation, and identity preservation. Register the new production and test files in `Tools Cat.xcodeproj/project.pbxproj`.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `@MainActor`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `final class KeepAwakeDurationManagementSessionModel`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `@Published var durations: [ManagedKeepAwakeDuration]`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `@Published var draftMinutesText: String`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `func beginAdd()`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `func beginEdit(durationID: UUID)`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `func saveDraft()`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `func requestDelete(durationID: UUID)`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `func confirmDelete()`
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` contains `KeepAwakeDurationStore`
    - `Tools Cat/KeepAwakeDurationManagementPresentation.swift` contains `enum KeepAwakeDurationManagementPresentation`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` contains `testReloadSeedsSortedManagedDurations`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` contains `testInvalidDraftBlocksSave`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` contains `testAddDurationPersistsAndSortsBySeconds`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` contains `testEditDurationPreservesIdentityAndResorts`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` contains `testDeleteRequiresConfirmationAndPersists`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeDurationManagementSessionModelTests'</automated>
  </verify>
  <done>The app now has one test-backed session owner for duration CRUD that future window and menu-entry work can plug into without reimplementing validation or persistence.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the duration manager session can load the current timed list, block invalid drafts before or through the shared store, preserve identity on edit, and require explicit confirmation before deletion.
</verification>

<success_criteria>
Phase 13 now has a truthful duration-management state machine: timed durations are editable user data, validation stays canonical, and CRUD outcomes persist through the shared store instead of view-local state.
</success_criteria>

<output>
After completion, create `.planning/phases/13-duration-management-surface/13-01-SUMMARY.md`
</output>
