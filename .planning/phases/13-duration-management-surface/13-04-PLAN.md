---
phase: 13-duration-management-surface
plan: 04
type: execute
wave: 4
depends_on:
  - "13-03-PLAN.md"
files_modified:
  - "Tools Cat/KeepAwakeDurationManagementView.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
gap_closure: true
requirements:
  - AWAKE-06
must_haves:
  truths:
    - "`常亮时长` 窗口中的定时时长区域必须一眼可辨识为列表，而不是和窗口背景融为一体。"
    - "列表视觉需要保持原生 macOS 风格：克制、清晰、有层次，但不能靠大面积花哨装饰掩盖信息。"
    - "现有 add/edit 小弹窗、删除确认、排序与根菜单实时同步行为都不能回退。"
  artifacts:
    - path: "Tools Cat/KeepAwakeDurationManagementView.swift"
      provides: "时长列表的可辨识容器样式和行级层次"
      contains: "populatedListContent"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "打开管理窗口的现有 UI 烟雾测试继续覆盖基础可达性"
      contains: "testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationManagementView.swift"
      to: ".planning/phases/13-duration-management-surface/13-UAT.md"
      via: "把用户指出的“列表和背景同色、看不出是列表”转成明确的 UI 可辨识修复"
      pattern: "populatedListContent|KeepAwakeDurationRow"
---

<objective>
Close the remaining Phase 13 cosmetic UAT gap by making the duration list read unmistakably as a list while preserving the compact native feel of the manager window.

Purpose: The feature now works, but the user still cannot visually recognize the timed-duration area as a list because it blends into the window background. This plan adds a native, visually distinct list surface without reopening any functional behavior.
Output: A duration manager whose timed rows sit inside a clearly separated list container with enough visual hierarchy that users immediately recognize it as a list.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-duration-management-surface/13-UAT.md
@.planning/phases/13-duration-management-surface/13-03-SUMMARY.md
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Give the timed-duration area a recognizable native list surface</name>
  <files>Tools Cat/KeepAwakeDurationManagementView.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-UAT.md
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: When the manager opens with seeded durations, the timed-duration area reads as a real list or grouped panel instead of bare text on the window background.
    - Test 2: Each row still presents title, minutes, edit, and delete affordances clearly, but the overall styling stays restrained and native.
    - Test 3: The existing add/edit sheet, delete confirmation, and root-menu sync behavior remain unchanged.
  </behavior>
  <action>Refine `KeepAwakeDurationManagementView.swift` so the populated duration area uses a visually distinct native container with clear separation from the window background. Use subtle macOS-appropriate treatments such as grouped background, inset padding, rounded container shape, border or shadow hierarchy, and better row spacing so the content unmistakably reads as a list. Do not introduce flashy styling, non-native ornament, or any layout regression to the current CRUD flow. Keep the existing UI smoke test green and extend it only if a small accessibility seam is needed to protect the new list surface.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementView.swift` gives the populated duration area a distinct visual container separate from the window background
    - `Tools Cat/KeepAwakeDurationManagementView.swift` preserves the current `添加时长` button placement and sheet-based add/edit flow
    - `Tools Cat/KeepAwakeDurationManagementView.swift` still renders all timed rows inside the same accessible list surface
    - The resulting UI feels like a native macOS utility list rather than plain text floating on the window background
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface'</automated>
    <manual>Re-run `$gsd-verify-work 13` and confirm the duration area now looks like a list at a glance.</manual>
  </verify>
  <done>The duration manager remains functionally identical, but the timed-duration area now reads immediately as a list and no longer disappears into the window background.</done>
</task>

</tasks>

<verification>
Treat this cosmetic gap closure as complete only when the user can distinguish the timed-duration area as a list without explanation, while all existing CRUD behavior remains intact.
</verification>

<success_criteria>
Phase 13 becomes visually acceptable as well as functionally correct: the duration manager keeps its native restraint, but the list surface is obvious and discoverable.
</success_criteria>

<output>
After completion, create `.planning/phases/13-duration-management-surface/13-04-SUMMARY.md`
</output>
