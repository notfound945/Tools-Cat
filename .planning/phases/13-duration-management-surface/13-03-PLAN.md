---
phase: 13-duration-management-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "13-01-PLAN.md"
  - "13-02-PLAN.md"
files_modified:
  - "Tools Cat/StatusBarController.swift"
  - "Tools Cat/KeepAwakeDurationManagementView.swift"
  - "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
  - "Tools Cat/KeepAwakeDurationManagementWindow.swift"
  - "Tools CatTests/StatusBarControllerEntryFlowTests.swift"
  - "Tools CatTests/StatusBarControllerMenuPolishTests.swift"
  - "Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift"
  - "Tools CatUITests/Tools_CatUITests.swift"
autonomous: true
gap_closure: true
requirements:
  - AWAKE-05
  - AWAKE-06
  - AWAKE-07
  - AWAKE-08
  - AWAKE-09
must_haves:
  truths:
    - "`管理常亮时长…` 是常亮分组的最后一项，固定出现在 keep-awake/WOL 分隔线之上，而不是落在 WOL 管理分组里。"
    - "添加和编辑都在时长列表上下文内通过同一个小弹窗完成，不再切换到独立表单页。"
    - "状态栏定时时长行直接来自 `KeepAwakeDurationStore` 的受管列表，并在添加、编辑、删除后立即按时长排序刷新；`无限常亮` 仍固定为第一项。"
  artifacts:
    - path: "Tools Cat/StatusBarController.swift"
      provides: "Keep-awake 菜单分组位置与受管时长动态渲染"
      contains: "manageKeepAwakeDurationsItem"
    - path: "Tools Cat/KeepAwakeDurationManagementView.swift"
      provides: "列表内复用的小弹窗添加/编辑交互"
      contains: ".sheet"
    - path: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      provides: "面向弹窗编辑器的共享草稿与保存状态"
      contains: "currentFormMode"
    - path: "Tools CatTests/StatusBarControllerMenuPolishTests.swift"
      provides: "菜单顺序与动态刷新回归保护"
      contains: "管理常亮时长…"
    - path: "Tools CatUITests/Tools_CatUITests.swift"
      provides: "时长管理弹窗与更新后菜单真值的 UI 烟雾测试"
      contains: "keep-awake-duration"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationStore.swift"
      to: "Tools Cat/StatusBarController.swift"
      via: "状态栏菜单直接监听受管时长仓库变化并重建定时时长行"
      pattern: "\\$durations|durationStore|rebuildKeepAwake"
    - from: "Tools Cat/KeepAwakeDurationManagementSessionModel.swift"
      to: "Tools Cat/KeepAwakeDurationManagementView.swift"
      via: "同一套 add/edit 草稿状态驱动列表内小弹窗"
      pattern: "currentFormMode|draftMinutesText|saveDraft"
    - from: "Tools Cat/StatusBarController.swift"
      to: "Tools CatTests/StatusBarControllerMenuPolishTests.swift"
      via: "菜单中管理入口位置和动态时长排序都有测试锁定"
      pattern: "管理常亮时长…|无限常亮"
---

<objective>
Close the Phase 13 UAT gaps by making the duration manager feel native-in-place and by keeping the root keep-awake menu truthful after CRUD changes.

Purpose: UAT confirmed that the shipped Phase 13 surface is functionally close but not product-acceptable. The management entry is grouped incorrectly, add/edit feel too heavy because they replace the whole window content, and deleting a duration leaves the root menu stale. This gap-closure plan fixes those concrete issues in one pass, even though the live menu refresh pulls the minimal required slice of Phase 14 forward.
Output: A corrected keep-awake menu structure, one shared add/edit mini-modal inside the manager list, and immediate root-menu synchronization from the managed duration store.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-duration-management-surface/13-UAT.md
@.planning/phases/13-duration-management-surface/13-01-PLAN.md
@.planning/phases/13-duration-management-surface/13-02-PLAN.md
@.planning/phases/13-duration-management-surface/13-01-SUMMARY.md
@.planning/phases/13-duration-management-surface/13-02-SUMMARY.md
@Tools Cat/KeepAwakeDurationStore.swift
@Tools Cat/StatusBarController.swift
@Tools Cat/AppDelegate.swift
@Tools Cat/KeepAwakeDurationManagementSessionModel.swift
@Tools Cat/KeepAwakeDurationManagementView.swift
@Tools Cat/KeepAwakeDurationManagementWindow.swift
@Tools CatTests/StatusBarControllerEntryFlowTests.swift
@Tools CatTests/StatusBarControllerMenuPolishTests.swift
@Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift
@Tools CatUITests/Tools_CatUITests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the management entry into the keep-awake section and rebuild timed rows from the managed duration store</name>
  <files>Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerEntryFlowTests.swift, Tools CatTests/StatusBarControllerMenuPolishTests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-UAT.md
    - Tools Cat/StatusBarController.swift
    - Tools Cat/KeepAwakeDurationStore.swift
    - Tools CatTests/StatusBarControllerEntryFlowTests.swift
    - Tools CatTests/StatusBarControllerMenuPolishTests.swift
  </read_first>
  <behavior>
    - Test 1: `管理常亮时长…` stays in the keep-awake section as the last keep-awake row immediately above the separator, never adjacent to `管理 WOL 设备…`.
    - Test 2: The timed keep-awake rows after `无限常亮` come from the managed duration store in ascending order and preserve active-state truth for the currently selected timed duration.
    - Test 3: After add/edit/delete mutations in the shared store, the root menu updates without relaunch and without reintroducing deleted durations.
  </behavior>
  <action>Refactor `StatusBarController.swift` so only `无限常亮` stays as a fixed first action while all timed keep-awake rows are generated from `KeepAwakeDurationStore.durations`. Anchor `管理常亮时长…` as the final keep-awake row above the existing keep-awake/WOL separator instead of placing it in the WOL management group. Subscribe the controller to duration-store changes and rebuild or refresh the keep-awake section whenever durations change, preserving current active-state rendering, pending disablement, stop-row truth, and status messaging. Update controller tests so they lock the corrected section order, the new dynamic timed rows, and at least one add/delete-style store mutation refresh path.</action>
  <acceptance_criteria>
    - `Tools Cat/StatusBarController.swift` contains `keepAwakeDurationStore`
    - `Tools Cat/StatusBarController.swift` contains a duration-store subscription such as `keepAwakeDurationStore.$durations`
    - `Tools Cat/StatusBarController.swift` contains `manageKeepAwakeDurationsItem`
    - `Tools Cat/StatusBarController.swift` no longer hardcodes only `15 分钟`, `30 分钟`, `1 小时`, and `2 小时` as the complete timed menu model
    - `Tools CatTests/StatusBarControllerMenuPolishTests.swift` proves `管理常亮时长…` is above the keep-awake/WOL separator and not grouped with `管理 WOL 设备…`
    - `Tools CatTests/StatusBarControllerMenuPolishTests.swift` proves a store duration change is reflected in root-menu ordering without recreating the controller
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/StatusBarControllerEntryFlowTests' -only-testing:'Tools CatTests/StatusBarControllerMenuPolishTests'</automated>
  </verify>
  <done>The status menu now tells the truth after duration CRUD changes: the management entry is placed with the keep-awake actions, and the timed rows are live managed data rather than stale fixed placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the full-page add/edit flow with one shared list-local mini modal</name>
  <files>Tools Cat/KeepAwakeDurationManagementView.swift, Tools Cat/KeepAwakeDurationManagementSessionModel.swift, Tools Cat/KeepAwakeDurationManagementWindow.swift, Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift, Tools CatUITests/Tools_CatUITests.swift</files>
  <read_first>
    - .planning/phases/13-duration-management-surface/13-UAT.md
    - Tools Cat/KeepAwakeDurationManagementView.swift
    - Tools Cat/KeepAwakeDurationManagementSessionModel.swift
    - Tools Cat/KeepAwakeDurationManagementWindow.swift
    - Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift
    - Tools CatUITests/Tools_CatUITests.swift
  </read_first>
  <behavior>
    - Test 1: Clicking `添加时长` opens a compact modal attached to the management list instead of replacing the whole window content.
    - Test 2: Clicking `编辑` on any row opens the same modal prefilled with that row’s minutes value.
    - Test 3: Saving from the modal preserves duplicate validation, sorted insertion/resorting, and closes back to the list context.
  </behavior>
  <action>Replace the current list-versus-form page switch with a single reusable add/edit mini modal presented from the list surface, keeping the list visible behind it. Reuse the same minutes field, validation copy, and save/cancel semantics for both add and edit modes, but model the UI so users never navigate away to a dedicated form screen. Keep delete confirmation as its own explicit destructive confirmation. Update the session model only as needed to support modal presentation state and shared draft lifecycle, and extend the UI smoke so it proves the modal affordance appears from the live manager surface.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationManagementView.swift` contains a sheet/popover presentation such as `.sheet`
    - `Tools Cat/KeepAwakeDurationManagementView.swift` does not rely on the current top-level `switch session.screen` full-page form replacement
    - `Tools Cat/KeepAwakeDurationManagementSessionModel.swift` still exposes one shared save path through `saveDraft()`
    - `Tools CatTests/KeepAwakeDurationManagementSessionModelTests.swift` still covers duplicate rejection, sorted insert/resort, and identity preservation
    - `Tools CatUITests/Tools_CatUITests.swift` contains one smoke that proves add or edit opens the compact modal from the live manager window
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeDurationManagementSessionModelTests' -only-testing:'Tools CatUITests/Tools_CatUITests/testLaunchWithSeededKeepAwakeDurationsShowsManagementSurface'</automated>
  </verify>
  <done>The duration manager now behaves like a compact native editor: add and edit happen in one small modal inside the list context instead of jarring full-page navigation.</done>
</task>

</tasks>

<verification>
Treat this gap-closure plan as complete only when the status menu immediately reflects managed-duration CRUD truth, `管理常亮时长…` is grouped with keep-awake actions, and add/edit both use the same compact modal interaction inside the manager window.
</verification>

<success_criteria>
Phase 13 becomes product-acceptable: users find duration management where they expect it, make changes without leaving the list context, and see the root keep-awake menu stay in sync with those changes in real time.
</success_criteria>

<output>
After completion, create `.planning/phases/13-duration-management-surface/13-03-SUMMARY.md`
</output>
