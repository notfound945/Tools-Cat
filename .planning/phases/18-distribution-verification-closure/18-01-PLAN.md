---
phase: 18-distribution-verification-closure
plan: 01
type: execute
wave: 1
depends_on:
  - "17-02-PLAN.md"
files_modified:
  - "README.md"
  - "docs/release/signing-readiness.md"
  - "scripts/release/verify-release-docs.sh"
  - "scripts/release/verify-friend-share-artifact.sh"
  - "scripts/release/verify-distribution-closure.sh"
autonomous: true
requirements:
  - DIST-06
  - DIST-07
must_haves:
  truths:
    - "The repo provides one repeatable verification command for the shipped non-notarized friend-share artifact, and that command checks the real `dist/Tools-Cat.dmg` layout rather than only static text."
    - "The release docs tell maintainers and friends the exact first-launch path truthfully: drag to `/Applications`, use “右键打开”, and only remove quarantine if Gatekeeper still blocks launch."
    - "Phase 18 re-runs a focused WOL and keep-awake regression slice so release hardening stays release-only in practice, not just in intent."
    - "The docs still state the real manual boundary: automated verification does not prove a fresh-machine or real friend-side Gatekeeper experience."
  artifacts:
    - path: "scripts/release/verify-friend-share-artifact.sh"
      provides: "Mount-and-inspect validation for the shipped `dist/Tools-Cat.dmg`"
      contains: "Tools Cat.app"
    - path: "scripts/release/verify-distribution-closure.sh"
      provides: "Single maintainer-facing verification entrypoint for Phase 18"
      contains: "verify-friend-share-artifact.sh"
    - path: "docs/release/signing-readiness.md"
      provides: "Canonical friend-share verification checklist and manual-open boundary"
      contains: "verify-distribution-closure.sh"
  key_links:
    - from: "scripts/release/verify-distribution-closure.sh"
      to: "scripts/run_menu_bar_verification_slice.sh"
      via: "Phase 18 should reuse the trusted menu-bar verification slice instead of inventing a new controller/UI harness."
      pattern: "run_menu_bar_verification_slice\\.sh"
    - from: "scripts/release/verify-distribution-closure.sh"
      to: "Tools CatTests/WOLSessionModelTests.swift"
      via: "Focused regression proof must include WOL runtime truth."
      pattern: "WOLSessionModelTests"
    - from: "scripts/release/verify-distribution-closure.sh"
      to: "Tools CatTests/KeepAwakeSessionModelTests.swift"
      via: "Focused regression proof must include keep-awake runtime truth."
      pattern: "KeepAwakeSessionModelTests"
    - from: "scripts/release/verify-release-docs.sh"
      to: "docs/release/signing-readiness.md"
      via: "The docs gate must reject drift away from the manual-open and verification contract."
      pattern: "verify-distribution-closure\\.sh|右键打开|xattr -dr com\\.apple\\.quarantine"
---

<objective>
Close the v1.6 milestone verification story around the current friend-share DMG by adding a real artifact/layout check, a single verification entrypoint, and updated release docs that match the manual-open truth and focused regression boundary.

Purpose: The 2026-04-17 pivot replaced notarization with non-notarized friend sharing, but the milestone is not closed until the repo can verify the actual DMG layout, rerun a focused WOL/keep-awake regression slice, and document exactly what still remains manual.
Output: One repeatable Phase 18 verification command, DMG artifact inspection, release docs aligned to the friend-share/manual-open truth, and focused regression evidence for WOL plus keep-awake behavior.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-distribution-verification-closure/18-CONTEXT.md
@.planning/phases/16-release-signing-readiness/16-VERIFICATION.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-VERIFICATION.md
@README.md
@docs/release/signing-readiness.md
@release.sh
@build_dmg.sh
@scripts/release/verify-release-readiness.sh
@scripts/release/verify-release-docs.sh
@scripts/run_menu_bar_verification_slice.sh
@Tools CatTests/WOLSessionModelTests.swift
@Tools CatTests/KeepAwakeSessionModelTests.swift
@Tools CatTests/KeepAwakeMenuStateTests.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the friend-share artifact inspection and one-shot verification entrypoint</name>
  <files>scripts/release/verify-friend-share-artifact.sh, scripts/release/verify-distribution-closure.sh</files>
  <read_first>
    - .planning/phases/18-distribution-verification-closure/18-CONTEXT.md
    - release.sh
    - build_dmg.sh
    - scripts/release/verify-release-readiness.sh
    - scripts/run_menu_bar_verification_slice.sh
    - Tools CatTests/WOLSessionModelTests.swift
    - Tools CatTests/KeepAwakeSessionModelTests.swift
    - Tools CatTests/KeepAwakeMenuStateTests.swift
  </read_first>
  <behavior>
    - Test 1: The phase-owned verification command fails if the expected Release app or `dist/Tools-Cat.dmg` is missing.
    - Test 2: The artifact helper mounts the DMG, proves `Tools Cat.app` exists inside it, and proves the `/Applications` install shortcut exists.
    - Test 3: The verification command reruns focused WOL and keep-awake regressions using existing trusted test seams.
    - Test 4: The verification command ends with a clear note about the remaining manual/fresh-machine boundary.
  </behavior>
  <action>Create `scripts/release/verify-friend-share-artifact.sh` with `set -euo pipefail`, checks for `build/DerivedData/Build/Products/Release/Tools Cat.app` plus `dist/Tools-Cat.dmg`, a temporary mountpoint, `hdiutil attach -nobrowse -readonly`, and validations that the mounted DMG contains `Tools Cat.app` and an `Applications` symlink targeting `/Applications`. Detach cleanly on exit. Then create `scripts/release/verify-distribution-closure.sh` as the one maintainer-facing Phase 18 verification command: run `verify-release-readiness.sh`, `verify-release-docs.sh`, and `verify-friend-share-artifact.sh`; rerun focused WOL/keep-awake tests using `WOLSessionModelTests`, `KeepAwakeSessionModelTests`, and `KeepAwakeMenuStateTests`; reuse `scripts/run_menu_bar_verification_slice.sh`; and end with a note that fresh-machine or real friend-side Gatekeeper validation remains manual. Do not rebuild the app in these scripts and do not reintroduce signing/notarization assumptions.</action>
  <acceptance_criteria>
    - `scripts/release/verify-friend-share-artifact.sh` contains `Tools Cat.app`
    - `scripts/release/verify-friend-share-artifact.sh` contains `Applications`
    - `scripts/release/verify-friend-share-artifact.sh` contains `hdiutil attach`
    - `scripts/release/verify-distribution-closure.sh` contains `verify-release-readiness.sh`
    - `scripts/release/verify-distribution-closure.sh` contains `verify-release-docs.sh`
    - `scripts/release/verify-distribution-closure.sh` contains `verify-friend-share-artifact.sh`
    - `scripts/release/verify-distribution-closure.sh` contains `WOLSessionModelTests`
    - `scripts/release/verify-distribution-closure.sh` contains `KeepAwakeSessionModelTests`
    - `scripts/release/verify-distribution-closure.sh` contains `run_menu_bar_verification_slice.sh`
  </acceptance_criteria>
  <verify>
    <automated>sh ./release.sh && bash scripts/release/verify-distribution-closure.sh</automated>
  </verify>
  <done>The repo has one repeatable post-release verification command that checks the real friend-share DMG and reruns focused WOL plus keep-awake regressions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update the release docs and docs gate to match the Phase 18 verification and manual-open contract</name>
  <files>README.md, docs/release/signing-readiness.md, scripts/release/verify-release-docs.sh</files>
  <read_first>
    - .planning/phases/18-distribution-verification-closure/18-CONTEXT.md
    - README.md
    - docs/release/signing-readiness.md
    - scripts/release/verify-release-docs.sh
    - scripts/release/verify-distribution-closure.sh
    - scripts/release/verify-friend-share-artifact.sh
  </read_first>
  <behavior>
    - Test 1: `README.md` names the post-release verification command and keeps the manual-open truth short and accurate.
    - Test 2: The dedicated release doc gives the exact repeatable verification command plus the exact friend-side first-launch fallback steps.
    - Test 3: The docs explicitly say automated verification does not equal fresh-machine or Gatekeeper-proof install validation.
    - Test 4: The docs gate rejects drift away from the new verification command and the manual-open wording.
  </behavior>
  <action>Update `README.md` so the Release section still points at `sh ./release.sh`, but now also names `bash scripts/release/verify-distribution-closure.sh` as the post-release verification command and keeps the manual-open boundary concise. Update `docs/release/signing-readiness.md` with a dedicated verification section describing what `verify-distribution-closure.sh` checks, including the mounted-DMG inspection and the focused WOL/keep-awake regressions, plus a step-by-step friend-side first-launch checklist that says drag to `/Applications`, then use “右键打开”, then fall back to `xattr -dr com.apple.quarantine "/Applications/Tools Cat.app"` only if needed. Keep an explicit note that fresh-machine or real friend-side Gatekeeper proof remains manual. Update `scripts/release/verify-release-docs.sh` so it requires the new verification command and manual-open guidance in both docs surfaces while still rejecting stale notarization guidance.</action>
  <acceptance_criteria>
    - `README.md` contains `verify-distribution-closure.sh`
    - `README.md` contains `右键打开`
    - `README.md` contains `xattr -dr com.apple.quarantine`
    - `docs/release/signing-readiness.md` contains `verify-distribution-closure.sh`
    - `docs/release/signing-readiness.md` contains `verify-friend-share-artifact.sh`
    - `docs/release/signing-readiness.md` contains `WOLSessionModelTests`
    - `docs/release/signing-readiness.md` contains `KeepAwakeSessionModelTests`
    - `docs/release/signing-readiness.md` contains `Fresh-machine`
    - `scripts/release/verify-release-docs.sh` contains `verify-distribution-closure.sh`
    - `scripts/release/verify-release-docs.sh` contains `verify-friend-share-artifact.sh`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release/verify-release-docs.sh</automated>
  </verify>
  <done>The repo docs and docs gate now agree on the current friend-share verification contract and the remaining manual-open boundary.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the repo can verify the real friend-share DMG layout, rerun focused WOL and keep-awake regressions, and document exactly what still remains manual about first launch.
</verification>

<success_criteria>
Phase 18 closes the distribution milestone truthfully: maintainers can build the DMG, verify the shipped artifact and focused runtime behavior repeatably, and hand friends a manual-open instruction set that matches the real non-notarized distribution contract.
</success_criteria>

<output>
After completion, create `.planning/phases/18-distribution-verification-closure/18-01-SUMMARY.md`
</output>
