---
phase: 03-saved-device-wake-flows
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - "Mac OS Swiss Knife/AppDelegate.swift"
  - "Mac OS Swiss Knife/StatusBarController.swift"
  - "Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift"
autonomous: true
requirements:
  - WOL-01
  - WOL-04
  - RELY-04
  - UX-03
must_haves:
  truths:
    - "The status menu exposes a short recent-devices section plus one `所有设备` path so every saved device remains wakeable without bloating the root menu."
    - "Menu-triggered wake actions route through the retained shared WOL session and disable while any send is already in flight."
    - "A disabled status row in the menu shows the current send in progress or the most recent truthful local success/failure afterward."
  artifacts:
    - path: "Mac OS Swiss Knife/StatusBarController.swift"
      provides: "Compact saved-device wake menu sections and persistent last-result menu row"
      contains: "所有设备"
    - path: "Mac OS Swiss Knife/AppDelegate.swift"
      provides: "Status controller wired to the shared device library and WOL session"
      contains: "StatusBarController("
    - path: "Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift"
      provides: "Regression coverage for recent rows, disabled wake actions, and persistent status-row behavior"
      contains: "final class StatusBarControllerWakeMenuTests"
  key_links:
    - from: "Mac OS Swiss Knife/AppDelegate.swift"
      to: "Mac OS Swiss Knife/StatusBarController.swift"
      via: "shared SavedDeviceLibraryStore and WOLSessionModel injection"
      pattern: "StatusBarController\\("
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/WOLSessionModel.swift"
      via: "menu wake actions dispatch through sendSavedDevice(id:) and shared send state"
      pattern: "sendSavedDevice\\(id:"
    - from: "Mac OS Swiss Knife/StatusBarController.swift"
      to: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      via: "recentDevices(limit:) and all-device submenu rendering"
      pattern: "recentDevices\\(limit: 3\\)"
---

<objective>
Wire Phase 3's compact saved-device wake flow into the native menu bar surface.

Purpose: Turn the saved-device library into a fast daily wake path without reopening device management or adding duplicate send paths.
Output: A recent-devices root section, a full-library path, and a persistent wake-status row in the existing status menu, all driven by the shared store/session seam.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
@.planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
@.planning/phases/03-saved-device-wake-flows/03-VALIDATION.md
@.planning/phases/03-saved-device-wake-flows/03-01-PLAN.md
@Mac OS Swiss Knife/AppDelegate.swift
@Mac OS Swiss Knife/StatusBarController.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife/WakeSendPresentation.swift

<interfaces>
Shared store/session contracts expected from `03-01`:
```swift
@MainActor
final class SavedDeviceLibraryStore: ObservableObject {
    @Published private(set) var devices: [SavedDevice]
    @Published private(set) var recentDeviceIDs: [UUID]
    @Published private(set) var lastUsedDeviceID: UUID?
    func recentDevices(limit: Int) -> [SavedDevice]
}

final class WOLSessionModel: ObservableObject {
    @Published private(set) var sendState: WakeSendState
    @Published private(set) var lastCompletedWake: CompletedWakeAttempt?
    func sendSavedDevice(id: UUID)
    var isSending: Bool { get }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact recent-device wake actions plus the full-library path to the status menu</name>
  <files>Mac OS Swiss Knife/AppDelegate.swift, Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift</files>
  <read_first>
    - .planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
    - .planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
    - Mac OS Swiss Knife/AppDelegate.swift
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
  </read_first>
  <behavior>
    - Test 1: The root menu shows up to three recent devices inline and keeps the full library under `所有设备`.
    - Test 2: Every saved device is reachable through the menu even when it is not in recents.
    - Test 3: Triggering a menu row uses the shared `sendSavedDevice(id:)` path instead of a second sender.
    - Test 4: All saved-device wake actions are disabled while `wolSession.isSending` is true.
  </behavior>
  <action>Update `StatusBarController.swift` so it accepts the shared `SavedDeviceLibraryStore` and `WOLSessionModel` in its initializer, with `AppDelegate.swift` passing the already-retained `savedDeviceLibrary` and `wolSession` instances into the controller during launch. Rebuild the menu structure so the keep-awake items remain first, then insert the saved-device wake section before `发送 WOL …`. In that section, render up to three recent devices from `deviceLibrary.recentDevices(limit: 3)` as direct root-menu items using the exact item title `device.name`. Add a submenu item with the exact title `所有设备` whose submenu contains every saved device in canonical `deviceLibrary.devices` order, also using `device.name` titles. Route both recent rows and submenu rows through `wolSession.sendSavedDevice(id: device.id)`; do not introduce a second sender or callback-only stub. While `wolSession.isSending` is true, disable every recent row and every `所有设备` submenu row. Add `StatusBarControllerWakeMenuTests.swift` with the exact test functions `testRootMenuShowsOnlyThreeRecentDevices()`, `testAllSavedDevicesRemainReachableUnderAllDevicesSubmenu()`, `testMenuWakeRowsDispatchThroughSharedSession()`, and `testWakeMenuActionsDisableWhileSendIsInFlight()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `recentDevices(limit: 3)`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `所有设备`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `sendSavedDevice(id:`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `isSending`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `StatusBarController(`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `savedDeviceLibrary`
    - `Mac OS Swiss Knife/AppDelegate.swift` contains `wolSession`
    - `Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift` contains `testRootMenuShowsOnlyThreeRecentDevices`
    - `Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeMenuActionsDisableWhileSendIsInFlight`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests'</automated>
  </verify>
  <done>The status menu becomes a real saved-device wake surface: recents are fast, every saved device stays reachable, and all wake actions still share one send path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the persistent wake-status row to the status menu</name>
  <files>Mac OS Swiss Knife/StatusBarController.swift, Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift</files>
  <read_first>
    - .planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
    - Mac OS Swiss Knife/StatusBarController.swift
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife/WakeSendPresentation.swift
    - Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift
  </read_first>
  <behavior>
    - Test 1: The menu shows `正在发送唤醒包...` while a wake is in flight.
    - Test 2: After a successful send, the status row preserves the exact last success message until another attempt replaces it.
    - Test 3: After a failed send, the status row preserves the exact failure message without implying the remote device woke.
  </behavior>
  <action>Add a disabled menu item named `wakeStatusItem` immediately below the saved-device wake actions section. Render it from shared session state using this exact rule set: when `wolSession.sendState` is `.sending`, set the title to `WakeSendMessage.sending.text`; when `wolSession.sendState` is `.idle` and `wolSession.lastCompletedWake` is non-nil, set the title to `lastCompletedWake.message`; when there is no in-flight or completed state, hide the item. Keep the row disabled at all times. Do not derive success or failure by string matching; use `wolSession.sendState` and `wolSession.lastCompletedWake` directly. Expand `StatusBarControllerWakeMenuTests.swift` with the exact test functions `testWakeStatusRowShowsSendingState()`, `testWakeStatusRowPersistsLastSuccessMessage()`, and `testWakeStatusRowPersistsLastFailureMessage()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `wakeStatusItem`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `WakeSendMessage.sending.text`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `lastCompletedWake?.message`
    - `Mac OS Swiss Knife/StatusBarController.swift` contains `wakeStatusItem.isEnabled = false`
    - `Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeStatusRowShowsSendingState`
    - `Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeStatusRowPersistsLastSuccessMessage`
    - `Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests.swift` contains `testWakeStatusRowPersistsLastFailureMessage`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/StatusBarControllerWakeMenuTests'</automated>
  </verify>
  <done>The status menu now exposes the current send state or last truthful result without requiring the user to reopen the WOL window.</done>
</task>

</tasks>

<verification>
Run the targeted status-controller tests after each task. Treat the plan as complete only when the recent-device section stays short, every saved device remains reachable through `所有设备`, wake actions disable during sends, and the persistent status row reflects sending/success/failure state correctly.
</verification>

<success_criteria>
The status menu becomes the fast daily wake path for saved devices while still staying compact, truthful, and resistant to duplicate sends.
</success_criteria>

<output>
After completion, create `.planning/phases/03-saved-device-wake-flows/03-02-SUMMARY.md`
</output>
