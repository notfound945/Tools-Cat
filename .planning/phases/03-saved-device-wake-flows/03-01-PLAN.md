---
phase: 03-saved-device-wake-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Mac OS Swiss Knife/SavedDeviceRepository.swift"
  - "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
  - "Mac OS Swiss Knife/WOLSessionModel.swift"
  - "Mac OS Swiss Knife/WakeSendPresentation.swift"
  - "Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests.swift"
  - "Mac OS Swiss KnifeTests/WOLSessionModelTests.swift"
autonomous: true
requirements:
  - WOL-03
  - WOL-04
  - RELY-04
  - UX-03
must_haves:
  truths:
    - "Recent-device ordering and last-used saved-device memory are persisted through the shared repository/store seam and update only after locally successful saved-device wake sends."
    - "The shared WOL session exposes one in-flight send state and one last-completed wake snapshot that both the menu and WOL window can observe."
    - "Deleting or replacing devices prunes stale remembered IDs so Phase 3 never points at a missing saved device."
  artifacts:
    - path: "Mac OS Swiss Knife/SavedDeviceRepository.swift"
      provides: "Repository contract and defaults-backed persistence for recent-device and last-used metadata"
      contains: "SavedDeviceWakeMetadata"
    - path: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      provides: "Shared store helpers for recent devices and last-used saved-device identity"
      contains: "func markWakeSucceeded"
    - path: "Mac OS Swiss Knife/WOLSessionModel.swift"
      provides: "Single shared wake-send contract with persistent last-completed wake snapshot"
      contains: "lastCompletedWake"
    - path: "Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests.swift"
      provides: "Regression coverage for metadata persistence and pruning"
      contains: "final class SavedDeviceLibraryStoreTests"
  key_links:
    - from: "Mac OS Swiss Knife/WOLSessionModel.swift"
      to: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      via: "successful saved-device sends record recent/last-used metadata"
      pattern: "markWakeSucceeded\\("
    - from: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      to: "Mac OS Swiss Knife/SavedDeviceRepository.swift"
      via: "wake metadata load/save stays behind the repository seam"
      pattern: "loadWakeMetadata|saveWakeMetadata"
    - from: "Mac OS Swiss Knife/WOLSessionModel.swift"
      to: "Mac OS Swiss Knife/WakeSendPresentation.swift"
      via: "truthful local-send copy reused in the persistent last-result snapshot"
      pattern: "WakeSendPresentation"
---

<objective>
Add the shared persistence and session contracts that Phase 3 needs before any menu wake UI is wired.

Purpose: Make recents, last-used selection, duplicate-send gating, and last-result visibility explicit shared state instead of view-local behavior.
Output: Repository/store/session support for recent-device memory, last-used saved-device memory, and a persistent last-completed wake snapshot with unit coverage.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
@.planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
@.planning/phases/03-saved-device-wake-flows/03-VALIDATION.md
@Mac OS Swiss Knife/SavedDeviceRepository.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife/WakeSendPresentation.swift
@Mac OS Swiss KnifeTests/WOLSessionModelTests.swift

<interfaces>
Existing persistence seam:
```swift
protocol SavedDeviceRepository: AnyObject {
    func loadDevices() throws -> [SavedDevice]
    func saveDevices(_ devices: [SavedDevice]) throws
}
```

Existing shared-session seam:
```swift
final class WOLSessionModel: ObservableObject {
    @Published var selectedSavedDeviceID: UUID?
    @Published var sendState: WakeSendState
    func sendCurrentSelection()
}
```

Required new shared metadata contract:
```swift
struct SavedDeviceWakeMetadata: Codable, Equatable {
    var recentDeviceIDs: [UUID]
    var lastUsedDeviceID: UUID?
}

struct CompletedWakeAttempt: Equatable {
    let deviceID: UUID?
    let message: String
    let wasSuccessful: Bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the saved-device repository and shared store with wake metadata</name>
  <files>Mac OS Swiss Knife/SavedDeviceRepository.swift, Mac OS Swiss Knife/SavedDeviceLibraryStore.swift, Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests.swift</files>
  <read_first>
    - .planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
    - .planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
    - Mac OS Swiss Knife/SavedDeviceRepository.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
  </read_first>
  <behavior>
    - Test 1: Successful wakes move the device ID to the front of recents and trim the list to three entries.
    - Test 2: `lastUsedDeviceID` changes only when a saved-device wake succeeds.
    - Test 3: Deleting or replacing devices prunes missing IDs from recents and clears an orphaned `lastUsedDeviceID`.
  </behavior>
  <action>Add a concrete `SavedDeviceWakeMetadata` type to `SavedDeviceRepository.swift` with the exact fields `recentDeviceIDs: [UUID]` and `lastUsedDeviceID: UUID?`. Extend `SavedDeviceRepository` with `loadWakeMetadata() throws -> SavedDeviceWakeMetadata` and `saveWakeMetadata(_ metadata: SavedDeviceWakeMetadata) throws`. In `UserDefaultsSavedDeviceRepository`, persist this metadata under the exact key `saved_device_wake_metadata`; when the key is absent, return `SavedDeviceWakeMetadata(recentDeviceIDs: [], lastUsedDeviceID: nil)`. In `SavedDeviceLibraryStore.swift`, add `@Published private(set) var recentDeviceIDs: [UUID]` and `@Published private(set) var lastUsedDeviceID: UUID?`, load them during init and reload, add `func recentDevices(limit: Int = 3) -> [SavedDevice]`, and add `func markWakeSucceeded(deviceID: UUID) throws` that removes duplicate IDs, inserts the just-used ID at index 0, trims to `prefix(3)`, sets `lastUsedDeviceID = deviceID`, and saves through the repository. Update `replaceAll(_:)` and `deleteDevice(id:)` so they normalize device order first, then prune metadata to IDs still present in `devices`, clearing `lastUsedDeviceID` when the remembered device no longer exists. Add `SavedDeviceLibraryStoreTests.swift` with exact test functions `testMarkWakeSucceededMovesDeviceToFrontAndTrimsRecents()`, `testMarkWakeSucceededSetsLastUsedDeviceID()`, and `testDeletingDevicePrunesRecentAndLastUsedMetadata()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `struct SavedDeviceWakeMetadata`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `func loadWakeMetadata()`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `func saveWakeMetadata(_ metadata: SavedDeviceWakeMetadata)`
    - `Mac OS Swiss Knife/SavedDeviceRepository.swift` contains `saved_device_wake_metadata`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `@Published private(set) var recentDeviceIDs: [UUID]`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `@Published private(set) var lastUsedDeviceID: UUID?`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `func recentDevices(limit: Int = 3) -> [SavedDevice]`
    - `Mac OS Swiss Knife/SavedDeviceLibraryStore.swift` contains `func markWakeSucceeded(deviceID: UUID) throws`
    - `Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests.swift` contains `testMarkWakeSucceededMovesDeviceToFrontAndTrimsRecents`
    - `Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests.swift` contains `testDeletingDevicePrunesRecentAndLastUsedMetadata`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/SavedDeviceLibraryStoreTests'</automated>
  </verify>
  <done>The shared saved-device store now owns recent-device and last-used metadata through the repository seam, and the metadata stays valid when devices change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shared WOL session with saved-device send helpers and persistent last-result state</name>
  <files>Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/WakeSendPresentation.swift, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
    - .planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife/WakeSendPresentation.swift
    - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
  </read_first>
  <behavior>
    - Test 1: `sendSavedDevice(id:)` uses the selected saved device's MAC and respects `guard !isSending`.
    - Test 2: Successful saved-device sends update `lastCompletedWake` and call `markWakeSucceeded(deviceID:)`.
    - Test 3: Failed sends still update `lastCompletedWake` but do not update recent-device metadata.
    - Test 4: Starting a new send leaves the previous completed snapshot intact until the new attempt completes, so the menu can keep showing the last result while a send is in flight.
  </behavior>
  <action>Add a concrete `CompletedWakeAttempt` type to `WOLSessionModel.swift` with the exact fields `deviceID: UUID?`, `message: String`, and `wasSuccessful: Bool`. Add `@Published private(set) var lastCompletedWake: CompletedWakeAttempt?` and a new public method `sendSavedDevice(id: UUID)`. Refactor `sendCurrentSelection()` and `sendSavedDevice(id:)` so both funnel through one internal helper with the explicit parameters `targetMACAddress: String` and `savedDeviceID: UUID?`. Preserve the existing `guard !isSending` duplicate-send gate. During `.sending`, do not clear `lastCompletedWake`; the previous result must remain available for menu rendering until the attempt completes. On a successful saved-device send, build the message with `WakeSendPresentation.successMessage(for: targetMACAddress)`, set `lastCompletedWake = CompletedWakeAttempt(deviceID: savedDeviceID, message: successMessage, wasSuccessful: true)`, and call `try? deviceLibrary.markWakeSucceeded(deviceID: savedDeviceID)` on the main actor only when `savedDeviceID` is non-nil. On failure, set `lastCompletedWake = CompletedWakeAttempt(deviceID: savedDeviceID, message: error.userMessage, wasSuccessful: false)` and do not touch the store metadata. Add `WOLSessionModelTests` coverage with the exact test functions `testSendSavedDeviceUsesSelectedSavedDeviceMAC()`, `testSavedDeviceSuccessUpdatesLastCompletedWakeAndStoreMetadata()`, `testFailedSendKeepsLastCompletedWakeButDoesNotUpdateRecents()`, and `testStartingNewSendDoesNotClearLastCompletedWakeUntilCompletion()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `struct CompletedWakeAttempt`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `@Published private(set) var lastCompletedWake: CompletedWakeAttempt?`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `func sendSavedDevice(id: UUID)`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `targetMACAddress: String`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `markWakeSucceeded(deviceID:`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testSendSavedDeviceUsesSelectedSavedDeviceMAC`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testSavedDeviceSuccessUpdatesLastCompletedWakeAndStoreMetadata`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testFailedSendKeepsLastCompletedWakeButDoesNotUpdateRecents`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testStartingNewSendDoesNotClearLastCompletedWakeUntilCompletion`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/WOLSessionModelTests'</automated>
  </verify>
  <done>The app now has one shared session contract that the menu and WOL window can both trust for in-flight state and the most recent wake result.</done>
</task>

</tasks>

<verification>
Run the new store tests and the expanded WOL session tests. Treat this plan as complete only when saved-device wake metadata persists correctly and the shared session exposes a stable last-result snapshot without weakening the cross-surface duplicate-send guard.
</verification>

<success_criteria>
Recent-device memory, last-used saved-device memory, and persistent last-result state exist as shared app contracts instead of scattered menu or view behavior, and those contracts are covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-saved-device-wake-flows/03-01-SUMMARY.md`
</output>
