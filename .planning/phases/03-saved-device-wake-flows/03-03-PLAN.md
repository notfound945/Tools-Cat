---
phase: 03-saved-device-wake-flows
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - "Mac OS Swiss Knife/WOLSessionModel.swift"
  - "Mac OS Swiss Knife/WOLView.swift"
  - "Mac OS Swiss Knife/WOLWindow.swift"
  - "Mac OS Swiss KnifeTests/WOLSessionModelTests.swift"
autonomous: true
requirements:
  - WOL-03
  - UX-03
must_haves:
  truths:
    - "Reopening the WOL window preselects the most recently used saved device when it is safe to do so."
    - "An unfinished manual MAC draft still survives close/reopen and is not overridden by last-used preset logic."
    - "If the remembered saved device no longer exists, the window falls back to a valid saved-device choice rather than a stale ID."
  artifacts:
    - path: "Mac OS Swiss Knife/WOLSessionModel.swift"
      provides: "Reopen logic that applies last-used preset defaults only when manual draft ownership allows it"
      contains: "handleWindowWillShow"
    - path: "Mac OS Swiss Knife/WOLView.swift"
      provides: "Window picker bound to the reopened selectedSavedDeviceID contract"
      contains: "Picker(\"选择设备\""
    - path: "Mac OS Swiss KnifeTests/WOLSessionModelTests.swift"
      provides: "Regression coverage for last-used preselection, deleted-last-used fallback, and manual-draft preservation"
      contains: "testWindowReopenPreselectsLastUsedSavedDevice"
  key_links:
    - from: "Mac OS Swiss Knife/WOLSessionModel.swift"
      to: "Mac OS Swiss Knife/SavedDeviceLibraryStore.swift"
      via: "lastUsedDeviceID drives safe preset re-selection on window show"
      pattern: "lastUsedDeviceID"
    - from: "Mac OS Swiss Knife/WOLSessionModel.swift"
      to: "Mac OS Swiss Knife/WOLView.swift"
      via: "selectedSavedDeviceID remains the picker source of truth after reopen"
      pattern: "selectedSavedDeviceID"
---

<objective>
Finish Phase 3 by wiring the WOL window to the new last-used selection contract without regressing Phase 1 draft-preservation behavior.

Purpose: Make the saved-device wake path feel fast both from the menu and when the user reopens the WOL window later.
Output: Reopen logic that safely preselects the last-used saved device, preserves manual drafts, and falls back cleanly when remembered devices disappear.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
@.planning/phases/03-saved-device-wake-flows/03-RESEARCH.md
@.planning/phases/03-saved-device-wake-flows/03-VALIDATION.md
@.planning/phases/01-truthful-foundations/01-CONTEXT.md
@Mac OS Swiss Knife/WOLSessionModel.swift
@Mac OS Swiss Knife/WOLView.swift
@Mac OS Swiss Knife/WOLWindow.swift
@Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
@Mac OS Swiss KnifeTests/WOLSessionModelTests.swift

<interfaces>
Contracts expected from `03-01`:
```swift
@MainActor
final class SavedDeviceLibraryStore: ObservableObject {
    @Published private(set) var lastUsedDeviceID: UUID?
    @Published private(set) var devices: [SavedDevice]
}

final class WOLSessionModel: ObservableObject {
    @Published var inputMode: InputMode
    @Published var selectedSavedDeviceID: UUID?
    @Published var customMac: String
    func handleWindowWillShow()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply last-used saved-device defaults on reopen without overriding manual drafts</name>
  <files>Mac OS Swiss Knife/WOLSessionModel.swift, Mac OS Swiss Knife/WOLView.swift, Mac OS Swiss Knife/WOLWindow.swift, Mac OS Swiss KnifeTests/WOLSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/03-saved-device-wake-flows/03-CONTEXT.md
    - .planning/phases/01-truthful-foundations/01-CONTEXT.md
    - Mac OS Swiss Knife/WOLSessionModel.swift
    - Mac OS Swiss Knife/WOLView.swift
    - Mac OS Swiss Knife/WOLWindow.swift
    - Mac OS Swiss Knife/SavedDeviceLibraryStore.swift
    - Mac OS Swiss KnifeTests/WOLSessionModelTests.swift
  </read_first>
  <behavior>
    - Test 1: Reopening the window after a successful saved-device wake preselects `lastUsedDeviceID` in preset mode.
    - Test 2: If manual mode has a non-empty `customMac` draft, reopen keeps manual mode and that draft instead of forcing preset mode.
    - Test 3: If `lastUsedDeviceID` no longer exists, reopen falls back to the first device in canonical `deviceLibrary.devices` order or `nil` when the library is empty.
  </behavior>
  <action>Update `WOLSessionModel.handleWindowWillShow()` to apply the exact reopen decision order required by Phase 3. First, set `isWindowVisible = true`. Then: if `inputMode == .custom` and `customMac.isEmpty == false`, return without changing `inputMode` or `selectedSavedDeviceID`; this preserves the unfinished manual draft from Phase 1. Otherwise, look up `deviceLibrary.lastUsedDeviceID`. If that ID exists and `deviceLibrary.device(id: rememberedID)` returns a device, set `inputMode = .preset` and `selectedSavedDeviceID = rememberedID`. If the remembered ID is missing, set `selectedSavedDeviceID = deviceLibrary.devices.first?.id`; if that produces a value, also set `inputMode = .preset`. If the library is empty, leave `selectedSavedDeviceID = nil`. Keep the existing retained-window wiring in `WOLWindow.swift`; do not replace the AppKit ownership model. Update `WOLView.swift` only as needed to keep the picker bound to `selectedSavedDeviceID` and to ensure the placeholder row still uses `tag(nil as UUID?)`. Add `WOLSessionModelTests` coverage with the exact test functions `testWindowReopenPreselectsLastUsedSavedDevice()`, `testWindowReopenPreservesManualDraftInsteadOfForcingPresetMode()`, and `testWindowReopenFallsBackWhenLastUsedDeviceWasDeleted()`.</action>
  <acceptance_criteria>
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `deviceLibrary.lastUsedDeviceID`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `if inputMode == .custom && customMac.isEmpty == false`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `selectedSavedDeviceID = deviceLibrary.devices.first?.id`
    - `Mac OS Swiss Knife/WOLSessionModel.swift` contains `inputMode = .preset`
    - `Mac OS Swiss Knife/WOLView.swift` contains `Picker("选择设备", selection: $session.selectedSavedDeviceID)`
    - `Mac OS Swiss Knife/WOLView.swift` contains `tag(nil as UUID?)`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testWindowReopenPreselectsLastUsedSavedDevice`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testWindowReopenPreservesManualDraftInsteadOfForcingPresetMode`
    - `Mac OS Swiss KnifeTests/WOLSessionModelTests.swift` contains `testWindowReopenFallsBackWhenLastUsedDeviceWasDeleted`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Mac OS Swiss Knife.xcodeproj" -scheme "Mac OS Swiss Knife" -destination 'platform=macOS' -only-testing:'Mac OS Swiss KnifeTests/WOLSessionModelTests'</automated>
  </verify>
  <done>The WOL window now reopens on the most likely saved-device fast path when appropriate, while still respecting unfinished manual input and deleted-device edge cases.</done>
</task>

</tasks>

<verification>
Run the expanded WOL session tests, then perform the manual reopen smoke from `03-VALIDATION.md`: wake a saved device, confirm it is preselected on reopen, then switch to manual mode with a partial draft and confirm reopen preserves the draft instead of forcing preset mode.
</verification>

<success_criteria>
Saved-device wake flows feel fast and stable across menu and window surfaces: the last used device comes back when helpful, manual drafts still belong to the user, and deleted-device memory does not produce stale selection state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-saved-device-wake-flows/03-03-SUMMARY.md`
</output>
