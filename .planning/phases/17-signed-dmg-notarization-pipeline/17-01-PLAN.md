---
phase: 17-signed-dmg-notarization-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "release.sh"
  - "build_dmg.sh"
  - "scripts/release/inspect-dmg-signature.sh"
  - "scripts/release/verify-release-readiness.sh"
autonomous: true
requirements:
  - DIST-02
must_haves:
  truths:
    - "The single public release command stays `sh ./release.sh`, but it no longer stops at the exported app; it now produces `dist/Tools-Cat.dmg` from the signed app produced in Phase 16."
    - "The DMG packaging seam stays deterministic and Apple-native: `build_dmg.sh` still owns staging plus `hdiutil create`, while `release.sh` signs and inspects the exact `dist/Tools-Cat.dmg` artifact."
    - "Phase 17 Wave 1 does not notarize yet; it only closes the signed-DMG seam so Wave 2 can add submission, stapling, and assessment on top of a stable artifact."
  artifacts:
    - path: "release.sh"
      provides: "The sole maintainer-facing release entrypoint that now emits a signed DMG"
      contains: "Tools-Cat.dmg"
    - path: "build_dmg.sh"
      provides: "Deterministic DMG packer for the exported signed app"
      contains: "hdiutil create"
    - path: "scripts/release/inspect-dmg-signature.sh"
      provides: "Post-packaging DMG signature inspection and verification"
      contains: "codesign --verify --verbose=4"
    - path: "scripts/release/verify-release-readiness.sh"
      provides: "Fast static gate proving the signed-DMG seam exists"
      contains: "inspect-dmg-signature.sh"
  key_links:
    - from: "release.sh"
      to: "build_dmg.sh"
      via: "The public release command must package the exported app into the final `Tools-Cat.dmg` instead of leaving DMG creation as a separate maintainer step"
      pattern: "build_dmg\\.sh|Tools-Cat\\.dmg"
    - from: "release.sh"
      to: "scripts/release/inspect-dmg-signature.sh"
      via: "The DMG signature must be inspected immediately after signing so Wave 2 starts from a trusted DMG artifact"
      pattern: "inspect-dmg-signature\\.sh|codesign --force --sign"
    - from: "scripts/release/verify-release-readiness.sh"
      to: "release.sh"
      via: "The static readiness gate must prove the signed-DMG seam exists before notarization work builds on it"
      pattern: "Tools-Cat\\.dmg|build_dmg\\.sh|inspect-dmg-signature\\.sh"
---

<objective>
Extend the Phase 16 release seam so `release.sh` produces a Developer ID signed `Tools-Cat.dmg` from the already-exported signed app, with a deterministic DMG inspection boundary.

Purpose: Phase 16 stopped correctly at `dist/export/Tools Cat.app`, but Phase 17 needs the outer packaging artifact that will be notarized and shared. This plan creates that signed-DMG seam first so notarization can target one stable file path in Wave 2.
Output: A `release.sh` flow that packages `dist/export/Tools Cat.app` into `dist/Tools-Cat.dmg`, signs the DMG, inspects the DMG signature, and proves the new seam via the static release-readiness gate.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-VALIDATION.md
@.planning/phases/16-release-signing-readiness/16-RESEARCH.md
@.planning/phases/16-release-signing-readiness/16-VERIFICATION.md
@release.sh
@build_dmg.sh
@scripts/release/preflight-signing.sh
@scripts/release/inspect-signature.sh
@scripts/release/verify-release-readiness.sh
@docs/release/signing-readiness.md

<interfaces>
Current verified release boundary from Phase 16:
```text
Public command: sh ./release.sh
Signed app output: dist/export/Tools Cat.app
Signed DMG output: not implemented yet
Notarization/stapling: not implemented yet
```

Wave 1 target boundary:
```text
Public command: sh ./release.sh
Archive output: build/archive/Tools Cat.xcarchive
Signed app output: dist/export/Tools Cat.app
Signed DMG output: dist/Tools-Cat.dmg
Wave boundary: notarization submit/wait/log, stapling, and Gatekeeper assessment remain deferred to 17-02
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Package and sign the final `Tools-Cat.dmg` from the exported app</name>
  <files>release.sh, build_dmg.sh, scripts/release/inspect-dmg-signature.sh</files>
  <read_first>
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
    - .planning/phases/16-release-signing-readiness/16-VERIFICATION.md
    - release.sh
    - build_dmg.sh
    - scripts/release/preflight-signing.sh
    - scripts/release/inspect-signature.sh
    - docs/release/signing-readiness.md
  </read_first>
  <behavior>
    - Test 1: `release.sh` still starts from the Phase 16 archive/export path and then packages `dist/export/Tools Cat.app` into `dist/Tools-Cat.dmg`.
    - Test 2: The final DMG is signed with `RELEASE_SIGNING_IDENTITY` and inspected immediately after signing.
    - Test 3: `build_dmg.sh` remains the deterministic packer seam but no longer claims the DMG is unnotarized or instructs manual Gatekeeper overrides.
    - Test 4: Wave 1 stops at a signed DMG; it does not submit to Apple or staple yet.
  </behavior>
  <action>Update `release.sh` so it keeps the existing preflight, archive, export, and app-signature inspection flow from Phase 16, then defines `DMG_PATH="$ROOT_DIR/dist/Tools-Cat.dmg"`, calls `bash "$ROOT_DIR/build_dmg.sh" "$SIGNED_APP_PATH" "Tools-Cat.dmg" "Tools Cat"`, signs the resulting DMG with `codesign --force --sign "$RELEASE_SIGNING_IDENTITY" --timestamp "$DMG_PATH"`, runs `bash "$ROOT_DIR/scripts/release/inspect-dmg-signature.sh" "$DMG_PATH"`, and ends by printing both the signed app path and the signed DMG path. Keep `build_dmg.sh` focused on staging plus `hdiutil create -format UDZO`, but remove the current output line that says the DMG is not notarized and requires manual allow. Add `scripts/release/inspect-dmg-signature.sh` with `set -euo pipefail`, one required DMG path argument, an existing-`.dmg` guard, `codesign -d --verbose=4 "$DMG_PATH"`, and `codesign --verify --verbose=4 "$DMG_PATH"`. Do not add `notarytool submit`, `stapler`, or `spctl` in this task; Wave 1 ends at the signed DMG boundary.</action>
  <acceptance_criteria>
    - `release.sh` contains `build_dmg.sh`
    - `release.sh` contains `Tools-Cat.dmg`
    - `release.sh` contains `codesign --force --sign "$RELEASE_SIGNING_IDENTITY" --timestamp "$DMG_PATH"`
    - `release.sh` contains `scripts/release/inspect-dmg-signature.sh`
    - `build_dmg.sh` contains `DMG_NAME=${2:-Tools-Cat.dmg}`
    - `build_dmg.sh` does not contain `DMG 未公证`
    - `scripts/release/inspect-dmg-signature.sh` contains `codesign -d --verbose=4`
    - `scripts/release/inspect-dmg-signature.sh` contains `codesign --verify --verbose=4`
    - `release.sh` does not contain `notarytool submit`
    - `release.sh` does not contain `stapler staple`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'build_dmg\\.sh|Tools-Cat\\.dmg|codesign --force --sign "\\$RELEASE_SIGNING_IDENTITY" --timestamp "\\$DMG_PATH"|inspect-dmg-signature\\.sh' release.sh && ! rg -n 'DMG 未公证' build_dmg.sh && rg -n 'codesign -d --verbose=4|codesign --verify --verbose=4' scripts/release/inspect-dmg-signature.sh</automated>
  </verify>
  <done>`release.sh` now produces and signs the final `dist/Tools-Cat.dmg`, and the repo has an explicit DMG-signature inspection boundary for Wave 2 to build on.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand the static release-readiness gate so it proves the signed-DMG seam exists</name>
  <files>scripts/release/verify-release-readiness.sh</files>
  <read_first>
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
    - release.sh
    - build_dmg.sh
    - scripts/release/inspect-dmg-signature.sh
    - scripts/release/verify-release-readiness.sh
  </read_first>
  <behavior>
    - Test 1: One fast shell gate proves the release flow now packages and signs `dist/Tools-Cat.dmg`.
    - Test 2: The gate still protects the Phase 16 archive/export seam and does not let DMG work regress back to DerivedData pickup.
    - Test 3: The gate stays credential-free so it can run on any machine.
  </behavior>
  <action>Update `scripts/release/verify-release-readiness.sh` so it still checks the Phase 16 archive/export seam in `release.sh`, `scripts/release/preflight-signing.sh`, `scripts/release/export-options-developer-id.plist.template`, `scripts/release/inspect-signature.sh`, and `Tools Cat.xcodeproj/project.pbxproj`, but now also requires `release.sh` to contain `build_dmg.sh`, `Tools-Cat.dmg`, `codesign --force --sign "$RELEASE_SIGNING_IDENTITY" --timestamp "$DMG_PATH"`, and `inspect-dmg-signature.sh`; requires `build_dmg.sh` to keep `hdiutil create` and no longer contain the stale `未公证` note; and requires `scripts/release/inspect-dmg-signature.sh` to contain both DMG inspection commands. Keep the script grep-based and credential-free. Do not add notarization-specific assertions yet; Wave 2 owns that static gate.</action>
  <acceptance_criteria>
    - `scripts/release/verify-release-readiness.sh` contains `Tools-Cat.dmg`
    - `scripts/release/verify-release-readiness.sh` contains `build_dmg.sh`
    - `scripts/release/verify-release-readiness.sh` contains `inspect-dmg-signature.sh`
    - `scripts/release/verify-release-readiness.sh` contains `codesign --force --sign`
    - `scripts/release/verify-release-readiness.sh` still contains `xcodebuild archive`
    - `scripts/release/verify-release-readiness.sh` still contains `xcodebuild -exportArchive`
    - `scripts/release/verify-release-readiness.sh` still contains `ENABLE_HARDENED_RUNTIME = YES;`
    - `scripts/release/verify-release-readiness.sh` still contains `DEVELOPMENT_TEAM = Y2YJ48R9GL;`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release/verify-release-readiness.sh</automated>
  </verify>
  <done>The repo can now statically prove the signed-DMG release seam exists and that the Phase 16 archive/export boundary was not regressed while adding DMG packaging.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when `release.sh` emits a signed `dist/Tools-Cat.dmg`, the DMG signature is explicitly inspected, and the static readiness gate proves the new signed-DMG seam without regressing the Phase 16 app-signing path.
</verification>

<success_criteria>
Phase 17 starts from the correct outer artifact: the release pipeline no longer stops at the app bundle and now produces the signed DMG that Wave 2 will notarize and staple.
</success_criteria>

<output>
After completion, create `.planning/phases/17-signed-dmg-notarization-pipeline/17-01-SUMMARY.md`
</output>
