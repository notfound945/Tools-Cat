---
phase: 17-signed-dmg-notarization-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01-PLAN.md"
files_modified:
  - "release.sh"
  - "scripts/release/notarize-dmg.sh"
  - "scripts/release/assess-notarized-dmg.sh"
  - "scripts/release/verify-release-notarization.sh"
  - "README.md"
  - "docs/release/signing-readiness.md"
  - "scripts/release/verify-release-docs.sh"
autonomous: true
requirements:
  - DIST-03
  - DIST-04
must_haves:
  truths:
    - "The release flow submits the exact final `dist/Tools-Cat.dmg` to Apple with `notarytool`, waits for completion, and retrieves the notary log automatically when the submission is rejected."
    - "A successful release staples the accepted ticket to `dist/Tools-Cat.dmg` and verifies the stapled DMG locally before reporting success."
    - "The maintainer-facing docs now describe the notarized-DMG flow truthfully and no longer treat DMG notarization as future work, while still leaving repeatable clean-environment install closure to Phase 18."
  artifacts:
    - path: "scripts/release/notarize-dmg.sh"
      provides: "Deterministic notary submit/wait/log helper for the final DMG"
      contains: "xcrun notarytool submit"
    - path: "scripts/release/assess-notarized-dmg.sh"
      provides: "Post-staple local validation seam"
      contains: "stapler validate"
    - path: "scripts/release/verify-release-notarization.sh"
      provides: "Fast static gate for the notarization and stapling flow"
      contains: "notarytool submit"
    - path: "docs/release/signing-readiness.md"
      provides: "Current maintainer runbook for the notarized DMG release flow"
      contains: "dist/Tools-Cat.dmg"
  key_links:
    - from: "release.sh"
      to: "scripts/release/notarize-dmg.sh"
      via: "The single public release command must own notarization instead of requiring a second manual step"
      pattern: "notarize-dmg\\.sh|stapler staple|assess-notarized-dmg\\.sh"
    - from: "scripts/release/notarize-dmg.sh"
      to: "docs/release/signing-readiness.md"
      via: "The documented release runbook must name the same notary metadata outputs and failure-reporting behavior as the script"
      pattern: "Tools-Cat-notary-submit\\.plist|Tools-Cat-notary-log\\.json|notarytool submit"
    - from: "scripts/release/verify-release-docs.sh"
      to: "README.md"
      via: "The docs gate must reject stale Phase 16 `.app`-only guidance and stale manual-allow messaging"
      pattern: "Tools-Cat\\.dmg|notarytool submit|stapler validate|Phase 18"
---

<objective>
Finish the Phase 17 release flow by notarizing the signed DMG, stapling the accepted ticket, assessing the final artifact locally, and updating the public release docs to match the new contract.

Purpose: Wave 1 creates the signed DMG seam, but the milestone goal is not complete until the release flow submits the DMG to Apple, surfaces rejection details clearly, staples the ticket on success, and documents the maintained release path truthfully.
Output: A release pipeline that emits a notarized/stapled `dist/Tools-Cat.dmg`, stores notarization metadata deterministically, runs local assessment on the final DMG, and documents that flow in the repo.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-VALIDATION.md
@.planning/phases/17-signed-dmg-notarization-pipeline/17-01-PLAN.md
@.planning/phases/16-release-signing-readiness/16-VERIFICATION.md
@README.md
@docs/release/signing-readiness.md
@release.sh
@build_dmg.sh
@scripts/release/verify-release-readiness.sh
@scripts/release/verify-release-docs.sh

<interfaces>
Wave 1 output assumed by this plan:
```text
Public command: sh ./release.sh
Signed app output: dist/export/Tools Cat.app
Signed DMG output: dist/Tools-Cat.dmg
```

Wave 2 target boundary:
```text
Notary submission result: build/notary/Tools-Cat-notary-submit.plist
Notary rejection log: build/notary/Tools-Cat-notary-log.json
Final shipped artifact: dist/Tools-Cat.dmg
Post-success checks:
  xcrun stapler validate dist/Tools-Cat.dmg
  spctl --assess --type open -v dist/Tools-Cat.dmg
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the notarization, stapling, and local-assessment flow around the signed DMG</name>
  <files>release.sh, scripts/release/notarize-dmg.sh, scripts/release/assess-notarized-dmg.sh, scripts/release/verify-release-notarization.sh</files>
  <read_first>
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-01-PLAN.md
    - release.sh
    - build_dmg.sh
    - scripts/release/preflight-signing.sh
    - scripts/release/verify-release-readiness.sh
    - docs/release/signing-readiness.md
  </read_first>
  <behavior>
    - Test 1: `release.sh` submits the exact final `dist/Tools-Cat.dmg` to Apple, waits for completion, and stops with an actionable error path when notarization is rejected.
    - Test 2: Successful notarization staples the DMG and then validates the stapled DMG locally before the script reports success.
    - Test 3: The notarization metadata lands in deterministic paths under `build/notary/`.
    - Test 4: One static gate can prove the notarization/stapling seam exists without requiring real credentials.
  </behavior>
  <action>Create `scripts/release/notarize-dmg.sh` with `set -euo pipefail`, one required DMG path argument, guards for an existing `.dmg`, `RELEASE_NOTARY_PROFILE`, `xcrun`, and `plutil`, and deterministic metadata paths `build/notary/Tools-Cat-notary-submit.plist` plus `build/notary/Tools-Cat-notary-log.json`. The script must run `xcrun notarytool submit "$DMG_PATH" --keychain-profile "$RELEASE_NOTARY_PROFILE" --wait --timeout 30m --no-progress -f plist > "$SUBMIT_PLIST"`, extract `id` and `status` with `plutil -extract ... raw -o -`, and if `status` is not `Accepted`, run `xcrun notarytool log "$SUBMISSION_ID" "$LOG_PATH" --keychain-profile "$RELEASE_NOTARY_PROFILE"` before exiting non-zero with a message that prints both the submit plist path and the log path. Create `scripts/release/assess-notarized-dmg.sh` with `set -euo pipefail`, one DMG path argument, and the exact checks `xcrun stapler validate "$DMG_PATH"` plus `spctl --assess --type open -v "$DMG_PATH"`. Update `release.sh` so after the signed-DMG Wave 1 seam it calls `bash "$ROOT_DIR/scripts/release/notarize-dmg.sh" "$DMG_PATH"`, `xcrun stapler staple "$DMG_PATH"`, and `bash "$ROOT_DIR/scripts/release/assess-notarized-dmg.sh" "$DMG_PATH"`, then prints the final stapled DMG path. Add `scripts/release/verify-release-notarization.sh` with `set -euo pipefail` and grep assertions that require `release.sh` to call `notarize-dmg.sh`, `stapler staple`, and `assess-notarized-dmg.sh`; require `scripts/release/notarize-dmg.sh` to contain `notarytool submit`, `--wait`, `Tools-Cat-notary-submit.plist`, `Tools-Cat-notary-log.json`, `plutil -extract id`, `plutil -extract status`, and `notarytool log`; and require `scripts/release/assess-notarized-dmg.sh` to contain `stapler validate` and `spctl --assess --type open -v`. Do not use `xcodebuild -exportNotarizedApp`, do not introduce plaintext Apple credentials, and do not move fresh-machine install verification into this task.</action>
  <acceptance_criteria>
    - `release.sh` contains `scripts/release/notarize-dmg.sh`
    - `release.sh` contains `stapler staple "$DMG_PATH"`
    - `release.sh` contains `scripts/release/assess-notarized-dmg.sh`
    - `scripts/release/notarize-dmg.sh` contains `xcrun notarytool submit`
    - `scripts/release/notarize-dmg.sh` contains `--wait`
    - `scripts/release/notarize-dmg.sh` contains `Tools-Cat-notary-submit.plist`
    - `scripts/release/notarize-dmg.sh` contains `Tools-Cat-notary-log.json`
    - `scripts/release/notarize-dmg.sh` contains `plutil -extract id`
    - `scripts/release/notarize-dmg.sh` contains `plutil -extract status`
    - `scripts/release/notarize-dmg.sh` contains `xcrun notarytool log`
    - `scripts/release/assess-notarized-dmg.sh` contains `stapler validate`
    - `scripts/release/assess-notarized-dmg.sh` contains `spctl --assess --type open -v`
    - `scripts/release/verify-release-notarization.sh` contains `notarytool submit`
    - `scripts/release/verify-release-notarization.sh` contains `stapler staple`
    - `scripts/release/verify-release-notarization.sh` contains `spctl --assess --type open -v`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release/verify-release-readiness.sh && bash scripts/release/verify-release-notarization.sh</automated>
  </verify>
  <done>The release flow now notarizes the final DMG, surfaces rejection logs clearly, staples the accepted ticket, and runs local assessment against the final artifact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update the public release docs and docs gate for the notarized DMG contract</name>
  <files>README.md, docs/release/signing-readiness.md, scripts/release/verify-release-docs.sh</files>
  <read_first>
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-RESEARCH.md
    - .planning/phases/17-signed-dmg-notarization-pipeline/17-01-PLAN.md
    - README.md
    - docs/release/signing-readiness.md
    - scripts/release/verify-release-docs.sh
    - scripts/release/notarize-dmg.sh
    - scripts/release/assess-notarized-dmg.sh
  </read_first>
  <behavior>
    - Test 1: `README.md` now describes the final `dist/Tools-Cat.dmg` release story instead of the Phase 16 `.app`-only boundary.
    - Test 2: The dedicated release doc names the exact notary metadata paths, stapling step, and local assessment commands.
    - Test 3: The docs still keep Phase 18 as the repeatable verification closure and do not claim fresh-machine install proof is already solved here.
    - Test 4: The docs gate rejects stale Phase 16 deferral language and stale manual-allow instructions.
  </behavior>
  <action>Update `README.md` so the release section still starts from `sh ./release.sh`, but now states the maintainer provides `RELEASE_TEAM_ID`, `RELEASE_SIGNING_IDENTITY`, and `RELEASE_NOTARY_PROFILE`, and the final release artifact is `dist/Tools-Cat.dmg`. Update `docs/release/signing-readiness.md` so the prerequisite list includes `hdiutil`, `stapler`, and `spctl`; the runbook still shows the Phase 16 app export inputs but now describes the final DMG path `dist/Tools-Cat.dmg`, the notary submission metadata paths `build/notary/Tools-Cat-notary-submit.plist` and `build/notary/Tools-Cat-notary-log.json`, the successful `stapler staple` + `stapler validate` flow, and the local Gatekeeper assessment command `spctl --assess --type open -v dist/Tools-Cat.dmg`. Remove the current wording that says DMG signing, notarization submission, and stapling are Phase 17+ work. Replace it with an explicit note that Phase 18 still owns repeatable fresh-machine install verification and broader release-regression closure. Update `scripts/release/verify-release-docs.sh` so it requires `README.md` to contain `dist/Tools-Cat.dmg`; requires `docs/release/signing-readiness.md` to contain `Tools-Cat-notary-submit.plist`, `Tools-Cat-notary-log.json`, `stapler validate`, and `spctl --assess --type open -v`; and fails if the doc still contains `Phase 17+`, `未公证`, `隐私与安全`, or `右键-打开` in the public release path.</action>
  <acceptance_criteria>
    - `README.md` contains `sh ./release.sh`
    - `README.md` contains `RELEASE_TEAM_ID`
    - `README.md` contains `RELEASE_SIGNING_IDENTITY`
    - `README.md` contains `RELEASE_NOTARY_PROFILE`
    - `README.md` contains `dist/Tools-Cat.dmg`
    - `README.md` contains `docs/release/signing-readiness.md`
    - `docs/release/signing-readiness.md` contains `Tools-Cat-notary-submit.plist`
    - `docs/release/signing-readiness.md` contains `Tools-Cat-notary-log.json`
    - `docs/release/signing-readiness.md` contains `stapler validate`
    - `docs/release/signing-readiness.md` contains `spctl --assess --type open -v`
    - `docs/release/signing-readiness.md` contains `Phase 18`
    - `docs/release/signing-readiness.md` does not contain `Phase 17+`
    - `scripts/release/verify-release-docs.sh` contains `dist/Tools-Cat.dmg`
    - `scripts/release/verify-release-docs.sh` contains `Tools-Cat-notary-submit.plist`
    - `scripts/release/verify-release-docs.sh` contains `stapler validate`
    - `scripts/release/verify-release-docs.sh` contains `spctl --assess --type open -v`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release/verify-release-readiness.sh && bash scripts/release/verify-release-notarization.sh && bash scripts/release/verify-release-docs.sh</automated>
  </verify>
  <done>The maintainer-facing release docs now match the notarized-DMG release flow and the repo can statically reject drift back to the Phase 16 `.app`-only or manual-allow story.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the release flow notarizes and staples the final DMG, surfaces notary failures with deterministic metadata paths, locally assesses the final artifact, and the public repo docs reflect the new notarized-DMG contract without claiming Phase 18 verification work is already finished.
</verification>

<success_criteria>
Phase 17 completes the release-chain trust loop: the maintainer can produce a signed, notarized, stapled `Tools-Cat.dmg` from `sh ./release.sh`, diagnose Apple rejection logs clearly, and rely on updated docs that describe the real shipped release path.
</success_criteria>

<output>
After completion, create `.planning/phases/17-signed-dmg-notarization-pipeline/17-02-SUMMARY.md`
</output>
