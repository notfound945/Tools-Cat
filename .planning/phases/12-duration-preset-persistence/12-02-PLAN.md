---
phase: 12-duration-preset-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - "Tools Cat/AppDelegate.swift"
  - "Tools Cat/KeepAwakePresentation.swift"
  - "Tools Cat/KeepAwakeSessionModel.swift"
  - "Tools Cat/StatusBarController.swift"
  - "Tools CatTests/KeepAwakeMenuStateTests.swift"
  - "Tools CatTests/KeepAwakeSessionModelTests.swift"
  - "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"
autonomous: true
requirements:
  - AWAKE-11
must_haves:
  truths:
    - "Keep-awake timed state is modeled with managed durations and canonical seconds, not the hardcoded preset enum."
    - "The app owns one shared duration store at launch, so keep-awake session consumers can resolve managed timed durations from persisted data."
    - "The current root menu keeps its fixed rows for now, but timed keep-awake starts and active-state rendering bridge through managed durations without reopening Phase 13 or Phase 14 UI scope."
  artifacts:
    - path: "Tools Cat/KeepAwakeSessionModel.swift"
      provides: "Managed-duration keep-awake mode and pending-action state for timed sessions"
      contains: "case timed(duration: ManagedKeepAwakeDuration, endDate: Date)"
    - path: "Tools Cat/KeepAwakePresentation.swift"
      provides: "Presentation helpers derived from managed durations instead of the preset enum"
      contains: "var activeTimedDuration: ManagedKeepAwakeDuration?"
    - path: "Tools Cat/AppDelegate.swift"
      provides: "One shared KeepAwakeDurationStore owned by the app lifecycle"
      contains: "private var keepAwakeDurationStore: KeepAwakeDurationStore!"
    - path: "Tools Cat/StatusBarController.swift"
      provides: "Fixed-row keep-awake bridge that resolves current timed actions through managed duration data"
      contains: "private let keepAwakeDurationStore: KeepAwakeDurationStore"
    - path: "Tools CatTests/KeepAwakeSessionModelTests.swift"
      provides: "Regression coverage for timed-session lifecycle after migrating away from the preset enum"
      contains: "testStartTimedSessionStoresManagedDurationAndEndDateAfterConfirmedEnable"
  key_links:
    - from: "Tools Cat/AppDelegate.swift"
      to: "Tools Cat/KeepAwakeDurationStore.swift"
      via: "app launch retains one shared managed-duration store alongside the shared keep-awake session"
      pattern: "keepAwakeDurationStore = KeepAwakeDurationStore"
    - from: "Tools Cat/StatusBarController.swift"
      to: "Tools Cat/KeepAwakeDurationStore.swift"
      via: "fixed timed menu rows resolve managed durations by canonical seed seconds instead of a preset enum"
      pattern: "duration\\(matchingSeconds:"
    - from: "Tools Cat/KeepAwakePresentation.swift"
      to: "Tools Cat/ManagedKeepAwakeDuration.swift"
      via: "pending copy, active selection, and countdown tooltips derive titles from the managed duration value"
      pattern: "menuTitle|ManagedKeepAwakeDuration"
---

<objective>
Migrate keep-awake timed state and controller wiring off `KeepAwakeDurationPreset` while keeping the current fixed root menu rows as a temporary bridge.

Purpose: Phase 12 must remove the hardcoded preset enum from the keep-awake domain now so later management UI and dynamic menu rendering can build on the persisted duration model, but it must not pull full dynamic root-menu rendering forward from Phase 14.
Output: A managed-duration keep-awake session model, presentation/controller wiring that resolves timed actions through the shared duration store, and updated regressions covering the bridge without changing the shipped root menu layout yet.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-duration-preset-persistence/12-RESEARCH.md
@.planning/phases/12-duration-preset-persistence/12-01-PLAN.md
@Tools Cat/AppDelegate.swift
@Tools Cat/KeepAwakePresentation.swift
@Tools Cat/KeepAwakeSessionModel.swift
@Tools Cat/StatusBarController.swift
@Tools CatTests/KeepAwakeMenuStateTests.swift
@Tools CatTests/KeepAwakeSessionModelTests.swift
@Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift

<interfaces>
Expected contracts from `12-01`:
```swift
struct ManagedKeepAwakeDuration: Codable, Equatable, Identifiable {
    let id: UUID
    let durationSeconds: Int
    var menuTitle: String { get }
}

@MainActor
final class KeepAwakeDurationStore: ObservableObject {
    @Published private(set) var durations: [ManagedKeepAwakeDuration]

    func reload() throws
    func addDuration(seconds: Int) throws
    func updateDuration(id: UUID, seconds: Int) throws
    func deleteDuration(id: UUID) throws
    func duration(matchingSeconds durationSeconds: Int) -> ManagedKeepAwakeDuration?
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace preset-enum timed session state with managed-duration values</name>
  <files>Tools Cat/KeepAwakePresentation.swift, Tools Cat/KeepAwakeSessionModel.swift, Tools CatTests/KeepAwakeMenuStateTests.swift, Tools CatTests/KeepAwakeSessionModelTests.swift</files>
  <read_first>
    - .planning/phases/12-duration-preset-persistence/12-RESEARCH.md
    - .planning/phases/12-duration-preset-persistence/12-01-PLAN.md
    - Tools Cat/ManagedKeepAwakeDuration.swift
    - Tools Cat/KeepAwakeSessionModel.swift
    - Tools Cat/KeepAwakePresentation.swift
    - Tools CatTests/KeepAwakeSessionModelTests.swift
    - Tools CatTests/KeepAwakeMenuStateTests.swift
  </read_first>
  <behavior>
    - Test 1: Starting a timed session stores the exact `ManagedKeepAwakeDuration` value and computes `endDate` from `durationSeconds`.
    - Test 2: Replacing one timed session with another cancels the previous countdown token and updates the confirmed mode to the replacement managed duration.
    - Test 3: Presentation pending text and active-state helpers use the managed duration's derived `menuTitle`, preserving the shipped Chinese copy for the four seed durations.
    - Test 4: Production keep-awake session and presentation files no longer reference `KeepAwakeDurationPreset`.
  </behavior>
  <action>In `Tools Cat/KeepAwakeSessionModel.swift`, replace the timed-state payloads so `KeepAwakeMode` uses `case timed(duration: ManagedKeepAwakeDuration, endDate: Date)` and `KeepAwakePendingAction` uses `case startingTimed(ManagedKeepAwakeDuration)`. Change `startTimed(_:)` to accept `ManagedKeepAwakeDuration`, compute `endDate` from `TimeInterval(duration.durationSeconds)`, and keep the existing confirmation, countdown, stop, and failure-recovery semantics untouched. In `Tools Cat/KeepAwakePresentation.swift`, replace `activeTimedPreset` with `activeTimedDuration`, derive pending copy from `duration.menuTitle`, and keep `statusText`, `iconSymbol`, `buttonToolTip`, `showsStopAction`, and countdown formatting behavior unchanged apart from the new timed payload type. Update `Tools CatTests/KeepAwakeSessionModelTests.swift` and `Tools CatTests/KeepAwakeMenuStateTests.swift` to build explicit managed-duration fixtures for 900, 1800, 3600, and 7200 seconds, rename the timed-session test to `testStartTimedSessionStoresManagedDurationAndEndDateAfterConfirmedEnable`, and keep the existing lifecycle expectations intact. Do not add menu-management UI, do not add new validation rules beyond the store contract from `12-01`, and do not keep a parallel enum-to-model conversion layer in production code.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `case timed(duration: ManagedKeepAwakeDuration, endDate: Date)`
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `case startingTimed(ManagedKeepAwakeDuration)`
    - `Tools Cat/KeepAwakeSessionModel.swift` contains `func startTimed(_ duration: ManagedKeepAwakeDuration)`
    - `Tools Cat/KeepAwakePresentation.swift` contains `var activeTimedDuration: ManagedKeepAwakeDuration?`
    - `Tools Cat/KeepAwakePresentation.swift` contains `case .startingTimed(let duration)`
    - `Tools Cat/KeepAwakePresentation.swift` contains `duration.menuTitle`
    - `Tools CatTests/KeepAwakeSessionModelTests.swift` contains `testStartTimedSessionStoresManagedDurationAndEndDateAfterConfirmedEnable`
    - `Tools CatTests/KeepAwakeMenuStateTests.swift` contains `ManagedKeepAwakeDuration`
    - `rg -n "KeepAwakeDurationPreset" "Tools Cat/KeepAwakeSessionModel.swift" "Tools Cat/KeepAwakePresentation.swift"` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeSessionModelTests' -only-testing:'Tools CatTests/KeepAwakeMenuStateTests'</automated>
  </verify>
  <done>The keep-awake domain model now carries persisted managed-duration values directly, and timed presentation/session behavior no longer depends on the hardcoded preset enum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the shared duration store into app startup and keep the fixed root menu as a managed-duration bridge</name>
  <files>Tools Cat/AppDelegate.swift, Tools Cat/StatusBarController.swift, Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift</files>
  <read_first>
    - .planning/phases/12-duration-preset-persistence/12-RESEARCH.md
    - .planning/phases/12-duration-preset-persistence/12-01-PLAN.md
    - Tools Cat/ManagedKeepAwakeDuration.swift
    - Tools Cat/KeepAwakeDurationStore.swift
    - Tools Cat/AppDelegate.swift
    - Tools Cat/StatusBarController.swift
    - Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift
  </read_first>
  <behavior>
    - Test 1: App startup retains one `KeepAwakeDurationStore` alongside the existing shared keep-awake session and injects it into `StatusBarController`.
    - Test 2: The fixed timed rows still show `15 分钟`, `30 分钟`, `1 小时`, and `2 小时`, but each action starts a managed duration resolved by canonical seconds from the duration store instead of the preset enum.
    - Test 3: If a fixed-row seed duration is missing from persisted data, the controller may create a non-persisted bridge `ManagedKeepAwakeDuration(durationSeconds:)` for runtime continuity, but it must not write that fallback back into the store or re-seed defaults.
    - Test 4: Controller regressions assert pending-action and active-row behavior through `durationSeconds` or `menuTitle`, not through `KeepAwakeDurationPreset`.
  </behavior>
  <action>In `Tools Cat/AppDelegate.swift`, add `private var keepAwakeDurationStore: KeepAwakeDurationStore!`, initialize it from a `UserDefaultsKeepAwakeDurationRepository(defaults: defaults)` inside `configureSharedStores()`, and pass the shared store into `StatusBarController` through a new initializer label named exactly `keepAwakeDurationStore:`. In `Tools Cat/StatusBarController.swift`, add `private let keepAwakeDurationStore: KeepAwakeDurationStore`, keep the current fixed keep-awake root-row titles and order unchanged, and replace each timed action's `.minutes15/.minutes30/.hour1/.hours2` preset dispatch with managed-duration resolution by canonical seconds 900, 1800, 3600, and 7200. Add a small local helper that returns `keepAwakeDurationStore.duration(matchingSeconds: seconds) ?? ManagedKeepAwakeDuration(durationSeconds: seconds)` so the controller can keep compiling and running even if a seed duration was removed before Phase 14; do not persist that fallback and do not modify the store during rendering. Update `renderKeepAwakePresentation()` so row checkmarks match `presentation.activeTimedDuration?.durationSeconds`. Update `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` so the dispatch assertions, pending-state assertions, and confirmed active-row assertions compare against `ManagedKeepAwakeDuration` values or `durationSeconds` instead of `KeepAwakeDurationPreset`. Keep the current fixed-row menu structure, stop-row truth contract, icon/tooltip rendering, and WOL sections untouched. Do not render arbitrary managed rows yet and do not add the Phase 13 management surface.</action>
  <acceptance_criteria>
    - `Tools Cat/AppDelegate.swift` contains `private var keepAwakeDurationStore: KeepAwakeDurationStore!`
    - `Tools Cat/AppDelegate.swift` contains `keepAwakeDurationStore = KeepAwakeDurationStore`
    - `Tools Cat/AppDelegate.swift` contains `keepAwakeDurationStore: keepAwakeDurationStore`
    - `Tools Cat/StatusBarController.swift` contains `private let keepAwakeDurationStore: KeepAwakeDurationStore`
    - `Tools Cat/StatusBarController.swift` contains `duration(matchingSeconds: 900)`
    - `Tools Cat/StatusBarController.swift` contains `ManagedKeepAwakeDuration(durationSeconds: 900)`
    - `Tools Cat/StatusBarController.swift` contains `activeTimedDuration?.durationSeconds`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `ManagedKeepAwakeDuration`
    - `Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift` contains `durationSeconds`
    - `rg -n "KeepAwakeDurationPreset" "Tools Cat/AppDelegate.swift" "Tools Cat/StatusBarController.swift" "Tools CatTests/StatusBarControllerKeepAwakeMenuTests.swift"` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeSessionModelTests' -only-testing:'Tools CatTests/KeepAwakeMenuStateTests' -only-testing:'Tools CatTests/StatusBarControllerKeepAwakeMenuTests'</automated>
  </verify>
  <done>The app now launches with one shared managed-duration store, timed keep-awake rows bridge through managed duration data, and the existing fixed root menu remains intact until the later dynamic-rendering phase.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when the keep-awake session/presentation no longer rely on `KeepAwakeDurationPreset`, the app owns one shared duration store at startup, and the existing controller tests still prove the fixed root menu while timed actions resolve through managed duration data.
</verification>

<success_criteria>
The keep-awake domain is now migrated off the hardcoded preset enum without dragging Phase 13 or Phase 14 UI work forward: persisted managed durations exist, the session model uses them directly, and the current root menu remains a temporary fixed-row bridge.
</success_criteria>

<output>
After completion, create `.planning/phases/12-duration-preset-persistence/12-02-SUMMARY.md`
</output>
