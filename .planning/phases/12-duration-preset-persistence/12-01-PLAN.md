---
phase: 12-duration-preset-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Tools Cat.xcodeproj/project.pbxproj"
  - "Tools Cat/ManagedKeepAwakeDuration.swift"
  - "Tools Cat/KeepAwakeDurationRepository.swift"
  - "Tools Cat/KeepAwakeDurationStore.swift"
  - "Tools CatTests/KeepAwakeDurationRepositoryTests.swift"
  - "Tools CatTests/KeepAwakeDurationStoreTests.swift"
autonomous: true
requirements:
  - AWAKE-06
  - AWAKE-10
  - AWAKE-11
must_haves:
  truths:
    - "A first-time Phase 12 launch creates exactly four persisted timed durations: `15 分钟`, `30 分钟`, `1 小时`, and `2 小时`."
    - "Once the timed-duration storage key exists, later reloads never silently re-seed or append deleted defaults."
    - "Managed timed durations reload in ascending duration order and reject invalid or duplicate duration seconds before persistence."
  artifacts:
    - path: "Tools Cat/ManagedKeepAwakeDuration.swift"
      provides: "Stable-ID managed timed-duration entity with derived menu copy from canonical seconds"
      contains: "struct ManagedKeepAwakeDuration"
    - path: "Tools Cat/KeepAwakeDurationRepository.swift"
      provides: "UserDefaults-backed exact-once seeding and normalized load/save boundary for managed timed durations"
      contains: "final class UserDefaultsKeepAwakeDurationRepository"
    - path: "Tools Cat/KeepAwakeDurationStore.swift"
      provides: "Observable duration store with reload, add, update, delete, and duplicate/invalid validation"
      contains: "final class KeepAwakeDurationStore"
    - path: "Tools CatTests/KeepAwakeDurationRepositoryTests.swift"
      provides: "Regression coverage for exact-once seeding and repository normalization"
      contains: "final class KeepAwakeDurationRepositoryTests"
    - path: "Tools CatTests/KeepAwakeDurationStoreTests.swift"
      provides: "Regression coverage for validation, mutation persistence, and sorted reload behavior"
      contains: "final class KeepAwakeDurationStoreTests"
  key_links:
    - from: "Tools Cat/KeepAwakeDurationRepository.swift"
      to: "UserDefaults.standard"
      via: "one storage key whose absence triggers exact-once default seeding and whose presence suppresses re-seeding"
      pattern: "managed_keep_awake_durations|object\\(forKey:"
    - from: "Tools Cat/KeepAwakeDurationStore.swift"
      to: "Tools Cat/KeepAwakeDurationRepository.swift"
      via: "store mutations normalize, validate, persist, and reload through the repository seam instead of raw defaults access"
      pattern: "loadDurations\\(|saveDurations\\("
    - from: "Tools Cat/ManagedKeepAwakeDuration.swift"
      to: "Tools Cat/KeepAwakeDurationStore.swift"
      via: "duplicate rejection and sorting use canonical `durationSeconds`, while titles are derived at render time"
      pattern: "durationSeconds|menuTitle"
---

<objective>
Create the persisted managed timed-duration foundation for keep-awake, with exact-once default seeding, normalized reload behavior, and centralized validation.

Purpose: Phase 13 and Phase 14 both need a real data source now. This plan establishes the persisted duration entity, repository seam, and observable store so later UI and menu work do not build on the hardcoded preset enum.
Output: A managed keep-awake duration model, a UserDefaults-backed repository with exact-once seeding, an observable duration store with validation, and focused repository/store regression tests.
</objective>

<execution_context>
@/Users/hailinpan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hailinpan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-duration-preset-persistence/12-RESEARCH.md
@Tools Cat/SavedDeviceRepository.swift
@Tools Cat/SavedDeviceLibraryStore.swift
@Tools CatTests/SavedDeviceRepositoryTests.swift
@Tools CatTests/SavedDeviceLibraryStoreTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the managed timed-duration entity and exact-once UserDefaults repository</name>
  <files>Tools Cat.xcodeproj/project.pbxproj, Tools Cat/ManagedKeepAwakeDuration.swift, Tools Cat/KeepAwakeDurationRepository.swift, Tools CatTests/KeepAwakeDurationRepositoryTests.swift</files>
  <read_first>
    - .planning/phases/12-duration-preset-persistence/12-RESEARCH.md
    - Tools Cat/SavedDeviceRepository.swift
    - Tools CatTests/SavedDeviceRepositoryTests.swift
    - Tools Cat/ManagedKeepAwakeDuration.swift
    - Tools Cat/KeepAwakeDurationRepository.swift
    - Tools CatTests/KeepAwakeDurationRepositoryTests.swift
    - Tools Cat.xcodeproj/project.pbxproj
  </read_first>
  <behavior>
    - Test 1: When the timed-duration storage key is absent, the first repository load returns exactly four ascending durations representing 900, 1800, 3600, and 7200 seconds.
    - Test 2: Once the storage key exists, repository load does not re-seed deleted defaults or append missing rows.
    - Test 3: Repository save/reload normalizes persisted rows by ascending `durationSeconds` and de-dupes exact duplicate seconds while preserving the first surviving row identity.
    - Test 4: Titles remain derived from `durationSeconds`; the repository does not persist a localized title field.
  </behavior>
  <action>Create `Tools Cat/ManagedKeepAwakeDuration.swift` with a Codable, Equatable, Identifiable value type named `ManagedKeepAwakeDuration` that stores exactly `id: UUID` and `durationSeconds: Int`, plus a derived `menuTitle` formatter that produces the shipped Chinese titles for 900, 1800, 3600, and 7200 seconds and supports minute/hour combinations for later phases. In `Tools Cat/KeepAwakeDurationRepository.swift`, add a `KeepAwakeDurationRepository` protocol with `loadDurations() throws -> [ManagedKeepAwakeDuration]` and `saveDurations(_ durations: [ManagedKeepAwakeDuration]) throws`, then implement `UserDefaultsKeepAwakeDurationRepository`. Use `UserDefaults.standard` by default, store JSON under a dedicated key named exactly `managed_keep_awake_durations`, and seed only when `object(forKey:)` reports that key is missing. Seed exactly four rows in ascending order for 900, 1800, 3600, and 7200 seconds. Normalize repository load/save by sorting ascending on `durationSeconds`, removing duplicate seconds while keeping the first surviving ID, and never persisting a title field. Add `Tools CatTests/KeepAwakeDurationRepositoryTests.swift` covering first-load seeding, no re-seed when the key already exists with a subset or empty array, and normalization/de-dup across save/reload. Register the new production and test files in `Tools Cat.xcodeproj/project.pbxproj`.</action>
  <acceptance_criteria>
    - `Tools Cat/ManagedKeepAwakeDuration.swift` contains `struct ManagedKeepAwakeDuration`
    - `Tools Cat/ManagedKeepAwakeDuration.swift` contains `let durationSeconds: Int`
    - `Tools Cat/ManagedKeepAwakeDuration.swift` contains `var menuTitle: String`
    - `Tools Cat/KeepAwakeDurationRepository.swift` contains `protocol KeepAwakeDurationRepository`
    - `Tools Cat/KeepAwakeDurationRepository.swift` contains `final class UserDefaultsKeepAwakeDurationRepository`
    - `Tools Cat/KeepAwakeDurationRepository.swift` contains `managed_keep_awake_durations`
    - `Tools Cat/KeepAwakeDurationRepository.swift` contains `object(forKey: Self.storageKey)`
    - `Tools CatTests/KeepAwakeDurationRepositoryTests.swift` contains `testFirstLoadSeedsDefaultDurationsExactlyOnce`
    - `Tools CatTests/KeepAwakeDurationRepositoryTests.swift` contains `testExistingStorageDoesNotReseedDeletedDefaults`
    - `Tools CatTests/KeepAwakeDurationRepositoryTests.swift` contains `testSaveReloadNormalizesAscendingOrderAndDuplicateSeconds`
    - `rg -n "title:|menuTitle" "Tools Cat/KeepAwakeDurationRepository.swift"` matches only derived-title references and no persisted stored property declaration
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeDurationRepositoryTests'</automated>
  </verify>
  <done>The app now has a persisted timed-duration entity and repository seam that seeds the four shipped defaults exactly once and reloads canonical duration data from one stable UserDefaults key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the observable duration store with centralized validation and mutation persistence</name>
  <files>Tools Cat/KeepAwakeDurationStore.swift, Tools CatTests/KeepAwakeDurationStoreTests.swift</files>
  <read_first>
    - .planning/phases/12-duration-preset-persistence/12-RESEARCH.md
    - Tools Cat/ManagedKeepAwakeDuration.swift
    - Tools Cat/KeepAwakeDurationRepository.swift
    - Tools Cat/SavedDeviceLibraryStore.swift
    - Tools CatTests/SavedDeviceLibraryStoreTests.swift
    - Tools Cat/KeepAwakeDurationStore.swift
    - Tools CatTests/KeepAwakeDurationStoreTests.swift
  </read_first>
  <behavior>
    - Test 1: The store publishes a sorted `durations` array loaded from the repository and `reload()` restores the same ascending order after persisted mutation.
    - Test 2: `addDuration(seconds:)` rejects `0` and negative values with a typed validation error, and rejects duplicate `durationSeconds` even if the duplicate would have a different UUID.
    - Test 3: `updateDuration(id:seconds:)` keeps row identity, re-sorts the list after a valid change, and rejects updates that collide with another row's `durationSeconds`.
    - Test 4: `deleteDuration(id:)` and successful add/update mutations persist across a fresh store instance backed by the same repository.
  </behavior>
  <action>Create `Tools Cat/KeepAwakeDurationStore.swift` as an `@MainActor` `ObservableObject` modeled after `SavedDeviceLibraryStore`, with `@Published private(set) var durations: [ManagedKeepAwakeDuration] = []`, repository-backed `reload()`, and mutation APIs named exactly `addDuration(seconds:)`, `updateDuration(id:seconds:)`, `deleteDuration(id:)`, and `duration(matchingSeconds:)`. Add a typed error enum named `KeepAwakeDurationStoreError` with at least `invalidDuration` and `duplicateDuration` cases. Centralize validation in the store: duration seconds must be strictly positive integers and duplicates are rejected by canonical `durationSeconds`, not by menu title. Persist every successful mutation through `KeepAwakeDurationRepository`, then update the published array with normalized ascending data. Do not re-seed missing defaults in the store; rely on repository load semantics only. Add `Tools CatTests/KeepAwakeDurationStoreTests.swift` to cover invalid and duplicate rejection, sorted reload after add/update/delete, stable ID preservation during edit, and persistence across a fresh store created from the same suite-backed repository.</action>
  <acceptance_criteria>
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `final class KeepAwakeDurationStore`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `@Published private(set) var durations: [ManagedKeepAwakeDuration] = []`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `enum KeepAwakeDurationStoreError`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `case invalidDuration`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `case duplicateDuration`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `func addDuration(seconds: Int) throws`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `func updateDuration(id: UUID, seconds: Int) throws`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `func deleteDuration(id: UUID) throws`
    - `Tools Cat/KeepAwakeDurationStore.swift` contains `func duration(matchingSeconds durationSeconds: Int) -> ManagedKeepAwakeDuration?`
    - `Tools CatTests/KeepAwakeDurationStoreTests.swift` contains `testAddDurationRejectsInvalidAndDuplicateSeconds`
    - `Tools CatTests/KeepAwakeDurationStoreTests.swift` contains `testUpdateDurationResortsAndPreservesIdentity`
    - `Tools CatTests/KeepAwakeDurationStoreTests.swift` contains `testDeleteAndReloadPersistManagedDurations`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project "Tools Cat.xcodeproj" -scheme "Tools Cat" -destination 'platform=macOS' -parallel-testing-enabled NO -only-testing:'Tools CatTests/KeepAwakeDurationRepositoryTests' -only-testing:'Tools CatTests/KeepAwakeDurationStoreTests'</automated>
  </verify>
  <done>The app now has one observable, validated, persistence-backed timed-duration store that later management UI and dynamic menu work can consume directly.</done>
</task>

</tasks>

<verification>
Treat this plan as complete only when a clean defaults suite seeds the four canonical timed durations exactly once, later reloads keep ascending order without silent re-seeding, and store mutation APIs reject invalid or duplicate duration seconds while persisting successful changes across a fresh store instance.
</verification>

<success_criteria>
The codebase now owns timed keep-awake durations as persisted managed data instead of an implied future concept: the four defaults exist in storage, validation lives in one store seam, and reload behavior is deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/12-duration-preset-persistence/12-01-SUMMARY.md`
</output>
